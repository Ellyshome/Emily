# Emily 项目代码审核报告

> 审核日期：2026-09-03
> 审核范围：emily-core、data/plugins/emily_agent、scripts、docker 配置
> 审核重点：功能完整性、断头代码、孤儿代码

---

## 一、项目概况

Emily 是一个容器化的 AI 智能体平台，服务于工程项目管理场景，桥接 QQ/企微聊天通道与 LLM 业务核心。

| 层次 | 技术栈 |
|------|--------|
| API 层 | FastAPI + Uvicorn（端口 18080/18081） |
| 业务核心 | emily_core（LangGraph 引擎 + 调度器 + 工具注册表） |
| 数据层 | PostgreSQL + pgvector（RAG）、SQLAlchemy 2.0、Alembic |
| IM 桥接 | AstrBot 插件（data/plugins/emily_agent），HTTP + SSE 通信 |
| 部署 | Docker Compose（4 种变体）+ go-task |

源码文件约 350 个 Python 文件，1,695 条 import 语句。

---

## 二、孤儿代码（无任何引用，可安全删除）

以下文件在整个代码库中**既无静态 import，也无动态/字符串引用，也不是任何入口点**。

### 2.1 确认的孤儿文件

| # | 文件路径 | 说明 | 建议 |
|---|---------|------|------|
| 1 | `emily-core/emily_core/tools/node_voice_entry_tool.py` | 语音录入完整实现（VoiceEntryState、voice_parse_input、voice_execute_create），但 registry.py 注册的是 stub 而非此文件；且内部 `from ..providers.llm_client` 指向**不存在的模块**，即使被调用也会 ImportError | **删除** |
| 2 | `emily-core/emily_core/workitem/pipeline/interfaces/auth.py` | AuthDecision 枚举 + AuthResult 数据类，唯一消费者 workitem_agent.py 已删除，interfaces/\_\_init\_\_.py 也不导出它 | **删除** |
| 3 | `emily-core/emily_core/scripts/build_world_book.py` | 与根目录 `scripts/build_world_book.py` 功能重复，后者已注册在 scripts_registry.yaml 且功能更完善（argparse、--all/--dry-run） | **删除** |
| 4 | `emily-core/emily_core/infrastructure/database/scripts/fill_users_required_fields.py` | 一次性数据迁移脚本，已有 SQL 孪生文件 `007_fill_users_required_fields.sql` | **删除** |
| 5 | `emily-core/emily_core/tools/base/` 整个目录 | 门面重导出包（re-export shell），registry.py 直接导入平铺模块，无任何外部导入者 | **删除** |
| 6 | `emily-core/emily_core/tools/business/` 整个目录 | 同上，无外部导入者 | **删除** |
| 7 | `data/plugins/emily_agent/adapters/standard/command.py` | 插件中从未导入 StandardCommand | **删除** |
| 8 | `data/plugins/emily_agent/adapters/standard/result.py` | 插件中从未导入 StandardResult | **删除** |
| 9 | `data/plugins/emily_agent/adapters/standard/route_decision.py` | 插件中从未导入 RouteDecision | **删除** |

### 2.2 观察期代码（技术上孤儿，但有意保留）

| 文件 | 说明 |
|------|------|
| `emily_core/workitem/pipeline/real_guardian.py` | RealGuardian 已被 LangGraph quality_gate 节点替代。session_agent.py:846 注释："Guardian 已停用（观察期），观察期后若无回退需求可删除"。技术安全删除，但需确认观察期已结束 |

---

## 三、断头代码（有实现但调用链断裂）

### 3.1 voice_entry 工具 — 功能完全断裂 【高严重度】

```
调用链状态：

registry.py:513-516
  └─ 注册 handle_voice_entry（来自 tools/project/__init__.py:43-45）
       └─ 硬编码返回 {"success": False, "message": "voice_entry 工具待接入 VoiceEntryState 管理"}

真正的实现：
  tools/node_voice_entry_tool.py
    ├─ VoiceEntryState（L96）
    ├─ voice_parse_input（L116）
    └─ voice_execute_create（L178）
    └─ 全代码库零导入 ✗
    └─ from ..providers.llm_client → 模块不存在 ✗
```

**结论**：语音录入功能注册端是 stub，实现端是孤儿且自身有坏导入，功能完全不可用。

### 3.2 rag_logger.py — 写入端断裂 【高严重度】

```
写入端：infrastructure/logging/rag_logger.py → 全代码库零导入 ✗
读取端：repositories/evolution_repo.py:536-564 → 查询 rag_retrieval_logs 表 ✓
数据表：models.py:1451 → RAGRetrievalLog 模型定义 ✓
```

**结论**：RAG 检索指标表被读取但永远不会被写入，evolution 的 RAG 统计永远为空。

### 3.3 morning_report — 有实现但未接入 【中严重度】

```
调度任务：scheduler/jobs/morning_report.py:25
  └─ TODO: 接入 OpsMonitor 的晨报组装逻辑
  └─ 发送占位文本 "🌅 {push_group} 晨报（待接入完整逻辑）"

真正实现：services/evolution/morning_report_builder.py → 存在但未被 handler 调用
```

---

## 四、功能完整性缺陷

### 4.1 调度任务 Stub（返回假成功）

以下调度任务**未实现任何逻辑**，但返回 `success=True`，造成调度器误判：

| # | 文件 | TODO 内容 | 配置状态 | 影响 |
|---|------|----------|---------|------|
| 1 | `scheduler/jobs/data_sync.py:25` | "接入具体同步逻辑" | 已注册 | 永远返回"数据同步完成"但什么都没做 |
| 2 | `scheduler/jobs/webhook.py:25` | "接入 httpx 异步请求" | 已注册 | 永远返回"Webhook 已调用"但从未发 HTTP 请求 |
| 3 | `scheduler/jobs/session_cleanup.py:27` | "接入 SessionPool 的超时关闭逻辑" | **enabled: true** | 永远报告关闭 0 个 session，超时 session 永不被清理 |
| 4 | `scheduler/jobs/morning_report.py:25` | "接入 OpsMonitor 的晨报组装逻辑" | enabled: false | 发送占位文本（当前已禁用，影响有限） |

> **最严重的是 session_cleanup**：调度器认为它在正常工作，实际上系统中有超时的 session 永远不会被清理。

### 4.2 工具注册表

| 工具名 | 状态 | 说明 |
|--------|------|------|
| voice_entry | 硬编码 stub | 见 §3.1 |
| knowledge_search | 条件 stub | 当 RAG provider 不存在时降级为 stub（registry.py:176-184），属设计行为，非缺陷 |
| 其余 35+ 工具 | 正常 | 所有 handler 均可解析到实际实现 |

### 4.3 API 路由层

**全部 14 个路由文件审查通过**——所有端点均调用实际存在的实现方法，无 TODO/stub 响应。

涵盖：node（17 端点）、monitor（8 端点）、permission（6 端点）、evolution、meta_cognition、trace、health、session、skills、message、groups。

### 4.4 服务层

**审查通过**——未发现未实现的服务方法。仅有以下合理的空实现：

- `providers/email/smtp_provider.py` — receive/mark_read 方法抛出"不支持"（SMTP 只发送）
- `providers/email/imap_provider.py` — send 方法抛出"不支持"（IMAP 只接收）

---

## 五、残留引用与配置问题

### 5.1 已删除模块 workitem_agent.py 的残留

`workitem_agent.py` 已被 `langgraph_engine/` 替代，但残留引用仍在：

| 位置 | 残留内容 | 影响 |
|------|---------|------|
| `emily_core/__init__.py:57` | `self._workitem_agent = None` | 死属性，全代码库无其他读写 |
| `CLAUDE.md:142` | 仍列出 workitem_agent.py 为当前模块 | 文档误导 |
| `workitem/pipeline/bus.py:12` | 注释引用旧文件名 | 无功能影响 |
| `workitem/langgraph_engine/agent/loop.py:31,296,336` | 注释引用旧文件 | 无功能影响 |
| `workitem/pipeline/context.py:10-11,40,96` | 注释引用旧文件 | 无功能影响 |
| `infrastructure/llm/prompt_loader.py:32,92` | 注释引用 "WorkItemAgent" 节点 | 无功能影响 |

### 5.2 插件适配器副本分歧

`data/plugins/emily_agent/adapters/standard/` 是 `emily_core/adapters/standard/` 的手动副本，已出现分歧：

| 文件 | 差异 |
|------|------|
| `message.py` | 插件版多了 `event_id: str = ""` 字段（企微去重用），核心版缺失 |
| `command.py` | 核心版多了 `file_category`、`purpose`、`project_ids`，插件版缺失（但插件不使用此文件） |
| `result.py` | 核心版多了 `data: dict \| None`，插件版缺失（但插件不使用此文件） |

> 插件运行在 AstrBot 容器中，emily_core 不在其路径内，因此插件使用的文件（message.py、reply.py）必须保持独立副本。建议将插件版的 `event_id` 字段同步到核心版。

### 5.3 Docker Compose 问题

| 文件 | 问题 | 严重度 |
|------|------|--------|
| `docker-compose-minimal.yml` | 挂载 `./emily-data/postgres_data`，**该目录不存在**，Docker 会创建空目录并初始化空白数据库，忽略 db_seeds 中的预置数据 | 高 |
| `docker-compose-napcat.yml` | 硬编码 Windows 路径 `D:\app\Emily\emily-data\db_seeds`，无法在其他环境运行 | 中 |
| `docker-compose-remote.yml` 与 `docker-compose-wecom.yml` | 内容完全相同，其中之一冗余 | 低 |

### 5.4 脚本注册遗漏

`scripts/` 目录下 12 个脚本未注册到 `emily-data/config/scripts_registry.yaml`：

| 脚本 | 评估 |
|------|------|
| `run_daily_file_parse.py` | 日常文件解析的手动触发入口，**可能是遗漏** |
| `ingest_knowledge.py` | 知识库向量化导入工具，**可能是遗漏** |
| `snapshot.py` | 快照工具包装器，**可能是遗漏** |
| `verify_langgraph_engine.py` | 引擎验证工具，**可能是遗漏** |
| `rag_batch_test.py` | RAG 批量测试 |
| `pdfread.py` | PDF 读取工具 |
| `gen_paper_tables.py` | 论文表格生成 |
| `migrate_file_purpose.py` | 文件用途迁移 |
| `scriptmgr.py` | 注册表管理器自身（合理排除） |
| `_diag_tools.py`、`_test_flows.py`、`_test_flows2.py` | 下划线前缀开发/测试脚本（合理排除） |

### 5.5 调度器包导出不全

`scheduler/jobs/__init__.py` 的 `__all__` 只导出 8 个 handler，实际有 13 个。缺失 5 个：

- `DailyInsightHandler`
- `RuleInductionHandler`
- `PatchValidationHandler`
- `WorldBookUpdateHandler`
- `SystemDescriptionUpdateHandler`

因 `emily_core/__init__.py` 直接按模块路径导入而能正常工作，但包导出不完整。

---

## 六、其他发现

### 6.1 代码质量小问题

| 位置 | 问题 |
|------|------|
| `api/routes/node.py:442-444` | 设置 `status_code = 400` 后又在一个无操作分支中重设 400 |
| `tools/__init__.py` | `__all__` 列出 `handle_query_data` 但未导入，`from emily_core.tools import *` 会触发 ImportError |
| `tools/email_tool.py:201` | TODO: "从用户长期记忆中获取凭证（Phase 2）" |

### 6.2 架构健康度

| 维度 | 评估 |
|------|------|
| API → 业务层调用链 | 完整，所有路由均有实际实现 |
| 工具注册表 | 基本完整，1 个 stub（voice_entry） |
| 调度器 | 4/13 handler 为 stub，其中 1 个已启用 |
| 数据库模型 ↔ 仓库层 | 完整 |
| LangGraph 引擎 | 完整，节点/状态/钩子均已实现 |
| 权限系统 | 完整，auth_engine + cache + code_compiler + row_security |

---

## 七、修复优先级

### P0 — 立即修复

| # | 行动项 | 原因 |
|---|--------|------|
| 1 | `session_cleanup` 在 scheduler_config.json 中设为 `enabled: true` 但 handler 是 TODO stub | 调度器误以为清理正常运作，实际超时 session 永不被回收 |
| 2 | `data_sync` 和 `webhook` 返回假成功 | 调度系统可信度受损 |
| 3 | `docker-compose-minimal.yml` 挂载不存在的 `postgres_data` 目录 | 新部署会得到空白数据库 |

### P1 — 尽快处理

| # | 行动项 | 原因 |
|---|--------|------|
| 4 | `voice_entry` 功能断裂 | 要么修复 node_voice_entry_tool.py 的坏导入并接入 registry，要么从注册表中移除 |
| 5 | `rag_logger.py` 写入端断裂 | evolution 的 RAG 统计永远为空 |
| 6 | `morning_report` handler 未接入已有的 MorningReportBuilder | 功能已实现但未连线 |

### P2 — 清理

| # | 行动项 |
|---|--------|
| 7 | 删除 9 个确认的孤儿文件（见 §2.1） |
| 8 | 删除 `self._workitem_agent` 死属性（emily_core/\_\_init\_\_.py:57），更新 CLAUDE.md |
| 9 | 同步插件适配器副本中的 event_id 字段，删除插件中不使用的 3 个副本文件 |
| 10 | 去重 docker-compose-remote.yml 与 docker-compose-wecom.yml |

### P3 — 改善

| # | 行动项 |
|---|--------|
| 11 | 注册遗漏的脚本到 scripts_registry.yaml |
| 12 | 补全 scheduler/jobs/\_\_init\_\_.py 的 \_\_all\_\_ 导出 |
| 13 | 修复 tools/\_\_init\_\_.py 的 \_\_all\_\_ 缺少导入问题 |
| 14 | 修复 napcat.yml 的硬编码 Windows 路径 |

---

## 附录：文件清单

### A. 建议删除的孤儿文件

```
emily-core/emily_core/tools/node_voice_entry_tool.py
emily-core/emily_core/workitem/pipeline/interfaces/auth.py
emily-core/emily_core/scripts/build_world_book.py
emily-core/emily_core/infrastructure/database/scripts/fill_users_required_fields.py
emily-core/emily_core/tools/base/__init__.py          （及整个 base/ 目录）
emily-core/emily_core/tools/business/__init__.py       （及整个 business/ 目录）
data/plugins/emily_agent/adapters/standard/command.py
data/plugins/emily_agent/adapters/standard/result.py
data/plugins/emily_agent/adapters/standard/route_decision.py
```

### B. 需要修复的 stub/TODO 文件

```
emily-core/emily_core/scheduler/jobs/data_sync.py:25
emily-core/emily_core/scheduler/jobs/webhook.py:25
emily-core/emily_core/scheduler/jobs/session_cleanup.py:27
emily-core/emily_core/scheduler/jobs/morning_report.py:25
emily-core/emily_core/tools/project/__init__.py:43-45    （voice_entry stub）
```

### C. 需要更新的配置文件

```
emily-data/config/scheduler_config.json                   （禁用或实现 stub handler）
docker-compose-minimal.yml                                （修正 postgres_data 路径）
docker-compose-napcat.yml                                 （去除硬编码路径）
emily-core/emily_core/scheduler/jobs/__init__.py          （补全 __all__）
emily-core/emily_core/tools/__init__.py                   （修复 __all__ 缺失导入）
```
