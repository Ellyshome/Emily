

# 只是沉淀抓手
emily的一个重要标志性理念：经验沉淀
达成主要方法：
1. 全景节点中，‘成果管控’的内容细节打磨。指定‘成果管控手册’
2. 业务流SOP的打磨
3. 材料备注，上传文件的描述
全景节点需要有成果管控字段，可以装载成品对应的‘成果管控手册’


---
# 文件外发管控
文件需要增加字段，有的文件只能查阅，不能发给用户，比如‘外宣资料’
是否可携带文件名
是否可发给用户

---
# 企业备注
项目用到的企业基本性质比较固定，应有固定定义模板，以供节点等消费者使用，通过模板生成而调用时能让llm检索到它

---
探索中发现 5 处既有缺陷：
1. SOP-011 的工具章节标题是「可调用工具」而解析器只认「使用的系统工具」，导致该 SOP 的 14 个节点工具声明一直没生效；
2. 模板索引的 bootstrap 自刷在容器态因路径计算失效而静默跳过，是典型假接线，且模板目录只读根本无法在容器内写索引；3. UNASSIGNED 是 007_migrate_project_events.sql:27-34 播下的一行假项目记录（project_id='UNASSIGNED'），不是按项目的真实节点；SessionContext.node_template_catalog 只写不读（占位符已从 prompt 移除）；
4. 模板库 README.md 第 8 节声称的 parser.py/mapper.py 在仓库里不存在。

---
关于群信息，emily一个重要职能就是在有‘群’这个概念的信道里监听群信息，如，企业微信建立的微信群，QQ群。当前emily的现状，对群消息的监听保存现状是怎样的？