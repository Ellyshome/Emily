# 权限与能力缺口清单 V1

> 日期：2026-09-17　|　范围：权限（L1–L6）与能力可达性缺口，含 emy-console ↔ IM 等价性
> 口径来源（以代码为准）：`emily-core/emily_core/permission/level.py`、`tool_registry`（`infrastructure/tools_consistency.py` TOOL_META_MAP + `seed_tool_registry.sql`）、各 SOP 文档 §3.2 声明、`workitem/langgraph_engine/agent/fallback_policy.py`、`.trae/skills/_shared/constitution.md` C13/Q6
> 配套：`Issues/权限矩阵/权限表_fixed.csv`（现状权限表，2026-09-17 按代码与库重写）

---

## 一、判定口径：IM 侧"可达"的四条通路

| # | 通路 | 事实源 | 说明 |
|---|------|--------|------|
| P1 | 级别层 | `ToolRegistryRepo.get_available(level)` | `base`=全员；`business`：`all` 全员 / `write_l2` L2+ / `write` L3+ / `admin` L5+；`project`=L5+ |
| P2 | SOP 声明层 | SOP 文档 §3.2 表格 → `SkillDoc.tools` → `fetch_available_tools._merge_sop_declared_tools` | 只做**并集放行**；用户持有该 SOP（`sop_allow`）时其声明工具一并可见 |
| P3 | 自由工具集 + 写护栏 | `session/capability_catalog.py` | 高危写语义（`overwrite`/`delete`）不进自由工具集；非 `read` 写能力须在档位白名单内（`FallbackPolicy.resolve`） |
| P4 | 兜底白名单 | `fallback_policy.py`（basic/advanced）+ SOP-999 派生白名单（`exposure_mode=sop_only` 排除） | 兜底/直调路径的唯一事实源 |

**判定**：存在 P1–P4 任一合法通路 = IM 可达；四条都不通 = IM 不可达（"仅后台"）。
**注意**：P3 的写护栏依赖 `BusinessFlowTool.write_mode` 标注，标注缺失即护栏失效（见 G-9）。

---

## 二、缺口清单

### A 类：能力缺失（两侧都无落点，需先立项）

| 编号 | 缺口 | 现状证据 | 影响 |
|---|---|---|---|
| G-1 | 企业主数据增删 | 无 API / 工具 / SOP；`company_info` 仅种子与脚本写入；参与单位靠 `services/company_resolver.py` 登记 | 企业档案只能改库 |
| G-2 | 人员等级调整 | 无 API / 工具 / 服务层校验；仅 `Issues/人员展示页面.md` 一句意向 | 等级调整只能改库，无留痕 |
| G-3 | 人员企业归属调整 | 同上（`users.company` 只能改库） | 归属变更无审计 |

### B 类：能力已具备，但 IM 无入口（仅后台 console）

| 编号 | 缺口 | 已有实现 | 缺什么 |
|---|---|---|---|
| G-4 | 文件密级调整 | `application/file_app.py:284 handle_update_confidentiality`；`services/file_service.py:83 update_confidentiality`（口径：上传人本人或 L5/L6）；console `POST /console/file-confidentiality` | 未注册为工具、无 SOP 声明 → IM 不可达 |
| G-5 | RAG 出库（删分块） | console `POST /console/rag-delete`（`repositories` 层 `delete_by_doc`） | IM 无工具；console 路径**无等级判定**，仅记 operator_id |
| G-6 | 节点参与单位维护 | `node_service.py:768` 起 `add/remove_node_participant`；console `POST /console/node-participant` | 无工具、无 SOP 声明 |
| G-7 | 节点共享文件关联（`node_accessible_files`） | `NodeAccessibleFileRepo`；console `POST /console/node-file`；`manage_nodes link-files` 脚本 | 无独立工具；IM 仅能经 `submit_node_deliverable` / `update_node_progress` 间接带文件 |

### C 类：接线与元数据缺陷（有工具、接线不对）

| 编号 | 缺口 | 证据 | 影响 |
|---|---|---|---|
| G-8 | `delete_file` 无 SOP 承载、却被标 `sop_only` | `tools_consistency.py:92`（sop_only）；`emily-data/sops/SOP-999-SYS-fallback.md:67` 明确排除；**无任何 SOP §3.2 声明它** | 规范上"必须走专属 SOP"却无 SOP 可走；实际又因 G-9 从自由工具集漏出 → 通路与设计不自洽 |
| G-9 | `write_mode` 标注普遍缺失 → 高危写护栏失效 | `tools/registry.py` 仅 `record_event/task/meeting/file` 传了 `write_mode="append"`（:257/:262/:267/:275）；`update_file_category`、`link_file`、`new_file_version`、`delete_file`、`link_to_master`、`unlink_attachment`、`update_file_purpose`、`create_task_node`、`confirm/return_node_deliverable`、8 个节点工具、专家工具、`embed_and_index` 全部落到默认 `read` | `capability_catalog` 的"高危不入自由工具集"判定失效；写能力被当作只读暴露。**部分已修（批次①）**：`delete_file` 已标 `delete`；其余待批次④（标注会使其退出自由工具集，须与 SOP 声明、逐工具授权口径成对落地，避免能力空窗） |
| G-10 | `create_task_node` 权限口径冲突 | 工具 `business/write`（L3+）；承载 SOP-011 准入 L5（`008_set_sop_min_level.sql:33`）；SOP-011 §3.5 写"L3+ 可见" | L3/L4 看得到工具却进不了 SOP；文档与实现互相矛盾 |
| G-11 | console `/upload` 在路由层复刻归档逻辑 | `api/routes/console_resources.py:588-640` 直接 `FileRepository.create` + 落盘 | 违反 C13/Q6；与 `record_file` 通道不同源：无 `purpose` 判定、`file_category` 固定 `OTHER`、不触发 RAG 自动入库 |
| G-12 | `delete_file` 服务层校验过宽 | `tools/file_tool.py:658-687` 仅 `file_manager.can_access(user_id, file)`（可见即可删） | 与密级调整（仅上传人/L5）口径不一致；L3+ 可见即可软删他人文件。**已定（D2）**：改为"上传人本人 或 L5/L6" |
| G-13 | 行级安全 fail-open | `permission/row_security.py`（原 `do_orm_execute` 钩子，注入失败 → ALLOW + WARNING）；`test/05_权限与可见范围.md` FR-G6 已登记。**批次⑤ 查明：白名单列 `*.company_id` 在库中不存在、`ORMExecuteState` 亦无 `select_statement` 属性，且权限快照从未注入 → 拦截器自建立起从未生效**；**批次⑥ 按 Q9 选项② 整段删除该拦截器**（详见「六、实施记录 批次⑤/⑥」） | 按单位维度的行级隔离**不在本期范围**，由服务层（`VisibleFileSetResolver` / `ParticipationRepo` / `query_service`）+ 工具层数据边界（`_session_scope`）承担；不再存在"看似有行级安全"的误导 |
| G-14 | 文档口径差 | `docs/Manual/权限矩阵.md`（0–5 级、含部门/企业类型默认级）vs `level.py`（1–6 级）；SOP 文档头 `allow_roles`（SOP-002/004/007 写 `all`）vs DB `min_level=2`（008 脚本）；SOP-011 头写 `admin` vs §3.5 写 L3+ | 排查/培训易误判；建议统一"以 DB + 代码为准"并在文档头加注 |
| G-15 | 工具层数据边界注入断线（`_session_scope` 无生产者） | `tools/query_tool.py:115` 消费 `params["_session_scope"]`，但 LangGraph/WorkItem 迁移后运行时注入只剩 `_user_id` / `_message_id` / `_conversation_id`（`workitem/langgraph_engine/agent/loop.py`、`session/loop.py`）→ `db_perms` 与 `project_ids` **恒为空**：表级门禁与项目范围过滤双双失效。**实测**：`query_data` 返回全表 22/22（与 `SELECT count(*) FROM events` 相等）。**批次⑥ 已修复** | 会话与工单两条路径均下发 `_session_scope`（`SessionContext.build_tool_scope`），项目范围过滤恢复生效（详见「六、实施记录 批次⑥」） |

---

## 三、emy-console 功能 ↔ IM 等价性对照

定位依据：`.trae/skills/_shared/constitution.md` C13"Web 控制台 = 观察窗口（不得自造功能）"，以及本次约定"console 有的**基本功能**，原则上都能经 IM 实现"。

### 3.1 业务写操作（**必须**有 IM 等价通路）

| console 端点 | 功能 | IM 等价通路 | 判定 |
|---|---|---|---|
| `POST /upload` | 上传归档 | `record_file`（SOP-004，L2+） | ✅ 有（但见 G-11：两者不同源） |
| `POST /file-delete` | 软删除文件 | `delete_file` | ⚠️ 工具在，但无 SOP 承载（G-8）、校验过宽（G-12） |
| `POST /file-confidentiality` | 密级调整 | 无 | ❌ **违反**（G-4） |
| `POST /rag-index` | RAG 入库 | `embed_and_index`（L3+） | ⚠️ 工具在，但无 SOP 声明 |
| `POST /rag-delete` | RAG 出库 | 无 | ❌ **违反**（G-5） |
| `POST /node-participant` | 节点参与单位维护 | 无 | ❌ **违反**（G-6） |
| `POST /node-file` | 节点共享文件增删 | 仅间接（成果/进度带文件） | ❌ **违反**（G-7） |
| `POST /chat/send`、`POST /chat/upload` | 消息模拟器 | —— | ⏭ 豁免：本身就是"模拟 IM"，是 IM 通道的观测入口，非业务功能 |

### 3.2 只读观测（豁免，不要求 IM 等价）

`/users`、`/resources`、`/node-table`、`/files`、`/project-events`、`/rag-search`、`/rag/backends`、`/prompts`、`/prompt`、`/langgraph-tools`、`/logs`、`/llm-trace`、`/self-check`、`/session-archives`、`/test-cases`、`/channels`

> 其中 `/users`、`/resources`、`/node-table`、`/files`、`/project-events`、`/rag-search` 在 IM 侧已有等价查询能力（`query_data` / `query_files` / `query_node` / `query_my_nodes` / `knowledge_search` / `meta_cognition_read`）；其余为**系统观测面**（日志、trace、自检、测试用例、模板），设计上属运维后台，不要求 IM 等价。

### 3.3 配置类写操作（豁免，但需留痕）

`/mcp/server`、`/mcp/toggle`、`/mcp/probe`、`/test-settings`、`/channels/wxmp-domain`、`/channels/wecom-params`

> 属"系统配置/接入参数"，非业务基本功能 → 豁免；但 `Issues/操作留痕治理_需求基线_V1.md` 要求这类写操作补业务事件留痕。

### 3.4 违反项汇总（需你确认处理方式）

1. **密级调整** —— IM 无入口（G-4）
2. **RAG 出库** —— IM 无入口，且 console 路径无等级判定（G-5）
3. **节点参与单位维护** —— IM 无入口（G-6）
4. **节点共享文件关联** —— IM 无直接入口（G-7）

---

## 四、修法建议与可执行验收

> 原则：能力先进工具层/脚本层，再接 console（C13）；每条验收须可执行（Q1）。

| 编号 | 建议落点 | 验收（示意） |
|---|---|---|
| G-4 | 新增工具 `update_file_confidentiality`（`permission_flag=write`，服务层沿用"上传人本人或 L5/L6"），挂 SOP-004-FILE | L3 改他人文件密级 → `permission_denied`；L5 改 → 成功且 `file-repo` 留痕；行级可见性即时变化 |
| G-5 | 新增工具 `rag_remove_document`（建议 `admin`），挂 SOP-004 或新 RAG SOP；console 端点改调该工具 | 入库 → `knowledge_search` 命中 → 出库 → 同一 query 不再命中；越权调用被拒且有审计 |
| G-6 | 新增工具 `manage_node_participant`（`project/admin`，L5+），挂 SOP-011 | L4 调用 → 拒绝；L5 调用 → `node_participant_companies` 出现/移除记录并留痕 |
| G-7 | 新增工具 `manage_node_file`（`write`，L3+，复用 `NodeService.add/remove_node_file`）或明确"仅经成果提交关联"并在 console 标注为后台补救通道 | 增/删后 `node_accessible_files` 变化，非参与单位人员不可见该文件 |
| G-8+G-9 | ① 为全部写工具补 `write_mode`（`append`/`transition`/`overwrite`/`delete`），同步 `tools_consistency.py`；② 决定 `delete_file` 归属：挂 SOP-004 或从 `tool_registry` 摘除 | `check_tools_consistency` 通过；L3 用户在自由工具集中看不到 `delete_file`（若判定为高危）；经 SOP 路径可正常删除 |
| G-10 | 二选一：下调 SOP-011 准入到 L3，或把 `create_task_node` 移出 SOP-011 另行承载 | 选定后改 `008_set_sop_min_level.sql` 与 SOP-011 §3.5 使其一致，并用 L3 用户回归 |
| G-11 | console `/upload` 改调 `record_file` 工具 handler（同源） | 经 console 上传 REFERENCE 类文件后 `rag_indexed` 按 purpose 策略处理；`file_category` 非固定 `OTHER` |
| G-12 | `handle_delete_file` 增加"上传人本人 / L5+"判定（与密级口径对齐）或明确"可见即可删"为设计并写入权限表 | L3 删除他人文件 → 拒绝（若采纳前者）；回归文件删除链路 |
| G-13 | **已按 Q9 选项② 关闭**：删除行级安全拦截器及其 thread-local 上下文（`permission/row_security.py` 仅保留 `PermissionAuditLogRepository`），启动日志不再有"拦截器不生效"ERROR；按单位维度的行级隔离不在本期范围，行可见性由服务层 + 工具层数据边界承担 | 全仓无 `register_row_security_listener` 等残留符号；`import emily_core` 正常、启动无告警；原"放行 + WARNING"语义随机制一并消失（不再存在该判定） |
| G-14 | 统一文档口径：`docs/Manual/权限矩阵.md` 对齐 1–6 级；SOP 文档头加注"准入以 `sop_business_flows.min_level` 为准" | 文档与 `006/008` 脚本、`level.py` 一致，抽检 3 个 SOP 无冲突 |

---

## 五、决策记录（2026-09-17 已定）

| # | 决策项 | 结论 | 落地要求 |
|---|--------|------|----------|
| D1 | 密级调整的 IM 授权口径 | **沿用现状**：上传人本人 或 L5/L6 可改 | 新增工具 `update_file_confidentiality` 并挂 SOP-004（G-4）；"改小密级（降级）"必须留痕 |
| D2 | 文件软删除的 IM 通道 | **保留 IM**，限"上传人本人 + L5/L6" | 补 `handle_delete_file` 服务层校验（G-12）；`write_mode` 标 `delete`，使其**只经 SOP 授权执行**、不进自由工具集（G-8/G-9） |
| D3 | 企业/人员管理（G-1~G-3） | **本期全做**：企业增删 + 人员等级调整 + 人员归属调整（2026-09-17 复核确认） | 新增工具 + 审计留痕；调整规则见 D6。**准入表述修正**：原"限 L5/L6"与 D6 的具体分级冲突，改为「**工具准入不得低于该能力实际可操作的最低等级**」，实际分级由 D6 决定（见 D8） |
| D4 | console 后台视角 | **折中**：日常写操作按等级判定；仅"纠错补救类"走后台特权并强制留痕 | 需先出"纠错类操作清单"（Q7 待确认）；console 侧写操作按所选操作人等级判定 |

## 五-B、决策记录（第二批，2026-09-17）

| # | 决策项 | 结论 |
|---|--------|------|
| D5 | 优先次序 | **采纳建议顺序**：① 护栏（G-9 write_mode、G-12 删除校验）→ ② 安全口子（G-4 密级、G-5 RAG 出库）→ ③ 业务闭环（G-6 节点参与单位、G-7 节点文件关联）→ ④ 口径一致性（G-8/G-10/G-11/G-14）→ ⑤ G-13 行级安全 fail-open |
| D6 | 人员/企业调整规则 | **按原表口径**：① 等级调整——L6 可调至 L6、L5 最高 L4、L4 最高 L3、L3 仅可调"自己下一级"、L2/L1 不可调；② 企业归属调整——L4+ 可调、L3 可管理本企业人员进出、L2/L1 不可；③ 增删企业——L6/L5/L4 可、L3/L2/L1 不可 |
| D7 | "纠错补救类"能力 | **本期都不做**：文件误删恢复 / RAG 分块重建清理 / 密级与 purpose 纠错 / 节点参与单位补登 / 数据回填脚本，一律不建"后台特权通道"。console 中依赖该特权的入口**暂时停用**（前端入口置灰 + 后端返回"暂未开放"），并加备注说明；后续需要再启用 |

## 五-C、决策记录（第三批，2026-09-17）

| # | 决策项 | 结论 |
|---|--------|------|
| D8 | **D3 与 D6 的准入口径冲突裁决**（Q4） | **以 D6 的分级为业务口径**（粒度更细且覆盖 D3），D3 的"限 L5/L6"表述按 D3 行内说明修正。配套三条：① **判定位置**——写能力（人员等级调整 / 人员企业归属调整 / 企业增删）的授权判定一律在**服务层**，**以操作人实际等级为准**，不信任前端与请求自述之外的信息；② **fail-closed**——取不到操作人等级时**默认拒绝**；③ **被拒也留痕**——动作已开始后的权限/规则拒绝属"被拒"，须走操作留痕治理的三态口径记录（含拒绝原因）。 |
| D8-b | **企业增删的等级口径** | 沿用 **D6 ③**：**L4 及以上可增删企业**，L3/L2/L1 不可。本期实现，落点与人员管理同一载体。 |
| D8-c | **能力载体** | G-1~G-3 的能力落地以 `Issues/人员展示页面/人员展示页面_需求基线_V1.md` 为需求基线；本清单继续作为权限口径的权威来源，二者不一致时以本清单的分级为准。 |

### D7 执行原则（"先建后停"，避免能力空窗）

| console 入口 | 归类 | 处理 |
|---|---|---|
| `/console/file-confidentiality`（密级） | 纠错类 | **等 G-4 的 IM 工具上线后停用**，避免出现"无人能改密级" |
| `/console/rag-index`、`/console/rag-delete`（RAG 重建/清理） | 纠错类 | **等 G-5 的 IM 工具上线后停用** |
| `/console/node-participant`（参与单位补登） | 纠错类 | **等 G-6 的 IM 工具上线后停用** |
| `/console/file-delete`（软删） | 日常写 | 不属纠错类：按 D4 补"按操作人等级判定"，保留可用（同时兜 D2 口径） |
| `/console/node-file`（节点共享文件） | 日常写 | 同上，按 D4 补等级判定，保留可用 |
| `/console/upload` | 日常写 | 按 D4 补等级判定 + 改调 `record_file`（G-11），保留可用 |
| ScriptManager 中的回填/迁移类脚本（`migrate_*` / `backfill_*`） | 运维脚本层 | 不属 console 功能，维持现状（脚本层能力，不受本清单约束） |

---

## 五-C、待确认（剩余）

无（Q5–Q7 已定）。批次②~④推进后新增 Q9 / Q10，见文末「追加待确认」。

---

## 六、实施记录

### 批次①（2026-09-17）：护栏 G-12 + G-9（delete_file 部分）

| 变更 | 文件 | 内容 |
|---|---|---|
| 服务层授权 | `emily-core/emily_core/tools/file_tool.py`（`handle_delete_file`） | 新增校验：**仅上传人本人 或 L5/L6** 可删（无操作人信息 fail-closed），与密级调整同口径（D2） |
| 写语义标注 | `emily-core/emily_core/tools/registry.py` | `delete_file` 注册补 `write_mode="delete"` → 退出 LLM 自由工具集，只能经 SOP + 人工确认执行 |
| 静态参考表同步 | `emily-core/emily_core/infrastructure/tools_consistency.py` | `TOOL_WRITE_MODE_MAP` 增 `delete_file: delete` |
| 接线（避免空窗） | `emily-data/sops/SOP-004-FILE-file_archive.md` | §3.2 声明 `delete_file`；新增 §3.6「权限映射（删除类高危操作）」写明授权口径与"软删无恢复入口" |

**验收命令与结果**

```powershell
uv run python scripts/check_fallback_tools.py       # ✅ 问题 0（fatal 0）
uv run python scripts/check_tools_consistency.py    # ✅ fatal 0（V13 为本地无 DB 的环境噪音）
# 元数据核对：delete_file.write_mode == 'delete' 且落在 high_risk 集合内
uv run python -c "...reg.get('delete_file').write_mode..."   # ✅ ('delete','write') | excluded: True
# SOP 解析核对
uv run python -c "...SkillRegistry(skill_directory='emily-data/sops')..."
# ✅ SOP-004 tools = ['record_file','query_data','query_files','query_my_nodes','delete_file']
```

**顺带发现（已登记，未改）**：`check_tools_consistency` 报 `write_user_memory` 缺参数 schema（V5），与本次无关，批次④一并处理。

**遗留收敛点（Q8）**：因 SOP-004 准入为 L2，`delete_file` 声明其后 L2 **上传人本人**也可删自己上传的文件（符合 D2 字面口径，但与"L2 仅增"的直觉可能冲突）——如需收紧为"L3+ 才能删"，在 `handle_delete_file` 增加 `level >= 3` 即可。

---

### 批次②（2026-09-17）：安全口子 —— G-4 密级调整 + G-5 RAG 出库

| 变更 | 文件 | 内容 |
|---|---|---|
| 新工具（G-4） | `tools/file_tool.py`、`tools/registry.py` | `update_file_confidentiality`（`business/write`，`write_mode=overwrite`）：值域 0/1/2 校验 + 授权沿用服务层「**上传人本人 或 L5/L6**」（`FileApplication.handle_update_confidentiality`，降级同样留痕、可见范围即时重算）；SOP-004 §3.2/§3.6 声明 |
| 新工具（G-5） | `tools/embed_tool.py`、`tools/registry.py` | `rag_remove_document`（`business/admin`，`write_mode=delete`）：handler 内 **L5/L6 判定（fail-closed：无操作人或取不到等级一律拒绝）**；动作与留痕委托 `KnowledgeService.remove_document`；支持 `file_no` / `doc_id` 二选一 |
| console 同源（G-5） | `api/routes/console_resources.py` | `/console/rag-delete` 改调 `handle_rag_remove_document`（原为直调 service、**无等级判定**）→ console 与 IM 同源同能力（约束 14） |
| 静态参考表 | `infrastructure/tools_consistency.py` | `REGISTERED_TOOLS` / `TOOL_META_MAP` / `TOOL_SCHEMA_MAP` / `TOOL_WRITE_MODE_MAP` 四处同步 |
| SOP | `SOP-004-FILE-file_archive.md` | §3.2 声明两工具；§3.6 更名「高危写操作」并补授权口径；版本 → v1.3 |

**验收（真实库 + 真实用户 UUID）**

- `rag_remove_document`：L3 / L4 / 无操作人 → `permission_denied`；L5 → `success`（删 0 分块，不误删）
- `update_file_confidentiality`：非法密级 → `invalid_confidentiality`；未注入 file_app → `service_unavailable`
- console `/console/rag-delete`：`operator_id=L3` → 拒绝「仅 L5/L6 管理员可执行知识库出库」；`operator_id=L5` → `code 0`
- IM 实测（L5 罗永强）：「把文件 FIL-20260916-0001 的密级改成公开」→ ✅ 执行并留痕（提示"调整已留痕"）

---

### 批次③（2026-09-17）：业务闭环 —— G-6 节点参与单位 + G-7 节点共享文件

| 变更 | 文件 | 内容 |
|---|---|---|
| 新工具（G-6） | `tools/node_tool.py`、`tools/registry.py` | `manage_node_participant`（`project/admin`，L5+，`write_mode=overwrite`）：`company`（单位名或 ID，经 `CompanyResolver` 按节点项目范围解析）与 `user_id` 二选一；动作委托 `NodeService.add/remove_participant_company`（**验收锚定 `node_participant_companies`**）与 `add/remove_node_participant`；handler 内 L5/L6 判定（fail-closed）；SOP-011 §3.3/§3.5 声明（版本 → v1.4） |
| 新工具（G-7） | `tools/node_tool.py`、`tools/registry.py` | `manage_node_file`（`business/write`，`write_mode=overwrite`）：按 `file_no` 解析后调 `NodeService.add/remove_node_file`；SOP-004 §3.2/§3.6 声明 |
| **服务层授权（新）** | `services/node_service.py` | 新增 `_check_node_file_permission(node_id, operator_id, action)`：**新增**＝节点责任人 / L5+ / 节点参与单位人员（与成果提交同口径）；**移除**＝仅 L5+；**fail-closed**（无操作人、用户或节点不存在、权限服务未就绪一律拒绝）。`add_node_file` / `remove_node_file` 内部调用 → console 与 IM 一并受管（D4/D8① 服务层单一判定点） |
| console 同源（G-6） | `api/routes/console_resources.py` | `/console/node-participant` 改调 `handle_manage_node_participant` → 补上 L5+ 判定（原无任何判定） |

**验收（真实库 + 真实用户 UUID）**

- `manage_node_participant`：L4 / 无操作人 → `permission_denied`；L5 → 通过授权进入服务层（返回「节点不存在：NOT-EXIST」，证明授权已过）
- `manage_node_file`：L3 + 不存在的文件 → `file_not_found`（参数与解析链路正常）

---

### 批次④（2026-09-17）：口径一致性 —— G-8 / G-9（其余） / G-10 / G-11 / G-14

| 缺口 | 处理 |
|---|---|
| G-8 | 批次①已落地（`delete_file` 挂 SOP-004 §3.2），本轮复核无残留 |
| G-9（其余 18 个写工具） | 全部补标 `write_mode`：文件维护类 `update_file_category`/`update_file_purpose`/`unlink_attachment`=overwrite、`link_file`/`new_file_version`/`link_to_master`/`embed_and_index`=append；节点类 `create_node`/`add_node_dependency`/`mount_child_node`/`create_task_node`=append、`update_node_progress`/`update_nodes`=overwrite、`acknowledge_nodes`/`discard_nodes`/`submit_node_deliverable`/`confirm_node_deliverable`/`return_node_deliverable`=transition。`registry.py` 注册处与 `TOOL_WRITE_MODE_MAP` 两侧同步；文件维护类在 SOP-004 §3.2/§3.6 **成对声明**（避免能力空窗） |
| G-10 | `create_task_node` 工具层口径 `write`（L3+）→ **`admin`（L5+）**，与承载 SOP-011 准入同口径；SOP-011 §3.3/§3.5 同步（原「其余任务工具 L3+」拆为「创建任务节点 L5+ / 确认·退回·查询 L3+」） |
| G-11 | 复核：`/console/upload` 已走 `FileManager.archive_upload`（同源同痕）。剩余「`file_category` 固定 OTHER、不按 purpose 触发 RAG」本轮补齐：`/console/upload` 新增 `purpose` / `file_category` 表单参数（`FilePurpose.validate` / `FileCategory.validate` 校验），`purpose=REFERENCE` 复用 `_index_reference_file` 异步入库；console 上传区新增「意图 / 分类」两个下拉（`index.html` / `console.js`）。**实测**：`curl -F purpose=REFERENCE -F file_category=MANAGEMENT_SPEC` → 记录 `purpose=REFERENCE, file_category=MANAGEMENT_SPEC`（不再固定 OTHER/RECORD），日志出现 `REFERENCE file scheduled for RAG indexing`；但本次 `rag_indexed` 仍为 `false`（异步链路未落库，疑为存储根解析/embedding 后端环境相关，**与本次改动无关——同一 helper 亦为 IM `record_file` 通道所用**，建议另立小项排查） |
| G-14 | `docs/Manual/权限矩阵.md` 升 V1.2：**0–5 级 → 1–6 级**、补树形继承链、补「以代码/DB 为准」口径声明、企业类型默认级改为"新建时参考值"；9 份 SOP 文档头加「准入判定：以 DB `sop_business_flows.min_level`（/`is_public`）为准」行 |
| 顺带（批次①登记项） | `write_user_memory` schema 上提为模块常量 `_MEMORY_TOOL_SCHEMA` 并登记 `TOOL_SCHEMA_MAP`，V5/V14 的例外分支删除 → 一致性检查 **0 schema 类问题** |

**护栏语义（补标注后的效果）**：标注后这些工具**退出 LLM 自由工具集**（`CapabilityCatalog`：非只读工具须在档位白名单内，而白名单仅 4 个 `record_*`）；兜底档位对 overwrite/delete 一律拒绝（`FallbackPolicy.assert_write_allowed`）；**匹配到 SOP 的路径仍可正常执行**（写入语义不拦 SOP 路径）——已用 IM 实测确认：L5「帮我把文件 FIL-20260916-0001 的分类改成工作记录」→ ✅ 执行成功（`update_file_category`，overwrite 语义，经 SOP-004）。

**验收命令与结果**

```powershell
uv run python scripts/check_fallback_tools.py       # ✅ 问题 0（fatal 0）
uv run python scripts/check_tools_consistency.py    # ✅ fatal 0，V5/V14 = 0（其余为本地无 DB 的 V13 噪音）
# 容器侧：注册 48 工具（base=4, business=25, project=11），orphan=0；tool_registry 同步 43 条（含 4 个新工具）
# 可见性抽检（fetch_available_tools）：
#   L6 → 含 全部 6 个新/改口径工具；L3 → 含 delete_file / update_file_confidentiality / manage_node_file / rag_remove_document，
#        不含 create_task_node、manage_node_participant（G-10 后 L3/L4 不再可见）
```

---

### 批次⑤（2026-09-17）：G-13 行级安全 —— **排查后未按字面实施**，改记机制性缺陷（待决策）

排查过程与结论：

1. 原实现（`permission/row_security.py`）用 `orm_execute_state.select_statement` 取语句 —— SQLAlchemy 2.0 的 `ORMExecuteState` **没有该属性**（只有 `statement`），注入必然抛 `AttributeError`，被 `except` 的 fail-open 分支吞掉 → **该拦截器自建立起从未真正生效**（启动日志里只有 WARNING，且只会出现在"注入失败"分支）。
2. 白名单声明的归属列 `events/tasks/files/messages.company_id` 在库中**并不存在**（实测四表的相关列为 `project_id` / `user_id` / `sender_user_id` / `related_company_ids`）——即便修好 API，也**无可注入之列**。
3. 因此把 fail-open 直接改成 fail-closed，等于**拒绝所有非管理员查询**（系统不可用），不能按 G-13 字面执行。
4. 行的可见性实际由服务层承担：`VisibleFileSetResolver`（文件四路可见集）、`query_service` 的项目/参与关系过滤、能力层 fail-closed 门禁。

本轮落地（最小、**零行为变更**）：`register_row_security_listener()` 增加启动自检 `_probe_filterable_columns()`，命中缺失列即打 **ERROR**（启动日志可见，见下），并在模块头写明现状、原因与去处；原注入逻辑保留待重写。

```
[ERROR] emily.permission.row_security: row_security 白名单列不存在，行级安全拦截器实际不生效（缺口 G-13）：
        events.company_id, files.company_id, messages.company_id, tasks.company_id —— 行可见性由服务层承担
        （VisibleFileSetResolver / query_service / 能力层门禁）；如需真正的行级隔离，须先补归属列或改用
        project_id/user_id 推导并单独立项
```

**待决策（Q9）**：行级隔离是否立项？三选一 —— ① 为四表补 `company_id`（或统一归属列）并由写入侧维护，再以 `with_loader_criteria` 重写拦截器（真行级隔离）；② 明确"行级隔离由服务层承担"并**删除**该拦截器与其 thread-local 上下文（避免"看似有行级安全"的误导）；③ 降级为按 `project_id` 维度的行级过滤（与现有项目归属模型一致，改动面较小）。

---

### 批次⑥（2026-09-18）：Q9 决策落地 —— 删除行级安全拦截器（G-13）+ 恢复工具层数据边界（G-15）

| 变更 | 文件 | 内容 |
|---|---|---|
| 删除拦截器（G-13 / Q9 选项②） | `emily-core/emily_core/permission/row_security.py` | 删除 `do_orm_execute` 钩子、`_FILTERABLE_TABLES` 白名单、thread-local 上下文（`set/get_current_permission_snapshot`、`skip/restore_auth_injection`）、`register_row_security_listener`、`_probe_filterable_columns` 及全部注入辅助函数；**保留 `PermissionAuditLogRepository`**（Core 与 `auth_engine` 仍在使用）；模块头改写为"审计仓储 + 历史说明（为何删 / 行可见性现归属 / 将来重做的前提）" |
| 清理接线 | `emily-core/emily_core/__init__.py`、`permission/__init__.py` | 移除启动注册调用与 5 个已删符号的导入 / `__all__` 导出；初始化日志去掉 `row_security` 字样 |
| 恢复数据边界（G-15） | `emily-core/emily_core/session/session_context.py` | 新增 `build_tool_scope(actor=None)`：由会话权限字段（或当前操作者快照）构建 `_session_scope` = `project_ids` / `db_perms` / `info_level` / `company_type` / `level`（部门维度已移除，不携带） |
| 注入接线（两条路径） | `session/loop.py`、`workitem/langgraph_engine/agent/loop.py` | 两个 `_inject_runtime_params` 均下发 `_session_scope`（会话侧取 `_last_actor` 快照，工单侧取 `BusContext.get_session_context()`） |

**验收（真实库 + 真实用户）**

```powershell
docker exec emily-core python -c "import emily_core, emily_core.permission as p; ..."   # ✅ 导入正常；已删符号归零
docker restart emily-core && docker logs --since 1m emily-core | Select-String "Permission module initialized"
# ✅ 启动仅 "Permission module initialized: service + cache + auth_engine + app"，无 row_security 告警；容器 healthy
# ✅ 注入路径（stub ctx）：_inject_runtime_params({'query_type':'event'}, ctx)._session_scope == {'project_ids':['P1'], ...}
```

- **端到端过滤（张正宏 L3，真实快照）**：`project_ids=['b8d80e88-…']`、`db_perms={'event':'read_write',…}` → `query_data` total **22**（=其参与项目）；把 `project_ids` 换成不存在的 UUID → total **0**（证明过滤已生效，非"恰好全在一个项目"）。
- **IM 实发（console 消息模拟器）**：请求已进入 `SessionLoop`，因容器内 LLM 端点 SSL 证书校验失败返回兜底话术（`CERTIFICATE_VERIFY_FAILED`，**与本改动无关**，属环境网络问题），未取得端到端回复样本。

**Q12 采纳①（同日落地）**：空 `project_ids` 改为**拒绝**（fail-closed，与权限表注1"无企业 / 无参与 → 空集"对齐）——`handle_query_data` 新增判定：调用方下发了 `_session_scope`、且 `level < 5`、且 `project_ids` 为空 → 返回"暂无可查询的项目数据"（`total = 0`）；L5/L6 不受项目范围限制（空集 = 看全部）；未下发 `_session_scope` 的直调 / 后台路径行为不变。

```powershell
# Q12 验收（容器内实测，query_type=event）
# 无 scope              → 22（后台/直调路径不变）
# L3 + 空 project_ids   → {"success": false, "reply": "暂无可查询的项目数据（当前账号未关联任何参建项目）", "total": 0}
# L3 + 真实 project     → 22（= 其参与项目）
# L5 + 空 project_ids   → 22（管理员不受项目范围限制）
```

---

### 批次⑦（2026-09-18）：Q10 决策落地 —— L2 不得维护文件元数据

| 变更 | 文件 | 内容 |
|---|---|---|
| 等级门禁（新增） | `tools/file_tool.py` | 新增 `resolve_operator_level(user_id)` 与 `check_min_level(user_id, min_level, action_label)`（无操作人 / 取不到等级一律拒绝，fail-closed）；常量 `FILE_MAINTENANCE_MIN_LEVEL = 3` |
| 接入 6 个文件维护工具 | 同上 | `update_file_category` / `update_file_purpose` / `new_file_version` / `link_file` / `link_to_master` / `unlink_attachment` 入口统一校验；`handle_delete_file` 内联的等级解析改用同一 helper（行为不变） |
| 接入 RAG 入库 | `tools/embed_tool.py` | `handle_embed_and_index` 新增 `user_id` 形参（缺省回落 `params["_user_id"]`）并加 L3 门禁（与注8「入库 L3+」口径一致） |
| SOP 口径 | `emily-data/sops/SOP-004-FILE-file_archive.md` | §3.6 文件维护行补「调用方须 L3+」及门禁来源；版本 → v1.6 |
| 现状表 / 测试口径 | `Issues/权限矩阵/权限表_fixed.csv`、`test/06_文件与密级管理.md` | 新增「文件元数据维护」行 + 注14；注8 更正为「经 SOP-004 §3.2 声明 + handler L3 门禁」；test/06 补回归口径 |

**验收（真实库 + 真实用户）**

```powershell
# L2（刘大勇 a6fc5d88-…）→ 拒绝；L3（张正宏 f7ef2cdf-…）→ 放行；无操作人 → 拒绝
# ✅ {'success': false, 'reply': '「修改文件分类」需要参建管理(L3)及以上权限', 'error_code': 'permission_denied'}
# ✅ handle_embed_and_index(user_id=L2) → permission_denied；user_id=L3 → 通过门禁进入服务层（chunks is empty）
```

**影响面**：L2 原经 SOP-004 声明可见并可用这 7 个工具（批次④ 副作用），现收敛为「仅 L3+ 可用」；`record_file`（上传归档）不受影响，仍是 L2 能力。

---

### 批次⑧（2026-09-18）：Q11 决策落地 —— 人员/企业三工具接入 IM（选项 a）

| 变更 | 文件 | 内容 |
|---|---|---|
| 工具层口径 | `tools/registry.py`、`infrastructure/tools_consistency.py` | 三者 `permission_flag` 由 `admin`(L5+) → **`write`(L3+)**（工具准入不得低于该能力可操作的最低等级；实际分级仍在服务层） |
| 承载 SOP（新增） | `emily-data/sops/SOP-012-SYS-personnel_org.md` | 准入 **L3**；§3.2 声明 `update_user_level` / `update_user_company` / `manage_company`；§3.4 写明服务层分级（L6 任意 / L5 最高 L4 / L4 最高 L3 / L3 仅低于自身；企业归属 L4+ 任意、L3 限本企业；企业增删 L4+）与 fail-closed / 被拒留痕要求；§3.5 记录"L3 可见 `manage_company` 但调用被拒"的取舍 |
| SOP 播种与准入 | `006_seed_permission_data.sql`、`008_set_sop_min_level.sql` | 006 直接写入 `min_level=3`（不留 NULL 窗口 —— NULL 会被跳级检查、无条件放行）；008 补幂等 UPDATE 与头注口径 |
| 工具表与种子对齐 | `seed_tool_registry.sql` | ① 补齐 9 个缺失/失准行（`update_user_level` / `update_user_company` / `manage_company` / `embed_and_index` / `manage_node_file` / `update_file_confidentiality` / `rag_remove_document` / `acknowledge_nodes` / `manage_node_participant`）② 删除运行库已不存在的 `activate_nodes` ③ 去掉重复行（`update_file_category` / `delete_file`）④ 修正 `create_task_node` 种子值 `write`→`admin`。**结果：种子 44 行 ↔ 运行库活跃非 MCP 工具 44 个，双向差集为空**（此前重建库会缺行、多行、与代码口径不一致） |
| 现状表 | `权限表_fixed.csv` 注9 | 工具层口径由"admin 粗闸"改为"write(L3+) 粗筛 + 承载 SOP-012（准入 L3）" |

**验收（真实库 + 真实用户）**

```
SOP-012 解析 tools = ['update_user_level','update_user_company','manage_company']
L3（张正宏）/ L4（李景利）/ L5（罗永强）：sop_allow 含 SOP-012-SYS → 三工具可见 ✅
L2（刘大勇）：sop_allow 不含 SOP-012-SYS、且 P1 层 `write`(L3+) 不覆盖 → 三工具不可见 ✅（P1 与 SOP 准入双重挡住）

# 两个一致性检查脚本（容器内，可连 DB）
check_tools_consistency.py → 注册工具 44 | 有 schema 44 | 问题 0 (fatal 0)；tool_registry 表 DB 44 条、双向差集 0
check_fallback_tools.py    → basic=3 / advanced_read=9 / advanced_append=4，问题 0 (fatal 0)
# 种子 ↔ 运行库对齐：seed_tool_registry.sql 44 行 ↔ 活跃非 MCP 工具 44 个，双向差集为空
```

> 说明：`get_available`（P1）按 `permission_flag` 放行、不感知 `exposure_mode`，所以口径改为 `write` 后三工具对 L3+ 在级别层即可见；**承载 SOP 的价值在执行通道** —— 三者均为 `sop_only`，不进 LLM 自由工具集，必须匹配到 SOP-012 才能在人工确认后执行。

**顺带修复（`embed_and_index` 可达性，三处断点）**：该工具被 SOP-004 §3.2 声明，但 ① `tool_registry` 表**缺行**（放行合并时按"未注册"忽略）② 内存注册被 `if tei is not None and kc_repo is not None` 包住 —— 本环境 TEI 未就绪 → 工具**根本不存在**（能力静默消失）③ 静态参考表 `REGISTERED_TOOLS` / `TOOL_META_MAP` / `TOOL_SCHEMA_MAP` 均无条目。三处已一并补齐：**改为无条件注册**、就绪性判断下沉到 handler（TEI/仓储缺失 → `service_unavailable`，fail-closed）、补 DB 行与静态表。实测：L3/L4 可见（TEI 未就绪时调用返回 `service_unavailable`）、L2 可见但被 L3 门禁拒（`permission_denied`）；一致性检查由「问题 1」回到「问题 0」。

**遗留观察（未改）**：SOP-000 §3.2 列了 5 个伪工具（`文件写入` / `内容自检` / `IntentRegistry 加载` / `generate_table_schema` / `inspect_existing_tables`），每次装配都会打"未注册工具已 fail-closed"告警 —— 属文档噪音，待清理。

---

### 追加待确认

| # | 待确认 | 说明 |
|---|---|---|
| ~~Q9~~ | **已决（2026-09-18）**：G-13 行级隔离——**选②**（明确"行级隔离由服务层承担"，删除拦截器）。行级隔离本身不立项；如将来确需统一兜底，须先补归属列或改用 `project_id` 推导再单独立项 | 见批次⑤排查 + 批次⑥落地 |
| ~~Q12~~ | **已决（2026-09-18）**：空 `project_ids` 的语义——**选①**（空集即拒绝，fail-closed，与权限表注1"无企业 / 无参与 → 空集"对齐）。落地与验收见批次⑥ |
| ~~Q10~~ | **已决（2026-09-18）**：L2 **不给**维护文件元数据的权限——handler 内加 L3 门禁（`file_tool.check_min_level`，fail-closed），覆盖分类 / 意图 / 版本 / 关联 / 附件链 / 入库 7 个工具；L2 仅可上传（`record_file`）。落地与验收见批次⑦ |
| ~~Q11~~ | **已决（2026-09-18）**：**选 a** —— 新增一份准入 L3 的承载 SOP（`SOP-012-SYS-personnel_org`）声明三个工具；工具层三者由 `admin`(L5+) 下调为 **`write`(L3+)**；实际分级仍由服务层 `PersonnelService` 判定。**已知取舍**：L3 可见 `manage_company` 但调用被服务层拒（企业增删业务口径为 L4+）。落地与验收见批次⑧ |

---

### 环境修复（2026-09-17）：SOP 准入补丁丢失导致访客可见写工具

改「权限表」时核查发现 `sop_business_flows.min_level / is_public` **全为 NULL**（`008_set_sop_min_level.sql` 补丁在当前库中丢失）——按 `008` 头注描述，NULL 会跳过级别检查并无条件放行，使 `sop_allow` 对**所有**已登记用户（含 L1）等于"全部 SOP 清单"，再经 SOP 声明层（P2 并集）把各 SOP 声明的写工具放给访客。

实测（修复前）：L1 用户可见 25 个工具，含 `delete_file` / `rag_remove_document` / `update_file_confidentiality` / `manage_node_file` / `record_*` 等写工具（FR-G10"访客=普惠只读三件套"在当时的库状态下不成立）。

处理：重跑 `008_set_sop_min_level.sql`（幂等 UPDATE）→ 收敛为 SOP-001/002/003/004/007=L2、SOP-005=L1、SOP-000/008/011=L5、SOP-999 公开；重启 emily-core 清权限缓存。

实测（修复后）：L1 `sop_allow={SOP-005, SOP-999}`、可见工具 12 个且**无任何写工具**；L3/L4 可见 33 个（含 SOP-004 声明的 `delete_file` / `update_file_confidentiality` / `manage_node_file` / `update_file_category`，符合设计）；L5/L6 可见 46 个（含全部新工具）。`Issues/权限矩阵/权限表_fixed.csv` 已按修复后的真实状态重写（并补 注10~注13）。

> 提示：该补丁为**手工 SQL**，DB 重建/重置后必须重跑（`006` 播种 → `008` 补准入）。建议后续把它并入初始化流程，避免再次静默失守。

---

## 七、变更记录

| 版本 | 日期 | 内容 |
|---|---|---|
| V1 | 2026-09-17 | 首版：基于代码现状盘点权限与能力可达性缺口，并给出 console ↔ IM 等价性对照 |
| V1.1 | 2026-09-17 | 补入决策记录 D1–D4（密级口径 / 删除通道 / 企业人员管理范围 / 后台视角折中），原待确认项收敛为 Q5–Q7 |
| V1.2 | 2026-09-17 | 补入决策 D5–D7（排期 / 人员企业规则 / 纠错类暂缓 + console 停用配对原则）；新增「六、实施记录」，登记批次①落地（G-12 + delete_file 写语义）与遗留点 Q8 |
| V1.3 | 2026-09-17 | 裁决 D3 与 D6 的准入口径冲突：新增「五-C」决策 D8 / D8-b / D8-c（以 D6 为业务口径、服务层按操作人等级判定、fail-closed、被拒也留痕；企业增删 L4+ 本期实现；能力载体指向人员展示页面需求基线），并修正 D3 行的准入表述 |
| V1.4 | 2026-09-17 | 执行批次②③④并记录：新增工具 `update_file_confidentiality`(G-4) / `rag_remove_document`(G-5) / `manage_node_file`(G-7) / `manage_node_participant`(G-6) 并成对挂 SOP（004/011），console `/rag-delete`、`/node-participant` 改走同一能力；新增服务层判定 `_check_node_file_permission`；G-9 其余 18 个写工具补 `write_mode` 并在 SOP-004 成对声明；G-10 `create_task_node` 口径收为 L5+；G-11 console 上传补 `purpose`/`file_category` 与 REFERENCE 入库；G-14 权限矩阵文档对齐 1–6 级 + 9 份 SOP 头部加准入口径行；`write_user_memory` schema 上提并登记（V5/V14 例外移除）。**批次⑤ G-13 未按字面实施**：查明拦截器自建立起从未生效（白名单列不存在 + `ORMExecuteState` 无 `select_statement`），改为启动 ERROR 告警 + 机制说明，登记 Q9（是否立项）与 Q10（L2 文件元数据维护口径） |
| V1.5 | 2026-09-17 | 按"当前真实状态"重写 `权限表_fixed.csv`（人员/企业类改为已落地的服务层分级、节点类补参与单位维护与 IM 删共享、文件类补软删与密级调整口径、RAG 补出库、新增 注10~注13）；核查中发现 `sop_business_flows.min_level/is_public` 全为 NULL（008 补丁丢失 → 访客亦可见写工具），重跑 `008_set_sop_min_level.sql` 收敛回设计口径并记入「环境修复」；新增 Q11（人员/企业三工具缺 IM 承载 SOP，当前仅 console 可达） |
| V1.6 | 2026-09-18 | **Q9 决策落地（批次⑥）**：选②删除行级安全拦截器（G-13 关闭，`row_security.py` 仅保留 `PermissionAuditLogRepository`，同步清理启动注册与导出）；排查中发现并修复**新缺口 G-15**（工具层 `_session_scope` 无生产者 → `db_perms` / `project_ids` 恒空、`query_data` 实测返回全表），新增 `SessionContext.build_tool_scope()` 并在会话/工单两条注入路径下发；新增 Q12（空 `project_ids` 语义）并**同日采纳①**：空集即拒绝（fail-closed，与权限表注1 对齐）；同步清理 3 处"等 G-13 立项"的陈旧引用 |
| V1.7 | 2026-09-18 | **Q10 决策落地（批次⑦）**：L2 不给维护文件元数据的权限——`file_tool` 新增 `resolve_operator_level` / `check_min_level`（fail-closed）并对分类 / 意图 / 版本 / 关联 / 附件链 6 个工具加 L3 门禁，`embed_and_index` 同步加 L3 门禁（补齐 `user_id` 形参）；SOP-004 §3.6 补口径（v1.6）；权限表现状表新增「文件元数据维护」行 + 注14、更正注8；test/06 补回归口径。剩余待确认仅 Q11（人员/企业三工具的 IM 承载 SOP） |
| V1.8 | 2026-09-18 | **Q11 决策落地（批次⑧，选项 a）**：新增准入 L3 的 `SOP-012-SYS-personnel_org`（声明 `update_user_level` / `update_user_company` / `manage_company`）→ 三工具首次经 IM 可达；工具层口径由 `admin`(L5+) 下调为 `write`(L3+)，实际分级由 `PersonnelService` 判定（L3 可见 `manage_company` 但调用被拒）；006/008 补 SOP 播种与准入（min_level=3）；`seed_tool_registry.sql` 与运行库**全量对齐**（44↔44，补 9 行 / 删 1 行 / 去 2 重复 / 修 1 失准）；顺带修复 `embed_and_index` 的三处断点（表缺行 + 条件注册 + 静态表缺失 → 改无条件注册、就绪性下沉 handler）；权限表注9 同步；两个一致性脚本均 **问题 0（fatal 0）**。**至此 Q9/Q10/Q11/Q12 全部决策落地**（Q8 为已知遗留口径） |
