# 遗留收敛项清单

> 登记范围：业务需求实施过程中，因内核冻结约束（C12）而暂不能收敛、须留待后续专项立项的机制缺口。
> 判定依据：人员管理 PRD `AC-US-09.3`（P0）。本条为唯一当前登记项。

| 编号 | 遗留项 | 现状 | 收敛路径 | 来源 |
|------|--------|------|----------|------|
| LC-001 | **会话拉起提示词装配「两份实现」** | 能力层 `SessionPromptService.generate`（`emily_core/services/session_prompt_service.py`）与内核 `session/loop.py` 内既有装配并存。两者以「同模板（`load_prompt("session_loop")`）+ 同变量（`SessionContext.get_prompt_variables`）+ 同能力目录（`CapabilityCatalog.list_capabilities`）」约束口径一致，暂不复制内核字面量。 | 收敛须将内核私有装配提炼为共享能力，属内核机制形态变更，**须以专项需求基线立项**，不得夹带在业务需求内实施。 | 人员管理 US-09.3（PRD `AC-US-09.3` / 计划 §M4） |

## 登记说明

- **为何不本期收敛**：内核冻结（C12）——`session/loop.py` 属会话主循环，永不因业务功能增长而修改；把内核装配提炼为共享能力属机制升级，须独立立项评审。
- **风险**：两份装配可能随模板/变量/能力目录演进漂移，导致「页面所见」与「实际所用」不一致。本期以 AC-US-07.3 同源判定 + LLM 流量日志取证兜底。
- **收敛时机**：内核机制专项立项（提示词装配唯一化）时，将本条目状态更新为「已收敛」并归档。
