# Emily 用户消息全生命周期流程

> **版本**：V2.0（废弃 PipelineBUS，LangGraph 为唯一执行引擎）
> **日期**：2026-07-28
>
> 本文档描述一条用户消息从 IM（QQ）进站到 Emily 回复出站的完整生命周期。所有环节均以当前真实代码为准。

---

## 一、总览：端到端数据流

```mermaid
flowchart TD
    A["👤 用户在 QQ 发消息"] --> B["NapCat（QQ 协议桥）"]
    B -->|"WebSocket"| C["AstrBot（消息平台）"]
    C -->|"插件调用"| D["emily_agent（薄插件）"]
    D -->|"HTTP POST /api/v1/message/send"| E["emily-core FastAPI :18080"]
    E --> F["EmilyCore.handle_message()"]
    F -->|"SessionPool.route()"| G["SessionAgent.handle()"]
    G -->|"意图识别"| H["LLM 路由（router_model）"]
    H -->|"拆分"| I["1~N 个 WorkItem"]
    I -->|"调度"| J["SessionScheduler._run_one()"]
    J --> K{"workitem_engine?"}
    K -->|"pipeline_bus"| L["PipelineBUS.run()<br/>4 节点顺序总线"]
    K -->|"langgraph"| M["graph.ainvoke()<br/>5 节点 StateGraph<br/>含 error_analysis 纠错"]
    L --> N["WorkItem 执行完毕"]
    M --> N
    N --> O["SessionAgent._synthesize_final_reply()"]
    O --> P["OutboundEventBus"]
    P -->|"SSE 回复事件"| D
    D -->|"AstrBot API"| C
    C -->|"QQ 消息"| A
```

---

## 二、命名体系

| 层级 | 名称 | 说明 |
|------|------|------|
| **整体** | **Session 主线** | 以 conversation_id 为键的会话生命周期，从用户进线到 Session 过期归档 |
| **意图层** | **Intent Routing** | SessionAgent 调用路由 LLM 将用户消息映射到 SOP + 拆分为 WorkItem |
| **执行层** | **WorkItem 执行引擎** | 每个 WorkItem 经 4 节点（pipeline_bus）或 5 节点 StateGraph（langgraph）执行 |
| **合成层** | **Reply Synthesis** | SessionAgent 汇总已完成的 WorkItem 结果，调 LLM 合成为最终回复 |
| **出站层** | **Outbound Delivery** | 回复经 SSE 事件总线推送回 emily_agent → AstrBot → QQ |

> **历史对照**：旧架构（EmyBot M15）叫 "WorkOrder 管道"，现已完全移除。当前唯一路径是 **Session 主线 + WorkItem 执行引擎**。其中执行引擎有两种实现（feature flag 切换），但对外接口统一。

---

## 三、分层详解

### 第 1 层：接入与接管判定

```mermaid
flowchart TD
    A["POST /api/v1/message/send"] --> B["EmilyCore.handle_message()"]
    B --> C{"DomainTakeoverService.decide()"}
    C -->|"不接管"| D["return None → HTTP 204"]
    C -->|"接管"| E{"takeover_mode"}
    E -->|"monitor"| F["群聊：静默收集仅归档<br/>私聊/被@：正常回复"]
    E -->|"collaborate"| G["全部接管"]
    E -->|"managed"| H["强制接管"]
    F --> I{"should_reply?"}
    G --> I
    H --> I
    I -->|"false（静默收集）"| D
    I -->|"true"| J["继续 → 用户绑定"]
```

**关键代码位置**：
- `DomainTakeoverService.decide()` — `emily_core/services/domain_takeover_service.py`
- `EmilyCore.handle_message()` — `emily_core/__init__.py:965`

---

### 第 2 层：用户绑定与消息持久化

```mermaid
flowchart TD
    A["接管判定通过"] --> B{"sender_id 像 UUID？"}
    B -->|"是"| C["直接查 users 表"]
    B -->|"否"| D["UserBindingService<br/>get_or_create_user()"]
    C --> E{"找到用户？"}
    D --> F{"auto_create_user？"}
    F -->|"true"| G["创建用户 → 返回"]
    F -->|"false"| H{"在白名单？"}
    H -->|"是"| G
    H -->|"否"| I["拒绝 → 继续（降级）"]
    E --> J["user_id 确定"]
    G --> J
    I --> J
    J --> K["MessageService.record_message()"]
    K --> L["入站消息持久化到 messages 表"]
    L --> M["附件异步下载（如有）"]
    M --> N["进入 SessionPool 路由"]
```

---

### 第 3 层：SessionPool 路由与会话生命周期

```mermaid
flowchart TD
    A["SessionPool.route(message, user_id)"] --> B{"conversation_id<br/>命中池？"}
    B -->|"命中"| C["取已有 SessionAgent"]
    B -->|"未命中"| D{"池满？"}
    D -->|"是"| E["sweep_expired() 清理过期"]
    D -->|"否"| F["SessionFactory.create()"]
    E --> F
    F --> G["SessionContext.create()<br/>全量知识灌注"]
    G --> H["新 SessionAgent 创建"]
    H --> I["SessionArchive header 写 md"]
    C --> J["async with entry.lock:<br/>→ agent.handle(message)"]
    I --> J
    J --> K["回复出站"]
    K --> L["后台 sweeper（60s）"]
    L --> M{"过期？<br/>hard_limit / task_idle"}
    M -->|"是"| N["agent.archive() → 移除"]
    M -->|"否"| L
```

**过期判定**：
| 条件 | 阈值 | 说明 |
|------|------|------|
| hard_limit | 1800s（30min） | 强制归档 |
| task_idle | 180s（3min） | 无活跃 WorkItem + 队列空 + 空闲超时 |

---

### 第 4 层：SessionAgent 意图识别与 WorkItem 拆分

```mermaid
flowchart TD
    A["SessionAgent.handle()"] --> B["群聊：取当前操作者权限快照"]
    B --> C["群聊：DB 回溯上下文 + 群记忆注入"]
    C --> D["归档：轮次开头写用户消息"]
    D --> E["_handle_impl()"]
    
    E --> F{"短路指令？<br/>/help, /status 等"}
    F -->|"是"| G["直接返回"]
    F -->|"否"| H{"焦点切换？"}
    H -->|"是"| I["clear_focus()"]
    H -->|"否"| J["_recognize_intent()"]
    
    J --> K["渲染 system prompt<br/>（session.md 模板 + 权限变量）"]
    K --> L["LLM chat_messages(json_mode=True)<br/>router_model（v4-flash）"]
    L --> M{"flash 返回正常？"}
    M -->|"data 为空"| N["fallback 主模型"]
    M -->|"正常"| O["解析 intent dict"]
    N --> O
    
    O --> P{"sop_id?"}
    P -->|"null"| Q["fallback WorkItem（通用规划）"]
    P -->|"SYS-confirm"| R["确认/取消待确认事件"]
    P -->|"SOP-XXX"| S{"is_compound?"}
    S -->|"是"| T["拆分为多个 WorkItem<br/>（最多 5 个）"]
    S -->|"否"| U["单个 WorkItem"]
    
    Q --> V["scheduler.enqueue()"]
    R --> V
    T --> V
    U --> V
    V --> W["scheduler.run_all_with_message()"]
```

**关键代码位置**：
- `_recognize_intent()` — `session_agent.py:350`
- `_split_into_workitems()` — `session_agent.py:474`
- `_build_rendered_system_prompt()` — `session_agent.py:204`

---

### 第 5 层：WorkItem 执行引擎（核心）

这是本次 LangGraph 替换的核心变更层。

```mermaid
flowchart TD
    A["SessionScheduler._run_one(wi, message)"] --> B["wi.transition_to(PLANNING)"]
    B --> C["创建 BusContext<br/>（绑定 work_item + message + user_id）"]
    C --> D{"workitem_engine?"}
    
    D -->|"pipeline_bus（旧引擎）"| E["self._bus.run(context)"]
    D -->|"langgraph（新引擎）"| F["self._run_graph(context, graph)"]
    
    subgraph E_BUS["PipelineBUS（顺序 4 节点）"]
        E1["node1: 意图验证+注入"] --> E2["node2: LLM 执行规划"]
        E2 --> E3["node3: 工具执行+验收"]
        E3 --> E4["node4: 成果总结"]
    end
    
    subgraph F_GRAPH["LangGraph StateGraph（5 节点 + 条件边）"]
        F1["wi_node1: 意图验证+注入"] --> F2["wi_node2: LLM 执行规划"]
        F2 --> F3["wi_node3: 工具执行+验收"]
        F3 -->|"成功"| F4["wi_node4: 成果总结"]
        F3 -->|"失败且 replan_count < max_replan"| F5["error_analysis: 错误分析"]
        F5 -->|"param_error / tool_mismatch"| F2
        F5 -->|"transient_failure"| F3
        F5 -->|"permission_denied / permanent_failure"| F_END["END（abort）"]
        F2 -->|"should_abort"| F_END
    end
    
    E --> G{"context.should_abort?"}
    F3 --> G
    F_END --> H["wi.transition_to(FAILED)"]
    
    G -->|"是"| H
    G -->|"否"| I["wi.transition_to(DONE)"]
    H --> J["返回 WorkItem"]
    I --> J
```

**关键代码位置**：
- `SessionScheduler._run_one()` — `scheduler.py:98`
- `PipelineBUS.run()` — `pipeline/bus.py:135`
- `SessionScheduler._run_graph()` — `scheduler.py:167`
- `build_workitem_graph()` — `langgraph_engine/graph.py`

#### 5.1：4 节点职能

| 节点 | 方法 | 职能 | 是否必须 |
|------|------|------|---------|
| node1 | `node1_intent()` | 验证路由决策 + KnowledgeInjector 增量灌注（SOP/工具/表权限） | ✅ |
| node2 | `node2_plan()` | LLM 执行规划：根据 SOP 拆解步骤，选择工具，评估风险等级 | ✅ |
| node3 | `node3_execute()` | 工具循环：逐步骤调用 `tool.handler(params)`，记录 step_results | ✅ |
| node4 | `node4_summary()` | 成果总结：基于 step_results 输出结构化结果供 Session 合成回复 | ❌（非必须） |

#### 5.2：LangGraph 引擎特有：error_analysis 纠错闭环

```mermaid
flowchart TD
    A["node3 执行完毕"] --> B{"有 failed step<br/>且 replan_count < max_replan?"}
    B -->|"否"| C["→ node4（正常结束）"]
    B -->|"是"| D["→ error_analysis 节点"]
    
    D --> E["ErrorAnalyzer.analyze(ctx)"]
    E --> F{"代码预分类"}
    F -->|"output 含权限关键词"| G["permission_denied<br/>→ abort（不调 LLM）"]
    F -->|"tool_name ∈ L3_TOOLS"| H["permanent_failure<br/>→ abort（不调 LLM）"]
    F -->|"不确定"| I["LLM 分析（error_analysis.md prompt）"]
    
    I --> J{"error_type?"}
    J -->|"param_error / tool_mismatch"| K["→ node2 重规划<br/>（注入 replan_hint）"]
    J -->|"transient_failure"| L["→ node3 直接重试"]
    J -->|"missing_info"| M["→ abort + 回复追问用户"]
```

**关键代码位置**：
- `ErrorAnalyzer.analyze()` — `langgraph_engine/error_analysis.py:82`
- `_code_pre_classify()` — `langgraph_engine/error_analysis.py:121`

---

### 第 6 层：回复合成与出站

```mermaid
flowchart TD
    A["所有 WorkItem 执行完毕"] --> B{"有待确认项？"}
    B -->|"是"| C["直接返回确认提示"]
    B -->|"否"| D["SessionAgent._synthesize_final_reply()"]
    
    D --> E["渲染 wi_results（结构化结果文本）"]
    E --> F["加载 session_reply.md prompt"]
    F --> G["LLM 合成（主模型 reasoner）"]
    G --> H{"reply 可用？"}
    H -->|"是"| I["_review_final_reply() 审核"]
    H -->|"否"| J["_fallback_join_results() 规则拼串"]
    I --> K["OutboundEventBus.publish('reply')"]
    J --> K
    K --> L["SSE → emily_agent → AstrBot → QQ"]
    L --> M["ChatArchiveService<br/>出站消息持久化到 messages 表"]
```

**关键代码位置**：
- `_synthesize_final_reply()` — `session_agent.py:605`
- `_review_final_reply()` — `session_agent.py`（M4 审核层）

---

### 第 7 层：Session 归档（全流程旁路）

```mermaid
flowchart TD
    A["Session 创建"] --> B["SessionArchiveWriter.ensure_header()<br/>写入头信息（用户/权限/项目）"]
    B --> C["每轮 turn：写入用户消息"]
    C --> D["写入意图识别结果（sop/confidence）"]
    D --> E["写入 LLM 调用链（model/tokens/耗时）"]
    E --> F["写入 Emily 回复 + 审核标记"]
    F --> G{"Session 过期？"}
    G -->|"是"| H["SessionArchiveWriter.append_footer()<br/>写入尾信息"]
    G -->|"否"| C
    H --> I["归档 md 文件落盘<br/>emily-data/session_archives/"]
```

---

## 四、两条执行路径对比

| 维度 | PipelineBUS（旧引擎） | LangGraph StateGraph（新引擎） |
|------|----------------------|-------------------------------|
| **节点数** | 4（node1→node2→node3→node4） | 5（+ error_analysis） |
| **拓扑** | 单向线性 | 条件图（含回边） |
| **失败处理** | node3 异常 → 直接 FAILED | node3 失败 → error_analysis → 重规划/重试/abort |
| **State** | BusContext（dataclass，在节点间传递） | WorkItemGraphState（纯可序列化 TypedDict）+ contextvars 承载 BusContext |
| **Checkpoint** | 无 | MemorySaver（后续切 PostgresSaver） |
| **Hook** | PipelineBUS 内触发 | HookAdapter 桥接到 graph 节点回调 |
| **启用方式** | `workitem_engine=pipeline_bus` | 环境变量 `EMILY_WORKITEM_ENGINE=langgraph` |

---

## 五、关键对象速查

| 对象 | 生命周期 | 持有者 |
|------|---------|--------|
| `StandardMessage` | 一次消息 | 全链路透传 |
| `SessionAgent` | 一个 conversation_id 从创建到过期 | SessionPool |
| `SessionContext` | 同上 | SessionAgent |
| `WorkItem` | 一次 SOP 执行 | SessionScheduler 队列 |
| `BusContext` | 一个 WorkItem 一次执行 | 节点间共享 / contextvars |
| `WorkItemGraphState` (TypedDict) | graph.ainvoke 期间 | LangGraph runtime |
| `ReplyMessage` | 一条回复 | 短暂（组装后即发） |

---

## 六、文件索引

| 文件 | 角色 |
|------|------|
| `emily-core/api/server.py` | FastAPI app + lifespan |
| `emily-core/api/routes/message.py` | `POST /api/v1/message/send` 消息入口 |
| `emily-core/emily_core/__init__.py` | `EmilyCore.handle_message()` 编排入口 |
| `emily-core/emily_core/adapters/session/session_pool.py` | `SessionPoolManager.route()` 会话路由 |
| `emily-core/emily_core/adapters/session/session_factory.py` | `SessionFactory.create()` 会话创建 |
| `emily-core/emily_core/session/session_agent.py` | `SessionAgent.handle()` 意图识别 + WorkItem 拆分 + 回复合成 |
| `emily-core/emily_core/workitem/scheduler.py` | `SessionScheduler._run_one()` WorkItem 调度 + 引擎切换 |
| `emily-core/emily_core/workitem/pipeline/bus.py` | `PipelineBUS.run()` 旧引擎：4 节点顺序执行 |
| `emily-core/emily_core/workitem/langgraph_engine/graph.py` | `build_workitem_graph()` 新引擎：5 节点 StateGraph |
| `emily-core/emily_core/workitem/langgraph_engine/state.py` | `WorkItemGraphState` (TypedDict) + contextvars 桥接 |
| `emily-core/emily_core/workitem/workitem_agent.py` | 4 个 node_handler 实现（新旧引擎共用） |
| `emily-core/emily_core/workitem/langgraph_engine/error_analysis.py` | ErrorAnalyzer（Self-Reflection 纠错） |
| `emily-data/prompts/session.md` | 意图识别 system prompt 模板 |
| `emily-data/prompts/planner.md` | node2 执行规划 prompt 模板 |
| `emily-data/prompts/error_analysis.md` | error_analysis 节点 prompt 模板 |

---

*本文档基于 2026-07-28 代码现状。执行引擎替换（PipelineBUS → LangGraph StateGraph）通过 `workitem_engine` feature flag 控制，默认 `pipeline_bus`。*
