# 全景节点图 V2 完整测试计划

> **测试范围**：Phase 1-1 至 Phase 2 全部特性
> **测试工具**：emy-test + 数据库直接验证
> **测试文件**：附件13：【雄安青藤小镇书院项目】蓝城伟业项目内控计划节点分解表.xlsx

---

## 一、测试特性清单

| 阶段 | 特性 | 优先级 | 验证方式 |
|------|------|--------|----------|
| Phase 1-1 | 节点CRUD（创建/查询/更新/废弃） | P0 | 对话流 + DB验证 |
| Phase 1-1 | 成果管理（创建/进度更新） | P0 | 对话流 + DB验证 |
| Phase 1-1 | 依赖管理（添加/移除） | P0 | 对话流 + DB验证 |
| Phase 1-1 | 父子节点挂载/移除 | P0 | 对话流 + DB验证 |
| Phase 1-1 | 5张数据表结构完整 | P0 | 直接DB查询 |
| Phase 1-2 | 状态机流转（条件→进行中→完成） | P0 | 对话流 + DB验证 |
| Phase 1-2 | 成果完成度计算 | P1 | 对话流 + DB验证 |
| Phase 1-2 | 父子进度加权汇总 | P1 | 对话流 + DB验证 |
| Phase 1-2 | 循环依赖检测（BFS） | P1 | 对话流 + DB验证 |
| Phase 1-3 | REST API 全部端点 | P1 | 直接HTTP调用 |
| Phase 1-3 | SSE 实时事件推送 | P2 | 端点验证 |
| Phase 1-4 | 权限集成（不同级别用户） | P1 | 对话流 |
| Phase 1-4 | 文件溯源（source_module_id/type） | P1 | DB验证 |
| Phase 1-4 | XLSX批量导入 | P0 | 脚本执行 + DB验证 |
| Phase 1-4 | Markdown导入 | P2 | 脚本执行 + DB验证 |
| Phase 2 | PDF文档智能解析 | P2 | 脚本执行 |
| Phase 2 | DOCX文档智能解析 | P2 | 脚本执行 |
| Phase 2 | LLM口述引导录入 | P1 | 对话流 |

---

## 二、测试环境准备

### 2.1 前置检查

```bash
# 1. 确认容器运行
docker ps | grep emily

# 2. 确认全景节点表存在
docker exec emily-postgres psql -U emily -d emily -c "\dt project_nodes node_dependencies node_deliverables node_accessible_files node_events"

# 3. 导入测试用户数据（如未导入）
docker exec -i emily-postgres psql -U emily -d emily < emily-core/emily_core/infrastructure/database/scripts/002_seed_test_data.sql

# 4. 检查emy-test配置
cat .env | grep EMILY
```

### 2.2 测试数据准备

- **测试项目ID**：`project-xiongan-001`（雄安青藤小镇书院项目）
- **测试用户**：王总（系统管理员）、李经理（建设主管）、张工（参建管理）
- **测试文件**：`需求文件/全景节点图V2/测试文件/附件13：【雄安青藤小镇书院项目】蓝城伟业项目内控计划节点分解表.xlsx`

---

## 三、测试场景设计

### 场景 1：XLSX批量导入（P0）

**测试目的**：验证从Excel批量导入节点、成果、依赖关系

**测试步骤**：
```bash
# 执行导入脚本
docker exec emily-core python scripts/import_nodes_xlsx.py \
    "需求文件/全景节点图V2/测试文件/附件13：【雄安青藤小镇书院项目】蓝城伟业项目内控计划节点分解表.xlsx" \
    --project-id project-xiongan-001 \
    --creator-id user-admin-wang
```

**数据库验证点**：
```sql
-- 验证节点数量
SELECT COUNT(*) as node_count FROM project_nodes 
WHERE project_id = 'project-xiongan-001';
-- 预期：> 0

-- 验证成果数量
SELECT COUNT(*) as deliverable_count FROM node_deliverables 
WHERE node_id IN (SELECT node_id FROM project_nodes WHERE project_id = 'project-xiongan-001');
-- 预期：> 0

-- 验证依赖数量
SELECT COUNT(*) as dependency_count FROM node_dependencies 
WHERE node_id IN (SELECT node_id FROM project_nodes WHERE project_id = 'project-xiongan-001');
-- 预期：>= 0

-- 验证事件记录
SELECT COUNT(*) as event_count FROM node_events 
WHERE node_id IN (SELECT node_id FROM project_nodes WHERE project_id = 'project-xiongan-001');
-- 预期：> 0
```

---

### 场景 2：节点CRUD对话流测试（P0）

**测试目的**：通过自然语言对话验证节点的创建、查询、更新、废弃

**对话流设计**：

| # | 用户（王总） | 预期系统响应 | 数据库验证 |
|---|-------------|-------------|-----------|
| 1 | "创建一个新节点，编号SG-TEST-001，名称'测试节点-施工图设计'，截止到2026年12月31日" | 节点创建成功提示 | `SELECT node_name, status FROM project_nodes WHERE node_id = 'SG-TEST-001'` |
| 2 | "查看节点SG-TEST-001的详细信息" | 返回节点详情（状态/进度/负责人等） | 验证返回数据与DB一致 |
| 3 | "更新节点SG-TEST-001的备注：这是一个测试节点" | 更新成功提示 | `SELECT remark FROM project_nodes WHERE node_id = 'SG-TEST-001'` |
| 4 | "废弃节点SG-TEST-001" | 废弃确认提示 | `SELECT is_discarded FROM project_nodes WHERE node_id = 'SG-TEST-001'` → true |

**emy-test执行代码**：
```python
from emy_test.tester import EmysTester

with EmysTester() as emy:
    # 1. 创建节点
    reply = emy.send_sync(
        "创建一个新节点，编号SG-TEST-001，名称'测试节点-施工图设计'，截止到2026年12月31日",
        sender_id="user-admin-wang",
        sender_name="王总"
    )
    print(f"创建节点响应: {reply.content if reply else '无响应'}")
    
    # 2. 查询节点
    reply = emy.send_sync(
        "查看节点SG-TEST-001的详细信息",
        sender_id="user-admin-wang",
        sender_name="王总"
    )
    print(f"查询节点响应: {reply.content if reply else '无响应'}")
    
    # 3. 更新节点
    reply = emy.send_sync(
        "更新节点SG-TEST-001的备注：这是一个测试节点",
        sender_id="user-admin-wang",
        sender_name="王总"
    )
    print(f"更新节点响应: {reply.content if reply else '无响应'}")
    
    # 4. 废弃节点
    reply = emy.send_sync(
        "废弃节点SG-TEST-001",
        sender_id="user-admin-wang",
        sender_name="王总"
    )
    print(f"废弃节点响应: {reply.content if reply else '无响应'}")
```

---

### 场景 3：成果管理 + 状态流转（P0）

**测试目的**：验证成果创建、进度更新触发状态机自动流转

**状态流转预期**：
```
CONDITIONS_NOT_MET → (依赖满足 + 有成果) → IN_PROGRESS → (所有必需成果100%) → COMPLETED
```

**对话流设计**：

| # | 用户（王总） | 预期系统响应 | 数据库验证 |
|---|-------------|-------------|-----------|
| 1 | "创建节点SG-TEST-002，名称'测试节点-土方工程'，截止2026-06-30" | 创建成功 | status=CONDITIONS_NOT_MET |
| 2 | "给节点SG-TEST-002添加成果：土方开挖方案，目标1份" | 成果创建成功 | `SELECT * FROM node_deliverables WHERE node_id = 'SG-TEST-002'` |
| 3 | "更新成果SG-TEST-002-DELV-001的进度：完成1份" | 进度更新成功 + 状态流转提示 | `SELECT status FROM project_nodes WHERE node_id = 'SG-TEST-002'` → COMPLETED |
| 4 | "查看节点SG-TEST-002的当前状态和进度" | 显示：已完成，进度100% | 验证一致性 |

**emy-test执行代码**：
```python
from emy_test.tester import EmysTester

with EmysTester() as emy:
    # 1. 创建节点
    reply = emy.send_sync(
        "创建节点SG-TEST-002，名称'测试节点-土方工程'，截止2026-06-30",
        sender_id="user-admin-wang",
        sender_name="王总"
    )
    print(f"1. 创建节点: {reply.content if reply else '无响应'}")
    
    # 2. 添加成果
    reply = emy.send_sync(
        "给节点SG-TEST-002添加成果：土方开挖方案，目标1份",
        sender_id="user-admin-wang",
        sender_name="王总"
    )
    print(f"2. 添加成果: {reply.content if reply else '无响应'}")
    
    # 3. 更新成果进度（触发状态流转）
    reply = emy.send_sync(
        "更新节点SG-TEST-002的成果'土方开挖方案'进度：完成1份",
        sender_id="user-admin-wang",
        sender_name="王总"
    )
    print(f"3. 更新进度: {reply.content if reply else '无响应'}")
    
    # 4. 查看状态
    reply = emy.send_sync(
        "查看节点SG-TEST-002的当前状态和进度",
        sender_id="user-admin-wang",
        sender_name="王总"
    )
    print(f"4. 状态查询: {reply.content if reply else '无响应'}")
```

**数据库验证SQL**：
```sql
-- 验证状态流转记录
SELECT event_type, old_value, new_value, created_at 
FROM node_events 
WHERE node_id = 'SG-TEST-002' 
ORDER BY created_at;

-- 验证成果完成度
SELECT deliverable_name, target_amount, current_amount, is_required, completed_at
FROM node_deliverables 
WHERE node_id = 'SG-TEST-002';
```

---

### 场景 4：依赖管理 + 阻塞机制（P0）

**测试目的**：验证前置依赖配置、权重计算、阻塞机制

**对话流设计**：

| # | 用户（王总） | 预期系统响应 | 数据库验证 |
|---|-------------|-------------|-----------|
| 1 | "创建节点SG-TEST-003-A，名称'前置节点-地质勘察'，成果：勘察报告1份" | 创建成功 | |
| 2 | "创建节点SG-TEST-003-B，名称'下游节点-基础设计'" | 创建成功 | status=CONDITIONS_NOT_MET |
| 3 | "节点SG-TEST-003-B依赖节点SG-TEST-003-A的勘察报告成果" | 依赖添加成功 | `SELECT * FROM node_dependencies WHERE node_id = 'SG-TEST-003-B'` |
| 4 | "查看节点SG-TEST-003-B的前置条件满足情况" | 显示：不满足（需要勘察报告） | |
| 5 | "完成节点SG-TEST-003-A的勘察报告成果" | 上游完成 + 下游状态更新提示 | |
| 6 | "再次查看节点SG-TEST-003-B的状态" | 显示：IN_PROGRESS（前置已满足） | status=IN_PROGRESS |

**阻塞机制验证**：
```sql
-- 添加权重=999的阻塞依赖
INSERT INTO node_dependencies (node_id, depends_on_deliverable_id, weight, dependency_type, created_at)
VALUES ('SG-TEST-003-B', 'BLOCKING-DELV-001', '999.0000', 'TIME', NOW());

-- 验证状态回退
SELECT status FROM project_nodes WHERE node_id = 'SG-TEST-003-B';
-- 预期：CONDITIONS_NOT_MET（因为有未满足的权重=999依赖）
```

---

### 场景 5：父子节点 + 进度汇总（P1）

**测试目的**：验证父子层级挂载、子节点进度向上汇总

**对话流设计**：

| # | 用户（王总） | 预期系统响应 | 数据库验证 |
|---|-------------|-------------|-----------|
| 1 | "创建父节点SG-PARENT-001，名称'一期工程'" | 创建成功 | |
| 2 | "创建子节点SG-CHILD-001，名称'地基工程'，权重0.4" | 创建成功 | |
| 3 | "创建子节点SG-CHILD-002，名称'主体结构'，权重0.6" | 创建成功 | |
| 4 | "将SG-CHILD-001挂载为SG-PARENT-001的子节点" | 挂载成功 | `SELECT parent_node_id FROM project_nodes WHERE node_id = 'SG-CHILD-001'` |
| 5 | "将SG-CHILD-002挂载为SG-PARENT-001的子节点" | 挂载成功 | |
| 6 | "完成子节点SG-CHILD-001的所有成果" | 子节点完成 + 父节点进度更新 | |
| 7 | "查看父节点SG-PARENT-001的整体进度" | 显示40%（0.4×100% + 0.6×0%） | `SELECT progress FROM project_nodes WHERE node_id = 'SG-PARENT-001'` |
| 8 | "完成子节点SG-CHILD-002的所有成果" | 子节点完成 + 父节点状态更新 | |
| 9 | "再次查看父节点进度" | 显示100%，状态COMPLETED | |

**emy-test执行代码**：
```python
from emy_test.tester import EmysTester

with EmysTester() as emy:
    # 创建父节点
    emy.send_sync("创建父节点SG-PARENT-001，名称'一期工程'", sender_name="王总")
    
    # 创建子节点
    emy.send_sync("创建子节点SG-CHILD-001，名称'地基工程'，权重0.4", sender_name="王总")
    emy.send_sync("创建子节点SG-CHILD-002，名称'主体结构'，权重0.6", sender_name="王总")
    
    # 挂载子节点
    emy.send_sync("将SG-CHILD-001挂载为SG-PARENT-001的子节点，权重0.4", sender_name="王总")
    emy.send_sync("将SG-CHILD-002挂载为SG-PARENT-001的子节点，权重0.6", sender_name="王总")
    
    # 给子节点添加成果并完成
    emy.send_sync("给SG-CHILD-001添加成果：地基验收报告，目标1份", sender_name="王总")
    emy.send_sync("完成SG-CHILD-001的地基验收报告", sender_name="王总")
    
    # 查看父节点进度（预期40%）
    reply = emy.send_sync("查看父节点SG-PARENT-001的整体进度", sender_name="王总")
    print(f"父节点进度（子1完成后）: {reply.content if reply else '无响应'}")
    
    # 完成第二个子节点
    emy.send_sync("给SG-CHILD-002添加成果：主体验收报告，目标1份", sender_name="王总")
    emy.send_sync("完成SG-CHILD-002的主体验收报告", sender_name="王总")
    
    # 再次查看父节点进度（预期100%）
    reply = emy.send_sync("再次查看父节点SG-PARENT-001的进度和状态", sender_name="王总")
    print(f"父节点进度（全部完成后）: {reply.content if reply else '无响应'}")
```

---

### 场景 6：循环依赖检测（P1）

**测试目的**：验证BFS循环检测能阻止非法依赖

**测试步骤**：

| # | 操作 | 预期结果 | 验证 |
|---|------|---------|------|
| 1 | 创建A → B → C 链式依赖 | 成功 | 3个节点正常 |
| 2 | 尝试添加 C → A 的依赖 | 被拒绝，提示"检测到循环依赖" | 依赖表无新增记录 |
| 3 | 尝试添加 A 的父节点为 C | 被拒绝，提示"检测到父子循环" | parent_node_id 未改变 |

**emy-test执行代码**：
```python
from emy_test.tester import EmysTester

with EmysTester() as emy:
    # 创建三个节点
    emy.send_sync("创建节点SG-CYCLE-A，名称'节点A'", sender_name="王总")
    emy.send_sync("创建节点SG-CYCLE-B，名称'节点B'", sender_name="王总")
    emy.send_sync("创建节点SG-CYCLE-C，名称'节点C'", sender_name="王总")
    
    # 给每个节点添加成果
    emy.send_sync("给SG-CYCLE-A添加成果：成果A，目标1份", sender_name="王总")
    emy.send_sync("给SG-CYCLE-B添加成果：成果B，目标1份", sender_name="王总")
    emy.send_sync("给SG-CYCLE-C添加成果：成果C，目标1份", sender_name="王总")
    
    # 添加正常依赖链
    emy.send_sync("节点SG-CYCLE-B依赖SG-CYCLE-A的成果", sender_name="王总")
    emy.send_sync("节点SG-CYCLE-C依赖SG-CYCLE-B的成果", sender_name="王总")
    
    # 尝试添加循环依赖（应该被拒绝）
    reply = emy.send_sync(
        "节点SG-CYCLE-A依赖SG-CYCLE-C的成果",
        sender_name="王总"
    )
    print(f"循环依赖尝试响应: {reply.content if reply else '无响应'}")
    assert "循环" in (reply.content if reply else "") or "检测到" in (reply.content if reply else ""), "循环依赖检测失败"
```

---

### 场景 7：权限集成测试（P1）

**测试目的**：验证不同权限级别用户对节点的操作权限

**测试用户矩阵**：

| 用户 | 权限级别 | 预期可执行操作 | 预期被拒绝操作 |
|------|----------|---------------|---------------|
| 王总 | 系统管理员(6) | 全部操作 | 无 |
| 李经理 | 建设主管(4) | 节点创建/编辑/成果更新 | 废弃关键节点 |
| 张工 | 参建管理(3) | 更新自己负责节点的成果 | 编辑节点基本信息/废弃节点 |
| 孙师傅 | 参建执行(2) | 上报任务进度 | 创建/编辑节点 |
| 周业务员 | 访客(1) | 仅查询公开节点 | 所有写入操作 |

**对话流设计（以张工为例）**：

| # | 用户（张工） | 操作 | 预期结果 |
|---|-------------|------|---------|
| 1 | "更新节点SG-TEST-002的成果进度：完成0.5份" | 写入 | ✅ 成功（权限内操作） |
| 2 | "修改节点SG-TEST-002的名称为'非法修改'" | 写入 | ❌ 被拒绝，提示"无权限编辑该节点" |
| 3 | "查看节点SG-TEST-002的状态" | 读取 | ✅ 成功 |

**emy-test执行代码**：
```python
from emy_test.tester import EmysTester

with EmysTester() as emy:
    # 张工（参建管理）尝试更新成果（应该成功）
    reply = emy.send_sync(
        "更新节点SG-TEST-002的成果'土方开挖方案'进度：完成0.5份",
        sender_id="user-zhang",
        sender_name="张工"
    )
    print(f"张工更新进度: {reply.content if reply else '无响应'}")
    
    # 张工尝试修改节点名称（应该被拒绝）
    reply = emy.send_sync(
        "修改节点SG-TEST-002的名称为'非法修改'",
        sender_id="user-zhang",
        sender_name="张工"
    )
    print(f"张工尝试改名称: {reply.content if reply else '无响应'}")
    assert "权限" in (reply.content if reply else "") or "拒绝" in (reply.content if reply else ""), "权限控制失败"
    
    # 周业务员（访客）尝试创建节点（应该被拒绝）
    reply = emy.send_sync(
        "创建节点SG-HACK-001，名称'我是黑客'",
        sender_id="user-sales",
        sender_name="周业务员"
    )
    print(f"周业务员尝试创建: {reply.content if reply else '无响应'}")
    assert "权限" in (reply.content if reply else "") or "拒绝" in (reply.content if reply else ""), "访客权限控制失败"
```

---

### 场景 8：文件溯源测试（P1）

**测试目的**：验证上传的文件能正确溯源到节点

**测试步骤**：
1. 用户上传文件作为节点的启动文档
2. 验证 `files.source_module_id` = 节点ID
3. 验证 `files.source_module_type` = 'NODE_STARTUP_DOC'

**数据库验证SQL**：
```sql
-- 查询文件溯源
SELECT f.id, f.file_name, f.source_module_id, f.source_module_type,
       p.node_name as source_node_name
FROM files f
LEFT JOIN project_nodes p ON f.source_module_id = p.node_id
WHERE f.source_module_id LIKE 'SG-TEST-%'
ORDER BY f.created_at DESC;
```

---

### 场景 9：LLM口述引导录入（P1）

**测试目的**：验证自然语言对话式节点录入

**对话流设计**：

| # | 用户 | 输入 | 预期系统响应 |
|---|------|------|-------------|
| 1 | 王总 | "我要录入一个新的项目节点" | 引导提问："好的，请告诉我节点编号和名称？" |
| 2 | 王总 | "节点编号是SG-VOICE-001，名称是景观设计" | 继续追问："好的，截止日期是什么时候？主责部门是？" |
| 3 | 王总 | "截止到2026年8月31日，主责是设计部" | 继续追问："需要定义什么成果物吗？" |
| 4 | 王总 | "景观施工图1份，苗木清单1份" | 确认信息："我将创建以下节点...确认创建吗？" |
| 5 | 王总 | "确认创建" | 创建成功提示 |

**emy-test执行代码**：
```python
from emy_test.tester import EmysTester

with EmysTester() as emy:
    # 步骤1：发起录入
    reply = emy.send_sync("我要录入一个新的项目节点", sender_name="王总")
    print(f"步骤1响应: {reply.content if reply else '无响应'}")
    
    # 步骤2：提供编号和名称
    reply = emy.send_sync("节点编号是SG-VOICE-001，名称是景观设计", sender_name="王总")
    print(f"步骤2响应: {reply.content if reply else '无响应'}")
    
    # 步骤3：提供截止日期和部门
    reply = emy.send_sync("截止到2026年8月31日，主责是设计部", sender_name="王总")
    print(f"步骤3响应: {reply.content if reply else '无响应'}")
    
    # 步骤4：提供成果
    reply = emy.send_sync("景观施工图1份，苗木清单1份", sender_name="王总")
    print(f"步骤4响应: {reply.content if reply else '无响应'}")
    
    # 步骤5：确认创建
    reply = emy.send_sync("确认创建", sender_name="王总")
    print(f"步骤5响应: {reply.content if reply else '无响应'}")
```

---

### 场景 10：Markdown导入测试（P2）

**测试目的**：验证结构化MD文档解析创建节点

**测试MD文件内容**（创建临时文件）：
```markdown
# SG-MD-001 一期工程总体策划

## 成果
- 项目总体策划书 1份（必需）
- 风险评估报告 1份

## 依赖
- SG-UPSTREAM-DELV-001（前置可研报告）

## 子节点
- SG-MD-001-01 设计招标
- SG-MD-001-02 施工招标
```

**执行命令**：
```bash
docker exec emily-core python scripts/import_nodes_md.py \
    /tmp/test_node.md \
    --project-id project-xiongan-001 \
    --creator-id user-admin-wang
```

---

### 场景 11：REST API端点测试（P1）

**测试端点清单**：

| 方法 | 端点 | 测试用例 |
|------|------|---------|
| POST | `/api/v1/project-nodes` | 创建新节点 |
| GET | `/api/v1/project-nodes/{node_id}` | 查询节点详情 |
| PATCH | `/api/v1/project-nodes/{node_id}` | 更新节点 |
| DELETE | `/api/v1/project-nodes/{node_id}` | 废弃节点 |
| POST | `/api/v1/project-nodes/{node_id}/deliverables` | 添加成果 |
| PATCH | `/api/v1/node-deliverables/{deliverable_id}` | 更新成果进度 |
| POST | `/api/v1/project-nodes/{node_id}/dependencies` | 添加依赖 |
| DELETE | `/api/v1/node-dependencies/{dependency_id}` | 移除依赖 |
| POST | `/api/v1/project-nodes/{parent_id}/children` | 挂载子节点 |
| GET | `/api/v1/events/nodes` | SSE事件订阅 |

**测试curl示例**：
```bash
# 创建节点
curl -X POST http://localhost:18080/api/v1/project-nodes \
  -H "Content-Type: application/json" \
  -d '{
    "project_id": "project-xiongan-001",
    "node_id": "SG-API-001",
    "node_name": "API测试节点",
    "deadline": "2026-12-31T23:59:59+08:00",
    "creator_id": "user-admin-wang"
  }'

# 查询节点
curl http://localhost:18080/api/v1/project-nodes/SG-API-001
```

---

## 四、测试执行脚本

### 4.1 完整测试套件执行脚本

```python
#!/usr/bin/env python3
"""全景节点图V2完整测试套件"""

import asyncio
import sys
from pathlib import Path

# 添加emy-test路径
sys.path.insert(0, str(Path(__file__).parent / ".claude/skills/emy-test"))

from tester import EmysTester


class PanoramaTestSuite:
    """全景节点图V2测试套件"""
    
    def __init__(self):
        self.results = []
        self.project_id = "project-xiongan-001"
        
    def log(self, test_name: str, status: str, message: str = ""):
        """记录测试结果"""
        self.results.append({
            "test_name": test_name,
            "status": status,  # PASS / FAIL / SKIP
            "message": message
        })
        icon = "✅" if status == "PASS" else "❌" if status == "FAIL" else "⏭️"
        print(f"{icon} {test_name}: {status} - {message}")
    
    async def test_scenario_1_xlsx_import(self):
        """场景1：XLSX批量导入"""
        print("\n" + "="*60)
        print("场景1：XLSX批量导入测试")
        print("="*60)
        
        # 注意：此测试需要在容器内执行导入脚本
        # 此处仅验证导入后的数据
        with EmysTester() as emy:
            # 查询导入后的节点数量
            with emy.get_db_session() as conn:
                from sqlalchemy import text
                result = conn.execute(text(
                    "SELECT COUNT(*) as cnt FROM project_nodes "
                    "WHERE project_id = :pid"
                ), {"pid": self.project_id})
                count = result.fetchone()[0]
                
            if count > 0:
                self.log("XLSX导入-节点数量", "PASS", f"成功导入{count}个节点")
            else:
                self.log("XLSX导入-节点数量", "FAIL", "未导入任何节点")
    
    async def test_scenario_2_node_crud(self):
        """场景2：节点CRUD对话流测试"""
        print("\n" + "="*60)
        print("场景2：节点CRUD对话流测试")
        print("="*60)
        
        with EmysTester() as emy:
            node_id = "SG-TEST-CRUD-001"
            
            # 1. 创建节点
            reply = emy.send_sync(
                f"创建节点{node_id}，名称'CRUD测试节点'，截止2026-12-31",
                sender_name="王总"
            )
            created = reply and ("成功" in reply.content or "创建" in reply.content)
            
            # 验证DB
            with emy.get_db_session() as conn:
                from sqlalchemy import text
                result = conn.execute(text(
                    "SELECT node_name, status FROM project_nodes WHERE node_id = :nid"
                ), {"nid": node_id})
                row = result.fetchone()
            
            db_ok = row and row[0] == "CRUD测试节点"
            
            if created and db_ok:
                self.log("节点CRUD-创建", "PASS", "节点创建成功，DB验证通过")
            else:
                self.log("节点CRUD-创建", "FAIL", f"创建={created}, DB={db_ok}")
    
    async def test_scenario_3_state_machine(self):
        """场景3：状态机流转测试"""
        print("\n" + "="*60)
        print("场景3：状态机流转测试")
        print("="*60)
        
        with EmysTester() as emy:
            node_id = "SG-TEST-STATE-001"
            
            # 创建节点
            emy.send_sync(f"创建节点{node_id}，名称'状态机测试节点'", sender_name="王总")
            
            # 添加成果
            emy.send_sync(f"给{node_id}添加成果：测试成果，目标1份", sender_name="王总")
            
            # 完成成果（触发状态流转）
            emy.send_sync(f"完成{node_id}的测试成果", sender_name="王总")
            
            # 验证状态
            with emy.get_db_session() as conn:
                from sqlalchemy import text
                result = conn.execute(text(
                    "SELECT status, progress FROM project_nodes WHERE node_id = :nid"
                ), {"nid": node_id})
                row = result.fetchone()
            
            if row and row[0] == "COMPLETED" and float(row[1]) >= 100.0:
                self.log("状态机流转", "PASS", f"状态={row[0]}, 进度={row[1]}%")
            else:
                self.log("状态机流转", "FAIL", f"状态={row[0] if row else None}, 进度={row[1] if row else None}")
    
    async def run_all(self):
        """运行所有测试"""
        print("🚀 开始全景节点图V2完整测试套件")
        print(f"测试项目ID: {self.project_id}")
        print(f"开始时间: {asyncio.get_event_loop().time():.0f}")
        
        # 运行各个场景
        await self.test_scenario_1_xlsx_import()
        await self.test_scenario_2_node_crud()
        await self.test_scenario_3_state_machine()
        # ... 可扩展更多场景
        
        # 输出汇总
        print("\n" + "="*60)
        print("测试汇总")
        print("="*60)
        
        total = len(self.results)
        passed = sum(1 for r in self.results if r["status"] == "PASS")
        failed = sum(1 for r in self.results if r["status"] == "FAIL")
        
        print(f"总测试数: {total}")
        print(f"通过: {passed} ✅")
        print(f"失败: {failed} ❌")
        print(f"通过率: {passed/total*100:.1f}%" if total > 0 else "无测试")
        
        if failed > 0:
            print("\n失败的测试:")
            for r in self.results:
                if r["status"] == "FAIL":
                    print(f"  - {r['test_name']}: {r['message']}")
        
        return failed == 0


if __name__ == "__main__":
    suite = PanoramaTestSuite()
    success = asyncio.run(suite.run_all())
    sys.exit(0 if success else 1)
```

---

## 五、数据库验证检查清单

执行所有测试后，运行以下SQL验证数据完整性：

```sql
-- ========================================
-- 全景节点图V2数据完整性检查
-- ========================================

-- 1. 节点完整性
SELECT 
    status,
    COUNT(*) as count,
    ROUND(COUNT(*)*100.0/SUM(COUNT(*))OVER(), 1) as pct
FROM project_nodes
WHERE project_id = 'project-xiongan-001'
GROUP BY status
ORDER BY status;

-- 2. 成果完成率
SELECT 
    n.node_id,
    n.node_name,
    COUNT(d.id) as total_deliverables,
    SUM(CASE WHEN CAST(d.current_amount AS DECIMAL) >= CAST(d.target_amount AS DECIMAL) 
             THEN 1 ELSE 0 END) as completed_deliverables,
    ROUND(SUM(CASE WHEN CAST(d.current_amount AS DECIMAL) >= CAST(d.target_amount AS DECIMAL) 
                   THEN 1 ELSE 0 END)*100.0/COUNT(d.id), 1) as completion_rate
FROM project_nodes n
LEFT JOIN node_deliverables d ON n.node_id = d.node_id
WHERE n.project_id = 'project-xiongan-001'
GROUP BY n.node_id, n.node_name
ORDER BY completion_rate DESC;

-- 3. 依赖关系图
SELECT 
    node_id,
    COUNT(*) as dependency_count,
    MIN(weight) as min_weight,
    MAX(weight) as max_weight,
    BOOL_OR(CAST(weight AS DECIMAL) >= 999) as has_blocking_dep
FROM node_dependencies
WHERE node_id IN (SELECT node_id FROM project_nodes WHERE project_id = 'project-xiongan-001')
GROUP BY node_id
ORDER BY dependency_count DESC;

-- 4. 事件审计日志
SELECT 
    event_type,
    COUNT(*) as count,
    MIN(created_at) as first_event,
    MAX(created_at) as last_event
FROM node_events
WHERE node_id IN (SELECT node_id FROM project_nodes WHERE project_id = 'project-xiongan-001')
GROUP BY event_type
ORDER BY count DESC;

-- 5. 父子层级结构
WITH RECURSIVE node_tree AS (
    SELECT node_id, node_name, parent_node_id, 0 as level,
           ARRAY[node_id] as path
    FROM project_nodes
    WHERE project_id = 'project-xiongan-001' AND parent_node_id = ''
    
    UNION ALL
    
    SELECT n.node_id, n.node_name, n.parent_node_id, nt.level + 1,
           nt.path || n.node_id
    FROM project_nodes n
    JOIN node_tree nt ON n.parent_node_id = nt.node_id
    WHERE n.project_id = 'project-xiongan-001'
      AND n.node_id != ALL(nt.path)  -- 防止循环
)
SELECT 
    LPAD('', level*2, ' ') || node_id as indented_id,
    node_name,
    level,
    ARRAY_TO_STRING(path, ' → ') as full_path
FROM node_tree
ORDER BY path;

-- 6. 检测潜在的数据不一致
-- a. 状态为COMPLETED但存在未完成的必需成果
SELECT 
    n.node_id,
    n.node_name,
    n.status,
    COUNT(*) as incomplete_required_deliverables
FROM project_nodes n
JOIN node_deliverables d ON n.node_id = d.node_id
WHERE n.project_id = 'project-xiongan-001'
  AND n.status = 'COMPLETED'
  AND d.is_required = true
  AND CAST(d.current_amount AS DECIMAL) < CAST(d.target_amount AS DECIMAL)
GROUP BY n.node_id, n.node_name, n.status
HAVING COUNT(*) > 0;

-- b. 进度计算不一致（父节点进度≠子节点加权平均）
SELECT 
    parent.node_id as parent_id,
    parent.node_name as parent_name,
    parent.progress as parent_progress,
    ROUND(SUM(CAST(child.progress AS DECIMAL) * CAST(child.child_weight AS DECIMAL)) / 
          NULLIF(SUM(CAST(child.child_weight AS DECIMAL)), 0), 2) as calculated_progress,
    ABS(CAST(parent.progress AS DECIMAL) - 
        ROUND(SUM(CAST(child.progress AS DECIMAL) * CAST(child.child_weight AS DECIMAL)) / 
              NULLIF(SUM(CAST(child.child_weight AS DECIMAL)), 0), 2)) as diff
FROM project_nodes parent
JOIN project_nodes child ON parent.node_id = child.parent_node_id
WHERE parent.project_id = 'project-xiongan-001'
GROUP BY parent.node_id, parent.node_name, parent.progress
HAVING ABS(CAST(parent.progress AS DECIMAL) - 
           ROUND(SUM(CAST(child.progress AS DECIMAL) * CAST(child.child_weight AS DECIMAL)) / 
                 NULLIF(SUM(CAST(child.child_weight AS DECIMAL)), 0), 2)) > 0.1;
```

---

## 六、测试报告模板

```markdown
# 全景节点图 V2 测试报告

> **测试日期**：YYYY-MM-DD
> **测试环境**：emily-core (Docker) + PostgreSQL
> **测试人员**：[姓名]

## 测试概览

| 指标 | 数值 |
|------|------|
| 总测试场景数 | 11 |
| 通过 | XX |
| 失败 | XX |
| 通过率 | XX% |
| 总执行时间 | XX分钟 |

## 各场景测试结果

### Phase 1-1: 数据层基础
- [ ] 场景1：XLSX批量导入 - ✅/❌
- [ ] 场景2：节点CRUD - ✅/❌

### Phase 1-2: 状态机引擎
- [ ] 场景3：状态机流转 - ✅/❌
- [ ] 场景4：依赖管理 + 阻塞 - ✅/❌
- [ ] 场景5：父子节点 + 进度汇总 - ✅/❌
- [ ] 场景6：循环依赖检测 - ✅/❌

### Phase 1-3: REST API
- [ ] 场景10：REST API端点 - ✅/❌

### Phase 1-4: 权限集成
- [ ] 场景7：权限集成测试 - ✅/❌
- [ ] 场景8：文件溯源测试 - ✅/❌

### Phase 2: 文档解析
- [ ] 场景9：口述引导录入 - ✅/❌
- [ ] 场景11：Markdown导入 - ✅/❌

## 发现的问题

| # | 问题描述 | 严重程度 | 状态 |
|---|---------|---------|------|
| 1 | ... | P0/P1/P2 | 新建/已修复 |

## 结论

[总体评估：通过/有条件通过/不通过]
```

---

## 七、清理测试数据

测试完成后，执行以下SQL清理测试数据：

```sql
-- 清理测试节点（保留真实导入的业务数据）
DELETE FROM node_events 
WHERE node_id LIKE 'SG-TEST-%' OR node_id LIKE 'SG-%-TEST-%';

DELETE FROM node_dependencies 
WHERE node_id LIKE 'SG-TEST-%' OR depends_on_node_id LIKE 'SG-TEST-%';

DELETE FROM node_deliverables 
WHERE node_id LIKE 'SG-TEST-%';

DELETE FROM node_accessible_files 
WHERE node_id LIKE 'SG-TEST-%';

DELETE FROM project_nodes 
WHERE node_id LIKE 'SG-TEST-%';

-- 验证清理结果
SELECT 'project_nodes' as table_name, COUNT(*) as count FROM project_nodes WHERE node_id LIKE 'SG-TEST-%'
UNION ALL
SELECT 'node_deliverables', COUNT(*) FROM node_deliverables WHERE node_id LIKE 'SG-TEST-%'
UNION ALL
SELECT 'node_dependencies', COUNT(*) FROM node_dependencies WHERE node_id LIKE 'SG-TEST-%'
UNION ALL
SELECT 'node_events', COUNT(*) FROM node_events WHERE node_id LIKE 'SG-TEST-%';
```

---

**文档完成时间**：2026-06-30
**版本**：v1.0
