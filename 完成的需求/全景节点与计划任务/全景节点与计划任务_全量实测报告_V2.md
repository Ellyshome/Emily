# 全景节点与计划任务 - 全量实测报告 V2

**测试日期**: 2026-07-09  
**测试环境**: Docker Compose (emily-core + emily-postgres)  
**测试人员**: 自动化全量测试

---

## 一、部署与修复汇总

### 1.1 数据库重置
- 问题：`create_all()` 重建全量表导致数据清空
- 解决：手动插入测试数据（1 company, 1 project, 5 users）

### 1.2 运行时发现并修复的 Bug

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| B7 | `api/routes/node.py:199` | `ConfirmNodeDeliverableCommand` 无 `reason` 参数但路由传入 `reason=body.reason` | 移除路由中的 `reason` 传参 |
| B8 | `services/node_service.py:885` | `_check_submission_permission()` 是同步方法但被 `await` 调用，导致 `TypeError` | 移除 `await` |
| B9 | `api/routes/node.py` | `/my-tasks` 路由在 `/{node_id}` 之后注册，被抢先匹配为 404 | 将 `/my-tasks` 移到 `/{node_id}` 之前 |
| B10 | `api/routes/node.py:258-264` | `find_pending_by_responsible_user` 调用缺 `node_status` 参数，导致 `page_size` 被当作 `node_status` 传入 | 补全 `"IN_PROGRESS"` 参数 |

---

## 二、API 全量测试结果

### Test 1: 创建节点（新字段 responsible_user_id, node_type）
```
POST /api/v1/project-nodes
Body: { "project_id":"PROJ-001", "node_id":"NODE-TEST-001", "responsible_user_id":"user-dev1", "node_type":"WORK_PACKAGE", ... }
```
**结果**: PASS  
- 响应: `code:0, node_id:"NODE-TEST-001", status:"CONDITIONS_NOT_MET"`  
- 原因: creator_id=user-pm (level 5) 为管理员，自动激活跳过审批  
- DB 验证: responsible_user_id="user-dev1", node_type="WORK_PACKAGE"

### Test 2: 提交交付物 (PENDING → SUBMITTED)
```
POST /api/v1/node-deliverables/NODE-TEST-001-DELV-001/submit?user_id=user-dev1
```
**结果**: PASS  
- DB 验证: submission_status="SUBMITTED", submitted_by="user-dev1"

### Test 3: 确认交付物 (SUBMITTED → CONFIRMED)
```
POST /api/v1/node-deliverables/NODE-TEST-001-DELV-001/confirm?user_id=user-approver
```
**结果**: PASS  
- DB 验证: submission_status="CONFIRMED", confirmed_by="user-approver"  
- 自动副作用: current_amount 被设为 target_amount (100.0), completed_at 已填充

### Test 4: 退回交付物 (SUBMITTED → RETURNED)
```
POST /api/v1/node-deliverables/NODE-TEST-001-DELV-002/return?user_id=user-approver
```
**结果**: PASS  
- DB 验证: submission_status 先为 RETURNED, return_reason="测试报告格式不符合要求，请修改"

### Test 5: 重新提交 (RETURNED → SUBMITTED)
```
POST /api/v1/node-deliverables/NODE-TEST-001-DELV-002/resubmit?user_id=user-dev1
```
**结果**: PASS  
- DB 验证: submission_status="SUBMITTED"

### Test 6: 变更责任人
```
PATCH /api/v1/project-nodes/NODE-TEST-001/assign
Body: { "responsible_user_id":"user-dev2", "operator_id":"user-pm" }
```
**结果**: PASS

### Test 7: 查询我的待办 (my-tasks)
```
GET /api/v1/project-nodes/my-tasks?user_id=user-dev2
```
**结果**: PASS  
- 返回: `total:2`, 包含 DELV-001(CONFIRMED) 和 DELV-002(SUBMITTED)  
- 已验证: node_type="WORK_PACKAGE", submission_status 正确

### Test 8: 查询节点详情
```
GET /api/v1/project-nodes/NODE-TEST-001
```
**结果**: PASS  
- status="IN_PROGRESS" (确认驱动自动流转), progress=50.0%  
- 交付物列表完整: DELV-001 (100/100, 已完成), DELV-002 (0/1, 待完成)

---

## 三、交付物状态流转验证

```
DELV-001: PENDING → SUBMITTED → CONFIRMED     ✓
DELV-002: PENDING → SUBMITTED → RETURNED → SUBMITTED  ✓
```

完整状态机：PENDING / SUBMITTED / CONFIRMED / RETURNED 四种状态均通过测试。

---

## 四、Scheduler 引擎验证

| 检查项 | 状态 |
|--------|------|
| SchedulerEngine 启动 | PASS (tick=60s, Advisory Lock) |
| 7 个 Handler 注册 | PASS (morning_report, node_deadlines, periodic_node, session_cleanup, health_check, data_sync, webhook) |
| 0 个 Hook 注册 | PASS (预期行为) |
| scheduler_jobs 表 | 0 rows (预期：作业需业务创建) |
| scheduler_executions 表 | 0 rows (预期：无作业执行) |

---

## 五、数据库验证

最终数据状态：
- users: 5 rows (admin/pm/dev1/dev2/approver)
- projects: 1 row (PROJ-001)
- company_info: 1 row (COMP-001)
- project_nodes: 1 row (NODE-TEST-001, status=IN_PROGRESS, responsible_user_id=user-dev2)
- node_deliverables: 2 rows (DELV-001=CONFIRMED, DELV-002=SUBMITTED)
- scheduler_jobs: 0 rows
- scheduler_executions: 0 rows

---

## 六、总结

| 类别 | 数量 | 状态 |
|------|------|------|
| API 端点测试 | 8 | 全部通过 |
| 运行时 Bug 修复 | 4 (B7-B10) | 全部修复 |
| 交付物状态流转 | 4 种状态 | 全覆盖验证 |
| Scheduler 引擎 | 就绪 | 等待作业创建 |
| 数据持久化 | 正常 | 跨重启保留 |

**结论**: 全景节点与计划任务模块（M1-M8）已通过全量运行时实测，核心流程完整可用。
