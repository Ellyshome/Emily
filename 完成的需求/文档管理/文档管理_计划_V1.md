# 文档管理 — AI 执行计划

> **基于需求**：[文档管理_需求_V2.md](需求文件/文档管理/文档管理_需求_V2.md)
> **计划版本**：v1.0
> **目标**：为 `files` 表新增 `file_category` 枚举字段，扩展 FileService/FileRepository 增加按分类查询和更新能力，新增 `query_files` / `update_file_category` 两个 BusinessFlowTool，更新 SOP prompt 让 LLM 归档时自动分类

---

## 你的角色

你作为 **Emily开发者资深架构师** + **实施计划编制专家**，严格按以下模块顺序执行，逐模块验证，验证不通过不进入下一个模块。

---

## 硬约束（违反即失败）

1. **禁止修改已有方法签名**：只能新增方法，不改现有方法参数列表（除非计划明确标注"修改方法签名"）
2. **分层不可跳**：`Tool → Application → Service → Repository → DB`，不允许 Tool 直接调 Repository
3. **Sync repo + `asyncio.to_thread`**：Repository 全 sync，async 层用 `asyncio.to_thread()` 包裹
4. **`file_category` 枚举在应用层定义**：不做 DB CHECK 约束，方便未来扩展
5. **每模块验收**：每个模块的验收检测必须通过，否则停止并报告
6. **参照已有模式**：所有新代码必须参照"代码模式参照表"中的源文件

---

## 上下文（执行前必读）

### 已有的可复用组件

| 组件 | 位置 | 关键方法 | 本次怎么用 |
|------|------|----------|-----------|
| `FileRepository` | `emily-core/emily_core/repositories/file_repo.py` | `create()`, `query_files()`, `get_by_file_no()` | 扩展：加 `file_category` 参数 + 新增 3 个方法 |
| `FileService` | `emily-core/emily_core/services/file_service.py` | `create_file_record()`, `format_reply()` | 扩展：传入 `file_category` + 新增 3 个方法 |
| `FileApplication` | `emily-core/emily_core/application/file_app.py` | `handle_file()` | 扩展：从 RouteResult.data 读取 `file_category` + 新增 2 个 handler |
| `FileCommand` | `emily-core/emily_core/adapters/standard/command.py:52` | dataclass 字段 | 扩展：加 `file_category` 字段 |
| `File` ORM | `emily-core/emily_core/infrastructure/database/models.py:282` | 26 列 | 扩展：加 `file_category` 列 |
| `BusinessFlowToolRegistry` | `emily-core/emily_core/tools/registry.py` | `register_all()` | 扩展：注册新工具 |
| `SessionAccessibleFileRepo` | `emily-core/emily_core/repositories/session_accessible_file_repo.py` | `get_file_summary()`, `search()` | 扩展：增加 `by_category` 统计 |
| `fetch_visible_files` | `emily-core/emily_core/session/fetchers/fetch_visible_files.py` | `fetch()`, `format_summary()` | 扩展：输出含分类信息 |

### 架构决策

选择方案 A（`file_category` 枚举字段）而非文件夹树，因为：Emily 是 IM 对话驱动的 AI 助手，用户通过"说一句话"查询文件而非浏览目录树。7 类分类是属性维度不是层级结构，一个枚举字段足以支撑，且与现有 `SessionAccessibleFileRepo.get_file_summary()` 的 `by_type` 统计逻辑天然兼容。

### 代码模式参照表

| 层 | 参照源（精确文件路径） | 要模仿的要点 |
|----|----------------------|-------------|
| 状态枚举 | `emily-core/emily_core/workitem/workitem_state.py` | `Enum` 类 + `TERMINAL_STATES` + `TRANSITIONS` |
| Repository | `emily-core/emily_core/repositories/file_repo.py` | `@staticmethod` + `get_session()` + 可选参数过滤 |
| Service | `emily-core/emily_core/services/file_service.py` | sync 方法 + `self.repo.xxx()` 调用 |
| Application | `emily-core/emily_core/application/file_app.py` | async + `RouteResult.data` 取参 + `HandlerResult` 返回 |
| Command | `emily-core/emily_core/adapters/standard/command.py` | `@dataclass` + Optional 字段 |
| ORM 模型 | `emily-core/emily_core/infrastructure/database/models.py:282` | `Column(String(50), default=...)` + comment |
| Tool handler | `emily-core/emily_core/tools/file_tool.py` | `async def handle_xxx(params, ...)` → 返回 `dict` |
| Tool 注册 | `emily-core/emily_core/tools/registry.py` | `_tool()` + `partial()` + `reg.register()` |
| SOP | `emily-data/sops/SOP-004-FILE-file_archive.md` | Markdown 手册格式 |

---

## 模块依赖图

```
M1(枚举+数据模型) ──→ M2(Repository) ──→ M3(Service) ──→ M4(Application+Command) ──→ M5(Tool) ──→ M6(SOP+Session集成)
                                                                                                  ↑
                                                                                            M7(迁移脚本) ← M1
```

---

## 交付物总览

| 模块 | 交付文件 | 新增/修改 | 核心类/函数 |
|------|----------|----------|------------|
| M1 | `emily-core/emily_core/infrastructure/database/models.py` | 修改 | `FileCategory` 枚举, `File.file_category` 列 |
| M1 | `emily-core/emily_core/adapters/standard/command.py` | 修改 | `FileCommand.file_category` 字段 |
| M2 | `emily-core/emily_core/repositories/file_repo.py` | 修改 | `create()` 加参数, `query_by_category()`, `update_category()`, `count_by_category()` |
| M3 | `emily-core/emily_core/services/file_service.py` | 修改 | `create_file_record()` 加参数, `list_by_category()`, `update_file_category()`, `get_category_summary()` |
| M4 | `emily-core/emily_core/application/file_app.py` | 修改 | `handle_file()` 加分类, `handle_list_by_category()`, `handle_update_category()` |
| M5 | `emily-core/emily_core/tools/file_tool.py` | 修改 | `_FILE_TOOL_SCHEMA` 加 `file_category`, `handle_query_files()`, `handle_update_file_category()` |
| M5 | `emily-core/emily_core/tools/registry.py` | 修改 | 注册 `query_files`, `update_file_category` |
| M6 | `emily-data/sops/SOP-004-FILE-file_archive.md` | 修改 | 增加 `file_category` 字段分级与分类决策规则 |
| M6 | `emily-core/emily_core/repositories/session_accessible_file_repo.py` | 修改 | `get_file_summary()` 增加 `by_category` |
| M6 | `emily-core/emily_core/session/fetchers/fetch_visible_files.py` | 修改 | `format_summary()` 输出分类信息 |
| M7 | `scripts/migrate_file_category.py` | 新增 | DDL + LLM 批量回填 |

---

## 现有模块改动清单

| 现有模块 | 改动类型 | 改动内容 |
|----------|----------|----------|
| `emily-core/emily_core/infrastructure/database/models.py` | 修改 | File 类前新增 `FileCategory` 枚举类；File 类新增 `file_category` Column |
| `emily-core/emily_core/adapters/standard/command.py` | 扩展 | `FileCommand` 新增 `file_category` 字段 |
| `emily-core/emily_core/repositories/file_repo.py` | 修改+扩展 | `create()` 新增 `file_category` 参数；新增 3 个方法 |
| `emily-core/emily_core/services/file_service.py` | 修改+扩展 | `create_file_record()` 传入 `file_category`；新增 3 个方法 |
| `emily-core/emily_core/application/file_app.py` | 修改+扩展 | `handle_file()` 从 data 取 `file_category`；新增 2 个 handler |
| `emily-core/emily_core/tools/file_tool.py` | 修改+扩展 | Schema 加 `file_category`；`handle_record_file()` 传递 `file_category`；新增 2 个 handler+schema |
| `emily-core/emily_core/tools/registry.py` | 扩展 | `_register_business()` 注册 `query_files` 和 `update_file_category` |
| `emily-data/sops/SOP-004-FILE-file_archive.md` | 修改 | 增加 `file_category` 字段分级、分类决策规则 |
| `emily-core/emily_core/repositories/session_accessible_file_repo.py` | 修改 | `get_file_summary()` 返回 `by_category` |
| `emily-core/emily_core/session/fetchers/fetch_visible_files.py` | 修改 | `format_summary()` 输出分类统计 |

---

## M1: 枚举定义 + 数据模型 + Command 扩展

**依赖**：无

**职责**：定义 `FileCategory` 枚举，`File` ORM 新增 `file_category` 列，`FileCommand` 新增 `file_category` 字段

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | `FileCategory` 枚举类 | `emily-core/emily_core/infrastructure/database/models.py` |
| 2 | `File.file_category` 列 | `emily-core/emily_core/infrastructure/database/models.py` |
| 3 | `FileCommand.file_category` 字段 | `emily-core/emily_core/adapters/standard/command.py` |

### 代码

#### `emily-core/emily_core/infrastructure/database/models.py` — 在 `class File(Base):` 之前追加

```python
class FileCategory:
    """文件业务分类枚举（应用层约束，无 DB CHECK）。

    7 类分类覆盖建筑工程项目全周期文档。
    枚举值为 Python 类属性，与 WorkItemState 模式一致。
    """
    PROJECT_LICENSE = "PROJECT_LICENSE"      # 项目证照
    CONTRACT = "CONTRACT"                    # 承包合同
    WORK_RECORD = "WORK_RECORD"              # 工作记录
    PHASE_DELIVERABLE = "PHASE_DELIVERABLE"  # 阶段成果
    PROCESS_DOC = "PROCESS_DOC"              # 过程文件
    MANAGEMENT_SPEC = "MANAGEMENT_SPEC"      # 管理规程
    OTHER = "OTHER"                          # 其他文件

    ALL = [
        PROJECT_LICENSE, CONTRACT, WORK_RECORD,
        PHASE_DELIVERABLE, PROCESS_DOC, MANAGEMENT_SPEC, OTHER,
    ]

    DISPLAY_NAMES = {
        PROJECT_LICENSE: "项目证照",
        CONTRACT: "承包合同",
        WORK_RECORD: "工作记录",
        PHASE_DELIVERABLE: "阶段成果",
        PROCESS_DOC: "过程文件",
        MANAGEMENT_SPEC: "管理规程",
        OTHER: "其他文件",
    }

    @classmethod
    def validate(cls, value: str) -> str:
        """校验并返回合法枚举值，非法值回退 OTHER。"""
        if value in cls.ALL:
            return value
        return cls.OTHER

    @classmethod
    def display(cls, value: str) -> str:
        """返回中文显示名。"""
        return cls.DISPLAY_NAMES.get(value, "其他文件")
```

#### `emily-core/emily_core/infrastructure/database/models.py` — 在 `File` 类中 `source_module_type` 之后追加

```python
    file_category = Column(String(50), default="OTHER", comment="文件业务分类：PROJECT_LICENSE/CONTRACT/WORK_RECORD/PHASE_DELIVERABLE/PROCESS_DOC/MANAGEMENT_SPEC/OTHER")
```

#### `emily-core/emily_core/adapters/standard/command.py` — 在 `FileCommand` 类中 `storage_path` 之后追加

```python
    file_category: str = "OTHER"
```

### 模块验收检测

```bash
# 验收 1：FileCategory 枚举类定义正确
uv run python -c "from emily_core.infrastructure.database.models import FileCategory; assert FileCategory.CONTRACT == 'CONTRACT'; assert FileCategory.validate('INVALID') == 'OTHER'; assert FileCategory.display('PHASE_DELIVERABLE') == '阶段成果'; print('FileCategory OK')"
→ 预期输出：FileCategory OK

# 验收 2：File ORM 有 file_category 列
uv run python -c "from emily_core.infrastructure.database.models import File; assert hasattr(File, 'file_category'); print('File.file_category OK')"
→ 预期输出：File.file_category OK

# 验收 3：FileCommand 有 file_category 字段
uv run python -c "from emily_core.adapters.standard.command import FileCommand; cmd = FileCommand(file_category='CONTRACT'); assert cmd.file_category == 'CONTRACT'; print('FileCommand.file_category OK')"
→ 预期输出：FileCommand.file_category OK
```

**失败处理**：检查 import 路径、列定义位置是否在 File 类内而非类外

---

## M2: Repository 层扩展

**依赖**：M1

**职责**：扩展 `FileRepository.create()` 接收 `file_category`，新增按分类查询、更新分类、按分类统计 3 个方法

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | `create()` 新增 `file_category` 参数 | `emily-core/emily_core/repositories/file_repo.py` |
| 2 | `query_by_category()` | `emily-core/emily_core/repositories/file_repo.py` |
| 3 | `update_category()` | `emily-core/emily_core/repositories/file_repo.py` |
| 4 | `count_by_category()` | `emily-core/emily_core/repositories/file_repo.py` |

### 代码

#### `emily-core/emily_core/repositories/file_repo.py` — 修改 `create()` 方法签名，在参数列表末尾加 `file_category: str = "OTHER"`

在 `create()` 方法的参数列表中 `parse_status: str = "pending"` 之后追加：
```python
        file_category: str = "OTHER",
```

在 `create()` 方法体内 `File(` 构造中 `parse_status=parse_status,` 之后追加：
```python
                file_category=file_category,
```

#### `emily-core/emily_core/repositories/file_repo.py` — 在 `query_files()` 方法之后追加 3 个新方法

```python
    @staticmethod
    def query_by_category(
        *,
        project_id: str | None = None,
        project_ids: list[str] | None = None,
        file_category: str | None = None,
        limit: int = 50,
    ) -> list[File]:
        """按分类查询文件记录（按创建时间倒序）。

        Args:
            project_id: 单项目过滤
            project_ids: 多项目范围过滤
            file_category: 文件分类枚举值
            limit: 返回数量上限
        """
        with get_session() as session:
            q = session.query(File).filter(File.is_deleted == False)

            if project_id:
                q = q.filter(File.project_id == project_id)
            if project_ids:
                q = q.filter(File.project_id.in_(project_ids))
            if file_category:
                q = q.filter(File.file_category == file_category)

            return q.order_by(File.created_at.desc()).limit(limit).all()

    @staticmethod
    def update_category(file_id: str, file_category: str) -> File | None:
        """更新文件分类。"""
        with get_session() as session:
            f = session.query(File).filter(File.id == file_id, File.is_deleted == False).first()
            if f is None:
                return None
            f.file_category = file_category
            session.commit()
            logger.info("File %s category updated: %s", f.file_no, file_category)
            return f

    @staticmethod
    def count_by_category(project_id: str | None = None) -> dict[str, int]:
        """按分类统计文件数量。"""
        with get_session() as session:
            q = session.query(File).filter(File.is_deleted == False)
            if project_id:
                q = q.filter(File.project_id == project_id)
            files = q.all()
            counts: dict[str, int] = {}
            for f in files:
                cat = f.file_category or "OTHER"
                counts[cat] = counts.get(cat, 0) + 1
            return counts
```

注意：需要确认 `File` 模型中有 `is_deleted` 列。已有，位于 `models.py:318`。

### 模块验收检测

```bash
# 验收 1：create() 接受 file_category 参数
uv run python -c "from emily_core.repositories.file_repo import FileRepository; import inspect; sig = inspect.signature(FileRepository.create); assert 'file_category' in sig.parameters; print('create() has file_category param OK')"
→ 预期输出：create() has file_category param OK

# 验收 2：3 个新方法存在
uv run python -c "from emily_core.repositories.file_repo import FileRepository; assert hasattr(FileRepository, 'query_by_category'); assert hasattr(FileRepository, 'update_category'); assert hasattr(FileRepository, 'count_by_category'); print('3 new methods OK')"
→ 预期输出：3 new methods OK
```

**失败处理**：检查方法是否在 `FileRepository` 类内、缩进是否正确

---

## M3: Service 层扩展

**依赖**：M2

**职责**：扩展 `FileService.create_file_record()` 传入 `file_category`，新增 `list_by_category()`、`update_file_category()`、`get_category_summary()` 方法

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | `create_file_record()` 传入 `file_category` | `emily-core/emily_core/services/file_service.py` |
| 2 | `list_by_category()` | `emily-core/emily_core/services/file_service.py` |
| 3 | `update_file_category()` | `emily-core/emily_core/services/file_service.py` |
| 4 | `get_category_summary()` | `emily-core/emily_core/services/file_service.py` |

### 代码

#### `emily-core/emily_core/services/file_service.py` — 修改 `create_file_record()`，在 `self.repo.create(` 调用中追加 `file_category` 参数

在 `self.repo.create(` 调用内 `parse_status="pending",` 之后追加：
```python
            file_category=cmd.file_category or "OTHER",
```

#### `emily-core/emily_core/services/file_service.py` — 在 `format_reply()` 方法之后追加 3 个新方法

```python
    def list_by_category(
        self,
        project_id: str | None = None,
        project_ids: list[str] | None = None,
        file_category: str | None = None,
        limit: int = 50,
    ) -> list[FileModel]:
        """按分类查询文件。"""
        return self.repo.query_by_category(
            project_id=project_id,
            project_ids=project_ids,
            file_category=file_category,
            limit=limit,
        )

    def update_file_category(self, file_id: str, file_category: str, operator_id: str = "") -> FileModel | None:
        """更新文件分类。

        Args:
            file_id: 文件 UUID
            file_category: 目标分类枚举值
            operator_id: 操作人 ID

        Returns:
            更新后的 File 对象，未找到返回 None
        """
        from ..infrastructure.database.models import FileCategory
        validated = FileCategory.validate(file_category)
        result = self.repo.update_category(file_id, validated)
        if result:
            logger.info("File category updated: %s → %s by %s", result.file_no, validated, operator_id)
        return result

    def get_category_summary(self, project_id: str | None = None) -> dict:
        """按分类统计文件数量。"""
        from ..infrastructure.database.models import FileCategory
        counts = self.repo.count_by_category(project_id=project_id)
        return {
            "by_category": {
                FileCategory.display(cat): count
                for cat, count in counts.items()
            },
            "total": sum(counts.values()),
        }
```

### 模块验收检测

```bash
# 验收 1：create_file_record() 传入 file_category
uv run python -c "from emily_core.services.file_service import FileService; fs = FileService(); print('FileService import OK')"
→ 预期输出：FileService import OK

# 验收 2：3 个新方法存在
uv run python -c "from emily_core.services.file_service import FileService; fs = FileService(); assert hasattr(fs, 'list_by_category'); assert hasattr(fs, 'update_file_category'); assert hasattr(fs, 'get_category_summary'); print('3 new service methods OK')"
→ 预期输出：3 new service methods OK
```

**失败处理**：检查 import 路径是否正确、方法是否在 `FileService` 类内

---

## M4: Application 层 + Command 层扩展

**依赖**：M3

**职责**：`FileApplication.handle_file()` 读取 `file_category` 并传给 Service；新增 `handle_list_by_category()` 和 `handle_update_category()` handler

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | `handle_file()` 传递 `file_category` | `emily-core/emily_core/application/file_app.py` |
| 2 | `handle_list_by_category()` | `emily-core/emily_core/application/file_app.py` |
| 3 | `handle_update_category()` | `emily-core/emily_core/application/file_app.py` |

### 代码

#### `emily-core/emily_core/application/file_app.py` — 修改 `handle_file()` 中 `FileCommand` 构造

在 `FileCommand(` 构造中 `source_message_id=message_id,` 之后追加：
```python
                file_category=data.get("file_category", "OTHER"),
```

#### `emily-core/emily_core/application/file_app.py` — 在 `handle_file()` 方法之后追加 2 个新 handler

```python
    async def handle_list_by_category(
        self,
        file_category: str | None = None,
        project_id: str | None = None,
        project_ids: list[str] | None = None,
        keyword: str = "",
        limit: int = 10,
    ) -> HandlerResult:
        """按分类查询文件列表。"""
        try:
            from ..infrastructure.database.models import FileCategory

            files = self.file_service.list_by_category(
                project_id=project_id,
                project_ids=project_ids,
                file_category=file_category,
                limit=limit,
            )

            if not files:
                cat_name = FileCategory.display(file_category) if file_category else "文件"
                return HandlerResult(
                    success=True,
                    reply=f"📎 {cat_name}类暂无文件记录。",
                )

            # 格式化列表
            cat_name = FileCategory.display(file_category) if file_category else "全部"
            lines = [f"📎 {cat_name}类文件（共 {len(files)} 份）", "──────────────"]
            for i, f in enumerate(files[:10], 1):
                cat_display = FileCategory.display(getattr(f, 'file_category', 'OTHER'))
                lines.append(f"{i}. {f.filename} ({f.file_no}) [{cat_display}]")
            if len(files) > 10:
                lines.append(f"... 还有 {len(files) - 10} 份")

            return HandlerResult(
                success=True,
                reply="\n".join(lines),
                data={
                    "total": len(files),
                    "category": file_category,
                    "files": [
                        {
                            "file_no": f.file_no,
                            "filename": f.filename,
                            "file_category": getattr(f, 'file_category', 'OTHER'),
                        }
                        for f in files[:20]
                    ],
                },
            )
        except Exception as e:
            logger.error("List files by category failed: %s", e, exc_info=True)
            return HandlerResult(
                success=False,
                error_code="file_query_failed",
                reply=f"文件查询失败：{e}",
            )

    async def handle_update_category(
        self,
        file_no: str,
        file_category: str,
        user_id: str = "",
    ) -> HandlerResult:
        """修改文件分类。"""
        try:
            from ..infrastructure.database.models import FileCategory

            # 按编号查找文件
            f = self.file_service.repo.get_by_file_no(file_no)
            if f is None:
                return HandlerResult(
                    success=False,
                    reply=f"找不到文件编号 {file_no}",
                )

            old_category = getattr(f, 'file_category', 'OTHER')
            old_display = FileCategory.display(old_category)
            new_display = FileCategory.display(file_category)

            result = self.file_service.update_file_category(
                file_id=f.id,
                file_category=file_category,
                operator_id=user_id,
            )

            if result is None:
                return HandlerResult(
                    success=False,
                    reply=f"文件分类更新失败",
                )

            return HandlerResult(
                success=True,
                reply=f"✅ 文件「{f.filename}」已从「{old_display}」改到「{new_display}」",
                data={
                    "file_no": file_no,
                    "old_category": old_category,
                    "new_category": file_category,
                },
            )
        except Exception as e:
            logger.error("Update file category failed: %s", e, exc_info=True)
            return HandlerResult(
                success=False,
                error_code="file_category_update_failed",
                reply=f"分类更新失败：{e}",
            )
```

### 模块验收检测

```bash
# 验收 1：FileApplication 新方法存在
uv run python -c "from emily_core.application.file_app import FileApplication; assert hasattr(FileApplication, 'handle_list_by_category'); assert hasattr(FileApplication, 'handle_update_category'); print('2 new handlers OK')"
→ 预期输出：2 new handlers OK

# 验收 2：handle_file() 传递 file_category（检查 FileCommand 构造含 file_category）
grep -n "file_category" emily-core/emily_core/application/file_app.py
→ 预期输出：至少 4 行匹配（data.get + FileCommand + 2 个新方法内的引用）
```

**失败处理**：检查 `FileApplication` 类内方法缩进、`FileCategory` import 位置

---

## M5: Tool 层 — 修改 record_file + 新增 query_files / update_file_category

**依赖**：M4

**职责**：`_FILE_TOOL_SCHEMA` 加 `file_category`，`handle_record_file()` 传递分类，新增 `handle_query_files()` 和 `handle_update_file_category()`，注册到 registry

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | Schema 加 `file_category` | `emily-core/emily_core/tools/file_tool.py` |
| 2 | `handle_record_file()` 传递 `file_category` | `emily-core/emily_core/tools/file_tool.py` |
| 3 | `_QUERY_FILES_SCHEMA` + `handle_query_files()` | `emily-core/emily_core/tools/file_tool.py` |
| 4 | `_UPDATE_CATEGORY_SCHEMA` + `handle_update_file_category()` | `emily-core/emily_core/tools/file_tool.py` |
| 5 | 注册 `query_files` / `update_file_category` | `emily-core/emily_core/tools/registry.py` |

### 代码

#### `emily-core/emily_core/tools/file_tool.py` — 修改 `_FILE_TOOL_SCHEMA` 的 `data.properties`，在 `file_type` 之后追加

```python
                "file_category": {
                    "type": "string",
                    "description": "文件业务分类：PROJECT_LICENSE(项目证照)/CONTRACT(承包合同)/WORK_RECORD(工作记录)/PHASE_DELIVERABLE(阶段成果)/PROCESS_DOC(过程文件)/MANAGEMENT_SPEC(管理规程)/OTHER(其他文件)",
                    "default": "OTHER",
                },
```

#### `emily-core/emily_core/tools/file_tool.py` — 修改 `handle_record_file()` 中 `RouteResult` 的 `data` 字典

在 `route_result` 构造的 `data={}` 中 `"file_type": data.get("file_type", ""),` 之后追加：
```python
                "file_category": data.get("file_category", "OTHER"),
```

#### `emily-core/emily_core/tools/file_tool.py` — 在文件末尾追加 2 个新 schema + 2 个新 handler

```python
# ══════════════════════════════════════════════════════════════════════════════
# 文件查询工具 — query_files
# ══════════════════════════════════════════════════════════════════════════════

_QUERY_FILES_SCHEMA = {
    "type": "object",
    "properties": {
        "file_category": {
            "type": "string",
            "description": "按分类过滤：PROJECT_LICENSE/CONTRACT/WORK_RECORD/PHASE_DELIVERABLE/PROCESS_DOC/MANAGEMENT_SPEC/OTHER",
        },
        "keyword": {
            "type": "string",
            "description": "按文件名关键词搜索（模糊匹配）",
        },
        "project_id": {
            "type": "string",
            "description": "项目 UUID（可选，默认取当前用户项目）",
        },
        "limit": {
            "type": "integer",
            "description": "返回数量上限，默认 10",
            "default": 10,
        },
    },
}

_QUERY_FILES_DESCRIPTION = (
    "按分类或关键词查询项目文件列表。\n"
    "\n"
    "字段分级：\n"
    "  [应有] file_category — 按业务分类过滤（如 CONTRACT 查承包合同类）\n"
    "  [应有] keyword — 按文件名关键词模糊搜索\n"
    "  [可选] project_id — 指定项目范围\n"
    "  [可选] limit — 返回数量上限"
)


async def handle_query_files(
    params: dict,
    file_app: FileApplication,
    user_id: str = "",
    message_id: str = "",
    project_ids: list[str] | None = None,
    **kwargs,
) -> dict:
    """处理文件分类查询。"""
    file_category = params.get("file_category")
    keyword = params.get("keyword", "")
    project_id = params.get("project_id")
    limit = params.get("limit", 10)

    result = await file_app.handle_list_by_category(
        file_category=file_category,
        project_id=project_id,
        project_ids=project_ids,
        keyword=keyword,
        limit=limit,
    )

    return {
        "success": result.success,
        "reply": result.reply,
        "data": getattr(result, 'data', {}),
        "error_code": result.error_code,
    }


# ══════════════════════════════════════════════════════════════════════════════
# 文件分类修改工具 — update_file_category
# ══════════════════════════════════════════════════════════════════════════════

_UPDATE_CATEGORY_SCHEMA = {
    "type": "object",
    "properties": {
        "file_no": {
            "type": "string",
            "description": "文件编号（如 FIL-20260709-0001）",
        },
        "file_category": {
            "type": "string",
            "description": "目标分类：PROJECT_LICENSE/CONTRACT/WORK_RECORD/PHASE_DELIVERABLE/PROCESS_DOC/MANAGEMENT_SPEC/OTHER",
        },
    },
    "required": ["file_no", "file_category"],
}

_UPDATE_CATEGORY_DESCRIPTION = (
    "修改文件的分类归属。\n"
    "\n"
    "必填字段：\n"
    "  file_no — 文件编号\n"
    "  file_category — 目标分类枚举值"
)


async def handle_update_file_category(
    params: dict,
    file_app: FileApplication,
    user_id: str = "",
    message_id: str = "",
    **kwargs,
) -> dict:
    """处理文件分类修改。"""
    file_no = params.get("file_no", "")
    file_category = params.get("file_category", "OTHER")

    if not file_no:
        return {"success": False, "reply": "请提供文件编号", "error_code": "missing_file_no"}

    result = await file_app.handle_update_category(
        file_no=file_no,
        file_category=file_category,
        user_id=user_id,
    )

    return {
        "success": result.success,
        "reply": result.reply,
        "data": getattr(result, 'data', {}),
        "error_code": result.error_code,
    }
```

#### `emily-core/emily_core/tools/registry.py` — 在 `_register_business()` 函数中 `record_file` 注册之后追加

在 `# 计划任务 (4 tools)` 注释之前追加：

```python
    # 文件查询 + 分类修改 (2 tools)
    _buc += _reg_biz(reg, "query_files", "按分类或关键词查询项目文件",
                     partial(_h("file_tool", "handle_query_files"),
                             file_app=core._file_app), "business", "all")
    _buc += _reg_biz(reg, "update_file_category", "修改文件分类归属",
                     partial(_h("file_tool", "handle_update_file_category"),
                             file_app=core._file_app), "business", "write")
```

注意：`query_files` 使用 `permission_flag="all"`（所有用户可查询），`update_file_category` 使用 `permission_flag="write"`（level ≥ 3 可修改）。

### 模块验收检测

```bash
# 验收 1：新 handler 可导入
uv run python -c "from emily_core.tools.file_tool import handle_query_files, handle_update_file_category; print('2 new handlers imported OK')"
→ 预期输出：2 new handlers imported OK

# 验收 2：Schema 包含 file_category
uv run python -c "from emily_core.tools.file_tool import _FILE_TOOL_SCHEMA, _QUERY_FILES_SCHEMA, _UPDATE_CATEGORY_SCHEMA; assert 'file_category' in _FILE_TOOL_SCHEMA['properties']['data']['properties']; assert 'file_category' in _QUERY_FILES_SCHEMA['properties']; assert 'file_no' in _UPDATE_CATEGORY_SCHEMA['properties']; print('Schemas OK')"
→ 预期输出：Schemas OK
```

**失败处理**：检查 import 路径、Schema JSON 语法、handler 函数签名与注册时 `partial()` 的参数是否匹配

---

## M6: SOP 更新 + Session 集成

**依赖**：M5

**职责**：更新文件归档 SOP 增加 `file_category` 决策规则；扩展 `SessionAccessibleFileRepo.get_file_summary()` 返回 `by_category`；更新 `fetch_visible_files` 输出分类信息

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | SOP 增加 `file_category` 字段 | `emily-data/sops/SOP-004-FILE-file_archive.md` |
| 2 | `get_file_summary()` 增加 `by_category` | `emily-core/emily_core/repositories/session_accessible_file_repo.py` |
| 3 | `format_summary()` 输出分类信息 | `emily-core/emily_core/session/fetchers/fetch_visible_files.py` |

### 代码

#### `emily-data/sops/SOP-004-FILE-file_archive.md` — 在 §3.2 使用的系统工具表格后追加

在 `| \`query_data\` | 项目查询 | 推断项目归属 |` 行之后追加：

```markdown
| \`query_files\` | 文件查询 | 查看已有文件避免重复 |
```

在 §3.3 处理流程中 `[信息整理]` 步骤内追加 ⑤：
```markdown
  ⑤ 文件分类（file_category）[应有，自动判断]
```

在 §4.1 字段分级表格中 `| **project_id** 关联项目 | 🟡 应有 | 自动匹配 |` 行之后追加：

```markdown
| **file_category** 文件分类 | 🟡 应有 | 按分类决策规则自动判断，默认 OTHER |
```

在 §4.1 表格之后追加分类决策规则段落：

```markdown
### 4.2 文件分类决策规则

归档时根据文件名和用户描述判断 `file_category`，规则如下：

| 分类枚举值 | 中文名 | 判定依据 |
|-----------|--------|---------|
| `PROJECT_LICENSE` | 项目证照 | 涉及政府许可、法律效力证书（施工许可证、预售证、土地出让合同等） |
| `CONTRACT` | 承包合同 | 涉及参建单位签约、合同文本、界面划分、预算清单 |
| `WORK_RECORD` | 工作记录 | 涉及完工确认、进场记录、巡检报告、验收单 |
| `PHASE_DELIVERABLE` | 阶段成果 | 涉及设计方案、施工图、专项施工方案 |
| `PROCESS_DOC` | 过程文件 | 涉及政府整改/通知、飞检评分、年终总结 |
| `MANAGEMENT_SPEC` | 管理规程 | 涉及操作手册、工艺规程、标准规范 |
| `OTHER` | 其他文件 | 无法归入以上分类 |

**分类依据是业务用途而非文件格式**。无法判断时默认 `OTHER`。
```

在 §7 业务流变更记录表格追加一行：

```markdown
| v1.1 | 2026-07-09 | 文档管理 | 增加 file_category 字段与分类决策规则 |
```

#### `emily-core/emily_core/repositories/session_accessible_file_repo.py` — 修改 `get_file_summary()` 方法

在 `get_file_summary()` 方法体内的 `by_type` 统计循环中，同时统计 `by_category`：

在 `by_type: dict[str, int] = {}` 行之后追加：
```python
                by_category: dict[str, int] = {}
```

在循环内 `by_type[ft] = by_type.get(ft, 0) + 1` 行之后追加：
```python
                    cat = getattr(f, 'file_category', 'OTHER') or 'OTHER'
                    by_category[cat] = by_category.get(cat, 0) + 1
```

在 `files_list.append({...})` 的 dict 内追加：
```python
                        "file_category": getattr(f, 'file_category', 'OTHER') or 'OTHER',
```

修改返回值，在 `return {"count": ..., "by_type": ..., "files": ...}` 中追加 `"by_category"`：
```python
                return {"count": len(files), "by_type": by_type, "by_category": by_category, "files": files_list}
```

同步修改空结果返回：`return {"count": 0, "by_type": {}, "by_category": {}, "files": []}`

#### `emily-core/emily_core/session/fetchers/fetch_visible_files.py` — 修改 `format_summary()`

在 `format_summary()` 方法内，在 `type_parts` 构建之后追加分类统计展示：

在 `type_parts` 构建循环之后追加：
```python
    by_category = visible_files.get("by_category", {})
    cat_parts = []
    from ...infrastructure.database.models import FileCategory
    for cat, cnt in sorted(by_category.items(), key=lambda x: x[1], reverse=True):
        cat_parts.append(f"{FileCategory.display(cat)}({cnt}个)")
```

修改返回语句：
```python
    cat_str = f"，分类：{', '.join(cat_parts)}" if cat_parts else ""
    return f"共 {count} 个文件（{', '.join(type_parts)}）{cat_str}" if type_parts else f"共 {count} 个文件{cat_str}"
```

### 模块验收检测

```bash
# 验收 1：SOP 文件包含 file_category
grep -c "file_category" emily-data/sops/SOP-004-FILE-file_archive.md
→ 预期输出：≥ 3

# 验收 2：get_file_summary() 返回 by_category
uv run python -c "from emily_core.repositories.session_accessible_file_repo import SessionAccessibleFileRepo; import inspect; src = inspect.getsource(SessionAccessibleFileRepo.get_file_summary); assert 'by_category' in src; print('by_category in get_file_summary OK')"
→ 预期输出：by_category in get_file_summary OK

# 验收 3：format_summary 包含分类
uv run python -c "from emily_core.session.fetchers.fetch_visible_files import format_summary; result = format_summary({'count': 5, 'by_type': {'图纸': 3}, 'by_category': {'PHASE_DELIVERABLE': 3, 'OTHER': 2}}); assert '阶段成果' in result; print('format_summary OK')"
→ 预期输出：format_summary OK
```

**失败处理**：检查 `FileCategory` import 路径、`get_file_summary()` 返回值结构

---

## M7: 数据库迁移脚本

**依赖**：M1

**职责**：DDL 加字段 + 加索引 + LLM 批量回填已有文件分类

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | 迁移脚本 | `scripts/migrate_file_category.py` |

### 代码

#### `scripts/migrate_file_category.py` — 新建

```python
"""文件分类迁移脚本 — 加字段 + LLM 批量回填。

用法：
  uv run python scripts/migrate_file_category.py --dry-run      # 预览模式
  uv run python scripts/migrate_file_category.py                # 实际写入
  uv run python scripts/migrate_file_category.py --ddl-only     # 仅执行 DDL
"""

import argparse
import json
import os
import sys

sys.stdout.reconfigure(encoding="utf-8")

DB_URL = os.getenv(
    "EMILY_DATABASE_URL",
    "postgresql://emily:emily_secret_2026@emily-postgres:5432/emily",
)


def run_ddl(conn):
    """执行 DDL：加字段 + 加索引。"""
    cur = conn.cursor()

    # 检查字段是否已存在
    cur.execute("""
        SELECT column_name FROM information_schema.columns
        WHERE table_name = 'files' AND column_name = 'file_category'
    """)
    if cur.fetchone():
        print("  file_category 列已存在，跳过 DDL")
        return

    cur.execute("ALTER TABLE files ADD COLUMN file_category VARCHAR(50) DEFAULT 'OTHER'")
    cur.execute("CREATE INDEX IF NOT EXISTS idx_files_category ON files(file_category)")
    conn.commit()
    print("  DDL 完成: file_category 列 + 索引已创建")


def run_backfill(conn, llm_client=None, dry_run=False):
    """LLM 批量回填已有文件的分类。"""
    import psycopg2.extras

    cur = conn.cursor(cursor_factory=psycopg2.extras.RealDictCursor)
    cur.execute("""
        SELECT id, file_no, filename, file_type, file_category
        FROM files
        WHERE file_category = 'OTHER' AND is_deleted = FALSE
        ORDER BY created_at DESC
        LIMIT 500
    """)
    rows = cur.fetchall()
    print(f"  需回填文件数: {len(rows)}")

    if not rows:
        print("  无需回填")
        return

    CATEGORY_PROMPT = """请根据以下文件信息判断其业务分类。

文件名: {filename}
文件类型: {file_type}

分类选项（只返回枚举值，不要解释）:
- PROJECT_LICENSE: 项目证照（政府许可、法律效力证书）
- CONTRACT: 承包合同（参建单位合同、界面划分、预算清单）
- WORK_RECORD: 工作记录（完工确认、进场记录、巡检报告、验收单）
- PHASE_DELIVERABLE: 阶段成果（设计方案、施工图、专项方案）
- PROCESS_DOC: 过程文件（政府整改/通知、飞检评分、年终总结）
- MANAGEMENT_SPEC: 管理规程（操作手册、工艺规程、标准规范）
- OTHER: 其他文件

只返回枚举值（如 CONTRACT），不要返回其他内容。"""

    updated = 0
    for row in rows:
        filename = row["filename"] or ""
        file_type = row["file_type"] or ""

        if llm_client:
            try:
                prompt = CATEGORY_PROMPT.format(filename=filename, file_type=file_type)
                result = llm_client.chat(prompt)
                category = result.strip().split("\n")[0].strip()
                # 校验
                from emily_core.infrastructure.database.models import FileCategory
                category = FileCategory.validate(category)
            except Exception as e:
                print(f"    LLM 回填失败 {row['file_no']}: {e}, 使用 OTHER")
                category = "OTHER"
        else:
            category = "OTHER"

        if not dry_run and category != "OTHER":
            cur.execute(
                "UPDATE files SET file_category = %s WHERE id = %s",
                (category, row["id"]),
            )
            updated += 1

        print(f"    {row['file_no']}: {filename[:30]:30} → {category}")

    if not dry_run:
        conn.commit()

    print(f"  回填完成: 更新 {updated} 条 (dry_run={dry_run})")


def main():
    parser = argparse.ArgumentParser(description="文件分类迁移")
    parser.add_argument("--dry-run", action="store_true", help="预览模式")
    parser.add_argument("--ddl-only", action="store_true", help="仅执行 DDL")
    parser.add_argument("--db-url", default=DB_URL, help="PostgreSQL 连接 URL")
    args = parser.parse_args()

    print("=== 文件分类迁移 ===")

    try:
        import psycopg2
        # 本地开发用 localhost 端口
        local_url = args.db_url.replace("emily-postgres", "localhost").replace(":5432", ":25432")
        conn = psycopg2.connect(local_url)
    except Exception as e:
        print(f"数据库连接失败: {e}")
        print("提示: 确保 PostgreSQL 可达，或通过 --db-url 指定连接串")
        return

    print("Step 1: DDL")
    run_ddl(conn)

    if not args.ddl_only:
        print("Step 2: 回填")
        run_backfill(conn, dry_run=args.dry_run)

    conn.close()
    print("Done!")


if __name__ == "__main__":
    main()
```

### 模块验收检测

```bash
# 验收 1：脚本可运行（预览模式，不写 DB）
uv run python scripts/migrate_file_category.py --dry-run
→ 预期输出：包含 "需回填文件数:" 或 "无需回填"

# 验收 2：DDL 执行后 files 表有 file_category 列
docker exec emily-postgres psql -U emily -d emily -c "\d files" | grep file_category
→ 预期输出：file_category 行存在，类型 varchar(50)，默认值 OTHER
```

**失败处理**：检查 DB 连接串、psycopg2 是否安装、DDL 是否幂等

---

## 组装验证

所有模块完成后，运行端到端组装验证：

```bash
# 1. 清除 pycache（Docker bind-mount 必须）
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +

# 2. 重启 emily-core
docker compose -f docker-compose-napcat.yml restart emily-core

# 3. 查看日志确认工具注册成功
docker logs --tail 20 emily-core 2>&1 | grep -E "query_files|update_file_category"
→ 预期输出：2 条注册日志

# 4. 查询真实用户 ID
docker exec emily-postgres psql -U emily -d emily -c "SELECT id, username, permission_level FROM users WHERE status = 'active' ORDER BY permission_level DESC LIMIT 3;"

# 5. emy-test 验证文件归档含分类
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我归档一份建筑施工许可证" --sender "真实用户名"
→ 预期输出：回复含 file_category=PROJECT_LICENSE

# 6. emy-test 验证分类查询
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "查一下承包合同类的文件" --sender "真实用户名"
→ 预期输出：返回 CONTRACT 分类的文件列表

# 7. emy-test 验证分类修改
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "把文件 FIL-xxx 改到管理规程类" --sender "真实用户名"
→ 预期输出：分类更新成功确认

# 8. 数据库验证 file_category 列和索引
docker exec emily-postgres psql -U emily -d emily -c "SELECT file_category, COUNT(*) FROM files WHERE is_deleted = FALSE GROUP BY file_category ORDER BY COUNT(*) DESC;"
→ 预期输出：按分类统计结果
```

---

## 阶段反思指令

每完成一个模块，在进入下一个模块之前，执行以下反思：

1. **检查产物**：列出本模块所有新建/修改的文件路径
2. **检查偏差**：是否有步骤与计划不符？记录差异
3. **判断是否继续**：
   - 如果偏差 ≤ 1 个文件路径变化 → 直接修改计划文档对应模块，继续
   - 如果偏差 2-4 个文件或步骤顺序调整 → 在计划文档末尾追加 "v1.1 修订记录"，继续
   - 如果偏差 > 4 个文件或架构方向变化 → **停止**，报告给用户，等用户决定是否重新生成计划

---

*本计划为 AI 可执行操作手册，由 req-plan 技能生成。*
