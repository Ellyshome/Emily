# 建立看板功能


---
## 项目状态机
- 意义：一个项目需要有状态机来记录当前项目的进程，正在展开的工作，和已经完成的工作，具备条件展开的工作阶段。未展开工作阶段所缺少的条件，条件不足但迫于实际需求强行展开的工作阶段以及其临时措施等。
- 被使用：它被用来被参考，对传入信息进行合理性分析。比如：
    - 有用户试图上传19#楼4层外窗安装完工工作量，Agent通过查阅项目状态机可以发现的问题如：当前项目进展是否在外墙工程施工阶段，若否，则判定信息异常

阶段推进机制，满足阶段目标条件，自动达成阶段完成，并通知对应的阶段负责人确认，
对应阶段负责人与其上级有权做对所辖阶段进行：完工确认、开启确认、强制开启，强制完工确认。
强制开启和强制完工确认需要给出备注理由。

---
完成以下任务：
1、当前项目有很多还处于moke数据支撑阶段，还有phase阶段没有完成，列出项目已经完成框架的所有待实现的模块和进度。
2、对所有带实现内容，分模块指定实现计划。先列出模块，下一步再考虑具体实现顺序与深化设计。
3、本轮任务生成文件：
    - 现状分析报告（列出有哪些主要业务功能其中哪些是mock或待实现的模块，形成清单）。
    - 根据分析报告的清单，指定粗略实现计划。我判断选择你再深化设计。
4、以上两项成果保留在当前项目根目录下，md格式存储。


---
## 当前项目需要一套鉴权系统。有几个原则
- 权限需要有大类分组和详细权限组成。
  - 大分类的权限范围由访客、参建、主管、一般管理员、系统管理员五层构成。
  - 细分权限，对每个数据库表、SOP流设置专属权限编号。单位数据库中备注了对应标识方可访问对应资源。
  - 细分权限以 公司单位表做初步细分，公司一般至少分参见、主管两集合。
- 信息存储分级
  - 至少分公开信息、内部信息两级
- 授权：session-agent灌注时，即将对应用户的权限表全量灌注进去，在session-agent进行数据库访问、sop业务流检索时，只能看到权限范围内的信息。默认有授权的就是它可拥有的权限，后续运作就不必重复鉴权了
- 人员初始都只有访客权限，可访问公共信息数据库。如项目名称、绿化面积、容积率等公开信息。
- 鉴权：session-agent鉴权时要审查的内容是：用户单位是否在履约中。这需要数据库的配合，还需要有项目总状态机的配合。数据库记录当前履约状态，状态机记录员工提交的任务是否为已激活未完工的状态。
- 参建单位可访问自己承包范围相关信息，无关信息无读写权限。如：景观承包单位、铺装班组人员上传电梯完工记录，应视为异常信息。
---

广播机制：
底层权限更新，在执行的session进行热更新

顺位上级

---
我需要在本地建立一组MD文件存放信息，关于多人写作开发的共享基础信息，它需要包含：
项目的代码文件层级目录，与各文件 的一句话说明。
业务逻辑全景路线图。
接口协议约定清单
数据库表清单与一句话功能 and 数据库表详细架构
