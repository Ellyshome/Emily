# 代码清理报告

> 执行日期：2026-06-26

---

## 总览

| 指标 | 数值 |
|------|------|
| 删除文件数 | **30** |
| 删除代码行（估算） | ~2,500+ |
| 保留的活跃模块 | agent/ (6) + pipeline/mocks (3) + pipeline/interfaces (6) |
| 导入链断裂 | **0** |

---

## 删除清单

### 1. 冷备 Agent 模块（5 文件）

| 文件 | 复杂度 | 原因 |
|------|--------|------|
| `agent/master_agent.py` | 412 行 | 原 M7 ReAct 主引擎，已被 `SessionAgent` 完全替代 |
| `agent/business_flow_agent.py` | 446 行 | 原 M9 Specialist Agent，M14 结构化输出模式已提取到 `WorkItemAgent` |
| `agent/conversation_context.py` | ~100 行 | 短期对话记忆，仅旧 `MasterAgent` 使用，`SessionAgent` 不 import |
| `agent/flow_renderer.py` | 142 行 | Mermaid 决策树管理器，仅旧 `MasterAgent` 使用 |
| `agent/mermaid_flow.py` | ~160 行 | Mermaid 图文解析器，仅旧 `FlowMapManager` 使用 |

### 2. 遗留 WorkOrder 引用（2 文件）

| 文件 | 原因 |
|------|------|
| `workitem/_work_order_ref.py` | 旧 M15 11 态 WorkOrder 状态机，已被 `WorkItem`（6 态）取代 |
| `workitem/_pipeline_context_ref.py` | 旧 M15 PipelineContext，已被 `BusContext` 取代 |

### 3. 旧工具（1 文件）

| 文件 | 原因 |
|------|------|
| `tools/business_flow_tool.py` | 仅包含 `_extract_allowed_tools_from_sop()`，唯一调用者位于已删除的 `master_agent.py` |

### 4. 已死亡 Mock（3 文件）

| 文件 | 原因 |
|------|------|
| `pipeline/mocks/mock_auth.py` | 0 个生产代码 import，由 `EmilyCore.auth` 直接放行替代 |
| `pipeline/mocks/mock_routing.py` | 0 个生产代码 import，由 `SessionAgent` 意图识别替代 |
| `pipeline/mocks/mock_risk.py` | 0 个生产代码 import，由 `workitem_agent.grade_risk` 直接返回 L2 替代 |

### 5. 一次性根目录脚本（19 文件）

- **数据库探查** (7)：`check_conversations.py`、`check_db.py`、`check_db2.py`、`check_db3.py`、`check_db4.py`、`check_task.py`、`check_users.py`
- **解析调试** (4)：`debug_full.py`、`debug_lines.py`、`debug_nodes.py`、`debug_parse.py`
- **一次性修复** (4)：`fix_anomaly_column.py`、`fix_columns.py`、`fix_permissions.py`、`fix_status_column.py`
- **数据/图表生成** (3)：`setup_test_data.py`、`setup_test_data2.py`、`gen_mermaid.py`
- **集成测试** (1)：`test_plan_task_direct.py`

---

## 保留清单

### Agent 包（6 文件 — 热路径活跃）

| 文件 | 热路径使用 |
|------|-----------|
| `guardian_agent.py` | `DeepAuditHook` 启用的深度调查，由 `EmilyCore` 工厂创建 |
| `guardian_review.py` | 轻量核验器，由 `WorkItemAgent` 在节点 3/4 使用 |
| `intent_registry.py` | `SOPIntentRegistry`，由 `EmilyCore._init_phase_b_deps()` 创建 |
| `sop_parser.py` | SOP Markdown 解析器，由 `SOPIntentRegistry` 使用 |
| `tool_registry.py` | `ToolRegistry` + `ToolDefinition`，由所有工具工厂使用 |
| `__init__.py` | 包导出 — 重写为仅导出上述活跃模块 |

### Pipeline Mock（3 文件 — 活跃回退）

| 文件 | 回退逻辑 |
|------|----------|
| `mock_planning.py` | 当 `EMILY_PLANNER_MODE != "real"` 时使用 |
| `mock_execution.py` | 当 `EMILY_EXECUTOR_MODE != "real"` 时使用 |
| `mock_guardian.py` | 当 `EMILY_GUARDIAN_MODE != "real"` 时使用 |

### Pipeline Interface（6 文件 — 活跃合约）

`auth.py`、`routing.py`、`planning.py`、`execution.py`、`guardian.py`、`risk.py`

---

## 验证结果

```
[OK] EmilyCore
[OK] tools factories (create_all_tools, create_business_flow_tools)
[OK] workitem (WorkItemAgent, PipelineBUS, WorkItemState)
[OK] agent surviving modules (ToolRegistry, ToolDefinition, SOPIntentRegistry,
                             GuardianReview, GuardianAgent)
[OK] surviving mocks (MockPlanner, MockWorkAgent, MockGuardian)
[OK] 5 cold-reserve modules confirmed removed
[OK] _work_order_ref confirmed removed
[OK] business_flow_tool confirmed removed
ALL PASSED — 0 import breaks, codebase stable
```
