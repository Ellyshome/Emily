# Session 原子化能力重构 — 实施计划

> 日期：2026-07-05
> 关联需求：[需求说明.md](需求说明.md)
> 版本：v2 — 根据意见修订

---

## 交付物清单

| # | 交付物 | 类型 | 说明 |
|---|--------|------|------|
| 1 | `tool_registry` DB 表 + ORM 模型 | 数据库 | API 注册表，含 ID / 签名 / 一句话说明 |
| 2 | `session_accessible_files` DB 表 + ORM 模型 | 数据库 | 用户→文件可见关系 |
| 3 | `ToolRegistryRepo` | Repository | API 注册表 CRUD + 查询 |
| 4 | `SessionAccessibleFileRepo` | Repository | 可见文件同步 + 查询 |
| 5 | `register_api.py` — API 注册器脚本 | 脚本 | 一次性完成：代码注册 + DB 录入 |
| 6 | `search_files.py` — 文件搜索工具脚本 | 工具脚本 | 双用途：API 调用 + 独立运行 |
| 7 | `SessionContext` 扩展 | 代码改动 | 新增 6 个原子化能力字段 |
| 8 | `SessionDataFetcher` 扩展 | 代码改动 | 新增 4 个子采集函数 |
| 9 | `SkillExecutor` 白名单软化 | 代码改动 | 元能力独立执行路径 |
| 10 | `session_accessible_files` 数据填充逻辑 | 代码改动 | 项目范围 + 节点关联自动授权 |
| 11 | Prompt 模板更新 | 配置 | session.md / workitem.md / planner.md |
| 12 | 文档更新 | 文档 | 数据库设计 / 业务全景 / CLAUDE.md |

---

## Step 1: 新建 DB 表 + ORM 模型 + Repository

### 1.1 `tool_registry` 表

**文件**：`emily-core/emily_core/infrastructure/database/models.py` 追加

```python
class ToolRegistryModel(Base):
    """API 注册表 —— 统一记录所有注册到系统的工具 API 元数据。"""
    __tablename__ = "tool_registry"

    id              = Column(String, primary_key=True)       # API 唯一标识，如 "search_files"
    signature       = Column(Text, nullable=False, default="{}")   # API 签名（参数+返回值 JSON Schema）
    display_name    = Column(String(200), nullable=False)          # 功能一句话说明
    category        = Column(String(20), nullable=False, default="base")  # base / business / project
    permission_flag = Column(String(50), nullable=False, default="all")
    handler_module  = Column(String(200), default="")
    is_active       = Column(Boolean, nullable=False, default=True)
    registered_at   = Column(String(50), nullable=False)
    updated_at      = Column(String(50), nullable=False)
```

> **必含字段**：API 的 ID（`id`）、API 签名（`signature`）、API 功能一句话说明（`display_name`）。

### 1.2 `session_accessible_files` 表

```python
class SessionAccessibleFile(Base):
    """Session 可见文件表 —— 记录用户→文件的可见关系。"""
    __tablename__ = "session_accessible_files"

    id          = Column(String, primary_key=True, default=_new_uuid)
    user_id     = Column(String, nullable=False, index=True)
    file_id     = Column(String, nullable=False, index=True)
    access_type = Column(String(50), nullable=False, default="project_scope")
        # project_scope / node_linked / explicit
    granted_by  = Column(String, default="")
    granted_at  = Column(String(50), nullable=False)
    expires_at  = Column(String(50), default="")

    __table_args__ = (
        UniqueConstraint("user_id", "file_id", name="uq_saf_user_file"),
    )
```

### 1.3 `ToolRegistryRepo`

**新建文件**：`emily-core/emily_core/repositories/tool_registry_repo.py`

核心方法：
- `upsert(api_id, signature, display_name, category, permission_flag, handler_module)` — 注册/更新
- `get_available(permission_level, sop_allow)` — 查询当前用户可用 API 列表
- `get_help(api_id)` — 获取 API 详细签名（从 BusinessFlowToolRegistry 实时取）
- `get_all_active()` — 获取全部活跃 API
- `deactivate(api_id)` — 停用 API

### 1.4 `SessionAccessibleFileRepo`

**新建文件**：`emily-core/emily_core/repositories/session_accessible_file_repo.py`

核心方法：
- `sync_for_user(user_id, project_ids, info_level, authorized_node_ids)` — 批量同步
- `get_file_summary(user_id)` — 可见文件摘要（count + type 分布）
- `search(user_id, keyword)` — 关键词搜索可见文件（供 search_files 工具调用）
- `add_explicit_grant(user_id, file_id, granted_by)` — 显式授权
- `revoke(user_id, file_id)` — 撤销

---

## Step 2: API 注册器脚本 `register_api.py`

**新建文件**：`scripts/register_api.py`

### 功能

一次执行同时完成：
1. **代码注册**：将 API handler 注册到 `BusinessFlowToolRegistry`
2. **DB 录入**：将 API 元数据写入 `tool_registry` 表

### 设计

```python
"""register_api.py — API 注册器。

将工具脚本注册为系统 API，同时完成：
  1. 代码注册到 BusinessFlowToolRegistry
  2. 元数据写入 tool_registry 表

用法：
  # 注册单个 API
  uv run python scripts/register_api.py --api search_files

  # 注册全部 API
  uv run python scripts/register_api.py --all

  # 查看已注册 API
  uv run python scripts/register_api.py --list

  # 查看某个 API 帮助
  uv run python scripts/register_api.py --help-api search_files
"""

# 核心逻辑：
# 1. 扫描 emily-core/emily_core/tools/scripts/ 目录下的工具脚本
# 2. 每个脚本暴露 register() 函数，返回 BusinessFlowTool 实例
# 3. 调用 register() 获得 BusinessFlowTool
# 4. 注册到 BusinessFlowToolRegistry（内存）
# 5. 同步写入 tool_registry 表（DB）
```

### 注册流程

```python
def do_register(api_id: str, core=None):
    """注册单个 API：代码注册 + DB 录入。"""

    # 1. 从工具脚本目录导入 handler
    module = importlib.import_module(f"emily_core.tools.scripts.{api_id}")
    bft = module.register(core)  # 返回 BusinessFlowTool

    # 2. 代码注册
    registry = core._business_flow_tools
    if not registry.has(bft.name):
        registry.register(bft)

    # 3. DB 录入
    ToolRegistryRepo.upsert(
        api_id=bft.name,
        signature=json.dumps({"params": bft.parameters, "returns": "dict"}, ensure_ascii=False),
        display_name=bft.description.split('。')[0][:80],
        category=getattr(bft, 'category', 'base'),
        permission_flag=getattr(bft, 'permission_flag', 'all'),
        handler_module=bft.handler.__module__,
    )
```

---

## Step 3: 演示工具 `search_files.py`

**新建文件**：`emily-core/emily_core/tools/scripts/search_files.py`

### 功能

根据自然语言描述，在可见文件中，通过关键词匹配文件名或一句话描述字段，查找最可能匹配的文件。

### 双用途设计

```python
"""search_files.py — 文件搜索工具。

双用途：
  1. 作为 API 供系统调用（register() 注册到 BusinessFlowToolRegistry）
  2. 作为独立脚本运行：uv run python -m emily_core.tools.scripts.search_files "精装施工图"

API 签名：
  params:
    query: str        # 自然语言描述或关键词
    top_k: int = 5    # 返回结果数上限
    user_id: str      # 内部注入，不暴露给调用者
  returns:
    dict: {success, total, files: [{file_id, filename, file_type, description, score}], reply}
"""
```

### 核心实现

```python
import sys
import json
import argparse

# ── API 签名 ──
SEARCH_FILES_SCHEMA = {
    "type": "object",
    "properties": {
        "query": {
            "type": "string",
            "description": "自然语言描述或关键词，用于匹配文件名或文件一句话描述",
        },
        "top_k": {
            "type": "integer",
            "description": "返回结果数上限，默认 5",
        },
    },
    "required": ["query"],
}

SEARCH_FILES_DISPLAY_NAME = "根据自然语言描述搜索可见文件"

# ── Handler（系统调用）──

async def handle_search_files(params: dict, **kwargs) -> dict:
    """搜索可见文件 handler。

    通过关键词匹配 session_accessible_files 中用户可见文件的
    文件名（filename）和一句话描述字段。
    """
    query = params.get("query", "").strip()
    top_k = min(int(params.get("top_k", 5)), 20)
    user_id = params.get("_user_id", "") or kwargs.get("user_id", "")

    if not query:
        return {"success": False, "reply": "请提供搜索关键词"}
    if not user_id:
        return {"success": False, "reply": "用户身份未识别"}

    from ...repositories.session_accessible_file_repo import SessionAccessibleFileRepo
    results = SessionAccessibleFileRepo.search(user_id, query, top_k=top_k)

    if not results:
        return {"success": True, "total": 0, "files": [],
                "reply": f"未找到与「{query}」相关的文件"}

    reply_parts = [f"找到 {len(results)} 个相关文件："]
    for f in results:
        reply_parts.append(f"  · {f['filename']}（{f.get('description', f.get('file_type', ''))}）")

    return {
        "success": True,
        "total": len(results),
        "files": results,
        "reply": "\n".join(reply_parts),
    }


# ── 注册函数（供 register_api.py 调用）──

def register(core=None):
    """返回 BusinessFlowTool 实例，供注册器使用。"""
    from ..business_flow_tools import BusinessFlowTool
    return BusinessFlowTool(
        name="search_files",
        description=SEARCH_FILES_DISPLAY_NAME,
        parameters=SEARCH_FILES_SCHEMA,
        handler=handle_search_files,
        # 扩展字段（register_api.py 会读取）
        category="base",
        permission_flag="all",
    )


# ── 独立脚本入口（运维/测试）──

def main():
    parser = argparse.ArgumentParser(description="搜索可见文件（独立脚本模式）")
    parser.add_argument("query", help="搜索关键词")
    parser.add_argument("--user-id", required=True, help="用户 UUID")
    parser.add_argument("--top-k", type=int, default=5, help="返回结果数上限")
    parser.add_argument("--json", action="store_true", help="JSON 格式输出")
    args = parser.parse_args()

    # 独立运行时需要初始化 DB
    from emily_core.infrastructure.database import init_db, get_session
    init_db()

    from emily_core.repositories.session_accessible_file_repo import SessionAccessibleFileRepo

    # 先同步可见文件
    # SessionAccessibleFileRepo.sync_for_user(...)

    results = SessionAccessibleFileRepo.search(args.user_id, args.query, top_k=args.top_k)

    if args.json:
        print(json.dumps({"total": len(results), "files": results}, ensure_ascii=False, indent=2))
    else:
        if not results:
            print(f"未找到与「{args.query}」相关的文件")
        else:
            print(f"找到 {len(results)} 个相关文件：")
            for f in results:
                desc = f.get('description', f.get('file_type', ''))
                print(f"  · {f['filename']}（{desc}）")


if __name__ == "__main__":
    main()
```

### SessionAccessibleFileRepo.search() 实现

```python
@staticmethod
def search(user_id: str, keyword: str, top_k: int = 5) -> list[dict]:
    """关键词搜索用户可见文件。

    匹配策略：
      1. 将 keyword 按中文分词/空格拆分为多个子词
      2. 在 files.filename 和 files.file_type 中进行 ILIKE 模糊匹配
      3. 按匹配得分排序（文件名匹配权重 > file_type 匹配权重）
    """
    from ..infrastructure.database import get_session
    from ..infrastructure.database.models import File, SessionAccessibleFile

    # 拆分关键词
    keywords = [k.strip() for k in keyword.replace(" ", " ").split() if k.strip()]
    if not keywords:
        return []

    with get_session() as session:
        # 先获取用户可见文件 ID
        accessible = session.query(SessionAccessibleFile.file_id).filter(
            SessionAccessibleFile.user_id == user_id
        ).subquery()

        # 在可见文件中搜索
        query = session.query(File).filter(
            File.id.in_(accessible),
            File.is_deleted == False,
        )

        # 逐关键词 ILIKE 匹配
        conditions = []
        for kw in keywords:
            conditions.append(File.filename.ilike(f"%{kw}%"))
            conditions.append(File.file_type.ilike(f"%{kw}%"))

        from sqlalchemy import or_
        files = query.filter(or_(*conditions)).limit(top_k * 2).all()

        # 计算得分
        scored = []
        for f in files:
            score = 0
            for kw in keywords:
                if kw.lower() in (f.filename or "").lower():
                    score += 10  # 文件名匹配权重高
                if kw.lower() in (f.file_type or "").lower():
                    score += 3
            scored.append({
                "file_id": f.id,
                "filename": f.filename or "",
                "file_type": f.file_type or "",
                "description": f.file_type or "",  # 一句话描述暂用 file_type
                "score": score,
            })

        # 按得分排序
        scored.sort(key=lambda x: x["score"], reverse=True)
        return scored[:top_k]
```

---

## Step 4: 扩展 `SessionContext`

**文件**：`emily-core/emily_core/session/session_context.py`

**新增字段**：

```python
# ── 原子化能力字段（🔥 可热更新）──
available_tools: list[dict] = field(default_factory=list)
# [{"api_id": "search_files", "display_name": "根据自然语言描述搜索可见文件"}, ...]

visible_schema_summary: str = ""
visible_files_count: int = 0
visible_files_summary: str = ""

rag_available: bool = False
rag_collections: list[str] = field(default_factory=list)
```

**改动方法**：
- `create()` — 从 SessionDataFetcher 数据灌注新字段
- `refresh()` — 支持热更新新字段
- `get_prompt_variables()` — 新增模板变量

```python
"{available_tools}": self._format_tools_summary(),
"{visible_schema}": self.visible_schema_summary,
"{visible_files}": self.visible_files_summary,
"{rag_info}": self._format_rag_summary(),
```

新增辅助方法：

```python
def _format_tools_summary(self) -> str:
    if not self.available_tools:
        return "（无可用工具）"
    lines = []
    for t in self.available_tools:
        lines.append(f"  · {t['api_id']}: {t['display_name']}")
    return "\n".join(lines)

def _format_rag_summary(self) -> str:
    if not self.rag_available:
        return "知识库不可用"
    collections = "、".join(self.rag_collections) if self.rag_collections else "默认知识库"
    return f"知识库可用（{collections}）"
```

---

## Step 5: 扩展 `SessionDataFetcher`

**文件**：`emily-core/emily_core/session/session_data_fetcher.py`

**新增 4 个子采集函数**：

### `_sub_fetch_available_tools(perms)`
- 从 `ToolRegistryRepo.get_available()` 获取 API 列表
- 权限过滤：base→全部可用，business→检查权限，project→admin_only

### `_sub_fetch_visible_schema(perms)`
- 从 `db_perms` 获取可访问表列表
- 格式化为精简文本

### `_sub_fetch_visible_files(user_id)`
- 调 `SessionAccessibleFileRepo.sync_for_user()` 同步
- 调 `SessionAccessibleFileRepo.get_file_summary()` 获取摘要

### `_sub_fetch_rag_info(core)`
- 从 `rag_provider.is_available()` + 知识库信息获取

---

## Step 6: SkillExecutor 白名单软化 — 独立执行路径

### 核心变更

**文件**：`emily-core/emily_core/skill/executor.py`

SOP-Skill 不再记录工具白名单，工具以 Session 可见 API 为准。改动：

1. 白名单检查从"硬约束"改为"Session 可见性检查"
2. 新增元能力独立执行路径：当步骤工具不在 Skill 推荐，但在 Session 可见 API 中时，允许调用

```python
# 构建 Session 可见 API 集合
session_api_ids = {t["api_id"] for t in ctx.session_available_tools}

for step in ctx.skill.steps:
    if not step.tool_name:
        # 纯逻辑步骤
        ...
        continue

    # 检查工具是否在 Session 可见 API 中
    if step.tool_name not in session_api_ids:
        results.append(StepResult(
            step_id=step.id,
            success=False,
            output=f"工具 '{step.tool_name}' 不在 Session 可见 API 中",
        ))
        break

    tool = ctx.business_flow_tools.get(step.tool_name)
    if tool is None:
        results.append(StepResult(
            step_id=step.id,
            success=False,
            output=f"工具 '{step.tool_name}' 未注册",
        ))
        break

    # ── 统一执行路径 ──
    # 不再区分 Skill 推荐路径 vs 元能力路径
    # 所有 Session 可见的 API 都可直接调用
    if step.tool_name in allowed_tools:
        # Skill 推荐路径：有 ParamMapping，走现有逻辑
        tool_params = await self._param_extractor.resolve_params(...)
    else:
        # 元能力路径：Skill YAML 没定义 ParamMapping，LLM 动态推导
        tool_params = await self._llm_resolve_params(step, tool, ctx)

    # 注入运行时上下文 + 调 handler
    ...
```

### `SkillExecutionContext` 扩展

**文件**：`emily-core/emily_core/skill/definition.py`

```python
session_available_tools: list[dict] = field(default_factory=list)
```

### `WorkItemAgent` 改造

**文件**：`emily-core/emily_core/workitem/workitem_agent.py`

`_execute_skill()` 注入 `session_available_tools`。

---

## Step 7: `session_accessible_files` 数据填充

**文件**：`emily-core/emily_core/repositories/session_accessible_file_repo.py`

`sync_for_user()` 逻辑：

1. 清除该用户旧的 `access_type='project_scope'` 记录
2. 从 `files` 表按 `project_ids` + `confidentiality ≤ info_level` 查询 → 批量写入
3. 从 `node_accessible_files` 按用户 `authorized_node_ids` 关联查询 → upsert
4. `access_type='explicit'` 的记录不清除

`get_file_summary()` 返回：

```python
{
    "count": 23,
    "by_type": {"施工图": 8, "报告": 5, "规范": 3, "其他": 7},
}
```

---

## Step 8: Prompt 模板更新

**文件**：`emily-data/prompts/session.md`

新增段落：

```markdown
## 你的元能力（Session 可见的 API 工具，随时可用）

### 可用工具
{available_tools}

### 可查询的数据库
{visible_schema}

### 可访问的文件
{visible_files}

### 知识库
{rag_info}

注意：以上工具你随时可以调用，不受匹配到的业务流程限制。
当业务流程的工具无法满足用户需求时，你可以自由选用。
```

`workitem.md` / `planner.md` 同理。

---

## Step 9: 文档更新

| 文档 | 更新内容 |
|------|----------|
| `docs/数据库设计.md` | 新增 `tool_registry` + `session_accessible_files` 表 |
| `docs/业务模块与运转全景.md` | 更新 Session 能力模型 |
| `CLAUDE.md` | 更新关键文件索引 |

---

## 关键文件清单

| 文件 | 改动类型 | 说明 |
|------|----------|------|
| `emily-core/emily_core/infrastructure/database/models.py` | 追加 | ToolRegistryModel + SessionAccessibleFile ORM |
| `emily-core/emily_core/repositories/tool_registry_repo.py` | **新建** | API 注册表 Repository |
| `emily-core/emily_core/repositories/session_accessible_file_repo.py` | **新建** | 可见文件 Repository + search() |
| `scripts/register_api.py` | **新建** | API 注册器脚本 |
| `emily-core/emily_core/tools/scripts/__init__.py` | **新建** | 工具脚本包 |
| `emily-core/emily_core/tools/scripts/search_files.py` | **新建** | 文件搜索工具（双用途） |
| `emily-core/emily_core/tools/registry.py` | 改造 | 注册时同步 DB + category/permission_flag |
| `emily-core/emily_core/session/session_context.py` | 扩展 | 6 个原子化能力字段 + prompt 变量 |
| `emily-core/emily_core/session/session_data_fetcher.py` | 扩展 | 4 个子采集函数 |
| `emily-core/emily_core/skill/executor.py` | 改造 | 白名单软化 + 元能力独立路径 |
| `emily-core/emily_core/skill/definition.py` | 小改 | SkillExecutionContext.session_available_tools |
| `emily-core/emily_core/workitem/workitem_agent.py` | 小改 | 注入 available_tools |
| `emily-data/prompts/session.md` | 扩展 | 新增元能力段落 |
| `emily-data/prompts/workitem.md` | 扩展 | 新增元能力段落 |
| `emily-data/prompts/planner.md` | 扩展 | 新增元能力段落 |
| `docs/数据库设计.md` | 更新 | 2 张新表 |
| `docs/业务模块与运转全景.md` | 更新 | Session 能力模型 |
| `CLAUDE.md` | 更新 | 关键文件索引 |

---

## 验证方案

### 1. 独立脚本模式验证

```bash
# 初始化 DB + 注册 API
uv run python scripts/register_api.py --api search_files

# 独立运行 search_files
uv run python -m emily_core.tools.scripts.search_files "精装施工图" --user-id <UUID> --json
```

### 2. DB 验证

```bash
docker compose -f docker-compose-napcat.yml restart emily-core

docker exec emily-postgres psql -U emily -d emily -c \
  "SELECT id, display_name, signature FROM tool_registry;"

docker exec emily-postgres psql -U emily -d emily -c \
  "SELECT user_id, count(*) FROM session_accessible_files GROUP BY user_id LIMIT 10;"
```

### 3. 生产实战测试

```bash
docker exec emily-postgres psql -U emily -d emily -c \
  "SELECT id, username, permission_level FROM users WHERE status = 'active' LIMIT 5;"

# 测试文件搜索（应走 search_files 元能力）
uv run python .claude/skills/emy-test/cli.py --managed --llm \
  --message "是否有精装施工图" --sender "真实用户名"

# 回归测试
uv run python .claude/skills/emy-test/cli.py --managed --llm \
  --message "帮我创建事件：样板段放线完成" --sender "真实用户名"
```

### 4. 注册器验证

```bash
# 查看已注册 API
uv run python scripts/register_api.py --list

# 查看 search_files 帮助
uv run python scripts/register_api.py --help-api search_files
```

### 5. 离线烟雾测试

```bash
uv run python scripts/smoke_test.py
```
