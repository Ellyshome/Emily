
对当前打开的文件进行一些 调整：
1、增加一段修改描述，以前项目对于鉴权的信息，是灌注到Session-agent中的，这个跟着鉴权系统实际实现方法，需要做出调整，在数据库访问权限定上，是通过权限可见范围白名单实现的，这个白名单列表并不适合直接灌给agent，现改为在session状态机内，增加对应的字段来存放。
2、workitem-agent对session状态机有只读权限，用以获取状态信息与权限列表。
3、sop的鉴权分两层，一层是给sop归类，数据库建表存放sop业务流文件特征信息，包括名称、一句话功能描述、权限组归属（如建设单位设计部专用，则只有特定公司（建设单位）的员工，且部门归属为设计部的，方可见可用，这应该至少有两层归属分类，一是企业，二是部门。需要对应的企业数据库也做分组，一般参见企业只做简单的管理组与业务组即可，建设方单位要细分部门以方便权限细分），


项目未来会有一套项目级别的状态机Project State Machine，这个状态机会至少分公共/内部两层，session-agent与workitem-agnet可根据session状态机对应的权限表，读取Project状态机的权限允许的字段，获取项目信息，如：所需更新数据归属的任务阶段是否是已经启动，且处于运转状态。例：景观施工承包单位，业务人员上报，完成编号为S4地块的铺装。查Project状态机发现‘大区景观工程施工’阶段模块已启动，其细分的‘大区景观工程施工’子状态机显示S4地块当前记录处于土方回填阶段，其后续阶段节点‘铺装施工’尚未启动激活，此时则判定信息异常，询问用户录入是否有误，若坚持录入，则应通知有此阶段节点启停的负责人（建设方景观工程主管）给于启动确认，或驳回丢弃