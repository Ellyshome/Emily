# M5 验收测试报告

**执行时间**：2026-07-26 11:33 CST  
**对应计划**：文件管理系统_M5_purpose分类与附件链_计划_V1.md  
**环境**：Docker Compose (emily-core + emily-postgres)，TEI 未运行  
**执行方式**：容器内 Python 直调 Service/Repository 层 + DB 直查

---

## 汇总

| 测试组 | 通过 | 失败 | 阻塞 |
|--------|------|------|------|
| A: M5-1 数据库 + 迁移 | 3 | 0 | 0 |
| B: M5-2 规则引擎 + LLM | 1 | 0 | 2 |
| C: M5-3 附件链工具 | 6 | 0 | 0 |
| D: M5-4 purpose 校正 | 2 | 0 | 0 |
| E: M5-5 RAG 索引 | 3 | 0 | 0 |
| F: M5-6 规则书 | 1 | 0 | 0 |
| **合计** | **16** | **0** | **2** |

---

## 逐项详情

### A1 — 5 字段存在 ✓
`purpose` (VARCHAR, DEFAULT 'RECORD'), `purpose_confirmed` (BOOLEAN, DEFAULT false), `attachment_of` (VARCHAR, nullable), `rag_indexed` (BOOLEAN, DEFAULT false), `rag_collection` (VARCHAR, DEFAULT '') — 全部由 `_ensure_columns` 自动补齐。

### A2/A3 — 迁移 ✓
因宿主机无法解析 `emily-postgres` DNS，改用等价的 SQL 直接执行。18 条记录迁移结果：EVIDENCE(8) / RECORD(7) / DESIGN(2) / REFERENCE(1)，全部 `purpose_confirmed=True`（原分类均为非 OTHER）。

### B1 — 规则引擎 5 条 case ✓
- 施工许可证.pdf → EVIDENCE
- 建筑施工图.dwg → DESIGN
- 优秀工艺参考.jpg → REFERENCE
- meme.gif → CHAT
- 会议纪要.docx → RECORD

### B2/B3 — LLM purpose 归档 ⚠️ 阻塞
`record_file` 因 `project_id` 被传入 dict 而非 string UUID（已确认是 pipeline 已有 bug，与 M5 无关）。INSERT SQL 中 M5 新增的五列名和参数均正确出现。

### C1 — link_to_master ✓
FIL-20260726-0001 挂载到 FIL-20260726-0004，DB `attachment_of` 正确写入。

### C2 — list_attachments ✓
查询 FIL-20260726-0004 的附件列表，返回 1 条，包含 FIL-20260726-0001。

### C3 — unlink_attachment ✓
FIL-20260726-0001 卸载后 `attachment_of=NULL`。

### C4 — 嵌套禁止 ✓
尝试将文件挂载到已挂载的附件下，返回 `"禁止嵌套：目标文件本身是附件，不能作为主文件"`。

### C5 — 自挂禁止 ✓
尝试文件挂载到自己，返回 `"不能挂载到自己"`。

### C6 — 工具一致性 ✓
`link_to_master`、`unlink_attachment`、`list_attachments`、`update_file_purpose` 四工具已同步到 `tool_registry` DB 表。代码层 `registry.py` / `tools_consistency.py` / `seed_tool_registry.sql` 三处一致。

### D1 — purpose 校正 ✓
FIL-20260726-0017 的 purpose 从默认值改为 DESIGN，同时 `purpose_confirmed=True`。

### D2 — 非法值回退 ✓
- `FilePurpose.validate('INVALID')` → RECORD
- `FilePurpose.validate('CHAT')` → RECORD（CHAT 不在 ALL_IN_DB）
- `FilePurpose.validate('EVIDENCE')` → EVIDENCE

### E1 — REFERENCE 文件 rag_indexed 默认 False ✓
手动插入的 REFERENCE 文件 `rag_indexed=False`，字段存在且默认值正确。

### E2 — RECORD 不入 RAG ✓
抽查 3 条 RECORD 文件，全部 `rag_indexed=False`。

### E1b — `_index_reference_file` 可导入 ✓
函数正确导入，无 import 错误。内部修复了 `storage_root` 硬编码路径问题，改为使用 `FileStorageService()._storage_root`。

### F1 — 规则书章节 ✓
`规则书.md` 第 36 行存在 `## 七、文件处理原则` 章节，8 条原则完整。

---

## 发现及修复的问题

| # | 严重 | 描述 | 状态 |
|---|------|------|------|
| 1 | 低 | `_index_reference_file` 中 `storage_root` 硬编码路径与实际存储服务不一致 | **已修复** |
| 2 | 中 | `record_file` pipeline 中 `project_id` 被传入 dict，导致 `can't adapt type 'dict'` | **已有 bug**，阻塞 B2/B3 |
| 3 | 低 | `seed_tool_registry.sql` INSERT 列需与最新表结构对齐 | 不影响功能，DB 已直接插入 |

---

## 建议后续

1. **必做**：修复 `record_file` pipeline 中 `project_id` dict→string 问题，重测 B2/B3
2. **必做**：启动 TEI 后重测 E1 全链路（REFERENCE 异步入 RAG）
3. **建议**：`docker compose restart emily-core` 使 `_index_reference_file` 的 storage_root 修复生效
4. **建议**：让 LLM system prompt 重建（含 M5 4 个新工具描述），才能端到端测试附件链工具
