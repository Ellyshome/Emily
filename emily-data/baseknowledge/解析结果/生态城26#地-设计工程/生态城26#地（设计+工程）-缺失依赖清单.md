# 生态城26#地（设计+工程） — 缺失依赖清单

> **生成时间**：2026-06-29 23:47
> **说明**：以下依赖关系为 Agent 自动推断或无法确认，需要人工逐条审核确认。

---

## 一、需人工确认的阶段间依赖

以下依赖基于阶段排序推断，**置信度低**，建议项目负责人逐条确认：

| # | 上游节点 | 下游节点 | 推断依据 | 待确认事项 |
|---|---------|---------|---------|-----------|
| 1 | SJ-0027 取得图审合格正 | SG-0001 开工、建设 | 阶段间推断 | 是否确实存在前后依赖关系？ |

## 二、WBS层级推断依赖

共 709 条 WBS 层级推断依赖（子节点→父节点）。以下为 Level 2 层级的依赖关系：

| 父节点 | 子节点（L2） | 推断依据 |
|--------|------------|---------|
| SG-0001 开工、建设 | SG-0002 取得工程规划许可证 | WBS父→子层级 |
| SG-0001 开工、建设 | SJ-0001 施工图完成并通过内外审核 | WBS父→子层级 |
| SG-0001 开工、建设 | SG-0016 取得施工许可证 | WBS父→子层级 |
| SG-0001 开工、建设 | SG-0027 开工准备 | WBS父→子层级 |
| SG-0001 开工、建设 | SG-0032 首批次取得预售许可证（含正负零） | WBS父→子层级 |
| SG-0001 开工、建设 | SG-0041 工程施工阶段 | WBS父→子层级 |
| SG-0001 开工、建设 | SG-0680 竣工验收完成 | WBS父→子层级 |

## 三、全局性问题

- **Excel中未标注任何节点间的显式依赖关系**
  - 影响：所有依赖均为推断，需人工逐条确认

- **阶段间依赖仅基于排序推断（最后一节点→下一阶段第一节点），不符合业务实际**
  - 影响：建议补充：设计阶段→工程阶段 的实际前置关系

- **并行节点间无依赖标记（如多个L2节点可能并行）**
  - 影响：无法区分串行/并行关系


## 四、审核操作指引

1. 确认阶段间依赖：检查"阶段间推断依赖"表中每条依赖关系的正确性
2. 补充缺失依赖：对确实存在的依赖关系，注明"已确认"
3. 标记并行关系：标注哪些 L2 节点可以并行推进
4. 添加外部依赖：补充与外部单位（政府审批、水电燃气等）的依赖关系
5. 更新全景节点图：确认后的依赖在 Mermaid 图中改为实线