# Emily 项目开发记录

> ← 返回 [CLAUDE.md](../CLAUDE.md) | 最后更新：2026-06-26

---

## 1. 演进时间线

### 旧纪元：EmyBot（2026-06-09 ~ 2026-06-22）

EmyBot 时代（包名 `teambrain_core`），架构是 AstrBot 内嵌插件 + 8 阶段 WorkOrder 管道。

| 里程碑 | 日期 | 内容 |
|--------|------|------|
| M1 | 06-09 | 插件壳 + 分域接管（@bot 接管/群聊放行） |
| M2 | 06-09 | PostgreSQL + SQLAlchemy 2.0，消息入库，用户绑定，event_id 幂等 |
| M3 | 06-10 | LLM 意图路由（RouterAgent），事件录入+确认流程 |
| M4 | 06-10 | Task/Meeting/File CRUD，编号生成（TSK/MTG/FIL-YYYYMMDD-NNNN） |
| M5 | 06-11 | 结构化查询 QueryService（9 种 query_type） |
| M7 | 06-11 | MasterAgent ReAct 主 Agent + 动态技能 + ToolRegistry |
| M6 | 06-12 | GuardianAgent 守护调查员 + invoke_guardian + write_notebook |
| M8 | 06-13 | 守护增强（启动体检+回复核验+前导消息+项目日记+长期记忆），Session 修复 5 bug |
| M9 | 06-14 | 业务流架构（SOPIntentRegistry 发现式路由 + BusinessFlowAgent + sop_routing_logs）；19 文件 |
| M10 | 06-18 | 地产领域知识注入（L1 核心认知 ~320 tokens + L2 按需 RAG），108 用例全部通过 |
| M11 | 06-18 | 全量聊天存档 + Agent 追踪（4 新表 + 3 服务 + chat_archive 工具）+ PG 引擎 |
| M12a | 06-19 | Hook 总线架构（9 阶段 MessagePipeline Hook 声明式）— 06-22 由 M15 全面接管废弃 |
| M12b | 06-19 | Checkpoint 持久化（SOPCheckpoint 表 + CheckpointService），16 列状态快照 |
| M13 | 06-20 | 文件传输（收发+Agent 按需读取），download 管道阶段 |
| M14 | 06-21 | 核心业务工具迁移 SOP 业务流（BusinessFlowToolRegistry，结构化输出模式） |
| M15 | 06-22 | 总线管道重构（WorkOrder 全息流转单 + PipelineScheduler 8 阶段 + 6 Mock），M12a 完全移除 |

**总计**：16 个 M 阶段（M1-M15，扣除空号） + Ex1(Web测试) + Ex4(MaxKB RAG)，约 50 份实施计划与测试报告。

---

### 新纪元：Emily（2026-06-23 ~ 至今）

旧 EmyBot 架构痛点驱动重构：8 阶段铁序管道僵化、MasterAgent 职责过重（路由+执行+合成三重角色）、AstrBot 容器绑定无法独立、确认流内存 dict 重启丢失。

**核心变化**：
- 包名 `teambrain_core` → `emily_core`（内核独立）
- AstrBot 内嵌插件 → **双容器**（薄插件 `emily_agent` + 独立 `emily-core` FastAPI）
- 8 阶段 WorkOrder 管道 → **Session 主线 + WorkItem 4 节点 PipelineBUS**
- Agent 编排（M14 结构化输出优先、Hook 声明式）保留，逻辑从旧 `agent/` 提取到 `SessionAgent`/`WorkItemAgent`

#### Phase 0：容器拆分 + 骨架搭建（06-23）

| 产出 | 说明 |
|------|------|
| `emily-core` 容器 | Python 3.12-slim + FastAPI + uvicorn，`emily_core` 包独立，不 import AstrBot |
| `emily_agent` 薄插件 | ~100 行，去重 + 标准化 + HTTP 转发 + SSE 监听，不含业务逻辑 |
| 5 服务 Docker Compose | napcat / astrbot / emily-core / maxkb / emily-postgres |
| `emily-data/` 挂载 | sops/prompts/config（只读）/ notebooks/logs/attachments/user_memory/journal（读写） |
| `EmilyCore` 惰性初始化 | 首消息时构建所有子系统 |

#### Phase A：Session 骨架 + PipelineBUS（06-23 ~ 06-24）

| 产出 | 说明 |
|------|------|
| `SessionPoolManager` | conversation_id→SessionAgent 哈希，lookup-or-create，per-session 串行锁，TTL 后台清理 |
| `SessionAgent` | 消息接收 + 回复聚合，骨架就位（快回/意图/拆解后期实现） |
| `WorkItem` + 6 态状态机 | CREATED→PLANNING→EXECUTING→DONE/FAILED，WAITING_CONFIRM 预留 |
| `PipelineBUS` 4 节点 | node1(意图+拆分) / node2(计划+标准) / node3(执行+验收) / node4(成果总结) |
| `BusContext` | 跨节点共享状态 + 只读 SessionContext |
| Hook 系统 | `Hook` 基类 + ALLOW/WARN/BLOCK 三态 + `HookRegistry` + `hook_config.json` 声明式 |
| `WorkItemAgent` | 全局单例 4 节点 handler，Mock→Real 模式切换 |
| `KnowledgeInjector` | 增量 SOP/工具/Schema 灌注 + token 预算 LRU |

**此阶段全部大脑为 Mock**（固定输出，不依赖 LLM）。

#### Phase B：SessionAgent 升级 + 意图识别 + 自规划（06-24）

| 产出 | 说明 |
|------|------|
| SessionAgent LLM 意图识别 | `_recognize_intent()`：注入 SOPIntentRegistry.dump_type_tree → LLM JSON 返回 `{sop_id, confidence, is_compound, sub_tasks}` |
| `KnowledgeInjector.analyze` | 换入真实 SOPLoader（按需加载 §1-7 全文） |
| `WorkItemAgent._llm_plan()` | 替代 MockPlanner：LLM 生成 `ExecutionPlan`，步骤绑定 `tool_name` |
| `FocusLock` + `ConfirmQueue` | 话题切换检测 + 待确认优先级排队 |
| `SessionAgent.archive()` | Session 关闭时清理状态 + 写结束标记 |
| `AuthHook` | `before:wi_node2` / `before:wi_node3` admin_check（读取 PermissionSnapshot） |
| 旧 `agent/` 声明冷备 | `master_agent.py` / `business_flow_agent.py` 等不再 import——逻辑提取到新位置 |

#### Phase C：执行器 + Guardian + 鉴权 + 风险 → Real（06-24）

| 产出 | 说明 |
|------|------|
| `WorkItemAgent._real_execute()` | M14 模式：遍历 ExecutionPlan.steps → `BusinessFlowTool.handler(tool_params)` 直调 |
| `Guardian` 集成 | 核验与深度审计（`EMILY_GUARDIAN_MODE` 开关） |
| `RealAuthEngine` | 基于 User.perm_list/grouping/permission_level + SOP allow_roles |
| `RealRiskGrader` | 基于意图类型 + 置信度 + 操作类型分级 L1/L2/L3 |
| 6 个 `EMILY_*_MODE` 开关 | PLANNER / EXECUTOR / GUARDIAN / AUTH / RISK + `_resolve_mode()` 无 LLM 自动回退 mock |
| 生产实战测试 | 13/13 用例通过，7 bug 修复（SOP 目录路径、baggage.set、create_all_tools 缺参、TraceHook 名称不匹配等） |

#### 计划任务模块（06-25）

| 产出 | 说明 |
|------|------|
| 4 张新表 | `plan_task_templates` / `instances` / `logs` / `deliverables`（29 表） |
| 7 态状态机 | WAITING→SUBMITTED→(RETURNED→SUBMITTED)→CONFIRMED→ARCHIVED；+ANOMALY_PENDING_REVIEW / CANCELLED |
| `PlanTaskService` | 模板/实例生命周期 + 鉴权 + 非周期匹配 |
| `PlanTaskScheduler` | 后台 asyncio 循环：PG advisory lock / 超时提醒 / 周期生成 / LLM 失败通知 / 自动归档 + 升级 |
| `PlanTaskMatchHook` | `before:wi_node2` 挂载，检测到期计划任务自动匹配 |
| SOP-009 + SOP-010 | 计划任务创建 / 审核流 |
| P0-P2 代码审核修复 | 13 项通过：sync/async 混用（to_thread 包裹）、instance_no vs id 混淆、缺 UniqueConstraint partial index、缺 permission_level 字段、advisory lock 释放时机、LLM failure 标志位位置、事务边界 |

#### 全景节点图 + 全局状态机（06-26）

| 产出 | 说明 |
|------|------|
| `state_machine/` 状态枚举 | `NodeStatus` 5 态（未启动/进行中/已阻塞/已延期/已完成）+ `StageStatus` 3 态（未启动/进行中/已完成）+ 转移矩阵 + 7 阶段名称/边界定义 |
| 7 张新表 | `sm_nodes` / `sm_node_dependencies` / `sm_node_deliverables` / `sm_stages` / `sm_status_history` / `sm_audit_logs` / `sm_simulation_results`（数据库扩至 36 表） |
| `StateMachineService` | 核心引擎：状态变更（合法性校验+审计）+ 前置满足度计算（加权依赖公式）+ BFS 级联更新（前置完成→自动启动下游）+ 阶段/整体进度 + 事件→节点自动匹配 |
| `StateMachineApplication` | 编排层——封装 Service，异常处理 + 格式化输出 |
| 3 个 Repository | sm_node_repo / sm_stage_repo / sm_audit_repo — sync 数据访问 |
| `query_sm_status` 工具 | LLM Agent 按 node_id/stage_id/keyword 搜索节点状态（注册到 ToolRegistry） |
| 10 个 REST 端点 | `GET/PUT /api/v1/state-machine/*` — 节点 CRUD + 阶段进度 + 整体进度 + 前置满足度 + 审计日志 |
| `全景节点.md` | ~90 个标准节点定义（7 阶段 / 主责部门 / 前置条件 / 任务成果） |
| `import_nodes.py` | 解析 `全景节点.md` → 写入 PostgreSQL（支持 --dry-run / --reset） |
| SOP-011 | 全局状态机管理 SOP——查询/变更/审核 7 种子意图 |
| `try_match_and_complete` | `record_event` 工具包装：事件录入后自动匹配全景节点并完成（difflib 关键词相似度） |

---

## 2. 当前状态（2026-06-26）

### 已实施

| 阶段 | 状态 | 说明 |
|------|------|------|
| Phase 0 | ✅ 完成 | 双容器 + `emily_core` API + `emily_agent` 薄插件 |
| Phase A | ✅ 完成 | Session 骨架 + WorkItem + 4 节点 BUS + Hook 系统 + KnowledgeInjector |
| Phase B | ✅ 完成 | SessionAgent LLM 意图 + SOP 路由 + 自规划 + AuthHook + 旧 agent/ 冷备 |
| Phase C | ✅ 完成 | RealExecutor(M14) + Guardian + RealAuth/Risk + 6 模式开关 |
| 计划任务 | ✅ 代码就绪 | 4 表 + 7 态机 + 调度器 + SOP-009/010 + P0-P2 修复；待真实环境验证 |
| 全景节点图 + 全局状态机 | ✅ 代码就绪 | 7 表 + 5 态节点 + 3 态阶段 + 级联更新 + REST API + Agent 工具；待真实数据导入验证 |
| 项目级 Agent | ✅ Phase 1 完成 | ProjectAgent 骨架 + 后台 Tick + 卡滞检测 + 里程碑预警（纯规则，不调 LLM）；Phase 2/3 预留 |
| 生产测试 | ✅ 13/13 通过 | Mock→Real 6 组件切换验证 + 端到端消息流 |

### 遗留问题

| 优先级 | 问题 | 现状 |
|--------|------|------|
| P1 | `knowledge_search` 未接线 RealExecutor 的 BusinessFlowToolRegistry | node3 内联 RAG 处理，但注册表中未正式注册 |
| P2 | ProgressHook `progress_sender` 在容器内为 NoneType | "处理中…" 静默失败 |
| P3 | AuthHook `sop_intent_registry` 初始化失败（dataclass 字段不匹配） | sop_access/resource_check hook 构建失败（非阻塞） |

---

## 3. 关键架构决策（ADR）

### ADR-E01：WorkOrder → WorkItem — 消息级处理上移 Session 层

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-23 |
| **决策** | 旧 M15 WorkOrder（8 阶段单消息流程单）→ 新 WorkItem（4 节点单任务执行单元）；消息级处理（intake/绑定/路由）上移至 Session 层；一条消息可拆分为 0..N 个 WorkItem |
| **原因** | WorkOrder 是"消息→回复"的单通道处理；WorkItem 支持复合请求拆解 + 并行执行；Session 层管理对话状态与确认队列 |
| **后果** | 旧 WorkOrder/PipelineContext 已完全移除；新系统 `WorkItem` + `BusContext` 为唯一路径 |

### ADR-E02：旧 `agent/` 冷备不 import

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-24 |
| **决策** | 原 `emily_core/agent/` 下 Agent 逻辑（MasterAgent/BusinessFlowAgent 等）提取到 `SessionAgent` / `WorkItemAgent`。当前 `agent/` 保留 SOPIntentRegistry / ToolRegistry / sop_parser 等工具模块，仍在热路径中 import。 |
| **原因** | (1) Agent 逻辑与工具注册表解耦；(2) SessionAgent/WorkItemAgent 提供更清晰的职责边界；(3) 新架构用更简洁的代码量表达同类概念 |
| **后果** | `agent/` 中 Agent 类已移除，保留工具注册表/SOP 解析等基础设施模块仍在活跃使用 |

### ADR-E03：M14 结构化输出优先于 ReAct

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-24 |
| **决策** | SOP 命中路径：LLM `chat_json` 提取 `{tool, params}` → 框架直接调用 `BusinessFlowTool.handler(params)`，不走 ReAct + function-calling。Unmatched 兜底路径仍走 ToolRegistry function-calling 自由推理。 |
| **原因** | (1) SOP §3.2 已明确声明所需工具；(2) LLM 核心价值是自然语言→结构化参数提取；(3) 减少 LLM tool-calling 往返延迟和 token 消耗；(4) 框架直接执行消除调用错误工具的风险 |
| **后果** | 5 个核心业务工具（record_event/task/meeting/file + query_data）仅在 `BusinessFlowToolRegistry` 中注册，不暴露为 LLM function-calling |

### ADR-E04：Hook 声明式 JSON 配置

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-23 |
| **决策** | Hook 通过 `emily-data/config/hook_config.json` 声明式挂载到管道节点，不改 Python 代码。三态决策 ALLOW/WARN/BLOCK。 |
| **原因** | 鉴权/审计/核验/追踪是横切关注点，应与业务逻辑分离；非开发人员可通过编辑 JSON 启用/禁用/调整 Hook |
| **后果** | Hook 子类在 Python 中实现（`pipeline/hook.py`），挂载配置在 JSON 中；`HOOK_TYPE_MAP` 实现 type 名→类映射 |

### ADR-E05：容器拆分 + 内核独立

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-23 |
| **决策** | 从 AstrBot 内嵌插件拆为独立 `emily-core` 容器 + 薄 `emily_agent` 插件。Emily Core 不 import `astrbot.*`。 |
| **原因** | (1) 业务内核可独立部署/扩展/测试；(2) 不绑定 AstrBot 容器环境；(3) HTTP+SSE 协议比 Python import 更清晰的边界 |
| **后果** | 插件 `adapters/standard/` 下 DTO 副本（message/reply/command/result）是刻意设计，使插件不依赖 Core 包 |

### ADR-E06：PermissionSnapshot 一次性灌注 + 只读

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-24 |
| **决策** | Session 创建时从 `User` 记录构建 `PermissionSnapshot`（grouping/company/project/SOP/DB 权限），注入 `SessionContext`，WorkItemAgent 通过 `BusContext` 只读访问。 |
| **原因** | (1) 权限在单次对话内不变，无需重复查询 DB；(2) 只读访问防止 AI Agent 越权篡改；(3) snapshot 模式符合 "权限在对话开始时确定" 的业务语义 |
| **后果** | 权限变更要新对话生效；长期运行 Session 可能需要 TTL 驱动的 re-auth |

### ADR-E07：Sync repo + `asyncio.to_thread` 妥协

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-24 |
| **决策** | Repository 层全 sync（SQLAlchemy sync session），async Service 方法通过 `asyncio.to_thread()` 包裹 repo 调用 |
| **原因** | SQLAlchemy async 与 PostgreSQL `pool_size=5` 配合不佳；sync repo 更易调试和测试；项目规模（~15K LOC）可接受此妥协 |
| **后果** | 不是纯粹 async；高并发下 thread pool 可能成为瓶颈；未来可迁移到 SQLAlchemy async |

### ADR-E09：运维模块（OpsScheduler）嵌入 ProjectAgent Phase 3

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-28 |
| **决策** | 新增 `emily_core/project/ops/` 包，实现运维调度模块。关键设计决策：(1) OpsScheduler 嵌入 ProjectAgent._do_tick() 而非独立 asyncio.Task，复用 advisory lock；(2) 邮箱轮询复用 EmailService.fetch_orders()，禁用 raw imaplib；(3) Probe 可插拔接口模式——新增运维检查只需实现 Probe ABC，注册到 OpsScheduler，零修改核心调度代码；(4) 优雅降级：DB 失败自动落盘 MD/JSONL；(5) 冷启动判定查 ops_startup_report 表而非内存计数器。 |
| **替代方案** | (1) 独立 asyncio.Task 后台循环 —— 增加第 3 个后台线程，advisory lock 管理复杂化；(2) 邮箱轮询用 raw imaplib —— 已有 EmailService 基础设施，重复实现违反 DRY。 |
| **影响** | 新增 16 个文件，修改 14 个文件。`config.py` 新增 10 个 `ops_*` 字段，`bootstrap.py` 新增 8 个 `EMILY_OPS_*` 环境变量映射。5 张新 DB 表（`ops_*`），`models.py` 表总数 36→41。 |
| **状态** | ✅ 已完成（Phase 1-5） |

### ADR-E08：新增项目级 Agent（ProjectAgent）— 三层作用域补齐

| 项 | 内容 |
|----|------|
| **日期** | 2026-06-26 |
| **决策** | 新增 `emily_core/project/` 包，引入 `ProjectAgent`——项目级后台 Tick Agent，填补 SessionAgent（会话级）/ WorkItemAgent（任务级）之上的项目级空白。Phase 1 实现状态机卡滞检测 + 里程碑预警（纯规则，不调 LLM）。Phase 2 预留健康度检查，Phase 3 预留 AI 自动运维。 |
| **原因** | (1) 状态机当前纯被动触发——无人主动扫描卡滞/延期节点；(2) 没有项目级健康度/诊断能力；(3) PlanTaskScheduler 只管 plan_task 实例，不碰状态机健康；(4) 三个职责（状态机维护、健康检查、自动运维）落在同一作用域——项目级——由一个 Agent 统一承担最合理 |
| **后果** | 与 PlanTaskScheduler 平级——各管各的 Tick 循环，职责边界清晰（Scheduler → plan_task 实例；ProjectAgent → 状态机节点健康）。复用现有 advisory-lock 防重、OutboundEventBus 推送、SMNodeRepository 查询模式。 |

---

## 4. 权威文档索引

| 文档 | 路径 | 说明 |
|------|------|------|
| **主系统架构蓝图** | `tem/0623Emily_主系统架构.md` | v0.6.0 权威架构：容器拓扑/逻辑分层/Session/WorkItem/PipelineBUS/基础设施/Hook/鉴权/配置/模块映射 |
| **v0.6.0 README** | `tem/0623README.md` | Emily 概述：容器拆分/Session主线/Mock大脑/数据流/快速启动/API表/演进路线 |
| **项目规模报告** | `tem/0623项目规模报告.md` | ~15,300 LOC / ~105 文件 / 10 SOP / 基础设施 50% / 真实 Agent 25% |
| **现状分析报告** | `tem/Emily_现状分析报告.md` | 2026-06-24 状态：Phase 0+A 完成 / Phase B pending / 已完成模块清单 |
| **PhaseB 实施计划** | `tem/Emily_PhaseB_详细实施计划.md` | v2.0：SessionAgent 意图升级 / KnowledgeInjector / WorkItemAgent 自规划 / Session 生命周期 / SOP-010 / AuthHook |
| **PhaseC 实施计划** | `tem/Emily_PhaseC_详细实施计划.md` | v1.0：RealExecutor(M14) / Guardian / RealAuth / RealRisk / 配置开关 |
| **生产测试报告** | `tem/Emily_test_report.md` | 13/13 通过 / 7 bug 修复 / 遗留 P1-P3 |
| **计划任务模块状态** | `需求文件/计划任务/计划任务系统_模块状态报告.md` | P0+P1+P2 全部修复 / 7 态机 / 4 表 / 索引约束 |
| **计划任务代码修正** | `需求文件/计划任务/计划任务系统_代码修正报告.md` | 13 审计项：sync/async / instance_no vs id / UniqueConstraint / permission_level / advisory lock |
| **鉴权系统设计** | `需求文件/鉴权系统/Emily_权限系统设计.md` | 5 级 grouping / per-table+SOP 权限 / PermissionSnapshot 灌注 |
| **全局状态机需求** | `需求文件/全局状态机/全局状态机需求-架构师完善版.md` | v1.1：三层架构（脉络层-节点层-数据层）/ 5 态枚举 / 转移矩阵 / 前置满足度公式 / 级联更新 / API 设计 |
| **全局状态机实施计划** | `需求文件/全局状态机/全局状态机-实施计划.md` | 分阶段实施：阶段一(核心引擎+7表) / 阶段二(级联更新+Agent集成) / 阶段三(REST API+进度) / 阶段四(推演模拟) |
| **全局状态机阶段一报告** | `需求文件/全局状态机/全局状态机-阶段一实施报告.md` | 阶段一完成报告 |
| **全局状态机阶段二报告** | `需求文件/全局状态机/全局状态机-阶段二实施报告.md` | 阶段二完成报告 |
| **全景节点图表设计** | `需求文件/全景节点图/地产全景节点图_数据库表结构设计.md` | 4 表架构 + 5 态状态转移矩阵 |
| **全景节点定义** | `emily-data/baseknowledge/全景节点.md` | ~90 标准节点（7 阶段 / 主责部门 / 前置条件 / 任务成果） |
