# Emily 代码文件目录

> ← 返回 [CLAUDE.md](../CLAUDE.md) | 最后更新：2026-06-26

---

## 架构概述

Emily v0.7.0 是双容器系统。**业务内核**（`emily-core` 容器，FastAPI + `emily_core` 包）拥有全部逻辑：分层架构 `adapters → session → workitem(pipeline) → application → services → repositories → infrastructure`。**AstrBot 插件**（`data/plugins/emily_agent/`）是薄通信垫片——它去重 IM 事件，转换为 `StandardMessage`，通过 HTTP 转发给 Core，并监听 Core 的 SSE 出站流将回复推回 IM。**插件中不含业务逻辑。**

一条消息的流转：AstrBot 事件 → `inbound_adapter` → `StandardMessage` → POST `/api/v1/message/send` → `EmilyCore.handle_message` → `DomainTakeoverService.decide` → `SessionPoolManager.route`（每个 `conversation_id` 一个 `SessionAgent`）→ `SessionAgent.handle` 做快回短路 / LLM SOP 意图识别 + WorkItem 拆分 → `SessionScheduler` 将每个 `WorkItem` 跑过全局单例 **`PipelineBUS`（4 固定节点）** + 全局状态机（BFS 级联）→ 回复通过 `OutboundEventBus` 发布 → SSE → 插件 → IM。

---

## 顶层目录结构

```
d:\app\Emily/
├── docker-compose-napcat.yml    # Docker Compose 编排（napcat/astrbot/emily-core/maxkb/emily-postgres）
├── notebook.md                  # 用户开发笔记（需求草案，非代码）
├── scripts/smoke_test.py        # 离线端到端烟雾测试（无 LLM）
├── scripts/import_nodes.py      # 全景节点导入脚本 — 解析`全景节点.md`，写入 sm_nodes/sm_node_dependencies/sm_node_deliverables/sm_stages 表，支持 --dry-run / --reset
├── emily-core/                  # ★ 业务内核容器（FastAPI + emily_core）
│   ├── Dockerfile               #    Python 3.12-slim，uvicorn api.server:app
│   ├── requirements.txt         #    fastapi/uvicorn/pydantic + sqlalchemy/psycopg2 + openai + aiohttp
│   ├── api/                     #    HTTP/SSE 协议层
│   └── emily_core/              #    业务内核 Python 包
├── data/plugins/emily_agent/    # ★ AstrBot 薄插件（~100 行，无业务逻辑）
├── emily-data/                  # ★ 人可编辑的数据与运行时工件
│   ├── sops/                    #    SOP 业务流手册（12 份 .md）
│   ├── prompts/                 #    LLM Prompt 模板
│   ├── config/                  #    运行时配置（core_config.json / hook_config.json）
│   ├── baseknowledge/项目资料/   #    RAG 源语料
│   ├── baseknowledge/全景节点.md  #    全局状态机节点定义（~90 节点 / 7 阶段）
│   └── (notebooks/logs/attachments/user_memory/journal/ — 运行时生成)
├── tem/                         # 开发文档与阶段报告
├── 需求文件/                     # 需求文档（全局状态机/鉴权/记忆/计划任务）
└── 看板内容/                     # 旧开发看板（本项目重构前，保留作历史参考）
```

---

## emily-core/api/ — HTTP/SSE 协议层

| 文件 | 一句话说明 |
|------|-----------|
| `api/server.py` | FastAPI app 入口，lifespan 里调用 `bootstrap.init()` 初始化 EmilyCore，注册 health/message/session/outbound 路由 |
| `api/routes/message.py` | `POST /api/v1/message/send`：反序列化 `MessageIn` → `StandardMessage`，调用 `core.handle_message()`，返回 `ReplyOut` 或 204（异步 SSE） |
| `api/routes/session.py` | `POST /api/v1/session/terminate`：强制终止指定 `conversation_id` 的 Session |
| `api/routes/health.py` | `GET /api/v1/health`：返回 Core 健康状态（sessions/uptime 等） |
| `api/sse/outbound.py` | `GET /api/v1/events/outbound`：SSE 流，订阅 `OutboundEventBus`，15s 心跳 |
| `api/middleware/auth.py` | `AuthMiddleware` 占位：若设 `EMILY_API_TOKEN` 则校验 `X-Emily-Token` header（health 豁免），否则全放行 |

### `api/routes/` — HTTP 路由定义

| 文件 | 一句话说明 |
|------|-----------|
| `api/routes/message.py` | `POST /api/v1/message/send`：反序列化 `MessageIn` → `StandardMessage`，调用 `core.handle_message()`，返回 `ReplyOut` 或 204（异步 SSE） |
| `api/routes/session.py` | `POST /api/v1/session/terminate`：强制终止指定 `conversation_id` 的 Session |
| `api/routes/health.py` | `GET /api/v1/health`：返回 Core 健康状态（sessions/uptime 等） |
| `api/routes/state_machine.py` | `GET/PUT /api/v1/state-machine/*`：10 个 REST 端点（节点 CRUD / 阶段进度 / 整体进度 / 前置满足度 / 审计日志） |

---

## emily-core/emily_core/state_machine/ — 全局状态机引擎

> 企业级全局状态机，基于 ~90 个标准节点定义（7 阶段），BFS 级联传播状态变更，集成到 PipelineBUS 中。

| 文件 | 一句话说明 |
|------|-----------|
| `state_machine/node_state.py` | `NodeStatus` 枚举 — 5 态（未启动/进行中/已阻塞/已延期/已完成） + `TRANSITIONS` 转移矩阵 + `is_valid_transition` / `is_terminal` |
| `state_machine/stage_state.py` | `StageStatus` 枚举 — 3 态（未启动/进行中/已完成） + 7 阶段名称/边界定义 |
| `state_machine/__init__.py` | 包导出 |

---

## emily-core/emily_core/project/ — 项目级自主 Agent（v0.7.0）

> 项目级作用域后台 Agent（Phase 1：卡滞检测 + 里程碑预警），填补 SessionAgent（会话级）/ WorkItemAgent（任务级）之上的项目级空白。

| 文件 | 一句话说明 |
|------|-----------|
| `project/__init__.py` | 包导出：`ProjectAgent` / `ProjectAgentConfig` / `StaleDetector` / `StaleDetectionResult` |
| `project/project_agent.py` | `ProjectAgent`：后台 asyncio Tick 循环（advisory-lock 防重），Phase 1 驱动 `StaleDetector`；生命周期 CREATED→ACTIVE→STOPPING→STOPPED |
| `project/project_agent_config.py` | `ProjectAgentConfig` dataclass：从全局 `Config` 提取的项目级 Agent 运行时配置 |
| `project/maintenance/__init__.py` | 状态机主动维护子模块导出 |
| `project/maintenance/stale_detector.py` | `StaleDetector`：卡滞检测 + 里程碑预警（纯规则，不调 LLM）。`StaleNode` / `MilestoneWarning` / `StaleDetectionResult` 数据类；24h 告警冷却 |
| `project/health/__init__.py` | 健康度检查子模块（Phase 2 预留） |
| `project/ops/__init__.py` | AI 自动运维子模块（Phase 3 预留） |

---

## emily-core/emily_core/ 根

| 文件 | 一句话说明 |
|------|-----------|
| `__init__.py` | `EmilyCore` 类：惰性构建 OutboundEventBus / SOP 注册表 / Tool 注册表 / BusinessFlowToolRegistry / PipelineBUS / SessionPool / PlanTask 模块；暴露 `handle_message` / `terminate_session` / `health` |
| `config.py` | `Config` dataclass：全部运行时配置（LLM/DB/RAG/Session池/管道节点模式/计划任务/Checkpoint 等），由 `from_dict` 加载 + env 覆盖 |
| `bootstrap.py` | `init()`：env→config 映射、日志初始化、PostgreSQL `init_db`、可选 MaxKB RAG provider 创建、返回 `EmilyCore` 实例 |
| `outbound_bus.py` | `OutboundEventBus`：asyncio.Queue pub/sub 出站总线，广播 reply/progress/file_send/session_closed 给 SSE 订阅者 |

---

## emily-core/emily_core/adapters/

### `standard/` — 跨平台标准协议对象

| 文件 | 一句话说明 |
|------|-----------|
| `message.py` | `StandardMessage` dataclass：统一入站消息协议（platform/conversation/sender/content/attachments/msg_type） |
| `reply.py` | `ReplyMessage` dataclass：统一出站回复协议（content/reply_to_message_id/references/file_paths） |
| `command.py` | `EventCommand` / `TaskCommand` / `MeetingCommand` / `FileCommand` / `QueryCommand`：Application 层参数 DTO |
| `result.py` | `RouteResult` / `HandlerResult` / `AgentResult` / `AgentStep`：工具→Application→Agent 之间的合约对象 |
| `route_decision.py` | `RouteDecision`：接管判断输出（takeover/mode/reason），由 `DomainTakeoverService` 产生 |

### `session/` — Session 池与工厂

| 文件 | 一句话说明 |
|------|-----------|
| `session_config.py` | `SessionConfig`：池 TTL / 并发数 / workitem 上限，从全局 `Config` 构建 |
| `session_factory.py` | `SessionFactory`：miss 时创建 `SessionAgent`，注入最小知识（SOP/工具目录摘要 + `PermissionSnapshot`）到 `SessionContext` |
| `session_pool.py` | `SessionPoolManager`：`conversation_id`→`SessionAgent` 哈希映射，lookup-or-create 路由，per-session 串行锁，后台 TTL 清理 |

---

## emily-core/emily_core/session/ — Session 核心

| 文件 | 一句话说明 |
|------|-----------|
| `session_agent.py` | `SessionAgent`：每会话大脑——快回短路、LLM SOP 意图识别、WorkItem 拆分、FocusLock、ConfirmQueue、通过 `SessionScheduler` 执行 WorkItem、聚合回复 |
| `session_context.py` | `SessionContext` + `PermissionSnapshot`：最小注入上下文（最近轮次/用户记忆摘要/SOP工具目录）+ 只读权限快照 |
| `session_state.py` | `SessionState` 枚举 + 转换表（CREATED→ACTIVE→WAITING_CONFIRM→ARCHIVING→CLOSED） |
| `focus_lock.py` | `FocusLock`：软优先级锁，跟踪当前焦点 WorkItem id，支持显式切换关键词检测 |
| `confirm_queue.py` | `ConfirmQueue`：优先级堆，管理等待用户确认的 WorkItems（FIFO 内优先级） |

---

## emily-core/emily_core/workitem/ — 工作项与管道

| 文件 | 一句话说明 |
|------|-----------|
| `workitem.py` | `WorkItem`：单任务全息记录（input/SOP/route/plan/step-results/result）+ 状态机；流经 4 节点 BUS 的执行单元 |
| `workitem_state.py` | `WorkItemState` 枚举 + 转换（CREATED→PLANNING→EXECUTING→DONE/FAILED，EXECUTING↔WAITING_CONFIRM） |
| `workitem_agent.py` | `WorkItemAgent`：**全局单例**——提供 4 个 BUS 节点 handler（node1 意图+注入 / node2 LLM 规划 / node3 M14 执行+guardian / node4 总结）、`authorize()`、`grade_risk()`、模式回退逻辑 |
| `scheduler.py` | `SessionScheduler`：每 Session 的 WorkItem 队列/优先级/active/done 列表；驱动 WorkItem 状态机并提交给共享 `PipelineBUS` |
| `injector.py` | `KnowledgeInjector` + `InjectionResult`：增量知识灌注（SOP 全文 via SOPLoader、工具定义 via ToolRegistry、DB schema），token 预算 LRU 淘汰 |

### `pipeline/` — 管道总线

| 文件 | 一句话说明 |
|------|-----------|
| `bus.py` | `PipelineBUS`：系统级共享 4 节点执行总线；WorkItem 顺序过节点，触发 before/after/on_error hooks（deny-wins） |
| `node.py` | `PipelineNode`：单节点 dataclass（name/title/handler/required），携带 3 个 hook 挂载点 |
| `context.py` | `BusContext`：每 WorkItem 共享状态（work_item / message / intent / agent_result / verified_reply / baggage），含只读 SessionContext/权限访问器 |
| `hook.py` | `Hook` 基类 + `HookDecision`(ALLOW/WARN/BLOCK) + `HookResult` + 具体 Hook 子类：`AuthHook` / `AuditHook` / `TraceHook` / `ProgressHook` / `PlanTaskMatchHook` |
| `hook_registry.py` | `HookRegistry`：挂载点索引的 Hook 存储，含优先级排序和 get/register/unregister |

#### `pipeline/interfaces/` — 管道阶段接口（ABC 合约）

| 文件 | 一句话说明 |
|------|-----------|
| `auth.py` | `AuthDecision` / `AuthResult` / `AuthEngine` ABC（`authorize(user_id, route_decision) → ALLOW/DENY`） |
| `routing.py` | `IntentType` 枚举 / `SubTask` dataclass / `RouteDecision`（intent_type/sop_id/confidence/compound/sub_tasks） |
| `planning.py` | `PlanStep` / `ExecutionPlan`（risk_level L1-L3 / steps / tool_params / acceptance_criteria） |
| `execution.py` | `ToolCallRecord` / `RagChunk` / `RagResult` / `DbResult` / `GuardianStepVerdict` / `StepResult` + `WorkAgent` ABC（plan + execute） |
| `guardian.py` | `GuardianVerdict` / `Guardian` ABC（review_step 陪跑 + review_reply 出站） |
| `risk.py` | `RiskGrader` ABC（`grade() → L1/L2/L3`） |

#### `pipeline/mocks/` — Phase 0 Mock 桩（`_source="mock"`，无 LLM 时回退）

| 文件 | 一句话说明 |
|------|-----------|
| `mock_auth.py` | `MockAuthEngine`：始终 ALLOW |
| `mock_routing.py` | `MockRouter`：固定 SOP-002-REC high 置信度决策 |
| `mock_planning.py` | `MockPlanner`：固定 3 步 ExecutionPlan |
| `mock_execution.py` | `MockWorkAgent`（写场景）+ `MockWorkAgentQuery`（查场景含 RAG）：固定 StepResult 列表 |
| `mock_guardian.py` | `MockGuardian`：始终 PASS |
| `mock_risk.py` | `MockRiskGrader`：始终 "L2" |

---

## emily-core/emily_core/agent/ — Agent 层（⚠ 冷备 / 逻辑已提取）

> **重要**：此目录下文件在当前 Session-mainline 架构中**不直接 import**。逻辑已提取到 `SessionAgent` / `WorkItemAgent`。保留作为参考和备选路径。

| 文件 | 一句话说明 |
|------|-----------|
| `master_agent.py` | `MasterAgent`：ReAct 编排器（chat_with_tools 循环/SOPMatchDecision 解析/复合请求拆解/`_dispatch_specialist`/routing-log 写入）——原始统一消息路径 |
| `business_flow_agent.py` | `BusinessFlowAgent` (Specialist) + `FlowTask` + `SOPLoader`：按 SOP 执行，M14 结构化输出模式（LLM JSON 参数→框架 handler）回退 ReAct |
| `intent_registry.py` | `SOPIntentRegistry` + `SOPIntentSpec`：加载器+目录格式化器——扫描 `sops/`，解析 §1+§2，持久化 `.sop_index.json`，生成类型树文本供 LLM 路由；**自身不做匹配**（LLM 做） |
| `sop_parser.py` | `parse_sop_markdown` / `extract_allowed_tools_from_sop`：mistune AST 块遍历，从 SOP Markdown 提取结构化字段 |
| `tool_registry.py` | `ToolDefinition` + `ToolRegistry`：LLM 工具注册表（register/get/list + OpenAI function-calling schema 导出） |
| `conversation_context.py` | `ConversationContext` / `ConversationTurn`：per-conversation 短期滑动窗口记忆，含 TTL 和 DeepSeek reasoning 透传 |
| `flow_renderer.py` | `MermaidFlowRenderer` + `FlowMapManager`：扫描 `prompts/flows/`，渲染 `FlowDefinition` 图到 prompt 文本 |
| `mermaid_flow.py` | `MermaidFlowParser` / `FlowDefinition` / `FlowNode` + NL2Flow：解析 Mermaid 决策图到结构化对象 |

---

## emily-core/emily_core/application/ — 应用编排层

| 文件 | 一句话说明 |
|------|-----------|
| `event_app.py` | `EventApplication`：事件记录编排（RouteResult→EventCommand→EventService pending create + confirm/cancel + 简报回复） |
| `task_app.py` | `TaskApplication`：任务创建编排（TaskCommand→TaskService） |
| `meeting_app.py` | `MeetingApplication`：会议记录编排（MeetingCommand→MeetingService） |
| `file_app.py` | `FileApplication`：文件元数据归档编排（FileCommand→FileService） |
| `query_app.py` | `QueryApplication`：查询编排（RouteResult→QueryCommand→QueryService→格式化回复） |
| `plan_task_app.py` | `PlanTaskApplication`：计划任务编排（create/submit/review/query commands→PlanTaskService，确认时触发 WorkflowIntegrator） |
| `state_machine_app.py` | `StateMachineApplication`：编排层，封装 `StateMachineService`，暴露 `change_node_status` / `force_activate` / `get_stage_progress` / `get_overall_progress` |

---

## emily-core/emily_core/services/ — 服务层

| 文件 | 一句话说明 |
|------|-----------|
| `domain_takeover_service.py` | `DomainTakeoverService`：@bot+会话类型接管决策 |
| `user_binding_service.py` | `UserBindingService`：IM平台用户→系统User 自动绑定/创建 |
| `message_service.py` | `MessageService`：入站消息持久化（event_id 幂等/status/sender_user_id 回填） |
| `event_service.py` | `EventService`：事件领域逻辑（pending create / confirm / cancel / 简报） |
| `task_service.py` | `TaskService`：任务创建领域逻辑 |
| `meeting_service.py` | `MeetingService`：会议记录领域逻辑 |
| `file_service.py` | `FileService`：文件元数据领域逻辑 |
| `file_storage_service.py` | `FileStorageService`：M11 附件下载（IM URL→本地）、file_no 生成、files/message_attachments 联动 |
| `query_service.py` | `QueryService`：跨 9 表结构化查询 + 统计聚合 + 回复格式化 |
| `event_journal.py` | `EventJournal`：追加模式项目事件日志写 Markdown |
| `pending_issues.py` | `PendingIssuesService`：Markdown 待办问题清单（编号/写/标记完成/列） |
| `user_memory_service.py` | `UserMemoryService`：每用户长期记忆 Markdown 文件（加载/追加） |
| `checkpoint_service.py` | `CheckpointService`：SOP 检查点持久化（快照/确认/过期/恢复），用于 user_interaction 节点 |
| `chat_archive_service.py` | `ChatArchiveService`：全量双向聊天归档（入站增强 + 出站+前导 + 附件关联） |
| `agent_trace_service.py` | `AgentTraceService`：Agent 推理/LLM交互/工具调用日志写入+查询（fail-safe，不阻塞主流程） |
| `plan_task_service.py` | `PlanTaskService`：计划任务核心——模板/实例生命周期 / 7 态机 / 鉴权 / 非周期匹配 / 监控统计 |
| `plan_task_scheduler.py` | `PlanTaskScheduler`：后台 asyncio 循环——advisory-lock / 超时提醒 / 周期实例生成 / LLM失败通知 / 自动归档 / 自动升级 |
| `plan_task_commands.py` | 计划任务命令 DTO（CreateTemplate/CreateInstance/SubmitDeliverable/Review/ReviewAnomaly/Escalate + AuthCheckResult） |
| `workflow_integrator.py` | `WorkflowIntegrator`：计划任务↔工作流系统集成（确认时启动流/回写实例 id/失败重试） |
| `state_machine_service.py` | `StateMachineService`：全局状态机核心引擎——`change_node_status` / `force_activate_node` / `calculate_precondition_score` / `_cascade_update`(BFS 级联) / `get_stage_progress` / `get_overall_progress` / `query_sm_status` / `try_match_and_complete`(事件→节点匹配) |
| `state_machine_commands.py` | CQRS 命令 DTO——`ChangeNodeStatusCommand` / `CreateNodeCommand` / `ForceActivateNodeCommand` / `AuditLogQuery` / `ChangeStatusResponse` / `StageProgress` / `OverallProgress` / `NodeInfo` |

---

## emily-core/emily_core/repositories/ — 数据访问层

| 文件 | 一句话说明 |
|------|-----------|
| `user_repo.py` | `UserRepository`：User + UserImBinding CRUD（**sync**，async Service 用 `asyncio.to_thread`） |
| `message_repo.py` | `MessageRepository`：messages CRUD + 入站增强 + 出站写入 + 全文搜索 |
| `event_repo.py` | `EventRepository`：events 表 CRUD |
| `task_repo.py` | `TaskRepository`：tasks 表 CRUD |
| `meeting_repo.py` | `MeetingRepository`：meetings 表 CRUD |
| `file_repo.py` | `FileRepository`：files 表 CRUD |
| `chat_archive_repo.py` | `ChatArchiveRepository`：带附件 join 的消息 / 会话-文件 / 日统计查询 |
| `agent_reasoning_repo.py` | `AgentReasoningRepository`：推理日志 CRUD |
| `llm_interaction_repo.py` | `LLMInteractionRepository`：LLM 交互日志 CRUD |
| `tool_call_repo.py` | `ToolCallRepository`：工具调用日志 CRUD |
| `plan_task_repo.py` | `PlanTaskTemplateRepo` / `PlanTaskInstanceRepo` / `PlanTaskLogRepo` / `PlanTaskDeliverableRepo`：四张计划任务表 CRUD |
| `sm_node_repo.py` | `SMNodeRepository`：sm_nodes / sm_node_dependencies / sm_node_deliverables 表的 CRUD（sync） |
| `sm_stage_repo.py` | `SMStageRepository`：sm_stages 表的 CRUD（sync） |
| `sm_audit_repo.py` | `SMAuditRepository`：sm_status_history + sm_audit_logs 写入/查询（sync） |

---

## emily-core/emily_core/infrastructure/ — 基础设施

### `database/`

| 文件 | 一句话说明 |
|------|-----------|
| `session.py` | Engine/`SessionLocal` 工厂：`init_db`（幂等 create-all）、`get_session` 上下文管理器（sync）；默认连 `emily-postgres:5432/emily` |
| `models.py` | SQLAlchemy ORM `Base` + 36 张表模型 + `BEIJING_TZ` 时区定义 |

### `llm/`

| 文件 | 一句话说明 |
|------|-----------|
| `client.py` | `LLMClient`：async OpenAI SDK 封装（DeepSeek/OpenAI 兼容），含 `chat_with_tools` / `chat_json` / `set_trace_callback` |

---

## emily-core/emily_core/providers/rag/ — 知识库检索

| 文件 | 一句话说明 |
|------|-----------|
| `base.py` | `RagProvider` ABC + `SearchResult` / `RagSearchResponse` 数据模型 |
| `maxkb_provider.py` | `MaxKBRagProvider`：MaxKB Admin `hit_test` API 纯向量检索（登录→向量搜索→返回文档段落+相似度） |
| `local_fallback.py` | `LocalFileRagProvider`：零依赖本地 TF 关键词搜索 `.md`/`.txt` 块，含元数据（stage/role）过滤 |

---

## emily-core/emily_core/tools/ — 工具层

### 工具注册表工厂

| 文件 | 一句话说明 |
|------|-----------|
| `__init__.py` | `create_all_tools()`（LLM ToolRegistry：query_data + query_sm_status + 条件工具）和 `create_business_flow_tools()`（M14 BusinessFlowToolRegistry：record_event/task/meeting/file + query_data + plan-task 工具；`create_business_flow_tools()` 同时将 `try_match_and_complete` 包装为 `record_event`） |
| `business_flow_tools.py` | `BusinessFlowTool` dataclass + `BusinessFlowToolRegistry`：框架直调工具定义（**不暴露**为 LLM function-calling） |
| `business_flow_tool.py` | `_extract_allowed_tools_from_sop`：从 SOP §3.2 提取声明工具名的辅助函数 |

### 核心业务流工具（M14 框架直调）

| 文件 | 一句话说明 |
|------|-----------|
| `event_tool.py` | `handle_record_event` handler + schema/description（含 force + related_event_ids） |
| `task_tool.py` | `handle_record_task` handler + schema |
| `meeting_tool.py` | `handle_record_meeting` handler + schema |
| `file_tool.py` | `handle_record_file` handler + M13 `send_file` / `read_local_file` 工具 |
| `query_tool.py` | `handle_query_data` handler + `create_query_tool`（保留供 unmatched 兜底） |
| `sm_tool.py` | `query_sm_status` LLM 工具——注册到 `ToolRegistry`，Agent 按 node_id/stage_id/keyword 搜索节点，返回格式化状态信息 |

### 条件工具（LLM function-calling，unmatched 路径）

| 文件 | 一句话说明 |
|------|-----------|
| `plan_task_tool.py` | 计划任务工具：`record_plan_task` / `submit_plan_task` / `review_plan_task` / `query_plan_tasks` |
| `knowledge_search_tool.py` | `create_knowledge_search_tool`：RAG 搜索（M10 stage/role 过滤） |
| `memory_tool.py` | `write_user_memory`：M8c 长期记忆写入 |
| `pending_issue_tool.py` | M8a 待解决问题清单管理（管理员标记完成） |
| `chat_archive_tool.py` | M11 聊天归档 search/history/user 检索 |

---

## data/plugins/emily_agent/ — AstrBot 薄插件（无业务逻辑）

| 文件 | 一句话说明 |
|------|-----------|
| `main.py` | `Main(Star)`：AstrBot 插件入口——SHA256 去重、`AstrBotInboundAdapter`→`StandardMessage`、`EmilyApiClient.send_message` 发往 Core、注册 event 等异步 SSE 回复、启动 `SSEListener` |
| `metadata.yaml` | 插件清单（name `emily_agent` / v0.7.0） |
| `_conf_schema.json` | 插件配置 schema（`emycore_url` / `emycore_sse_url` / `emycore_api_token`） |
| `adapters/api_client.py` | `EmilyApiClient`：aiohttp HTTP 客户端发往 Core（`/message/send` / `/session/terminate` / `/health`） |
| `adapters/sse_listener.py` | `SSEListener`：aiohttp SSE 消费者，`conversation_id`→event 映射，派发 reply/progress/file_send/session_closed 给 Sender |
| `adapters/astrbot/inbound_adapter.py` | `AstrBotInboundAdapter`：AstrMessageEvent→StandardMessage |
| `adapters/astrbot/outbound_sender.py` | `AstrBotOutboundSender`：ReplyMessage→IM 发送，`send_progress` 前导消息，`send_files` 文件发送 |
| `adapters/standard/*.py` | Core `adapters/standard/` 的**刻意副本**（message/reply/command/result/route_decision），使插件可序列化而不引入 Core 依赖 |

---

## emily-data/ — 人可编辑数据 & 运行时工件

### `sops/` — SOP 业务流手册（SOPIntentRegistry 解析）

| 文件 | 一句话说明 |
|------|-----------|
| `SOP-000-SYS-standard.md` | SOP 编写标准（管理员专用；Registry 跳过，定义文档结构模板） |
| `SOP-001-REC-meeting_summary.md` | 会议纪要记录流 |
| `SOP-002-REC-event_record.md` | 项目事件记录流 |
| `SOP-003-REC-task_manage.md` | 任务管理流 |
| `SOP-004-FILE-file_archive.md` | 文件归档流 |
| `SOP-005-QRY-data_query.md` | 数据查询流 |
| `SOP-007-REC-user_memory.md` | 长期用户记忆管理流 |
| `SOP-008-SYS-pending_issue.md` | 待解决问题处理流 |
| `SOP-009-REC-plan_task.md` | 计划任务创建流 |
| `SOP-010-REC-plan_task_review.md` | 计划任务审核流 |
| `SOP-011-SYS-state_machine.md` | 全局状态机管理 SOP（管理员/项目经理——查询节点状态/变更状态/查看进度/审核日志 7 种子意图） |
| `SOP-999-SYS-fallback.md` | 全涵兜底流（`__FALLBACK_SOP__`） |
| `.sop_index.json` | 持久化解析后的 SOP 索引（mtime 校验缓存，Registry 构建） |

### `prompts/` — LLM Prompt 模板

| 文件 | 一句话说明 |
|------|-----------|
| `master_agent.txt` | MasterAgent system-prompt 模板（含占位符 `{TOOL_LIST}`/`{SOP_CATALOG}`/`{DOMAIN_KNOWLEDGE}` 等） |
| `domain_knowledge.md` | L1 房地产领域知识（7 阶段/12 项专项验收/四证）— ~320 tokens |
| `flows/main.md` | Mermaid 根决策树（路由流，SOP 无关） |
| `flows/unmatched.md` | Mermaid 兜底子图（原子工具自由推理 + 笔记本记录） |

### `config/` — 运行时配置

| 文件 | 一句话说明 |
|------|-----------|
| `core_config.json` | 非机密运行时配置（bot_name/takeover_mode/pipeline_mode/session/agent/kb/checkpoint 开关） |
| `hook_config.json` | Hook 声明式配置——per-node before/after/on_error hook 定义（auth/audit/verify/trace/progress/deep_audit/plan_task_match），由 `PipelineBUS.register_hooks_from_config` 挂载 |

### `baseknowledge/项目资料/` — RAG 源语料

| 文件 | 一句话说明 |
|------|-----------|
| `生命周期.md` | 项目生命周期知识文档 |
| `材料文件.md` | 材料文件清单知识 |
| `组织管理体系.md` | 组织管理体系知识 |
| `消防验收指南.md` | 消防验收知识文档 |
| `城市绿化工程竣工验收服务指南（2024版）.docx` | 城市绿化验收指南（二进制） |
| `TCSES 77—2022《城市景观水体水质提升技术指南》.pdf` | 景观水体技术指南（PDF） |

### `baseknowledge/全景节点.md` — 全局状态机节点定义

~90 个标准节点定义（7 阶段 / 主责部门 / 前置条件 / 任务成果），由 `scripts/import_nodes.py` 解析后写入 sm_nodes / sm_node_dependencies / sm_node_deliverables / sm_stages 表。

### 运行时工件目录（`.gitkeep` 占位，运行时填充）

| 目录 | 说明 |
|------|------|
| `notebooks/` | Agent 笔记输出 |
| `logs/` | 运行日志（如 `emily_20260624.log`） |
| `attachments/` | IM 附件物理存储 |
| `user_memory/` | 用户长期记忆 Markdown 文件 |
| `journal/` | 项目事件日志 |

---

## ⚠ 弃用/冷备/Mock 标注汇总

| 状态 | 文件/目录 | 说明 |
|------|-----------|------|
| **冷备（不 import）** | `agent/` 整个目录 | Agent 逻辑已提取到 SessionAgent/WorkItemAgent，当前保留 SOPIntentRegistry / ToolRegistry / sop_parser 等工具模块仍被热路径 import |
| **Mock（无 LLM 回退）** | `workitem/pipeline/mocks/` | Phase 0 确定性桩，`EMILY_*_MODE=mock`（默认）时使用 |
| **刻意副本** | `data/plugins/emily_agent/adapters/standard/` | DTO 副本使插件不需 import Core 包——非 bug，有意的设计 |
| **占位** | `api/middleware/auth.py` | API 认证中间件（当前全放行，除非设 `EMILY_API_TOKEN`） |
