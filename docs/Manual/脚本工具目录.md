<!-- AUTO-GENERATED from emily-data/config/scripts_registry.yaml by scriptmgr export. Do not edit by hand. -->

# 脚本工具目录

> 本文档收录 `scripts/` 目录下全部脚本，按功能域分组，说明职责、聚合归属、典型用法、调度归属。
>
> **设计约定**：每个功能以独立可执行脚本交付，聚合薄壳仅串联入口不含业务逻辑。独立脚本提供双通道：① CLI 可独立执行 + `--dry-run` 预览；② 核心函数可被系统 import 调用返回 dict。
>
> **由 `scriptmgr export` 自动生成，请勿手动编辑。**

---

## 目录

- [1. 进化闭环（聚合壳：evolution.py）](#进化闭环聚合壳evolutionpy)
- [2. 冷启动（聚合壳：cold_start.py）](#冷启动聚合壳cold_startpy)
- [3. 认知进化周期（聚合壳：cognition_cycle.py）](#认知进化周期聚合壳cognition_cyclepy)
- [4. 全景节点管理](#全景节点管理)
- [5. 系统维护](#系统维护)
- [6. 文件与 API 管理](#文件与-api-管理)
- [7. 数据收集](#数据收集)
- [8. 一次性工具](#一次性工具)

---

## 1. 进化闭环（聚合壳：evolution.py）

### evolution.py — evolution.py — 进化闭环薄聚合脚本。

**职责**：evolution.py — 进化闭环薄聚合脚本。

**路径**：[scripts/evolution.py](../scripts/evolution.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/evolution.py {args}
```

---

### evolution_anomaly.py — evolution_anomaly.py — 硬规则异常检测脚本。

**职责**：evolution_anomaly.py — 硬规则异常检测脚本。

**路径**：[scripts/evolution_anomaly.py](../scripts/evolution_anomaly.py)

**聚合归属**：`evolution.py`

**调用**：
```bash
uv run python scripts/evolution_anomaly.py {args}
```

---

### evolution_apply.py — evolution_apply.py — 补丁审批/应用/回滚脚本。

**职责**：evolution_apply.py — 补丁审批/应用/回滚脚本。

**路径**：[scripts/evolution_apply.py](../scripts/evolution_apply.py)

**聚合归属**：`evolution.py`

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/evolution_apply.py {args}
```

**写数据库**：是

---

### evolution_insight.py — evolution_insight.py — 日洞察生成脚本。

**职责**：evolution_insight.py — 日洞察生成脚本。

**路径**：[scripts/evolution_insight.py](../scripts/evolution_insight.py)

**聚合归属**：`evolution.py`

**自检**：`--preview`

**调用**：
```bash
uv run python scripts/evolution_insight.py {args}
```

**写数据库**：是

---

### evolution_metrics.py — evolution_metrics.py — 指标聚合脚本。

**职责**：evolution_metrics.py — 指标聚合脚本。

**路径**：[scripts/evolution_metrics.py](../scripts/evolution_metrics.py)

**聚合归属**：`evolution.py`

**自检**：`--preview`

**调用**：
```bash
uv run python scripts/evolution_metrics.py {args}
```

---

### evolution_morning.py — evolution_morning.py — 晨报生成脚本。

**职责**：evolution_morning.py — 晨报生成脚本。

**路径**：[scripts/evolution_morning.py](../scripts/evolution_morning.py)

**聚合归属**：`evolution.py`

**自检**：`--preview`

**调用**：
```bash
uv run python scripts/evolution_morning.py {args}
```

**写数据库**：是

---

### evolution_node_close.py — evolution_node_close.py — 节点闭合总结脚本。

**职责**：evolution_node_close.py — 节点闭合总结脚本。

**路径**：[scripts/evolution_node_close.py](../scripts/evolution_node_close.py)

**聚合归属**：`evolution.py`

**自检**：`--preview`

**调用**：
```bash
uv run python scripts/evolution_node_close.py {args}
```

---

### evolution_patch.py — evolution_patch.py — 补丁生成脚本。

**职责**：evolution_patch.py — 补丁生成脚本。

**路径**：[scripts/evolution_patch.py](../scripts/evolution_patch.py)

**聚合归属**：`evolution.py`

**自检**：`--preview`

**调用**：
```bash
uv run python scripts/evolution_patch.py {args}
```

**写数据库**：是

---

### evolution_rules.py — evolution_rules.py — 规则归纳脚本。

**职责**：evolution_rules.py — 规则归纳脚本。

**路径**：[scripts/evolution_rules.py](../scripts/evolution_rules.py)

**聚合归属**：`evolution.py`

**自检**：`--preview`

**调用**：
```bash
uv run python scripts/evolution_rules.py {args}
```

**写数据库**：是

---

### evolution_validate.py — evolution_validate.py — 补丁效果验证脚本。

**职责**：evolution_validate.py — 补丁效果验证脚本。

**路径**：[scripts/evolution_validate.py](../scripts/evolution_validate.py)

**聚合归属**：`evolution.py`

**自检**：`--preview`

**调用**：
```bash
uv run python scripts/evolution_validate.py {args}
```

---

## 2. 冷启动（聚合壳：cold_start.py）

### build_world_book.py — build_world_book.py — 构建/重建单个项目的世界书。

**职责**：build_world_book.py — 构建/重建单个项目的世界书。

**路径**：[scripts/build_world_book.py](../scripts/build_world_book.py)

**聚合归属**：`cold_start.py`

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/build_world_book.py {args}
```

**写数据库**：是

---

### check_initialization.py — check_initialization.py — 检查项目初始化层级和缺失项。

**职责**：check_initialization.py — 检查项目初始化层级和缺失项。

**路径**：[scripts/check_initialization.py](../scripts/check_initialization.py)

**聚合归属**：`cold_start.py`

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/check_initialization.py {args}
```

---

### cold_start.py — cold_start.py — 冷启动流程薄聚合脚本。

**职责**：cold_start.py — 冷启动流程薄聚合脚本。

**路径**：[scripts/cold_start.py](../scripts/cold_start.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/cold_start.py {args}
```

**写数据库**：是

---

### self_check.py — self_check.py — 系统级自检（复用 V1）。

**职责**：self_check.py — 系统级自检（复用 V1）。

**路径**：[scripts/self_check.py](../scripts/self_check.py)

**聚合归属**：`cold_start.py`

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/self_check.py {args}
```

**参数**（Web 控制台据此渲染表单）：

| 参数 | 类型 | 说明 | 取值 |
|------|------|------|------|
| `--dry-run` | flag | 只做只读检查，不触碰实际连接 | — |
| `--json` | flag | 以 JSON 结构输出结果，便于机器解析 | — |
| `--db-url` | str | 留空则用 EMILY_DATABASE_URL 环境变量 | — |

---

## 3. 认知进化周期（聚合壳：cognition_cycle.py）

### cognition_cycle.py — cognition_cycle.py — 认知进化周期执行薄聚合脚本。

**职责**：cognition_cycle.py — 认知进化周期执行薄聚合脚本。

**路径**：[scripts/cognition_cycle.py](../scripts/cognition_cycle.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/cognition_cycle.py {args}
```

**写数据库**：是

---

### detect_cognition_drift.py — detect_cognition_drift.py — 检测项目世界书与实际数据的偏差。

**职责**：detect_cognition_drift.py — 检测项目世界书与实际数据的偏差。

**路径**：[scripts/detect_cognition_drift.py](../scripts/detect_cognition_drift.py)

**聚合归属**：`cognition_cycle.py`

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/detect_cognition_drift.py {args}
```

---

### update_world_book.py — update_world_book.py — 根据偏差增量更新世界书。

**职责**：update_world_book.py — 根据偏差增量更新世界书。

**路径**：[scripts/update_world_book.py](../scripts/update_world_book.py)

**聚合归属**：`cognition_cycle.py`

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/update_world_book.py {args}
```

**写数据库**：是

---

## 4. 全景节点管理

### manage_nodes.py — manage_nodes.py — 全景节点图管理 CLI 脚本（创建 + 更新 + 查询）。

**职责**：manage_nodes.py — 全景节点图管理 CLI 脚本（创建 + 更新 + 查询）。

**路径**：[scripts/manage_nodes.py](../scripts/manage_nodes.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/manage_nodes.py {args}
```

**写数据库**：是

---

## 5. 系统维护

### backfill_doc_id.py — backfill_doc_id.py — 识别历史断链 chunk（doc_id 无对应 files 记录）。

**职责**：backfill_doc_id.py — 识别历史断链 chunk（doc_id 无对应 files 记录）。

**路径**：[scripts/backfill_doc_id.py](../scripts/backfill_doc_id.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/backfill_doc_id.py {args}
```

---

### build_system_description.py — build_system_description.py — 构建/重建/检测认知书（系统自我描述）。

**职责**：build_system_description.py — 构建/重建/检测认知书（系统自我描述）。

**路径**：[scripts/build_system_description.py](../scripts/build_system_description.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/build_system_description.py {args}
```

**写数据库**：是

---

### check_fallback_policy.py — check_fallback_policy.py — 分级兜底策略一致性检查（白名单自洽 + Config 对齐）。

**职责**：check_fallback_policy.py — 分级兜底策略一致性检查（白名单自洽 + Config 对齐）。

**路径**：[scripts/check_fallback_policy.py](../scripts/check_fallback_policy.py)

**调用**：
```bash
uv run python scripts/check_fallback_policy.py {args}
```

---

### check_fallback_tools.py — check_fallback_tools.py — 分级兜底白名单 ↔ 工具写语义一致性检查。

**职责**：check_fallback_tools.py — 分级兜底白名单 ↔ 工具写语义一致性检查。

**路径**：[scripts/check_fallback_tools.py](../scripts/check_fallback_tools.py)

**调用**：
```bash
uv run python scripts/check_fallback_tools.py {args}
```

---

### check_tools_consistency.py — check_tools_consistency.py — BusinessFlowToolRegistry 一致性检查 CLI。

**职责**：check_tools_consistency.py — BusinessFlowToolRegistry 一致性检查 CLI。

**路径**：[scripts/check_tools_consistency.py](../scripts/check_tools_consistency.py)

**调用**：
```bash
uv run python scripts/check_tools_consistency.py {args}
```

---

### dump_session_prompt_live.py — 真实 core 渲染 Session prompt —— 容器内 bootstrap.init 走生产路径，落盘 md+manifest

**职责**：真实 core 渲染 Session prompt —— 容器内 bootstrap.init 走生产路径，落盘 md+manifest

**路径**：[scripts/dump_session_prompt_live.py](../scripts/dump_session_prompt_live.py)

**调用**：
```bash
docker exec -w /app emily-core python /tmp/dump_session_prompt_live.py {args}
```

---

### generate_session_prompt.py — generate_session_prompt.py — 为指定用户生成 Session prompt（世界书+规则书）。

**职责**：generate_session_prompt.py — 为指定用户生成 Session prompt（世界书+规则书）。

**路径**：[scripts/generate_session_prompt.py](../scripts/generate_session_prompt.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/generate_session_prompt.py {args}
```

---

### ingest_knowledge.py — ingest_knowledge.py — 知识库文档向量化入库脚本。

**职责**：ingest_knowledge.py — 知识库文档向量化入库脚本。

**路径**：[scripts/ingest_knowledge.py](../scripts/ingest_knowledge.py)

**调用**：
```bash
uv run python scripts/ingest_knowledge.py {args}
```

**写数据库**：是

---

### maintain_node_template_index.py — 扫描 node_templates/*.md 模板，生成/更新 index.yaml 索引

**职责**：扫描 node_templates/*.md 模板，生成/更新 index.yaml 索引

**路径**：[scripts/maintain_node_template_index.py](../scripts/maintain_node_template_index.py)

**自检**：`--check`

**调用**：
```bash
uv run python scripts/maintain_node_template_index.py {args}
```

---

### pdfread.py — pdfread.py — AI 友好的 PDF 文本提取 CLI。

**职责**：pdfread.py — AI 友好的 PDF 文本提取 CLI。

**路径**：[scripts/pdfread.py](../scripts/pdfread.py)

**调用**：
```bash
uv run python scripts/pdfread.py {args}
```

---

### rag_batch_test.py — rag_batch_test.py — RAG 批量入库 + 回归查询测试脚本。

**职责**：rag_batch_test.py — RAG 批量入库 + 回归查询测试脚本。

**路径**：[scripts/rag_batch_test.py](../scripts/rag_batch_test.py)

**自检**：`--probe`

**调用**：
```bash
uv run python scripts/rag_batch_test.py {args}
```

**参数**（Web 控制台据此渲染表单）：

| 参数 | 类型 | 说明 | 取值 |
|------|------|------|------|
| `--top-k` | int | 默认 5 条 | 1 ~ 10 |
| `--probe` | flag | 不进行入库和查询，只确认 RAG 基座是否可达 | — |
| `--query-only` | flag | 跳过入库环节，只跑回归查询 | — |
| `--clean` | flag | 先删除已有关键词对应的知识块，再重新入库（⚠ 写库操作需二次确认） | — |

**写数据库**：是

---

### rag_dry_run.py — rag_dry_run.py — RAG 基座 dry-run 测试脚本。

**职责**：rag_dry_run.py — RAG 基座 dry-run 测试脚本。

**路径**：[scripts/rag_dry_run.py](../scripts/rag_dry_run.py)

**自检**：`--probe`

**调用**：
```bash
uv run python scripts/rag_dry_run.py {args}
```

**参数**（Web 控制台据此渲染表单）：

| 参数 | 类型 | 说明 | 取值 |
|------|------|------|------|
| `query` | str | 自然语言查询；勾选「仅探测」时可留空 | — |
| `--top-k` | int | 默认取 config.kb_top_k（5），handler 内限上限 10 | 1 ~ 10 |
| `--stage` | enum | 按阶段过滤检索范围 | 投资决策、专项审查、规划报建、招标采购、施工建设、竣工验收 等 7 项 |
| `--role` | enum | 按岗位过滤检索范围 | 项目总经理、设计部经理、开发部经理、工程部经理、成本部经理、营销部经理 等 41 项 |
| `--probe` | flag | 只检查配置 + TEI 可达性，不发检索请求 | — |

---

### rag_governance.py — rag_governance.py — 零可见治理：识别无任何可见来源的文件。

**职责**：rag_governance.py — 零可见治理：识别无任何可见来源的文件。

**路径**：[scripts/rag_governance.py](../scripts/rag_governance.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/rag_governance.py {args}
```

---

### rag_visible_check.py — rag_visible_check.py — 手动校验某用户可见文件集与检索结果。

**职责**：rag_visible_check.py — 手动校验某用户可见文件集与检索结果。

**路径**：[scripts/rag_visible_check.py](../scripts/rag_visible_check.py)

**自检**：`--user-id __placeholder__`

**调用**：
```bash
uv run python scripts/rag_visible_check.py {args}
```

---

### run_daily_file_parse.py — run_daily_file_parse.py — 每日文件解析盘点手动触发脚本。

**职责**：run_daily_file_parse.py — 每日文件解析盘点手动触发脚本。

**路径**：[scripts/run_daily_file_parse.py](../scripts/run_daily_file_parse.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/run_daily_file_parse.py {args}
```

**写数据库**：是

---

### startup_report.py — startup_report.py — 冷启动邮件报告预览/发送脚本。复刻 bootstrap 启动邮件内容。

**职责**：startup_report.py — 冷启动邮件报告预览/发送脚本。复刻 bootstrap 启动邮件内容。

**路径**：[scripts/startup_report.py](../scripts/startup_report.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/startup_report.py {args}
```

---

### three_books_dump.py — 三书产出观察工具 —— 导出世界书/规则书/认知书内容与 manifest（--rebuild 重建并落 DB）

**职责**：三书产出观察工具 —— 导出世界书/规则书/认知书内容与 manifest（--rebuild 重建并落 DB）

**路径**：[scripts/three_books_dump.py](../scripts/three_books_dump.py)

**调用**：
```bash
uv run python scripts/three_books_dump.py {args}
```

---

### toolmgr.py — ToolManager CLI — 业务工具 smoke 测试与调试

**职责**：ToolManager CLI — 业务工具 smoke 测试与调试

**路径**：[scripts/toolmgr.py](../scripts/toolmgr.py)

**调用**：
```bash
uv run python scripts/toolmgr.py {args}
```

---

### verify_langgraph_engine.py — verify_langgraph_engine.py — LangGraph 执行引擎验证脚本（含 error_analysis 纠错路径）。

**职责**：verify_langgraph_engine.py — LangGraph 执行引擎验证脚本（含 error_analysis 纠错路径）。

**路径**：[scripts/verify_langgraph_engine.py](../scripts/verify_langgraph_engine.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/verify_langgraph_engine.py {args}
```

**参数**（Web 控制台据此渲染表单）：

| 参数 | 类型 | 说明 | 取值 |
|------|------|------|------|
| `--dry-run` *(互斥组 mode)* | flag | 输出 5 节点 StateGraph 拓扑，不执行 | — |
| `--mock` *(互斥组 mode)* | flag | 走一遍成功路径 | — |
| `--mock-failure` *(互斥组 mode)* | flag | 触发 error_analysis 纠错闭环 | — |
| `--mock-permission` *(互斥组 mode)* | flag | 验证权限拒绝时的中止行为 | — |
| `--status` *(互斥组 mode)* | flag | 打印 workitem_engine / langgraph_max_replan | — |

---

## 6. 文件与 API 管理

### register_api.py — register_api.py — API 注册器。

**职责**：register_api.py — API 注册器。

**路径**：[scripts/register_api.py](../scripts/register_api.py)

**调用**：
```bash
uv run python scripts/register_api.py {args}
```

**写数据库**：是

---

### rescan_files.py — rescan_files.py — 扫描本地文件目录，对照 files 表补录缺失记录。

**职责**：rescan_files.py — 扫描本地文件目录，对照 files 表补录缺失记录。

**路径**：[scripts/rescan_files.py](../scripts/rescan_files.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/rescan_files.py {args}
```

**写数据库**：是

---

## 7. 数据收集

### collect_session_data.py — collect_session_data.py — Session 聚合根数据收集脚本（薄壳）

**职责**：collect_session_data.py — Session 聚合根数据收集脚本（薄壳）

**路径**：[scripts/collect_session_data.py](../scripts/collect_session_data.py)

**调用**：
```bash
uv run python scripts/collect_session_data.py {args}
```

---

### snapshot.py — snapshot.py — 进化快照采集工具（从项目根目录运行的便捷入口）。

**职责**：snapshot.py — 进化快照采集工具（从项目根目录运行的便捷入口）。

**路径**：[scripts/snapshot.py](../scripts/snapshot.py)

**自检**：`--json`

**调用**：
```bash
uv run python scripts/snapshot.py {args}
```

---

## 8. 一次性工具

### backup.py — PostgreSQL 数据备份 PowerShell 脚本

**职责**：PostgreSQL 数据备份 PowerShell 脚本

**路径**：[scripts/backup.ps1](../scripts/backup.ps1)

**调用**：
```bash
.\scripts\backup.ps1
```

---

### fix_code_fences.py — 修复 程序鉴别材料_完整版.md 中缺少代码围栏的问题。

**职责**：修复 程序鉴别材料_完整版.md 中缺少代码围栏的问题。

**路径**：[scripts/fix_code_fences.py](../scripts/fix_code_fences.py)

**调用**：
```bash
uv run python scripts/fix_code_fences.py {args}
```

**写数据库**：是

---

### migrate_file_purpose.py — migrate_file_purpose.py — 一次性迁移：按 file_category 推断 purpose。

**职责**：migrate_file_purpose.py — 一次性迁移：按 file_category 推断 purpose。

**路径**：[scripts/migrate_file_purpose.py](../scripts/migrate_file_purpose.py)

**自检**：`--dry-run`

**调用**：
```bash
uv run python scripts/migrate_file_purpose.py {args}
```

**写数据库**：是

---

---

## 附录 A：速查表

| # | 脚本 | 功能域 | 聚合壳 | 调度归属 | `--dry-run` | 写 DB |
|---|------|--------|--------|----------|-------------|-------|
| 1 | `evolution.py` | 进化闭环（聚合壳：evolution.py） | —（独立） | 纯手动 | `--dry-run` | 否 |
| 2 | `evolution_anomaly.py` | 进化闭环（聚合壳：evolution.py） | evolution.py | 双重（调度器+手动） | — | 否 |
| 3 | `evolution_apply.py` | 进化闭环（聚合壳：evolution.py） | evolution.py | 纯手动 | `--dry-run` | ✅ |
| 4 | `evolution_insight.py` | 进化闭环（聚合壳：evolution.py） | evolution.py | 纯手动 | `--preview` | ✅ |
| 5 | `evolution_metrics.py` | 进化闭环（聚合壳：evolution.py） | evolution.py | 双重（调度器+手动） | `--preview` | 否 |
| 6 | `evolution_morning.py` | 进化闭环（聚合壳：evolution.py） | evolution.py | 纯手动 | `--preview` | ✅ |
| 7 | `evolution_node_close.py` | 进化闭环（聚合壳：evolution.py） | evolution.py | 纯手动 | `--preview` | 否 |
| 8 | `evolution_patch.py` | 进化闭环（聚合壳：evolution.py） | evolution.py | 纯手动 | `--preview` | ✅ |
| 9 | `evolution_rules.py` | 进化闭环（聚合壳：evolution.py） | evolution.py | 纯手动 | `--preview` | ✅ |
| 10 | `evolution_validate.py` | 进化闭环（聚合壳：evolution.py） | evolution.py | 纯手动 | `--preview` | 否 |
| 11 | `build_world_book.py` | 冷启动（聚合壳：cold_start.py） | cold_start.py | 纯手动 | `--dry-run` | ✅ |
| 12 | `check_initialization.py` | 冷启动（聚合壳：cold_start.py） | cold_start.py | 纯手动 | `--dry-run` | 否 |
| 13 | `cold_start.py` | 冷启动（聚合壳：cold_start.py） | —（独立） | 纯手动 | `--dry-run` | ✅ |
| 14 | `self_check.py` | 冷启动（聚合壳：cold_start.py） | cold_start.py | 纯手动 | `--dry-run` | 否 |
| 15 | `cognition_cycle.py` | 认知进化周期（聚合壳：cognition_cycle.py） | —（独立） | 纯手动 | `--dry-run` | ✅ |
| 16 | `detect_cognition_drift.py` | 认知进化周期（聚合壳：cognition_cycle.py） | cognition_cycle.py | 纯手动 | `--dry-run` | 否 |
| 17 | `update_world_book.py` | 认知进化周期（聚合壳：cognition_cycle.py） | cognition_cycle.py | 纯手动 | `--dry-run` | ✅ |
| 18 | `manage_nodes.py` | 全景节点管理 | —（独立） | 纯手动 | `--dry-run` | ✅ |
| 19 | `backfill_doc_id.py` | 系统维护 | —（独立） | 纯手动 | `--dry-run` | 否 |
| 20 | `build_system_description.py` | 系统维护 | —（独立） | 纯手动 | `--dry-run` | ✅ |
| 21 | `check_fallback_policy.py` | 系统维护 | —（独立） | 纯手动 | — | 否 |
| 22 | `check_fallback_tools.py` | 系统维护 | —（独立） | 纯手动 | — | 否 |
| 23 | `check_tools_consistency.py` | 系统维护 | —（独立） | 纯手动 | — | 否 |
| 24 | `dump_session_prompt_live.py` | 系统维护 | —（独立） | 纯手动 | — | 否 |
| 25 | `generate_session_prompt.py` | 系统维护 | —（独立） | 纯手动 | `--dry-run` | 否 |
| 26 | `ingest_knowledge.py` | 系统维护 | —（独立） | 纯手动 | — | ✅ |
| 27 | `maintain_node_template_index.py` | 系统维护 | —（独立） | bootstrap 自动 | `--check` | 否 |
| 28 | `pdfread.py` | 系统维护 | —（独立） | 纯手动 | — | 否 |
| 29 | `rag_batch_test.py` | 系统维护 | —（独立） | 纯手动 | `--probe` | ✅ |
| 30 | `rag_dry_run.py` | 系统维护 | —（独立） | 纯手动 | `--probe` | 否 |
| 31 | `rag_governance.py` | 系统维护 | —（独立） | 纯手动 | `--dry-run` | 否 |
| 32 | `rag_visible_check.py` | 系统维护 | —（独立） | 纯手动 | `--user-id __placeholder__` | 否 |
| 33 | `run_daily_file_parse.py` | 系统维护 | —（独立） | 纯手动（对应调度任务 DailyFileParseHandler 的 CLI 版） | `--dry-run` | ✅ |
| 34 | `startup_report.py` | 系统维护 | —（独立） | 纯手动 | `--dry-run` | 否 |
| 35 | `three_books_dump.py` | 系统维护 | —（独立） | 纯手动 | — | 否 |
| 36 | `toolmgr.py` | 系统维护 | —（独立） | 纯手动 | — | 否 |
| 37 | `verify_langgraph_engine.py` | 系统维护 | —（独立） | 纯手动 | `--dry-run` | 否 |
| 38 | `register_api.py` | 文件与 API 管理 | —（独立） | 纯手动 | — | ✅ |
| 39 | `rescan_files.py` | 文件与 API 管理 | —（独立） | 纯手动 | `--dry-run` | ✅ |
| 40 | `collect_session_data.py` | 数据收集 | —（独立） | 纯手动 | — | 否 |
| 41 | `snapshot.py` | 数据收集 | —（独立） | 纯手动 | `--json` | 否 |
| 42 | `backup.py` | 一次性工具 | —（独立） | 一次性 | — | 否 |
| 43 | `fix_code_fences.py` | 一次性工具 | —（独立） | 一次性 | — | ✅ |
| 44 | `migrate_file_purpose.py` | 一次性工具 | —（独立） | 一次性 | `--dry-run` | ✅ |

---

## 附录 B：聚合关系图

```
evolution.py  ─── 进化闭环薄聚合壳（daily / weekly / morning / validate / full）
  ├── evolution_metrics.py ← evolution_metrics.py — 指标聚合脚本。
  ├── evolution_anomaly.py ← evolution_anomaly.py — 硬规则异常检测脚本。
  ├── evolution_insight.py ← evolution_insight.py — 日洞察生成脚本。
  ├── evolution_rules.py ← evolution_rules.py — 规则归纳脚本。
  ├── evolution_patch.py ← evolution_patch.py — 补丁生成脚本。
  ├── evolution_apply.py ← evolution_apply.py — 补丁审批/应用/回滚脚本。
  ├── evolution_validate.py ← evolution_validate.py — 补丁效果验证脚本。
  ├── evolution_morning.py ← evolution_morning.py — 晨报生成脚本。
  └── evolution_node_close.py ← evolution_node_close.py — 节点闭合总结脚本。
```

```
cold_start.py  ─── 冷启动薄聚合壳
  ├── self_check.py ← self_check.py — 系统级自检（复用 V1）。
  ├── check_initialization.py ← check_initialization.py — 检查项目初始化层级和缺失项。
  └── build_world_book.py ← build_world_book.py — 构建/重建单个项目的世界书。
```

```
cognition_cycle.py  ─── 认知周期薄聚合壳
  ├── detect_cognition_drift.py ← detect_cognition_drift.py — 检测项目世界书与实际数据的偏差。
  └── update_world_book.py ← update_world_book.py — 根据偏差增量更新世界书。
```

