# Emily 系统架构图

> 更新：2026-08-21 ｜ 依据 [CLAUDE.md](../CLAUDE.md)、[业务模块与运转全景.md](业务模块与运转全景.md)、[开发记录.md](开发记录.md)、论文正文 4.1–4.4 节口径绘制。
>
> 本图只表达**架构语义与模块关联**，不体现具体技术栈（IM 品牌、桥接组件、数据库 / 向量库 / LLM 型号、执行框架等实现细节）。
>
> 术语对照（工程名 ↔ 论文名）：世界书 = 项目态势书 ｜ 规则书 = 组织规则书 ｜ 系统自我描述 = 系统能力书。

```mermaid
flowchart TB
    %% ═══════════ ① 用户与 IM 接入（轻接入） ═══════════
    subgraph ACCESS["① 用户与接入层（轻接入）"]
        USER["企业员工<br/>（现场 / 项目 / 管理）"]
        IM["IM 平台（自然语言交互界面）"]
        PLUGIN["接入插件（薄网关）<br/>去重 + 标准化 + 转发<br/>无业务逻辑"]
    end

    %% ═══════════ ② 智能体分工（组织化执行） ═══════════
    subgraph AGENTS["② 智能体分工（组织化执行）"]
        direction TB
        SA["SessionAgent 会话智能体<br/>前台组织者<br/>快回 / 意图识别 / 拆解 / 回复聚合"]
        WA["WorkItemAgent 任务智能体<br/>中台执行者<br/>执行引擎（含纠错闭环）"]
        GA["GuardianAgent 审核智能体<br/>后台审核者<br/>越权 / 合规 / 产出约束验证"]
        EX["ExpertAgent 专家智能体<br/>按需加载（能力书驱动唤起）"]
    end

    %% ═══════════ ③ 三书认知基座 ═══════════
    subgraph BOOKS["③ 三书认知基座（共享信息地图 · 通用 LLM → 垂直领域团队智能体）"]
        direction LR
        B1["项目态势书<br/>以全景节点为骨架的<br/>七层认知聚合"]
        B2["组织规则书<br/>公司架构 / 职责边界 / 管控规则"]
        B3["系统能力书<br/>能力范围 / 数据可见边界<br/>按权限裁剪"]
    end

    %% ═══════════ ④ 三书源头数据 ═══════════
    subgraph SRC["④ 三书源头（生成 / 更新依据）"]
        direction LR
        POL["集团规范库<br/>公司制度 / 管理制度 / 验收标准"]
        CAP["能力包<br/>插件 / 工具注册库 / 专家注册库"]
        LOG["业务日志<br/>项目日志流水 +<br/>事件 / 任务 / 会议 / 文件记录"]
    end

    %% ═══════════ ⑤ 业务底座 ═══════════
    subgraph BASE["⑤ 业务底座（SOP 引导 + 工具/信息交互）"]
        direction TB
        SOP["Skill 化 SOP 库<br/>会议 / 事件 / 任务 / 文件<br/>查询 / 记忆 / 待解决 / 节点"]
        TOOLS["工具模块<br/>业务流工具直调<br/>+ 工具注册表兜底"]
        INFO["信息模块<br/>事件 / 任务 / 会议 / 文件<br/>查询 / 长期记忆 / 待解决"]
        NODES["全景节点图（项目态势感知）<br/>LLM 增强 FSM · 三态流转<br/>父子加权 · 依赖链"]
    end

    %% ═══════════ ⑥ 基础设施 ═══════════
    subgraph INFRA["⑥ 基础设施（抽象）"]
        direction LR
        DB[("关系型数据库")]
        RAG[("RAG 知识库<br/>向量检索")]
        LLM[("大语言模型 LLM")]
    end

    %% ═══════════ ⑦ 定时复盘闭环 ═══════════
    subgraph REVIEW["⑦ 定时复盘闭环（自进化 · 态势同步 · 经验积累）"]
        direction TB
        SNAP["信息快照采集"]
        ANOM["硬规则异常检测"]
        INS["LLM 洞察报告<br/>（日）"]
        RULE["进化规则归纳 → 提案生成<br/>（周）"]
        HUMAN["👤 人审批放行<br/>（管理员 / 责任人）<br/>系统仅提案 · 不放行"]
        MORN["节点风险晨报<br/>（日复盘 / 晨推送）"]
    end

    %% ── 接入主链路 ──
    USER --> IM --> PLUGIN --> SA

    %% ── 智能体协作回路 ──
    SA -->|"work_spec 拆解下发"| WA
    WA -->|"步骤 / 回复草稿"| GA
    GA -->|"审核通过 → 回复"| SA
    SA -->|"出站回复"| PLUGIN
    PLUGIN --> IM

    %% ── 三书注入（认知基座 → 各智能体） ──
    B1 -.->|"态势 + 能力"| SA
    B3 -.->|"能力边界"| SA
    B2 -.->|"规则边界"| GA
    B1 -.->|"态势上下文"| WA

    %% ── 专家智能体按需加载（能力书驱动） ──
    B3 -.->|"按需唤起"| EX
    EX -.->|"承接垂直领域任务"| WA

    %% ── 三书源头 → 三书（生成 / 更新） ──
    NODES -.->|"骨架聚合"| B1
    LOG -.->|"态势聚合（流水）"| B1
    POL -.->|"规则对齐"| B2
    CAP -.->|"能力自感知 / 动态生长"| B3

    %% ── SOP 路由与执行 ──
    SA -->|"语义路由命中 SOP"| SOP
    WA -->|"按 SOP 步骤调用"| TOOLS
    SOP -.->|"约束执行流程"| WA
    TOOLS -->|"读写"| INFO
    INFO --> DB

    %% ── 全景节点与态势书 ──
    WA -->|"节点操作"| NODES
    TOOLS --> NODES

    %% ── 基础设施 ──
    TOOLS --> RAG
    WA --> LLM
    GA --> LLM
    INFO --> DB
    NODES --> DB
    RAG --> DB
    POL --> RAG

    %% ── 复盘闭环：采集自底座 / 源头 → 调整三书 / SOP / 全景节点 ──
    DB -.->|"项目数据 + 业务日志 + 会话记录"| SNAP
    NODES -.-> SNAP
    POL -.->|"集团规范库"| SNAP
    CAP -.->|"能力包"| SNAP
    SNAP --> ANOM --> INS
    INS --> RULE
    SNAP -.-> MORN

    RULE -->|"提案（待审批）"| HUMAN
    HUMAN -.->|"审批放行"| B1
    HUMAN -.->|"审批放行"| B2
    HUMAN -.->|"审批放行"| B3
    HUMAN -.->|"审批放行"| SOP
    HUMAN -.->|"审批放行（结构变更）"| NODES
    MORN -.->|"风险推送"| SA

    %% ── 样式 ──
    classDef access fill:#e3f2fd,stroke:#1976d2
    classDef agent fill:#fff3e0,stroke:#f57c00
    classDef book fill:#e8f5e9,stroke:#388e3c
    classDef src fill:#ede7f6,stroke:#5e35b1
    classDef base fill:#f3e5f5,stroke:#7b1fa2
    classDef infra fill:#eceff1,stroke:#455a64
    classDef review fill:#fff8e1,stroke:#f9a825
    classDef human fill:#ffebee,stroke:#c62828,stroke-width:2px

    class USER,IM,PLUGIN access
    class SA,WA,GA,EX agent
    class B1,B2,B3 book
    class POL,CAP,LOG src
    class SOP,TOOLS,INFO,NODES base
    class DB,RAG,LLM infra
    class SNAP,ANOM,INS,RULE,MORN review
    class HUMAN human
```

## 图例与关系速读

| 箭头类型 | 含义 |
|---------|------|
| 实线 `-->` | 数据 / 控制流（主链路） |
| 虚线 `-.->` | 认知注入 / 回写 / 采集（异步或非主链路） |

**七层结构**：
1. **接入层** — IM 平台 → 薄网关插件（去重 / 标准化 / 转发，无业务逻辑）
2. **智能体分工** — 三主力（Session / WorkItem / Guardian）+ 专家（按需加载）
3. **三书基座** — 通用 LLM 特化为垂直领域团队智能体的「共享认知地图」
4. **源头数据** — 三书各自的生成/更新依据：态势书是「全景节点（骨架）+ 业务日志（流水）」双源；规则书 ← 集团规范库；能力书 ← 能力包
5. **业务底座** — SOP 引导用户与工具模块、信息模块交互；全景节点承载态势感知
6. **基础设施** — 关系型数据库 / RAG 知识库 / 大语言模型
7. **复盘闭环** — 采集 → 洞察 → 规则归纳 → 提案 → 人审批放行 → 回写三书 / SOP / 全景节点

**关键设计语义**：
- **三智能体是三段式组织分工**（前台组织 / 中台执行 / 后台审核），非平级并列；Guardian 独立于执行链路，对应地产「执行与审核分离」的条线制衡。
- **三书是系统级共享基座 + 角色化按需取用**，不是某一 agent 的私有物；携带归属：Session 代表「态势书 + 能力书」，Guardian 代表「规则书」。
- **项目态势书 = 全景节点（骨架）+ 业务日志（流水）双源聚合**：全景节点提供结构化骨架，业务日志（事件/任务/会议/文件记录与项目日志流水）提供过程信息，二者聚合为态势书，再注入智能体，形成「节点 + 日志 → 态势书 → 智能体」的态势感知链。
- **复盘是双向闭环**：输入侧采集项目数据 / 规范库 / 能力包 / 业务日志 / 会话记录；输出侧通过周度「规则归纳 → 提案」产出变更建议，经**人审批放行**后回写三书、SOP、全景节点，推动态势同步与经验积累。
- **受控演进（Human-in-the-loop）**：三书、SOP、全景节点（状态空间）的变更，系统只能产出**提案**，最终**审批放行必须由人（管理员 / 责任人）完成**，系统不自主落库。全景节点需区分两类更新：节点**状态流转**（如 CONDITIONS_NOT_MET → IN_PROGRESS → COMPLETED）由 LLM 增强 FSM 自动判定，无需审批；节点**增删改（结构 / 状态空间变更）** 则必须经人审批放行。
- **专家智能体按需加载**：由系统能力书（注册专家库）驱动唤起，承接垂直领域任务，与三常驻主力 agent 区分。
