
### 运维看板
http://localhost:18080


### 主要模块
session-workitem会话数据流，全周期


### emy-test cli对话
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我查一下翠湖庭院项目的整体进度情况" --sender "李景利"

### 人物session 拉起时的prompt
uv run python scripts/dump_session_prompt.py

### mitmproxy
- mitmweb UI : http://localhost:8081 ，密码 emily_proxy_2026
- 代理端口 : mitmproxy:8888 （容器间）
- 关闭代理 : 注释 docker-compose-napcat.yml 的 HTTPS_PROXY 和 SSL_CERT_FILE 两行 + docker compose restart emily-core
通过mitmproxy Addon获取实时通讯流给AI工具

开关：environment:
  - LLM_TRACE_ENABLED=1           # 注释此行关闭 jsonl 落盘
- 开启 ：保留 LLM_TRACE_ENABLED=1 ，addon 在每个 DeepSeek API 响应到达时自动写 jsonl
- 关闭 ：注释该行（或设为 0 ），addon 不写任何数据，代理功能不受影响
- 无需重建镜像 ，改 docker-compose 后 docker compose up -d mitmproxy 即可