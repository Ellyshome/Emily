# Emily 交付准备计划

> 版本：v1.0 | 编制日期：2026-07-10
>
> 本文档按优先级排列 13 项交付准备任务，每项包含**目标、检查步骤、执行步骤、验证标准、涉及文件**。
> 完成每项后，在对应 ☐ 打 ☑。

---

## 优先级总览

| 优先级 | # | 任务 | 风险等级 | 预计耗时 |
|--------|---|------|---------|---------|
| P0 | 4 | 数据清洗 | 高 | 2h |
| P0 | 8 | 端到端全链路回归 | 高 | 3h |
| P0 | 6 | 配置定稿 | 高 | 1h |
| P1 | 3 | 版本号统一 | 中 | 30min |
| P1 | 1 | 提交残留变更 | 中 | 1h |
| P1 | 10 | 文档与代码对齐 | 中 | 2h |
| P1 | 7 | 密钥与敏感信息审计 | 中 | 1h |
| P2 | 2 | 清理废弃代码与表 | 低 | 2h |
| P2 | 5 | 知识库状态确认 | 低 | 1h |
| P2 | 9 | 边界场景确认 | 低 | 1h |
| P2 | 11 | 补齐非技术文档 | 低 | 2h |
| P3 | 12 | 备份与恢复方案 | 低 | 1.5h |
| P3 | 13 | 监控与告警基线 | 低 | 1h |

---

## P0-4：数据清洗

### 目标
清除测试产生的脏数据，确保验收时数据库中的每条记录都是真实业务数据或种子数据。

### 检查步骤（只读）
```bash
# 1. 统计 users 表中疑似测试用户（auto_create 产生、permission_level=1、无 IM 绑定）
docker exec emily-postgres psql -U emily -d emily -c "
  SELECT u.id, u.username, u.level, u.is_admin, u.status,
         (SELECT COUNT(*) FROM user_im_bindings b WHERE b.user_id = u.id) AS binding_count
  FROM users u
  WHERE u.is_deleted = false
  ORDER BY u.created_at DESC;
"

# 2. 检查 messages 表中的测试对话
docker exec emily-postgres psql -U emily -d emily -c "
  SELECT m.id, m.sender_name, m.content, m.direction, m.created_at
  FROM messages m
  WHERE m.content LIKE '%测试%' OR m.content LIKE '%test%' OR m.content LIKE '%hello%'
  ORDER BY m.created_at DESC LIMIT 20;
"

# 3. 检查 events/tasks/meetings 中的测试数据
docker exec emily-postgres psql -U emily -d emily -c "
  SELECT 'events' AS tbl, COUNT(*) FROM events
  UNION ALL SELECT 'tasks', COUNT(*) FROM tasks
  UNION ALL SELECT 'meetings', COUNT(*) FROM meetings
  UNION ALL SELECT 'files', COUNT(*) FROM files WHERE is_deleted = false;
"

# 4. 检查测试产生的权限记录
docker exec emily-postgres psql -U emily -d emily -c "
  SELECT * FROM permission_grants WHERE grant_no LIKE 'PGR-%' ORDER BY created_at DESC LIMIT 10;
"

# 5. 检查日志表中的可疑记录
docker exec emily-postgres psql -U emily -d emily -c "
  SELECT COUNT(*) AS total_logs,
         COUNT(CASE WHEN is_hit = false THEN 1 END) AS miss_count
  FROM sop_routing_logs;
"
```

### 执行步骤

1. **备份当前数据库**（在执行任何删除之前）
   ```bash
   docker exec emily-postgres pg_dump -U emily emily > backup_before_cleanup_$(date +%Y%m%d%H%M%S).sql
   ```

2. **删除测试用户**（手动确认每条后再删）
   ```bash
   # 先软删除，验证无误后再硬删除
   docker exec emily-postgres psql -U emily -d emily -c "
     UPDATE users SET is_deleted = true
     WHERE id IN (
       -- 列出确认要删除的用户 ID
     );
   "
   ```

3. **清理测试消息和对应业务数据**
   ```bash
   # 按消息 ID 删除关联的 events/tasks/meetings
   # 按消息 ID 删除关联的 messages 记录
   # 注意 FK 依赖顺序：events/tasks/meetings → messages
   ```

4. **验证编号连续性**
   ```bash
   docker exec emily-postgres psql -U emily -d emily -c "
     SELECT event_no FROM events ORDER BY event_no;
     SELECT task_no FROM tasks ORDER BY task_no;
   "
   ```

### 验证标准
- ☐ users 表无非真实用户（或已软删除）
- ☐ events/tasks/meetings 无测试记录
- ☐ messages 无明显测试对话
- ☐ 业务编号连续无跳号
- ☐ FK 关系无悬空记录

---

## P0-8：端到端全链路回归

### 目标
确保每个 SOP 业务流在实际运行环境中可正常走通，从消息发送到数据库写入全链路。

### 前置条件
- 5 容器全部 Running
- LLM API Key 已配置且可用
- 至少一个真实用户存在于 users 表

### 执行步骤

**Step 1：环境健康检查**
```bash
docker compose -f docker-compose-napcat.yml ps
curl http://127.0.0.1:18080/api/v1/health
```

**Step 2：获取测试用户 ID**
```bash
docker exec emily-postgres psql -U emily -d emily -c "
  SELECT id, username, level FROM users WHERE status = 'active' AND is_deleted = false ORDER BY level DESC LIMIT 5;
"
```

**Step 3：逐 SOP 功能测试**

| # | SOP | 测试命令 | 预期结果 | 验证 SQL |
|---|-----|---------|---------|---------|
| 1 | SOP-002 事件记录 | `uv run python .claude/skills/emy-test/cli.py --managed --llm --message "记录事件：3#楼主体结构封顶" --sender "真实用户名"` | AI 确认录入 + 返回事件编号 | `SELECT * FROM events WHERE title LIKE '%3#楼%' ORDER BY created_at DESC LIMIT 1;` |
| 2 | SOP-003 任务管理 | `--message "创建任务：下周三前完成样板段验收，负责人张工"` | AI 确认创建 + 返回任务编号 | `SELECT * FROM tasks WHERE title LIKE '%样板段%' ORDER BY created_at DESC LIMIT 1;` |
| 3 | SOP-001 会议纪要 | `--message "记录会议：今天监理例会，讨论了进度滞后问题，决定增加夜班施工"` | AI 确认记录 + 返回会议编号 | `SELECT * FROM meetings WHERE title LIKE '%监理%' ORDER BY created_at DESC LIMIT 1;` |
| 4 | SOP-004 文件归档 | 通过 QQ 发送图片/文件给 Bot | AI 确认归档 + 返回文件编号 | `SELECT * FROM files WHERE is_deleted = false ORDER BY created_at DESC LIMIT 1;` |
| 5 | SOP-005 数据查询 | `--message "查一下最近有哪些事件"` | AI 返回事件列表 | 检查 reply 内容准确性 |
| 6 | SOP-007 用户记忆 | `--message "记住我喜欢简短的回复"` | AI 确认记忆 | `SELECT long_term_memory FROM users WHERE username = '真实用户名';` |
| 7 | SOP-011 节点管理 | `--message "创建节点：1#楼主体结构，类型 STAGE，截止2026-12-31"` | AI 确认创建 | `SELECT * FROM project_nodes WHERE node_name LIKE '%1#楼%' ORDER BY created_at DESC LIMIT 1;` |
| 8 | SOP-000 系统标准 | `--message "你是谁"` | 返回自我介绍 | 检查回复内容 |
| 9 | SOP-999 兜底 | `--message "今天天气怎么样"` | 友好兜底回复 | 检查回复内容 |
| 10 | 权限阻断 | 用 level=1 用户执行管理操作 | 被 Hook BLOCK | `SELECT * FROM hook_execution_logs WHERE decision = 'block' ORDER BY created_at DESC LIMIT 1;` |

**Step 4：日志链路完整性验证**
```bash
# 每个功能测试后，检查 6 类日志表是否有对应记录
docker exec emily-postgres psql -U emily -d emily -c "
  SELECT 'sop_routing' AS log_type, COUNT(*) FROM sop_routing_logs
  UNION ALL SELECT 'agent_reasoning', COUNT(*) FROM agent_reasoning_logs
  UNION ALL SELECT 'llm_interaction', COUNT(*) FROM llm_interaction_logs
  UNION ALL SELECT 'tool_call', COUNT(*) FROM tool_call_logs
  UNION ALL SELECT 'hook_execution', COUNT(*) FROM hook_execution_logs
  UNION ALL SELECT 'pipeline_execution', COUNT(*) FROM pipeline_execution_logs;
"
```

### 验证标准
- ☐ 10 项 SOP 测试全部通过
- ☐ 每项测试后对应数据库表有正确记录
- ☐ 6 类日志表均有新增记录
- ☐ 权限阻断场景正常生效
- ☐ AI 回复内容准确且格式正确（IM 纯文本，无 Markdown 符号）

---

## P0-6：配置定稿

### 目标
将所有运行时配置切换到生产值，确保验收时系统以真实模式运行。

### 检查步骤

**1. 核对当前配置值**
```bash
# 检查容器环境变量
docker exec emily-core env | grep EMILY_

# 检查 core_config.json
cat emily-data/config/core_config.json
```

**2. 检查关键配置项**

| 配置项 | 当前值 | 生产建议值 | 位置 |
|--------|-------|-----------|------|
| `planner_mode` | real | real ✅ | Config 默认值 |
| `executor_mode` | real | real ✅ | Config 默认值 |
| `auth_mode` | real | real ✅ | Config 默认值 |
| `risk_mode` | real | real ✅ | Config 默认值 |
| `kb_enabled` | false | **true**（如需 RAG） | core_config.json |
| `auto_create_user` | false | false ✅（生产环境必须 false） | Config 默认值 |
| `log_level` | INFO | INFO ✅ | core_config.json |
| `permission_fail_open` | true | **评估**（生产建议 false） | Config 默认值 |
| `EMILY_API_TOKEN` | （空） | **设值**（生产必须） | 环境变量 |

### 执行步骤

1. **修改 core_config.json**
   ```json
   {
     "kb_enabled": true,
     "log_level": "INFO"
   }
   ```
   > 注意：如果 RAG 环境未就绪（MaxKB 未配置知识库），`kb_enabled` 应保持 `false`，否则每次查询会触发 RAG 调用失败日志。

2. **设置环境变量（.env 文件或 docker-compose 环境变量）**
   ```bash
   # .env 文件
   EMILY_API_TOKEN=<生成一个强随机 token>
   EMILY_LLM_API_KEY=<确认有效>
   ```

3. **确认 .env 是否在 .gitignore 中**
   ```bash
   grep "^\.env$" .gitignore
   # 应输出 .env
   ```

4. **重启 emily-core 使配置生效**
   ```bash
   docker compose -f docker-compose-napcat.yml restart emily-core
   ```

5. **验证配置生效**
   ```bash
   # health 接口应反映新配置
   curl http://127.0.0.1:18080/api/v1/health

   # 无 Token 请求应被拒绝
   curl -X POST http://127.0.0.1:18080/api/v1/message/send \
     -H "Content-Type: application/json" \
     -d '{"platform":"test","conversation_type":"private","conversation_id":"test","sender_id":"test","sender_name":"test","content":"hello"}'
   # 预期：401 或 403

   # 有 Token 请求应通过
   curl -X POST http://127.0.0.1:18080/api/v1/message/send \
     -H "Content-Type: application/json" \
     -H "X-Emily-Token: <你设置的 token>" \
     -d '{"platform":"test","conversation_type":"private","conversation_id":"test","sender_id":"test","sender_name":"test","content":"hello"}'
   # 预期：200 或 204
   ```

### 验证标准
- ☐ `kb_enabled` 已根据 RAG 环境就绪状态设置
- ☐ `auto_create_user` = false
- ☐ `EMILY_API_TOKEN` 已设值且校验生效
- ☐ `EMILY_LLM_API_KEY` 有效
- ☐ .env 文件在 .gitignore 中
- ☐ 重启后 health 接口正常

---

## P1-3：版本号统一

### 目标
全项目文档和代码中的版本号必须一致。

### 检查步骤
```bash
# 搜索所有版本号出现位置
grep -rn "v0\.[0-9]\.[0-9]" --include="*.md" --include="*.py" --include="*.json" .
grep -rn "版本" --include="*.md" . | head -20
```

当前已知不一致：
- `CLAUDE.md`：v0.6.0
- `docs/代码文件目录.md`：v0.7.0

### 执行步骤

1. **确定最终版本号**（根据实际开发阶段确定，建议以当前代码文件目录为准）
2. **全局替换**：将所有 `v0.6.0` / `v0.7.0` 替换为统一版本号
3. **涉及文件**：
   - `CLAUDE.md`（第 7 行附近）
   - `docs/代码文件目录.md`（第 9 行附近）
   - `docs/业务模块与运转全景.md`
   - `docs/接口协议与调用约定.md`
   - `docs/数据库设计.md`
   - `docs/开发记录.md`
   - `docs/技术踩坑备忘录.md`
   - `emily-data/sops/*.md`（如有版本号引用）
   - `emily-core/emily_core/__init__.py`（如有 `__version__`）

### 验证标准
- ☐ 全项目版本号一致
- ☐ `grep -rn` 只有一个版本号出现

---

## P1-1：提交残留变更

### 目标
清理 git 工作区，确保所有变更都有清晰的 commit message。

### 检查步骤
```bash
git status
git log --oneline -10
```

当前已知问题：
- 多个 "Please enter the commit message for your changes" 空提交
- 大量已修改/已删除文件未提交

### 执行步骤

1. **逐批审查并提交变更**
   ```bash
   # 查看具体变更内容
   git diff --stat

   # 按逻辑分组提交
   git add emily-core/emily_core/infrastructure/database/models.py
   git commit -m "refactor: 数据库模型更新（新增全景节点/调度器/进化日志表）"

   git add emily-core/api/server.py emily-core/emily_core/__init__.py
   git commit -m "refactor: EmilyCore 初始化与 API 路由更新"

   # ... 根据实际变更分组
   ```

2. **处理已删除文件**
   ```bash
   git add -u  # 暂存所有已删除文件
   git commit -m "chore: 清理旧架构文档（PlanTask/WorkOrder 阶段产物）"
   ```

3. **修正空 commit message**
   ```bash
   # 如果需要修改最近的 commit message
   git commit --amend -m "有意义的提交信息"

   # 如果需要 rebase 修改历史 commit
   git rebase -i HEAD~5
   ```

### 验证标准
- ☐ `git status` 显示 clean（无未提交变更）
- ☐ `git log --oneline -20` 无空 message 提交
- ☐ 每个 commit message 清晰描述变更内容

---

## P1-10：文档与代码对齐

### 目标
确保 docs/ 下 6 份文档反映当前代码真实状态。

### 检查步骤

逐文档核对关键数据点：

| 文档 | 核对项 | 已知问题 |
|------|--------|---------|
| 数据库设计.md | 表数量 | 文档说 34 表，实际 models.py 约 40+ 表（新增全景节点 5 表、调度器 2 表、进化 5 表、元认知 2 表、Session 3 表等） |
| 代码文件目录.md | 文件列表 | 新增文件（scheduler/、evolution/、world_book 等）可能未收录 |
| 业务模块与运转全景.md | SOP 数量 | 文档写 12 份，实际 sops/ 目录 10 份 |
| 接口协议与调用约定.md | StandardMessage 字段 | 需核对与代码一致 |
| 开发记录.md | Phase C 内容 | 可能需要补充最新进展 |
| 技术踩坑备忘录.md | 踩坑条目 | 最近踩的坑可能未记录 |

### 执行步骤

1. **核实实际表数量**
   ```bash
   docker exec emily-postgres psql -U emily -d emily -c "\dt" | wc -l
   # 或者在 Python 中：
   # from emily_core.infrastructure.database.models import Base
   # print(len(Base.metadata.tables))
   ```

2. **更新数据库设计.md**
   - 表清单从 34 更新为实际数量
   - 新增表：project_nodes / node_dependencies / node_deliverables / node_accessible_files / node_events / scheduler_jobs / scheduler_executions / session_archives / tool_registry / session_accessible_files / pipeline_execution_logs / evolution_llm_interaction_logs / rag_retrieval_logs / session_lifecycle_logs / scheduler_job_logs / user_feedback_signals / business_event_logs / evolution_daily_insights / evolution_rules / evolution_patches / project_world_books / system_descriptions / data_masking_rules / pending_data / permission_review_tasks

3. **更新代码文件目录.md**
   - 新增 scheduler/ 目录下的 10 个文件
   - 新增 services/evolution/ 目录
   - 新增 services/world_book_builder.py / world_book_service.py 等
   - SOP 数量修正为 10

4. **更新业务模块与运转全景.md**
   - SOP 清单修正
   - 补充进化闭环模块 / 元认知模块描述

5. **更新其他文档的日期标记**

### 验证标准
- ☐ 数据库设计.md 表数量与 `\dt` 一致
- ☐ 代码文件目录.md 文件列表与实际目录一致
- ☐ 业务模块与运转全景.md SOP 数量为 10
- ☐ 所有文档"最后更新"日期为当天

---

## P1-7：密钥与敏感信息审计

### 目标
确保没有 API Key、密码等敏感信息硬编码在代码或配置文件中。

### 检查步骤

```bash
# 1. 搜索可能的 API Key 模式
grep -rn "sk-[a-zA-Z0-9]" --include="*.py" --include="*.json" --include="*.md" --include="*.yml" .
grep -rn "api_key\s*=\s*['\"][^'\"]{10,}" --include="*.py" .

# 2. 搜索密码模式
grep -rn "password\s*=\s*['\"][^'\"]" --include="*.py" --include="*.json" .
grep -rn "secret" --include="*.py" --include="*.json" --include="*.yml" .

# 3. 检查 docker-compose 中的凭据
grep -rn "emily_secret_2026" docker-compose-napcat.yml

# 4. 检查 .env 是否在 .gitignore 中
grep "^\.env$" .gitignore

# 5. 检查 git 历史中是否有敏感信息泄露
git log --all -p -- "*.env" 2>/dev/null | head -50
```

### 执行步骤

1. **如果发现硬编码密钥**：
   - 将密钥移至环境变量
   - 用占位符替换代码中的实际值
   - 使用 `git filter-branch` 或 BFG 清除 git 历史中的泄露

2. **docker-compose-napcat.yml 中的数据库密码**：
   - 当前为明文 `emily_secret_2026`
   - 交付选项 A：改为强密码并通知验收方
   - 交付选项 B：接受 Docker Compose 中明文密码（仅限内网部署场景）
   - **推荐选项 A**，修改为至少 16 位随机密码

3. **确认 .env.example 完整性**：
   - 补齐所有需要环境变量的条目
   - 当前 `.env.example` 缺少 `EMILY_DATABASE_URL`、`EMILY_KB_ENABLED`、`EMILY_MAXKB_*` 等

4. **更新 .env.example**
   ```bash
   # 补齐缺失项
   cat >> .env.example << 'EOF'
   EMILY_DATABASE_URL=postgresql://emily:PASSWORD@emily-postgres:5432/emily
   EMILY_KB_ENABLED=false
   EMILY_MAXKB_URL=http://maxkb:8080
   EMILY_MAXKB_ADMIN_PASSWORD=
   EMILY_MAXKB_KNOWLEDGE_ID=
   EMILY_STORAGE_ROOT=/app/attachments
   EMILY_HOOK_CONFIG_PATH=/app/config/hook_config.json
   EMILY_SOP_REPOSITORY_DIR=/app/sops
   EMILY_EXECUTOR_MODE=real
   EMILY_PLANNER_MODE=real
   EMILY_AUTH_MODE=real
   EMILY_RISK_MODE=real
   EOF
   ```

### 验证标准
- ☐ 代码中无硬编码 API Key
- ☐ 代码中无硬编码密码
- ☐ .env 在 .gitignore 中
- ☐ .env.example 包含所有必需环境变量
- ☐ docker-compose 中密码已改为强密码（或确认内网场景可接受）
- ☐ git 历史无敏感信息泄露

---

## P2-2：清理废弃代码与表

### 目标
移除已标注废弃但未删除的代码和数据库表，避免验收方将其视为"未完成项"。

### 需清理项

**ORM 层已标注废弃（models.py）**：
- `sop_checkpoints` 表——注释写"CheckpointService 无调用方，ORM 已删除"，但需确认 DB 中物理表是否存在
- `plan_task_templates / plan_task_instances / plan_task_logs / plan_task_deliverables`——注释写"ORM 类已删除"，但需确认 DB 中物理表是否存在

**代码目录**：
- `emily-core/emily_core/agent/` 目录——除 `sop_parser` 外是否还有残留
- 旧 `看板内容/` 目录——确认保留还是删除

### 执行步骤

1. **检查 DB 中废弃表是否存在**
   ```bash
   docker exec emily-postgres psql -U emily -d emily -c "
     SELECT tablename FROM pg_tables WHERE schemaname = 'public'
     AND tablename IN ('sop_checkpoints', 'plan_task_templates', 'plan_task_instances', 'plan_task_logs', 'plan_task_deliverables');
   "
   ```

2. **如物理表存在，删除**
   ```bash
   docker exec emily-postgres psql -U emily -d emily -c "
     DROP TABLE IF EXISTS sop_checkpoints CASCADE;
     DROP TABLE IF EXISTS plan_task_templates CASCADE;
     DROP TABLE IF EXISTS plan_task_instances CASCADE;
     DROP TABLE IF EXISTS plan_task_logs CASCADE;
     DROP TABLE IF EXISTS plan_task_deliverables CASCADE;
   "
   ```

3. **检查 agent/ 目录**
   ```bash
   ls emily-core/emily_core/agent/
   # 确认除 sop_parser 外是否有残留模块
   ```

4. **确认看板内容/ 目录处理方式**
   - 如保留：在 CLAUDE.md 和交付手册中明确标注"历史参考，非当前版本"
   - 如删除：`git rm -r 看板内容/`

### 验证标准
- ☐ DB 中无废弃表（sop_checkpoints / plan_task_*）
- ☐ ORM 中无废弃类残留
- ☐ agent/ 目录干净（仅保留 sop_parser）
- ☐ 看板内容/ 已明确处理方式

---

## P2-5：知识库状态确认

### 目标
确认 MaxKB 知识库配置正确、语料完整、RAG 检索可用。

### 检查步骤

1. **MaxKB 容器状态**
   ```bash
   docker compose -f docker-compose-napcat.yml ps maxkb
   curl http://localhost:8080 -I
   ```

2. **知识库文档清单**
   ```bash
   ls -la emily-data/baseknowledge/
   ```

3. **RAG 配置检查**
   ```bash
   # 检查 MaxKB 知识库 ID 和 admin 密码是否配置
   docker exec emily-core env | grep EMILY_MAXKB
   ```

4. **RAG 命中率基线测试**
   ```bash
   # 使用 emy-test 发送知识库相关查询
   uv run python .claude/skills/emy-test/cli.py --managed --llm --message "项目的设计标准是什么" --sender "真实用户名"

   # 检查 RAG 日志
   docker exec emily-postgres psql -U emily -d emily -c "
     SELECT query_text, hit_count, top_score, provider, was_used_by_llm
     FROM rag_retrieval_logs
     ORDER BY created_at DESC LIMIT 10;
   "
   ```

### 执行步骤

1. 如果 RAG 未配置但 `kb_enabled=true`，设为 `false` 避免错误日志
2. 如果 RAG 已配置，运行 5-10 条领域查询验证命中率
3. 记录 RAG 基线数据（命中率、平均分数）供交付手册使用

### 验证标准
- ☐ MaxKB 容器运行正常
- ☐ RAG 查询有命中（如 kb_enabled=true）
- ☐ RAG 日志表有记录
- ☐ baseknowledge/ 语料与 MaxKB 已导入内容一致

---

## P2-9：边界场景确认

### 目标
验证系统在异常/边界条件下的行为是否符合预期。

### 测试场景

| # | 场景 | 测试方式 | 预期行为 |
|---|------|---------|---------|
| 1 | LLM API 不可达 | 临时设置无效 API Key → 发送消息 | 优雅降级（返回友好提示或走 Mock），不崩溃 |
| 2 | 并发消息 | 同一用户连续快速发送 3 条消息 | SessionPool 串行处理，不丢失消息 |
| 3 | 超长消息 | 发送 5000+ 字消息 | 系统正常处理或提示过长 |
| 4 | 空消息 | 发送纯 @bot 无内容 | 友好回复，不报错 |
| 5 | 特殊字符 | 发送含 emoji/换行/制表符消息 | 正常处理 |
| 6 | 数据库连接中断 | 临时停止 postgres → 发送消息 | 返回 500 + 错误日志，不崩溃 |
| 7 | Session TTL 过期 | 等待 10 分钟后继续对话 | 新 Session 创建，旧 Session 归档 |

### 验证标准
- ☐ 7 个边界场景全部测试
- ☐ 无场景导致容器崩溃或无响应
- ☐ 异常场景有友好的用户提示
- ☐ 异常场景有完整的错误日志

---

## P2-11：补齐非技术文档

### 目标
为验收人员提供他们能看懂的使用说明和权限矩阵。

### 需新增文档

1. **用户使用说明**（`docs/用户使用说明.md`）
   - 如何与 Emily 交互（@bot / 私聊）
   - 支持的功能清单（非技术描述）
   - 常见对话示例
   - 不支持的场景说明

2. **用户角色与权限矩阵**（`docs/权限矩阵.md`）
   - 6 级权限层级说明（访客→参建执行→参建管理→建设主管→管理员→系统管理员）
   - 每级可使用的 SOP 功能清单
   - 企业类型与权限关系
   - 示例场景：不同角色用户的操作范围

3. **SOP 业务流人可读版本**（`docs/SOP业务流概览.md`）
   - 10 份 SOP 的非技术描述
   - 触发方式（说什么话会激活）
   - 输出结果（AI 会做什么）
   - 与当前 SOP .md 的区别：当前 SOP 写给 AI 看，此文档写给验收人员看

### 验证标准
- ☐ 3 份文档已创建
- ☐ 非技术人员可理解
- ☐ 权限矩阵与代码逻辑一致

---

## P2-12：备份与恢复方案

### 目标
确保验收方有可执行的数据备份与恢复方案。

### 执行步骤

1. **编写备份脚本**（`scripts/backup.sh`）
   ```bash
   #!/bin/bash
   BACKUP_DIR="./backups/$(date +%Y%m%d_%H%M%S)"
   mkdir -p "$BACKUP_DIR"

   # PostgreSQL 全库备份
   docker exec emily-postgres pg_dump -U emily emily > "$BACKUP_DIR/emily_db.sql"

   # 配置文件备份
   cp -r emily-data/config "$BACKUP_DIR/config"
   cp -r emily-data/sops "$BACKUP_DIR/sops"
   cp -r emily-data/prompts "$BACKUP_DIR/prompts"

   # 附件目录备份
   cp -r emily-data/attachments "$BACKUP_DIR/attachments"

   echo "Backup completed: $BACKUP_DIR"
   ```

2. **编写恢复脚本**（`scripts/restore.sh`）
   ```bash
   #!/bin/bash
   BACKUP_DIR=$1
   if [ -z "$BACKUP_DIR" ]; then
     echo "Usage: ./restore.sh <backup_directory>"
     exit 1
   fi

   # 恢复 PostgreSQL
   docker exec -i emily-postgres psql -U emily emily < "$BACKUP_DIR/emily_db.sql"

   # 恢复配置（需手动确认覆盖）
   echo "Please manually restore: config/ sops/ prompts/ attachments/"
   ```

3. **执行一次完整恢复演练**
   ```bash
   # 备份
   bash scripts/backup.sh

   # 清空测试数据（仅用于演练，生产谨慎！）
   # docker exec emily-postgres psql -U emily -d emily -c "DROP SCHEMA public CASCADE; CREATE SCHEMA public;"

   # 恢复
   bash scripts/restore.sh ./backups/<timestamp>

   # 验证
   docker exec emily-postgres psql -U emily -d emily -c "\dt" | wc -l
   ```

### 验证标准
- ☐ backup.sh 脚本可执行
- ☐ restore.sh 脚本可执行
- ☐ 至少一次完整恢复演练成功
- ☐ 恢复后数据完整（表数量、记录数一致）

---

## P3-13：监控与告警基线

### 目标
记录系统正常运行的基线指标，供验收时参考。

### 检查步骤

1. **Health 接口基线**
   ```bash
   curl -s http://127.0.0.1:18080/api/v1/health | python -m json.tool
   # 记录输出作为基线
   ```

2. **资源使用基线**
   ```bash
   docker stats --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}\t{{.MemPerc}}"
   ```

3. **日志中 ERROR 级别的已知良性条目**
   ```bash
   docker logs emily-core 2>&1 | grep "\[ERROR\]" | sort | uniq -c | sort -rn | head -20
   # 标注哪些是已知良性（如 RAG 未配置时的 MaxKB 连接失败）
   ```

4. **数据库连接池状态**
   ```bash
   docker exec emily-postgres psql -U emily -d emily -c "
     SELECT count(*) AS active_connections FROM pg_stat_activity WHERE datname = 'emily';
   "
   ```

5. **磁盘使用**
   ```bash
   docker exec emily-postgres psql -U emily -d emily -c "
     SELECT pg_size_pretty(pg_database_size('emily')) AS db_size;
   "
   du -sh emily-data/attachments emily-data/logs
   ```

### 验证标准
- ☐ Health 接口返回正常
- ☐ 资源使用记录存档
- ☐ 已知良性 ERROR 条目清单
- ☐ 数据库和磁盘使用量记录

---

## 附录：交付物签收单模板

| # | 交付物 | 路径 | 状态 | 验收人签字 | 日期 |
|----|--------|------|------|-----------|------|
| 1 | 源代码仓库 | `d:\app\Emily\` | ☐ | | |
| 2 | 业务内核代码 | `emily-core/` | ☐ | | |
| 3 | 薄插件代码 | `data/plugins/emily_agent/` | ☐ | | |
| 4 | 数据库建表脚本 | `models.py` (create_all 幂等) | ☐ | | |
| 5 | Docker Compose 编排 | `docker-compose-napcat.yml` | ☐ | | |
| 6 | Dockerfile | `emily-core/Dockerfile` | ☐ | | |
| 7 | 运行时配置 | `emily-data/config/` | ☐ | | |
| 8 | SOP 业务流手册 | `emily-data/sops/` (10 份) | ☐ | | |
| 9 | Prompt 模板 | `emily-data/prompts/` (12 份) | ☐ | | |
| 10 | 管理脚本 | `scripts/` | ☐ | | |
| 11 | 技术文档 | `docs/` (6 份) | ☐ | | |
| 12 | 环境变量模板 | `.env.example` | ☐ | | |
| 13 | 备份恢复脚本 | `scripts/backup.sh` / `restore.sh` | ☐ | | |
| 14 | 用户使用说明 | `docs/用户使用说明.md` | ☐ | | |
| 15 | 权限矩阵 | `docs/权限矩阵.md` | ☐ | | |