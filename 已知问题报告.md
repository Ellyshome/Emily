# Emily v0.7.0 已知问题报告

> 生成日期：2026-07-10 | 更新日期：2026-07-11 | 来源：交付准备计划执行过程中发现

---

## 问题总览

| # | 问题 | 严重程度 | 类型 | 状态 |
|---|------|---------|------|------|
| 1 | SOP-003 任务创建失败 | 高 | 功能缺陷 | ✅ 已修复 |
| 2 | SOP-007 用户记忆未持久化 | 中 | 功能缺陷 | ✅ 已修复 |
| 3 | SOP-999 兜底回复行为异常 | 低 | 体验问题 | ✅ 已修复 |
| 4 | 会议标题未被正确捕获 | 中 | 数据完整性问题 | ✅ 已修复 |
| 5 | SOP-008 待解决问题无法写入 | 中 | 功能缺陷 | ✅ 已修复 |
| 6 | 日志链路不完整 | 中 | 可观测性 | ✅ 已修复 |
| 7 | 数据库密码为弱密码 | 中 | 安全性 | ⏳ 建议修复 |
| 8 | RAG 知识库未完成配置 | 低 | 功能未启用 | ⏳ 待配置 |
| 9 | Git 历史含多条空 commit message | 低 | 规范问题 | ⏳ 建议修正 |
| 10 | 业务编号空库起始非连续 | 低 | 规范问题 | ✅ 需确认（非缺陷） |

---

## 修复记录

### Bug #1-5 共享根因：SkillExecutor 跳过 null-tool 步骤时不做数据提取

**根因分析**：所有 Skill YAML 定义中的"信息提取"步骤（如提取 title/owner/due_date）都设置了 `tool_name: null`。SkillExecutor 旧逻辑直接跳过这些步骤，不调用 LLM 提取数据，导致 `step_results` 为空，后续依赖 `prev_step` 引用的步骤全部拿到空值。

**修复方式**：

| 修改文件 | 修改内容 |
|----------|---------|
| [executor.py](emily-core/emily_core/skill/executor.py) | null-tool 步骤若含 `tool_params`，调用 `_extract_null_step_data()` 用 LLM 提取数据并写入 `step_results` |
| [task_tool.py](emily-core/emily_core/tools/task_tool.py) | 添加 flat params 回退：`data` 为空时从顶层 params 提取业务字段 |
| [meeting_tool.py](emily-core/emily_core/tools/meeting_tool.py) | 同上，flat params 回退 |
| [file_tool.py](emily-core/emily_core/tools/file_tool.py) | 同上，flat params 回退 |
| [SOP-999-SYS.skill.yaml](emily-data/skills/SOP-999-SYS.skill.yaml) | 替换不存在的工具（`query_project_info` 等）为实际注册的工具（`query_data`/`record_event`/`record_task`/`record_meeting`），简化步骤 |
| [SOP-008-SYS.skill.yaml](emily-data/skills/SOP-008-SYS.skill.yaml) | 新增 `action: add` 步骤，支持"记录新待解决问题"场景 |

**影响范围**：所有 10 个 Skill YAML 定义的 SOP 均受益于 SkillExecutor 修复。Task/Meeting/File handler 的 flat params 回退确保 Skill 路径和 LLM 规划路径都能正确传参。

---

### Bug #6：日志链路不完整

**根因分析**：新 Pipeline 架构写入新的进化日志表（pipeline_execution_logs / evolution_llm_interaction_logs 等），不再写入 M11 时代的 4 张旧表（sop_routing_logs / agent_reasoning_logs / llm_interaction_logs / tool_call_logs）。

**修复方式**：

| 修改文件 | 修改内容 |
|----------|---------|
| [legacy_log_bridge.py](emily-core/emily_core/infrastructure/logging/legacy_log_bridge.py) | 新增：Pipeline 完成后从 BusContext/WorkItem 提取数据，写入 4 张旧表 |
| [bus.py](emily-core/emily_core/workitem/pipeline/bus.py) | `_log_execution()` 末尾追加调用 `write_legacy_logs()` |

**注意**：旧表数据为回填摘要，不如新表字段完整。审计查询建议优先使用新表（pipeline_execution_logs / evolution_llm_interaction_logs / business_event_logs），旧表仅作兼容查询。

---

## 待处理项

### #7 数据库密码为弱密码

**严重程度**：中  
**位置**：[docker-compose-napcat.yml](docker-compose-napcat.yml) 第 37、94 行  
**建议**：生产环境改为 16 位以上随机密码，通过环境变量注入。修改密码后需同步更新 PostgreSQL 容器内密码。

### #8 RAG 知识库未完成配置

**严重程度**：低  
**现象**：MaxKB 容器运行正常，`baseknowledge/` 含 5 份语料，但 `EMILY_MAXKB_ADMIN_PASSWORD` 和 `EMILY_MAXKB_KNOWLEDGE_ID` 未设置，`kb_enabled=false`  
**影响**：知识库问答功能不可用（当前已正确禁用，不影响其他功能）

### #9 Git 历史含多条空 commit message

**严重程度**：低  
**建议**：如需清理，可用 `git rebase -i` 合并修改，但需 force push

### #10 业务编号空库起始非连续

**严重程度**：低  
**说明**：编号从 0001 起始是常见做法，非缺陷。如验收方有疑问可解释编号规则。

---

## 附加说明

- **数据已备份**：E2E 测试产生的 3 条事件、2 条会议、1 个节点共 6 条记录保留在数据库中，如需清理可执行 `DELETE FROM events/meetings/project_nodes`
- **测试用户保留**：`Emily测试员`（id: user-test-001, level: 5）保留在数据库中，可用于后续验收测试
- **API Token**：已设置 `EMILY_API_TOKEN`，值保存在 `.env` 文件中（已在 `.gitignore` 排除）

---

> 报告结束 | 编制：交付准备计划自动执行 | 更新：Bug #1-6 已修复 | 审核待定
