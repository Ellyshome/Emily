# 工具权限修复 — AI 执行计划

> **基于需求**：3 层工具权限修复方案讨论（session_api_ids 断层 B + _real_execute 权限缺口）
> **计划版本**：v1.0
> **目标**：建立统一的工具权限体系——填 tool_registry 表 + _llm_plan 按 session_api_ids 过滤工具可见性 + _real_execute 检查 session_api_ids，让未授权用户既看不到也调不到无权限工具

---

## 你的角色

你作为 **Emily 开发者资深架构师** + **安全工程师** + **实施计划编制专家**，严格按以下模块顺序执行，逐模块验证，验证不通过不进入下一个模块。

---

## 硬约束（违反即失败）

1. **不改 handler 签名**：`handle_record_event` / `handle_query_data` 等 handler 函数签名一行不动
2. **不改 BusinessFlowToolRegistry / register_all**：工具内存注册逻辑不动
3. **不改 SkillExecutor 的 session_api_ids 检查**：已有，保留
4. **分层约束**：权限检查在 WorkItemAgent 层（_llm_plan / _real_execute），不污染 Service/Repository
5. **每模块验收**：每个模块验收必须通过，否则停止
6. **执行顺序严格**：M1（填表）→ M2（过滤）→ M3（检查）。**绝不能先做 M2/M3 再做 M1**——表空时 session_api_ids 为空，fail-closed 会导致工具全不可用，Emily 瘫痪
7. **拦截不暴露工具名**：M3 拦截无权限工具时，回复不能含工具名（如"该操作无法执行"，不是"您无权限使用 create_node"）

---

## 上下文（执行前必读）

### 问题背景

当前工具权限有两个缺口：

| 缺口 | 位置 | 后果 |
|------|------|------|
| **_llm_plan tools_text 全量** | [workitem_agent.py:250-258](emily-core/emily_core/workitem/workitem_agent.py#L250-L258) 从 `BusinessFlowToolRegistry.list_names()` 取全部 27 个工具 | LLM 规划时看到所有工具（含管理员工具），未授权用户能"观察到能力存在" |
| **_real_execute 不检查权限** | [workitem_agent.py:413](emily-core/emily_core/workitem/workitem_agent.py#L413) 只查 `tool_name in self._business_flow_tools` | 未授权用户能直接调任何工具（如 level 1 调 create_node） |
| **tool_registry 表空** | `register_api.py --all` 从未执行 | session_api_ids 永远空，SkillExecutor 的权限检查全阻断（断层 B） |

### 架构决策

选 3 层统一权限方案而非"只修 session_api_ids"——因为只修 SkillExecutor 治标不治本，_real_execute 兜底路径仍绕过权限。3 层方案从"可见性"（_llm_plan 过滤）和"可执行性"（_real_execute 检查）两个维度统一，且数据基础（tool_registry 表）先行。

**为什么不给 business 工具加 register() 函数**：business 工具 handler 需依赖注入（event_app 等），register() 拿不到 EmilyCore 实例。而 register_api.py 只取工具元数据（name/display_name/schema/category/permission_flag）写 DB，不调 handler。所以 M1 改 register_api.py 直接从模块 import schema 常量，不依赖 register()，避免改 12+ 个工具模块。

### 已有的可复用组件

| 组件 | 位置 | 关键方法 | 本次怎么用 |
|------|------|----------|-----------|
| `ToolRegistryRepo.upsert` | `emily-core/emily_core/repositories/tool_registry_repo.py:22` | `upsert(api_id, signature, display_name, category, permission_flag, handler_module)` | M1 填表 |
| `ToolRegistryRepo.get_available` | `emily-core/emily_core/repositories/tool_registry_repo.py:66` | `get_available(level, sop_allow)` → `[{api_id, ...}]` | session_api_ids 数据源（已接线，表填了就有数据） |
| `context.get_session_context()` | `emily-core/emily_core/workitem/pipeline/context.py:90` | 返回 SessionContext，含 `available_tools` | M2/M3 取 session_api_ids |
| 各工具 `_*_SCHEMA` 常量 | `emily-core/emily_core/tools/*_tool.py` + `tools/project/__init__.py` | M1 构建 signature |

### 代码模式参照表

| 层 | 参照源（精确文件路径） | 要模仿的要点 |
|----|----------------------|-------------|
| register_api.py 扩展 | `scripts/register_api.py:77-154`（现有 _TOOL_SCRIPTS + do_register） | _TOOL_SCRIPTS 列表 + do_register 的 import + upsert 流程 |
| _llm_plan 改造 | `emily-core/emily_core/workitem/workitem_agent.py:241-293` | 现有 tools_text 构建逻辑，加 session_api_ids 过滤 |
| _real_execute 改造 | `emily-core/emily_core/workitem/workitem_agent.py:376-508` | 现有 tool_name 检查逻辑，加 session_api_ids 检查 |

---

## 模块依赖图

```
M1(register_api.py 扩展 + 填表) ──→ M2(_llm_plan tools_text 过滤)
        │
        └──→ M3(_real_execute session_api_ids 检查)
```

**严格顺序**：M1 必须先完成（填表），否则 M2/M3 的 session_api_ids 为空，fail-closed 导致工具全不可用。

---

## 交付物总览

| 模块 | 交付文件 | 新增/修改 | 核心改动 |
|------|----------|----------|----------|
| M1 | `scripts/register_api.py` | 修改 | _TOOL_SCRIPTS 扩展 27 条 + do_register 改用 schema 常量 |
| M2 | `emily-core/emily_core/workitem/workitem_agent.py` | 修改 | `_llm_plan` 的 tools_text 按 session_api_ids 过滤 |
| M3 | `emily-core/emily_core/workitem/workitem_agent.py` | 修改 | `_real_execute` 加 session_api_ids 检查 + fail-closed |

---

## 现有模块改动清单

| 现有模块 | 改动类型 | 改动内容 |
|----------|----------|----------|
| `scripts/register_api.py` | 修改 | _TOOL_SCRIPTS 从 1 条扩展到 27 条；do_register 从"调 register()"改为"import schema 常量构建 signature" |
| `emily-core/emily_core/workitem/workitem_agent.py` | 修改 | `_llm_plan`（line 250-258）tools_text 加 session_api_ids 过滤；`_real_execute`（line 413）加 session_api_ids 检查 |
| `emily-core/emily_core/tools/*.py` | 不变 | —（handler/schema 不动） |
| `emily-core/emily_core/tools/registry.py` | 不变 | —（register_all 不动） |
| `emily-core/emily_core/skill/executor.py` | 不变 | —（SkillExecutor 的 session_api_ids 检查保留） |

---

## M1: register_api.py 扩展 + 填 tool_registry 表

**依赖**：无（首建模块，数据基础）

**职责**：扩展 register_api.py 支持全量 27 个工具注册到 tool_registry 表，跑 `--all` 填表。

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | _TOOL_SCRIPTS 扩展 27 条 | `scripts/register_api.py` |
| 2 | do_register 改用 schema 常量 | `scripts/register_api.py` |

### 代码

#### `scripts/register_api.py` — 替换 `_TOOL_SCRIPTS` 定义（line 77-81）

打开 `scripts/register_api.py`，定位到 line 77-81：

```python
# ── 工具脚本索引 ──
# 每个条目: (api_id, module_path, category, permission_flag)
_TOOL_SCRIPTS = [
    ("search_files", "emily_core.scripts.search_files", "base", "all"),
]
```

替换为：

```python
# ── 工具脚本索引 ──
# 每个条目: (api_id, module_path, schema_var_or_None, display_name, category, permission_flag)
# schema_var_or_None: 该工具的 schema 常量名（如 _EVENT_TOOL_SCHEMA）；无 schema 常量的工具填 None
# category/permission_flag 与 register_all（tools/registry.py）保持一致
_TOOL_SCRIPTS = [
    # base
    ("query_data", "emily_core.tools.query_tool", "_QUERY_TOOL_SCHEMA", "查询项目数据", "base", "all"),
    ("knowledge_search", "emily_core.tools.knowledge_search_tool", "_KNOWLEDGE_SEARCH_SCHEMA", "搜索知识库获取领域知识", "base", "all"),
    # business — 4 个核心 CRUD
    ("record_event", "emily_core.tools.event_tool", "_EVENT_TOOL_SCHEMA", "记录项目事件", "business", "write"),
    ("record_task", "emily_core.tools.task_tool", "_TASK_TOOL_SCHEMA", "创建任务", "business", "write"),
    ("record_meeting", "emily_core.tools.meeting_tool", "_MEETING_TOOL_SCHEMA", "归档会议纪要", "business", "write"),
    ("record_file", "emily_core.tools.file_tool", "_FILE_TOOL_SCHEMA", "记录文件元数据", "business", "write"),
    # business — 文件查询 + 分类
    ("query_files", "emily_core.tools.file_tool", "_QUERY_FILES_SCHEMA", "按分类或关键词查询项目文件", "business", "all"),
    ("update_file_category", "emily_core.tools.file_tool", "_UPDATE_CATEGORY_SCHEMA", "修改文件分类归属", "business", "write"),
    # business — 用户记忆
    ("write_user_memory", "emily_core.tools.memory_tool", None, "写入用户长期记忆", "business", "all"),
    # business — 节点任务 5 个
    ("create_task_node", "emily_core.tools.node_task_tool", None, "创建TASK类型叶子节点", "business", "write"),
    ("submit_node_deliverable", "emily_core.tools.node_task_tool", None, "提交节点成果", "business", "write"),
    ("confirm_node_deliverable", "emily_core.tools.node_task_tool", None, "确认节点成果", "business", "write"),
    ("return_node_deliverable", "emily_core.tools.node_task_tool", None, "退回节点成果", "business", "write"),
    ("query_my_nodes", "emily_core.tools.node_task_tool", None, "查询我负责的节点", "business", "write"),
    # project — 全景节点 8 个
    ("create_node", "emily_core.tools.node_tool", "_CREATE_NODE_SCHEMA", "创建全景节点", "project", "admin"),
    ("query_node", "emily_core.tools.node_tool", "_QUERY_NODE_SCHEMA", "查询全景节点", "project", "admin"),
    ("update_node_progress", "emily_core.tools.node_tool", "_UPDATE_PROGRESS_SCHEMA", "更新节点进度", "project", "admin"),
    ("add_node_dependency", "emily_core.tools.node_tool", "_ADD_DEPENDENCY_SCHEMA", "添加节点依赖", "project", "admin"),
    ("mount_child_node", "emily_core.tools.node_tool", "_MOUNT_CHILD_SCHEMA", "挂载子节点", "project", "admin"),
    ("update_nodes", "emily_core.tools.node_tool", "_UPDATE_NODES_SCHEMA", "批量更新节点", "project", "admin"),
    ("activate_nodes", "emily_core.tools.node_tool", "_ACTIVATE_NODES_SCHEMA", "批量激活节点", "project", "admin"),
    ("discard_nodes", "emily_core.tools.node_tool", "_DISCARD_NODES_SCHEMA", "批量废弃节点", "project", "admin"),
    # project — 邮箱 2 个
    ("send_email", "emily_core.tools.project", "_SEND_EMAIL_SCHEMA", "发送邮件", "base", "all"),
    ("fetch_inbox", "emily_core.tools.project", "_FETCH_INBOX_SCHEMA", "获取收件箱", "base", "all"),
    # project — 其他
    ("chat_archive", "emily_core.tools.project", "_CHAT_ARCHIVE_SCHEMA", "聊天归档查询", "base", "all"),
    ("manage_pending_issues", "emily_core.tools.project", "_PENDING_ISSUE_SCHEMA", "管理待解决问题", "base", "all"),
    ("voice_entry", "emily_core.tools.project", "_VOICE_ENTRY_SCHEMA", "语音入口", "base", "all"),
    # emily_core/scripts 下的工具
    ("search_files", "emily_core.scripts.search_files", "_SEARCH_FILES_SCHEMA", "搜索可见文件", "base", "all"),
]
```

#### `scripts/register_api.py` — 替换 `do_register` 函数（line 84-154）

定位到 `def do_register(api_id: str) -> bool:`（line 84），整个函数替换为：

```python
def do_register(api_id: str) -> bool:
    """注册单个 API：从模块取 schema 常量构建 signature，写入 tool_registry 表。

    Returns:
        True 注册成功，False 注册失败
    """
    _init_db()

    # 查找条目
    entry = None
    for e in _TOOL_SCRIPTS:
        if e[0] == api_id:
            entry = e
            break

    if entry is None:
        print(f"[ERROR] Unknown API: {api_id}")
        return False

    api_id, module_path, schema_var, display_name, category, perm_flag = entry

    # 从模块 import schema 常量（构建 signature）
    import importlib
    parameters = {"type": "object", "properties": {}}
    handler_module = module_path
    try:
        m = importlib.import_module(module_path)
        if schema_var:
            schema = getattr(m, schema_var, None)
            if isinstance(schema, dict):
                parameters = schema
        # handler_module 取模块里 handle_xxx 函数的 __module__（若存在）
        for attr_name in dir(m):
            if attr_name.startswith("handle_") or attr_name == "register":
                attr = getattr(m, attr_name)
                if hasattr(attr, "__module__"):
                    handler_module = attr.__module__
                    break
    except Exception as e:
        print(f"[WARN] import {module_path} failed: {e}（仍写入 DB，parameters 用空 schema）")

    # DB 录入
    from emily_core.repositories.tool_registry_repo import ToolRegistryRepo

    signature = json.dumps(
        {"params": parameters, "returns": "dict"},
        ensure_ascii=False,
    )

    ok = ToolRegistryRepo.upsert(
        api_id=api_id,
        signature=signature,
        display_name=display_name,
        category=category,
        permission_flag=perm_flag,
        handler_module=handler_module,
    )

    if ok:
        print(f"  [DB]   Tool '{api_id}' registered ({category}/{perm_flag})")
    else:
        print(f"  [DB]   Tool '{api_id}' DB upsert FAILED")
        return False

    return True
```

#### `scripts/register_api.py` — 更新 `--all` 命令（如果已有的话，确认遍历 _TOOL_SCRIPTS）

定位到 `--all` 的处理逻辑（通常在 main 里），确认它遍历 `_TOOL_SCRIPTS` 全量注册。如果现有 `--all` 只遍历 `_TOOL_SCRIPTS`，无需改；如果它调各模块的 `register()`，改为调 `do_register(e[0])`。检查 main 函数的 `--all` 分支，确保是：

```python
if args.all:
    for entry in _TOOL_SCRIPTS:
        do_register(entry[0])
```

### 模块验收检测

```bash
# 验收 1：跑 --all 填表
cd d:\app\Emily
uv run python scripts/register_api.py --all 2>&1 | tail -5
→ 预期输出：最后几行含 "Tool 'voice_entry' registered" 等，共 28 条（27 + search_files）

# 验收 2：确认 tool_registry 表有 28 行
docker exec emily-postgres psql -U emily -d emily -c "SELECT count(*) FROM tool_registry;"
→ 预期输出：count=28（27 个 register_all 工具 + search_files）

# 验收 3：确认 category 分布
docker exec emily-postgres psql -U emily -d emily -c "SELECT category, count(*) FROM tool_registry GROUP BY category;"
→ 预期输出：base 12 + business 12 + project 8（或接近，取决于 send_email 等的 category）

# 验收 4：用验证机制脚本确认 V13 通过
uv run python scripts/check_tools_consistency.py 2>&1 | grep "V13"
→ 预期输出：[V13] tool_registry 表: DB 28 条 | 内存缺 DB 0 | DB 缺内存 0（或接近）
```

**失败处理**：
- 验收 1 某工具 import 失败 → 检查 _TOOL_SCRIPTS 里该工具的 module_path / schema_var 是否正确（对照 tools/ 目录实际文件）
- 验收 2 count=0 → do_register 的 upsert 失败，检查 ToolRegistryRepo.upsert 的 DB 连接（_init_db 是否正确）
- 验收 4 "内存缺 DB" 有值 → _TOOL_SCRIPTS 漏了某工具，对照 REGISTERED_TOOLS（验证机制里硬编码的 27 个）补全

---

## M2: _llm_plan tools_text 按 session_api_ids 过滤

**依赖**：M1（session_api_ids 要有数据）

**职责**：让 LLM 规划时只看到用户有权限的工具，未授权工具不可见。

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | _llm_plan tools_text 过滤 | `emily-core/emily_core/workitem/workitem_agent.py` |

### 代码

#### `emily-core/emily_core/workitem/workitem_agent.py` — 替换 `_llm_plan` 的 tools_text 构建逻辑（line 250-265）

定位到 line 250-265（`_llm_plan` 内的 tools_text 构建和 prompt format）：

```python
        # 构建可用工具列表（从 BusinessFlowToolRegistry 动态生成）
        tools_text = ""
        if self._business_flow_tools:
            tool_entries = []
            for name in sorted(self._business_flow_tools.list_names()):
                tool = self._business_flow_tools.get(name)
                if tool:
                    tool_entries.append(f"- {name}: {tool.description}")
            tools_text = "\n".join(tool_entries) if tool_entries else "（无可用工具）"

        planner_prompt = _load_planner_prompt()
        system_prompt = planner_prompt.format(
            sop_text=sop_text[:4000] if sop_text else f"SOP: {wi.sop_id or '未知'}（全文未加载）",
            user_input=wi.user_input,
            available_tools=tools_text,
        )
```

替换为：

```python
        # 构建可用工具列表 —— 按 session_api_ids 过滤（权限可见性）
        # 只列用户有权限的工具，避免未授权用户"观察到能力存在"
        # session_api_ids 来自 SessionContext.available_tools（tool_registry 表权限过滤结果）
        session_ctx = context.get_session_context() if context else None
        session_api_ids = set()
        if session_ctx:
            for t in getattr(session_ctx, "available_tools", []) or []:
                api_id = t.get("api_id") if isinstance(t, dict) else None
                if api_id:
                    session_api_ids.add(api_id)

        tools_text = ""
        if self._business_flow_tools and session_api_ids:
            tool_entries = []
            for name in sorted(self._business_flow_tools.list_names()):
                if name not in session_api_ids:
                    continue  # 用户无权限，不暴露给 LLM
                tool = self._business_flow_tools.get(name)
                if tool:
                    tool_entries.append(f"- {name}: {tool.description}")
            tools_text = "\n".join(tool_entries) if tool_entries else "（无可用工具）"
        elif not session_api_ids:
            # fail-closed：session_api_ids 为空（tool_registry 表未填充）→ 不给 LLM 任何工具
            # 避免表空时 LLM 看到全量工具绕过权限
            tools_text = "（无可用工具——工具权限表未初始化，请联系管理员）"
            logger.warning("_llm_plan: session_api_ids 为空，tool_registry 表可能未填充，tools_text 已 fail-closed")
        else:
            tools_text = "（无可用工具）"

        planner_prompt = _load_planner_prompt()
        system_prompt = planner_prompt.format(
            sop_text=sop_text[:4000] if sop_text else f"SOP: {wi.sop_id or '未知'}（全文未加载）",
            user_input=wi.user_input,
            available_tools=tools_text,
        )
```

### 模块验收检测

```bash
# 验收 1：import 不报错
cd d:\app\Emily
uv run python -c "import sys; sys.path.insert(0,'emily-core'); from emily_core.workitem.workitem_agent import WorkItemAgent; print('import OK')"
→ 预期输出：import OK

# 验收 2：清空 emily-core 容器 pycache（bind-mount 不自动刷新）
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +
→ 预期输出：无（清缓存成功）

# 验收 3：重启 emily-core 使代码生效
docker compose -f docker-compose-napcat.yml restart emily-core
→ 预期输出：容器重启

# 验收 4：用 emy-test 发消息给 level 1 用户，看日志确认 tools_text 过滤
# 先查一个 level 1 用户
docker exec emily-postgres psql -U emily -d emily -c "SELECT id, username FROM users WHERE status='active' AND is_admin=false LIMIT 1;"
→ 预期输出：一个 level 1 用户（如 刘大勇）

# 用该用户发消息，看 emily-core 日志的 tools_text
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "你好" --sender-id "<上面查到的 UUID>" 2>&1 | tail -5
docker logs --tail 30 emily-core 2>&1 | grep -i "tools_text\|available_tools\|_llm_plan"
→ 预期：日志显示 tools_text 只含 base 工具（query_data/knowledge_search 等），不含 create_node/record_event（level 1 无权限）
```

**失败处理**：
- 验收 1 报错 → 检查替换的代码缩进（_llm_plan 方法体内，8 空格缩进）
- 验收 4 tools_text 仍含全量工具 → session_api_ids 为空（M1 未填表或 available_tools 未灌注），检查 SessionContext.available_tools 是否有值
- 验收 4 日志有"session_api_ids 为空"告警 → M1 填表未成功，或 fetch_available_tools 没查到数据，回头查 M1

---

## M3: _real_execute 检查 session_api_ids

**依赖**：M1

**职责**：_real_execute 执行工具前检查 session_api_ids，拦截无权限工具调用（拦截不暴露工具名）。

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | _real_execute session_api_ids 检查 | `emily-core/emily_core/workitem/workitem_agent.py` |

### 代码

#### `emily-core/emily_core/workitem/workitem_agent.py` — 在 `_real_execute` 方法开头加 session_api_ids 构建，并在工具调用前加检查

定位到 `_real_execute`（line 376）。在方法开头（`if self._business_flow_tools is None:` 之前）加 session_api_ids 构建：

```python
    async def _real_execute(self, plan: ExecutionPlan, context: BusContext) -> list[StepResult]:
        """真实执行引擎 —— 按 PlanStep 调用工具 handler。

        对有 tool_name 且在 BusinessFlowToolRegistry 中注册的步骤，
        调用 handler(tool_params) 直接执行；其他步骤返回纯文本结果。
        """
        # 构建 session_api_ids（权限可见性集合，来自 SessionContext.available_tools）
        session_ctx = context.get_session_context() if context else None
        session_api_ids = set()
        if session_ctx:
            for t in getattr(session_ctx, "available_tools", []) or []:
                api_id = t.get("api_id") if isinstance(t, dict) else None
                if api_id:
                    session_api_ids.add(api_id)

        if self._business_flow_tools is None:
            logger.error("RealExecutor: no BusinessFlowToolRegistry available, returning empty results")
            return []
```

然后定位到 line 412-414（工具调用前的检查）：

```python
            try:
                if tool_name and tool_name in self._business_flow_tools:
                    # 框架直接调用 handler
                    tool = self._business_flow_tools.get(tool_name)
```

替换为（加 session_api_ids 检查）：

```python
            try:
                if tool_name and tool_name in self._business_flow_tools:
                    # 权限检查：工具必须在 session_api_ids 里（fail-closed）
                    # 拦截时不暴露工具名，避免未授权用户得知能力存在
                    if not session_api_ids:
                        # fail-closed：session_api_ids 空（表未填充）→ 拒绝所有工具调用
                        logger.warning(
                            "Step %s: session_api_ids 为空，tool_registry 表可能未填充，工具调用 fail-closed",
                            step.step_id,
                        )
                        sr = StepResult(
                            step_id=step.step_id,
                            success=False,
                            output="该操作暂不可用，请联系管理员检查系统配置。",
                        )
                        results.append(sr)
                        break
                    if tool_name not in session_api_ids:
                        # 用户无权限调用此工具——不暴露工具名
                        logger.info(
                            "Step %s: 工具调用被权限拦截（不在 session_api_ids，不暴露工具名）",
                            step.step_id,
                        )
                        sr = StepResult(
                            step_id=step.step_id,
                            success=False,
                            output="该操作无法执行，您可能没有相应权限。",
                        )
                        results.append(sr)
                        break
                    # 框架直接调用 handler
                    tool = self._business_flow_tools.get(tool_name)
```

### 模块验收检测

```bash
# 验收 1：import 不报错
cd d:\app\Emily
uv run python -c "import sys; sys.path.insert(0,'emily-core'); from emily_core.workitem.workitem_agent import WorkItemAgent; print('import OK')"
→ 预期输出：import OK

# 验收 2：清 pycache + 重启
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +
docker compose -f docker-compose-napcat.yml restart emily-core

# 验收 3：用 emy-test 发"创建节点"给 level 1 用户，确认 _real_execute 拦截
# （level 1 无 create_node 权限——create_node 是 project/admin，需 level>=5）
# 用 M2 验收 4 查到的 level 1 用户 UUID
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我创建一个节点" --sender-id "<level1 UUID>" 2>&1 | tail -3
→ 预期输出：回复不含"create_node"工具名，不含事件编号；可能是"该操作无法执行"或 LLM 基于 tools_text（不含 create_node）给的通用回复

# 验收 4：看日志确认拦截
docker logs --tail 30 emily-core 2>&1 | grep -i "权限拦截\|session_api_ids\|fail-closed"
→ 预期：日志含"工具调用被权限拦截"或"session_api_ids 为空"（取决于 LLM 是否规划出 create_node）

# 验收 5：用管理员（level 6）发同样消息，确认能调（不误拦）
# 管理员 UUID（之前查过：王建国 0ea04628-ec92-436b-9214-33900d8ffa0c）
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我创建一个节点" --sender-id "0ea04628-ec92-436b-9214-33900d8ffa0c" 2>&1 | tail -3
→ 预期输出：管理员能调 create_node（session_api_ids 含 create_node，因为 level 6 >= 5）
```

**失败处理**：
- 验收 1 报错 → 检查替换代码缩进
- 验收 3 level 1 用户成功调了 create_node → session_api_ids 检查未生效，检查 _real_execute 是否拿到 session_api_ids（context.get_session_context() 是否返回非 None）
- 验收 5 管理员也被拦 → session_api_ids 没含 create_node，检查 M1 填表的 category/permission_flag 和 ToolRegistryRepo.get_available 的过滤逻辑（level 6 应通过 project/admin）

---

## 组装验证

所有模块完成后，运行端到端权限隔离验证：

```bash
cd d:\app\Emily

# 1. 确认 tool_registry 表已填（M1）
docker exec emily-postgres psql -U emily -d emily -c "SELECT count(*) FROM tool_registry;"
→ 预期：28

# 2. 确认验证机制 V13 通过（表与内存一致）
uv run python scripts/check_tools_consistency.py 2>&1 | grep "V13"
→ 预期：DB 28 条 | 内存缺 DB 0 | DB 缺内存 0

# 3. 权限隔离测试：level 1 用户不能调 create_node
LEVEL1_UUID=$(docker exec emily-postgres psql -U emily -d emily -t -c "SELECT id FROM users WHERE status='active' AND is_admin=false LIMIT 1;" | tr -d ' \n')
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我创建一个节点 SG-TEST" --sender-id "$LEVEL1_UUID" 2>&1 | tail -3
→ 预期：回复不含节点创建成功，不含 create_node 工具名
→ 检查日志：docker logs --tail 20 emily-core 2>&1 | grep "权限拦截"

# 4. 管理员能调 create_node（不误拦）
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我创建一个节点 SG-TEST" --sender-id "0ea04628-ec92-436b-9214-33900d8ffa0c" 2>&1 | tail -3
→ 预期：管理员能创建节点（或至少 _real_execute 不拦 create_node）

# 5. level 1 用户能调 base 工具（query_data/knowledge_search 不误拦）
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "查询我的项目列表" --sender-id "$LEVEL1_UUID" 2>&1 | tail -3
→ 预期：query_data 是 base/all，level 1 能调，回复含项目列表
```

---

## 阶段反思指令

每完成一个模块，在进入下一个模块之前，执行以下反思：

1. **检查产物**：列出本模块所有修改的文件路径
2. **检查偏差**：是否有步骤与计划不符？记录差异
3. **判断是否继续**：
   - 如果偏差 ≤ 1 个文件路径变化 → 直接修改计划文档对应模块，继续
   - 如果偏差 2-4 个文件或步骤顺序调整 → 在计划文档末尾追加 "v1.1 修订记录"，继续
   - 如果偏差 > 4 个文件或架构方向变化 → **停止**，报告给用户

---

## 关键风险与注意事项

### 风险 1：M2/M3 必须在 M1 之后

**绝不能先做 M2/M3**——session_api_ids 来自 tool_registry 表，表空时 session_api_ids 为空，M2 的 fail-closed 会让 LLM 看不到任何工具，M3 的 fail-closed 会拒绝所有工具调用，Emily 瘫痪。

**执行顺序严格**：M1（填表）→ M2（过滤）→ M3（检查）。M1 验收通过（表有 28 行）才进 M2。

### 风险 2：level 1 用户能力变化

M2/M3 后权限按 tool_registry 表的 category/permission_flag 严格执行：
- level 1 用户只能用 base/all 工具（query_data / knowledge_search / send_email 等）
- record_event/task/meeting/file（business/write）需 level >= 3
- create_node 等（project/admin）需 level >= 5

**如果业务上 level 1 用户需要创建事件**，需调整 permission_flag（如 record_event 改 all）或用户 level——但这超出本计划范围（本计划只做权限检查机制，不改权限规则）。执行前请与业务确认 level 分布是否符合预期。

### 风险 3：pycache 不刷新

emily-core 是 bind-mount，改 workitem_agent.py 后必须清 pycache + 重启容器（M2/M3 验收都含此步）。否则跑的是旧字节码。

### 风险 4：拦截不暴露工具名

M3 的拦截回复是"该操作无法执行，您可能没有相应权限"——**不含工具名**。这是安全设计，避免未授权用户通过拦截消息得知工具存在。执行者不要把工具名加进回复。

---

## 与其他工作的关系

| 相关工作 | 关系 |
|---------|------|
| 验证机制（`scripts/check_tools_consistency.py`） | M1 填表后，V13 检查从"表空"变为"表与内存一致"，验证机制成为本修复的验收工具 |
| Skill YAML 修复（`需求/需求文件/Skill_YAML一致性修复计划_已执行完毕.md`） | 本修复不依赖 Skill YAML，但 Skill 路径启用后本修复的 M3 检查与 SkillExecutor 的 session_api_ids 检查一致 |
| 挂载 /app/skills（断层 A） | 本修复不依赖 skills 挂载。但 skills 挂载后 Skill 路径触发，本修复确保两条路径权限一致 |

---

*本计划为 AI 可执行操作手册，由 req-plan 技能生成。基于 3 层工具权限修复方案讨论 + 项目现状探查。*
