# 三书认知装配层与会话编排 — 详细设计（DD）

> **基于规格（PRD）**：[三书认知装配层_PRD_V1.md](三书认知装配层_PRD_V1.md)
> **基于计划（SD）**：[三书认知装配层_计划_V1.md](三书认知装配层_计划_V1.md)（含"PRD 约束落实"与"追溯矩阵"）
> **宪法版本**：v1.0
> **设计版本**：v1.0
> **级别**：Detailed Design（实现级，可直接编码）
> **目标**：给出每个模块的**精确文件路径、完整代码、逐行 diff、边界处理与降级路径**，工程师可照抄落地；并逐条应答 PRD 附录 B 的方案型技术问题。

---

## 你的角色

你作为 **Emily开发者资深架构师 + 系统设计师**，按 SD 的拓扑序（M1a→M1b→M1c→M0→M1→M2→M7→M8→M9）逐模块落地。每模块编码后立即跑本文件给出的**自检命令**，不通过不进入下一模块。

**本 DD 的编码约定**（与现有代码库一致）：

| 约定 | 说明 |
|------|------|
| 类型标注 | 使用 `from __future__ import annotations`；容器类型用内建泛型（`list[str]` / `dict[str, str]`），与 `session_context.py` 一致 |
| 日志 | 模块级 `logger = logging.getLogger("emily.<域>.<模块>")`，命名对照现有（`emily.session.fetchers.fetch_system_description`、`emily.rule_book_loader`、`emily.scheduler`） |
| 异常 | 裁剪/摘要类 **fail-open**（异常→空串 + `logger.warning`）；权限类 **fail-closed**（异常→最小可见集） |
| 分层 | 裁剪函数为**纯函数**（无 IO）；IO 只在 Repository / Loader / Fetcher 层发生 |
| iOS 归属 | `Repository.*` 全 sync；async 侧一律 `await asyncio.to_thread(...)` 包裹 |
| 幂等 | 无 DDL、无数据迁移脚本写入（仅重跑 Builder），无需 `IF NOT EXISTS` 保护 |
| 无新依赖 | frontmatter 用项目已依赖的 `pyyaml`（`session_context.py` 已在用 `import yaml`） |

---

## 一、铁律遵循声明（宪法 §2 / §3 对照）

| 铁律 | 本 DD 如何遵守 | 涉及 DD 章节 |
|------|---------------|-------------|
| **C0 根治而非迁就** | 摘要按 actor 权限实时裁剪（根因：群聊多用户越权），而非"用 Session 创建者权限凑合" | DD-M1 |
| **C1 业务内核独立** | 全部改动位于 `emily-core/emily_core/`，无 `astrbot.*` import | 全部 |
| **C2 分层不可跳** | 裁剪纯函数（Session/Fetcher 层）→ 变量装配（Session 层）；数据读取仍走 Repository | DD-M1a/b/c/M1 |
| **C3 SOP 即路由** | 不改 SOP 机制，不新增 SOP | — |
| **C4 Hook 声明式** | 不新增 Hook | — |
| **C5 结构化输出优先** | 不改工具调用范式 | — |
| **C6 Sync repo + to_thread** | `meta_cognition_read` 的 DB 读取经 `asyncio.to_thread` 包裹 | DD-M2 |
| **C7 Hook 三态** | 不涉及 | — |
| **C8 唯一执行引擎** | `run_dag` 复用 `_run_one` → `_run_graph` → `graph.ainvoke()`，不新建执行路径 | DD-M8 |
| **C9 ToolManager/ScriptManager 边界** | 两个新增脚本走 subprocess CLI（`scripts/`），不 import 工具运行时；共享 service 层 | DD-脚本 |
| **C10 工具带 schema** | `meta_cognition_read` 三步齐备：`_META_COG_SCHEMA` → `registry.py` 传 `params=` → `TOOL_SCHEMA_MAP` | DD-M2 |
| **C11 功能注册接入** | 工具经 `tools/registry.py`；配置经 `config.py`；无孤儿文件 | DD-M2/M8 |
| **Q1 验收可执行** | 每模块附可复制命令 + 预期输出 | 各模块 §自检 |
| **Q2 证据驱动** | 验收证据链指向 DB 查询 / 单测输出 / prompt 落盘 | 各模块 §自检 |
| **Q3 真实用户测试** | 端到端用例强制 `--sender` 真实用户名 | 验收计划 §4 |
| **Q4 无残留** | 迁移脚本无临时桩；新增脚本为长期资产（已登记 SD 脚本清单） | DD-脚本 |
| **Q5 无越界** | 不新增依赖；不顺带重构无关代码（唯一"顺带"是把 `_split_into_workitems` 拆为可复用工厂，属本需求必需） | 全部 |

---

## 二、交付物总览（文件清单）

| # | 文件 | 类型 | 归属模块 | 预估规模 |
|---|------|------|---------|---------|
| 1 | `emily-core/emily_core/session/fetchers/fetch_world_book.py` | **新增** | M1a | ~95 行 |
| 2 | `emily-core/emily_core/services/world_book_builder.py` | 修改 | M1a | +25 / -2 行 |
| 3 | `emily-data/rules/规则书.md` | 修改 | M1b | +30 行（7 处 frontmatter） |
| 4 | `emily-core/emily_core/services/rule_book_loader.py` | 修改 | M1b | +80 行 |
| 5 | `emily-core/emily_core/session/fetchers/fetch_system_description.py` | 修改 | M1c | +55 / -8 行 |
| 6 | `emily-data/prompts/session.md` | 修改 | M0 + M1 | 结构重排 + 3 占位符 |
| 7 | `emily-core/emily_core/session/session_context.py` | 修改 | M1 | +55 行 |
| 8 | `emily-core/emily_core/session/session_data_fetcher.py` | 修改 | M1 | +45 行 |
| 9 | `emily-core/emily_core/session/session_agent.py` | 修改 | M1 + M7 | +130 / -90 行 |
| 10 | `emily-core/emily_core/tools/meta_cognition_tool.py` | **新增** | M2 | ~140 行 |
| 11 | `emily-core/emily_core/tools/registry.py` | 修改 | M2 | +9 行 |
| 12 | `emily-core/emily_core/infrastructure/tools_consistency.py` | 修改 | M2 | +3 行 |
| 13 | `emily-core/emily_core/workitem/langgraph_engine/agent/fallback_policy.py` | 修改 | M2 | +1 行 |
| 14 | `emily-core/emily_core/session/orchestrator.py` | **新增** | M7 + M9 | ~200 行 |
| 15 | `emily-core/emily_core/adapters/session/session_factory.py` | 修改 | M7 | +1 行 |
| 16 | `emily-core/emily_core/workitem/workitem.py` | 修改 | M7 | +8 行 |
| 17 | `emily-core/emily_core/workitem/workitem_state.py` | 修改 | M8 | +3 / -2 行 |
| 18 | `emily-core/emily_core/workitem/scheduler.py` | 修改 | M8 | +95 行 |
| 19 | `emily-core/emily_core/config.py` | 修改 | M8 | +12 行 |
| 20 | `scripts/dump_meta_cognition.py` | **新增** | M2（验证） | ~60 行 |
| 21 | `scripts/migrate_world_book_segments.py` | **新增** | M1a（迁移） | ~70 行 |

**无新增依赖，无 DDL，无新表/新列。**

---

## 三、实施顺序与阶段性验证

```
Step 1  DD-M1a 世界书裁剪层       → 自检 A1/A2/A3     （无依赖）
Step 2  DD-M1b 规则书裁剪层       → 自检 B1/B2/B3     （无依赖，可与 Step1 并行）
Step 3  DD-M1c 认知书摘要层       → 自检 C1/C2/C3     （无依赖，可与 Step1 并行）
Step 4  DD-M0  使命与边界         → 自检 D1           （无依赖）
        ⛔ 里程碑 A：三书裁剪函数就绪（自检 A/B/C 全绿）
Step 5  DD-M1  摘要注入装配层      → 自检 E1/E2/E3     （依赖 Step1-4）
        ⛔ 里程碑 B：摘要进 prompt 且缓存稳定
Step 6  DD-M2  meta_cognition_read → 自检 F1/F2/F3/F4  （依赖 Step1-3）
Step 7  DD-M7  SessionOrchestrator → 自检 G1/G2/G3     （依赖 Step5）
Step 8  DD-M8  Scheduler.run_dag   → 自检 H1/H2/H3/H4  （依赖 Step7）
Step 9  DD-M9  跨域检索编排        → 自检 I1/I2/I3     （依赖 Step7-8）
Step 10 脚本 + 迁移               → 自检 J1/J2
        ⛔ 里程碑 C：端到端就绪
```

---

## DD-M1a：世界书裁剪层

### A. 新增文件 `emily-core/emily_core/session/fetchers/fetch_world_book.py`

```python
"""fetch_world_book —— 世界书裁剪纯函数（按授权节点裁剪）。

被两处消费：
  1) SessionContext.get_prompt_variables() → render_brief()  生成 {project_brief} 常驻摘要
  2) tools/meta_cognition_tool.py          → render_full()   生成 book="world" 全文

设计约束：
  - 纯函数，无 IO（世界书原文由 SessionContext 缓存 / tool 层读取后传入）
  - fail-open：解析失败或异常一律返回空串，不阻断 prompt 装配
  - 权限 fail-closed：authorized_node_ids 为空 → 不返回任何节点信息

参照模式：emily_core/session/fetchers/fetch_system_description.py（纯函数 + CLI 入口）
"""

from __future__ import annotations

import json
import logging
import argparse

logger = logging.getLogger("emily.session.fetchers.fetch_world_book")

_STATUS_CN = {
    "COMPLETED": "已完成",
    "IN_PROGRESS": "进行中",
    "CONDITIONS_NOT_MET": "条件未满足",
    "NOT_ACTIVATED": "未激活",
}


def _load(content_json: str) -> dict:
    """解析世界书 content_json；失败返回空 dict（fail-open）。"""
    try:
        data = json.loads(content_json or "{}")
        return data if isinstance(data, dict) else {}
    except (json.JSONDecodeError, TypeError, ValueError) as e:
        logger.warning("world book content_json parse failed: %s", e)
        return {}


def _truncate(text: str, max_chars: int) -> str:
    """按行边界截断到 max_chars。"""
    if len(text) <= max_chars:
        return text
    cut = text[:max_chars]
    nl = cut.rfind("\n")
    return cut[:nl] if nl > 0 else cut


def render_brief(content_json: str, authorized_node_ids: list[str], max_chars: int = 200) -> str:
    """生成常驻摘要（≤max_chars）。

    结构（缺数据则整行省略）：
      📋 {项目名}（{编号}） ｜ 阶段：{阶段}
      📊 {N}节点：{完成}完成 / {进行中}进行中 / {逾期}逾期 ｜ {整体进度}
      🔖 我可见节点（{k}）：{节点名}[状态] / ...
      🔴 逾期：{节点名} / ...
    """
    data = _load(content_json)
    if not data:
        return ""
    auth = set(authorized_node_ids or [])
    lines: list[str] = []

    o = data.get("ontology") or {}
    if o.get("name"):
        code = f"（{o['code']}）" if o.get("code") else ""
        stage = o.get("lifecycle_stage_label") or ""
        lines.append(f"📋 {o['name']}{code}" + (f" ｜ 阶段：{stage}" if stage else ""))

    s = data.get("structure") or {}
    if int(s.get("total_nodes", 0) or 0) > 0:
        lines.append(
            f"📊 {s.get('total_nodes', 0)}节点：{s.get('completed', 0)}完成 / "
            f"{s.get('in_progress', 0)}进行中 / {s.get('overdue', 0)}逾期 ｜ "
            f"{s.get('overall_progress', '')}"
        )
        segs = s.get("node_segments") or {}
        if segs:
            visible = [(nid, seg) for nid, seg in segs.items() if nid in auth]
            names = [
                f"{seg.get('name', '')}[{_STATUS_CN.get(seg.get('status', ''), seg.get('status', ''))}]"
                for _, seg in visible[:3]
            ]
            lines.append(f"🔖 我可见节点（{len(visible)}）：" + (" / ".join(names) if names else "无"))

    oi = (data.get("temporal") or {}).get("overdue_items") or []
    if oi:
        lines.append("🔴 逾期：" + " / ".join(str(i.get("name", "")) for i in oi[:3]))

    return _truncate("\n".join(lines), max_chars)


def render_full(content_json: str, authorized_node_ids: list[str]) -> str:
    """生成裁剪后全文（供 meta_cognition_read）。无权节点整段剔除。"""
    data = _load(content_json)
    if not data:
        return ""
    auth = set(authorized_node_ids or [])
    lines: list[str] = []

    brief = render_brief(content_json, authorized_node_ids, max_chars=100000)
    if brief:
        lines.append(brief)

    auth_set = auth
    segs = ((data.get("structure") or {}).get("node_segments")) or {}
    if segs and auth_set:
        rows = []
        for nid, seg in segs.items():
            if nid not in auth_set:
                continue
            rows.append(
                f"{nid} {seg.get('name', '')}"
                f"[{_STATUS_CN.get(seg.get('status', ''), seg.get('status', ''))}]"
                f" 进度{seg.get('progress', 0)}%"
                + ("（里程碑）" if seg.get("milestone") else "")
            )
        if rows:
            lines.append("")
            lines.append("【节点明细】")
            lines.extend(rows)

    t = data.get("temporal") or {}
    for label, key in (("近期事件", "recent_events"), ("7天内到期", "upcoming_deadlines")):
        items = t.get(key) or []
        if items:
            lines.append("")
            lines.append(f"【{label}】")
            for it in items[:10]:
                lines.append(f"· {it.get('date', it.get('deadline', ''))} {it.get('name', it.get('summary', ''))}")

    r = data.get("relation") or {}
    blocked = r.get("blocked_nodes") or []
    if blocked:
        lines.append("")
        lines.append("【阻塞】")
        for b in blocked[:10]:
            lines.append(f"· {b.get('blocked_by', '')} → {b.get('node', '')}等待")

    return "\n".join(lines)


def main() -> None:
    """独立运行入口：python -m emily_core.session.fetchers.fetch_world_book --project-id <UUID> --node-ids SG-001,SG-002"""
    import sys
    sys.stdout.reconfigure(encoding="utf-8")
    parser = argparse.ArgumentParser(description="世界书裁剪预览")
    parser.add_argument("--project-id", required=True)
    parser.add_argument("--node-ids", default="", help="逗号分隔的授权节点编号")
    parser.add_argument("--full", action="store_true", help="输出全文而非摘要")
    parser.add_argument("--db-url", default="")
    args = parser.parse_args()

    from emily_core.infrastructure.database import init_db
    from emily_core.repositories.world_book_repo import ProjectWorldBookRepo
    init_db(db_url=args.db_url) if args.db_url else init_db()

    wb = ProjectWorldBookRepo.get_by_project(args.project_id)
    if wb is None:
        print("（无世界书记录）")
        return
    node_ids = [x.strip() for x in args.node_ids.split(",") if x.strip()]
    out = render_full(wb.content_json, node_ids) if args.full else render_brief(wb.content_json, node_ids)
    print(out)


if __name__ == "__main__":
    main()
```

### B. 修改 `emily-core/emily_core/services/world_book_builder.py`

**B-1. `_build_structure()` 新增 `node_segments`**（在 `return` 前插入，`milestones` 之后）：

```python
                # M1a: 按节点分段的骨架（供会话侧按 authorized_node_ids 裁剪）
                node_segments: dict[str, dict] = {}
                for n in nodes:
                    try:
                        progress = float(n.progress or 0)
                    except (TypeError, ValueError):
                        progress = 0.0
                    node_segments[n.node_id] = {
                        "name": n.node_name or "",
                        "status": n.status or "",
                        "progress": round(progress, 1),
                        "milestone": (getattr(n, "node_type", "") == "MILESTONE"),
                    }

                return {
                    "total_nodes": total,
                    "completed": completed,
                    "in_progress": in_progress,
                    "conditions_not_met": conditions_not_met,
                    "not_activated": not_activated,
                    "overdue": overdue,
                    "overall_progress": f"{total_progress:.1f}%",
                    "milestones": milestones[:10],
                    "node_segments": node_segments,          # ★新增
                }
```

> 列表规模保护：`nodes` 已由 `is_discarded == False` 过滤，且 `ProjectNode` 有 `idx_nodes_project` 索引。若单项目节点数 > 2000，`node_segments` 会显著增大 `content_json`——本设计**不做截断**（截断会破坏权限裁剪的正确性），转为在 DD-风险清单登记（R3）。

**B-2. `_format_content_text()` 兼容旧数据**（第 3 层结构段，`lines.append(f"🏁 {ms_str}")` 之后插入）：

```python
            # M1a: 有 node_segments 时补一行"可见节点概览"占位（实际裁剪在会话侧）
            # 这里只输出不带权限过滤的全景骨架前 3 个，保持 content_text 语义不变
            segs = s.get("node_segments") or {}
            if segs:
                head = list(segs.values())[:3]
                seg_str = " / ".join(
                    f"{seg.get('name', '')}[{_STATUS_CN.get(seg.get('status', ''), seg.get('status', ''))}]"
                    for seg in head
                )
                if seg_str:
                    lines.append(f"🔖 节点骨架：{seg_str}")
```

同文件顶部新增常量（与 fetcher 保持一致的中文状态映射）：

```python
_STATUS_CN = {
    "COMPLETED": "已完成",
    "IN_PROGRESS": "进行中",
    "CONDITIONS_NOT_MET": "条件未满足",
    "NOT_ACTIVATED": "未激活",
}
```

> **注意**：`_format_content_text` 只在有 `node_segments` 时多输出一行，旧记录（无该键）输出与改造前**逐字节一致**，`token_count` 估算随之变化仅限新数据。

### C. 模块自检

```bash
# A1：Builder 产出含 node_segments（重跑存量后查询）
uv run python scripts/migrate_world_book_segments.py --all
docker exec emily-postgres psql -U emily -d emily -c \
  "SELECT project_id, jsonb_typeof((content_json::json->'structure'->'node_segments')) FROM project_world_books LIMIT 3;"
→ 预期：object

# A2：裁剪按权限生效（授权集为空 → 不含节点名）+ 字数上限
docker exec emily-core python -c "
import json
from emily_core.session.fetchers.fetch_world_book import render_brief
raw=json.dumps({'ontology':{'name':'测试项目'},'structure':{'total_nodes':2,'completed':1,
 'node_segments':{'SG-001':{'name':'基础工程','status':'IN_PROGRESS'},'SG-002':{'name':'主体结构','status':'NOT_ACTIVATED'}}}})
b1=render_brief(raw,['SG-001']); b2=render_brief(raw,[])
print('AUTH:',b1); print('NONE:',b2); print('LEN_OK:',len(b1)<=200); print('LEAK:',('主体结构' in b1) or ('基础工程' in b2))
"
→ 预期：AUTH 含"基础工程"；NONE 含"我可见节点（0）：无"；LEN_OK: True；LEAK: False

# A3：旧数据降级（无 node_segments 不报错、仍出统计行）
docker exec emily-core python -c "
import json
from emily_core.session.fetchers.fetch_world_book import render_brief
raw=json.dumps({'structure':{'total_nodes':2,'completed':1,'overall_progress':'50.0%'}})
print('DEGRADED:',render_brief(raw,['SG-001']))
"
→ 预期：DEGRADED: 📊 2节点：1完成 / 0进行中 / 0逾期 ｜ 50.0%（无异常）
```

**失败处理**：A1 为 null → 检查 `_build_structure` 的 `return` 是否真的带上 `node_segments`（缩进易错）；A2 `LEAK: True` → 检查 `render_brief` 的 `if nid in auth` 判定是否被写成"取前 N 个"。

---

## DD-M1b：规则书裁剪层

### A. 修改 `emily-data/rules/规则书.md`（加 frontmatter）

```markdown
---
meta:
  version: 2
  last_updated: "2026-09-10"
---

# Emily 规则书

> 项目管理者可随时编辑本文件，Emily 重启或热重载后生效。

## 一、身份与边界
---
applicable_levels: [1, 2, 3, 4, 5, 6]
section_type: identity_boundary
---
1. Emily 是项目团队的公共 AI 大脑，不是任何个人的私人助手
2. Emily 可以建议、提醒、记录，但不能代替人做决策
3. 超出 Emily 当前能力范围时，如实告知并建议替代方案

## 二、主动行为规则
---
applicable_levels: [1, 2, 3, 4, 5, 6]
section_type: proactive_behavior
---
1. 只在用户触发（IM 消息）或调度器触发（晨报/预警）时响应，不主动打扰用户
2. 项目未达到 T2（有组织级）时，只回答基本查询，不执行录入操作，并提示先完成初始化

## 三、信息处理原则
---
applicable_levels: [1, 2, 3, 4, 5, 6]
section_type: info_policy
---
1. 只记录用户明确告知的内容，不推断、不添加用户未说的细节
2. 查询结果优先展示原始数据，不编造、不填充不存在的信息
3. 不确定的信息明确标注"我不确定"，不猜测

## 四、权限与安全
---
applicable_levels: [1, 2, 3, 4, 5, 6]
section_type: permission_security
---
1. 严格按用户权限等级处理请求，低权限请求高权限操作时拒绝并说明原因
2. 删除操作必须经用户二次确认，不自动执行

## 五、初始化规则
---
applicable_levels: [1, 2, 3, 4, 5, 6]
section_type: initialization
---
项目初始化未达 T2 时阻断录入操作。用户可随时询问初始化进度，管理员可跳过或自定义必备项。

## 六、错误处理
---
applicable_levels: [1, 2, 3, 4, 5, 6]
section_type: error_handling
---
1. 工具执行失败时，告知用户原因并建议替代方案
2. 外部服务不可用时降级继续服务，不阻塞用户

## 七、文件处理原则
---
applicable_levels: [1, 2, 3, 4, 5, 6]
section_type: file_policy
llm_inject: false
---
1. 文件按业务意图（purpose）分 5 类：EVIDENCE（凭证）/ RECORD（记录）/ DESIGN（图纸）/ REFERENCE（参考）/ CHAT（闲聊，不入库）
2. purpose 判断依据：文件名 + 用户对话 + 上下文，不读文件内容
3. 文件关联三维度正交：版本链（parent_file_id）/ 附件链（attachment_of）/ 业务关联（source_module_id）
4. 附件链禁止嵌套：附件只能挂到主文件，不能挂到另一个附件
5. 一套文件有且仅有一个攒总主文件（attachment_of=NULL），其余为附件
6. RAG 入库策略：仅 REFERENCE 自动入通用参考库，跨项目可检索；其余不入库
7. CHAT 类（梗图/gif/表情包）不入 files 表，只留 IM URL，过期即失
8. purpose 原则载体：record_file 工具 description（LLM 按需可见）；本章节为人读权威，不注入 LLM
```

> **`llm_inject: false` 的落地**：落实原文第 8 条"本章节为人读权威，不注入 LLM"（原实现无过滤，全文注入；本次改为显式声明并生效）。
> **`applicable_levels` 现状**：7 个章节当前均全级可见（不改变现有可见性语义，仅建立可裁剪机制）。后续运营可按需收紧（如"四、权限与安全"仅 L3+）。

### B. 修改 `emily-core/emily_core/services/rule_book_loader.py`

**B-1. 顶部新增 import 与常量/纯函数**（在 `logger = ...` 之后插入）：

```python
import re

# 章节未标注 applicable_levels 时的默认可见级别（fail-open：避免改版后静默失效）
_SECTION_DEFAULT_LEVELS: list[int] = [1, 2, 3, 4, 5, 6]

_RE_SECTION = re.compile(r"^##\s+(.+?)\s*$", re.MULTILINE)
_RE_FRONTMATTER = re.compile(r"^---\s*\n(.*?)\n---\s*\n", re.DOTALL)


def _safe_yaml(text: str) -> dict:
    """解析章节 frontmatter；失败返回空 dict（fail-open）。"""
    try:
        import yaml
        data = yaml.safe_load(text) or {}
        return data if isinstance(data, dict) else {}
    except Exception as e:
        logger.warning("rule book frontmatter parse failed: %s", e)
        return {}


def parse_sections(raw: str) -> list[dict]:
    """按 '## 章节' 切分规则书并解析章节级 frontmatter。

    Returns:
        [{title: str, levels: list[int], section_type: str,
          llm_inject: bool, body: str}]
    """
    if not raw:
        return []
    matches = list(_RE_SECTION.finditer(raw))
    sections: list[dict] = []
    for idx, m in enumerate(matches):
        start = m.end()
        end = matches[idx + 1].start() if idx + 1 < len(matches) else len(raw)
        chunk = raw[start:end]
        stripped = chunk.lstrip("\n")
        fm = _RE_FRONTMATTER.match(stripped)
        if fm:
            meta = _safe_yaml(fm.group(1))
            body = stripped[fm.end():].strip()
        else:
            meta = {}
            body = chunk.strip()
        levels = meta.get("applicable_levels")
        if isinstance(levels, list) and levels:
            parsed: list[int] = []
            for x in levels:
                try:
                    parsed.append(int(x))
                except (TypeError, ValueError):
                    continue
            levels = parsed or list(_SECTION_DEFAULT_LEVELS)
        else:
            levels = list(_SECTION_DEFAULT_LEVELS)
        sections.append({
            "title": m.group(1).strip(),
            "levels": levels,
            "section_type": str(meta.get("section_type", "") or ""),
            "llm_inject": bool(meta.get("llm_inject", True)),
            "body": body,
        })
    return sections


def _truncate(text: str, max_chars: int) -> str:
    if len(text) <= max_chars:
        return text
    cut = text[:max_chars]
    nl = cut.rfind("\n")
    return cut[:nl] if nl > 0 else cut


def render_full(sections: list[dict], level: int) -> str:
    """按 level 过滤后输出规则全文（供 meta_cognition_read）。"""
    parts: list[str] = []
    for s in sections:
        if not s.get("llm_inject", True):
            continue
        if level not in (s.get("levels") or []):
            continue
        parts.append(f"## {s['title']}\n{s.get('body', '')}")
    return "\n\n".join(parts)


def render_brief(sections: list[dict], level: int, max_chars: int = 200) -> str:
    """按 level 过滤后生成常驻摘要（≤max_chars）。"""
    picked = [
        s for s in sections
        if s.get("llm_inject", True) and level in (s.get("levels") or [])
    ]
    if not picked:
        return ""
    text = "适用规则（{}）：{}".format(len(picked), " / ".join(s["title"] for s in picked))
    bullets: list[str] = []
    for s in picked[:4]:
        body_lines = (s.get("body") or "").splitlines()
        first = body_lines[0].strip() if body_lines else ""
        if first:
            bullets.append(f"{s['title']}：{first[:40]}")
    if bullets:
        text += "\n" + "；".join(bullets)
    return _truncate(text, max_chars)
```

**B-2. `RuleBookLoader` 新增方法**（在 `reload()` 之后插入，`content` property 之前）：

```python
    def trim_for_level(self, level: int, max_chars: int = 200) -> str:
        """按权限等级裁剪生成摘要（便捷入口）。"""
        return render_brief(parse_sections(self.content), level, max_chars)

    def render_for_level(self, level: int) -> str:
        """按权限等级裁剪生成全文。"""
        return render_full(parse_sections(self.content), level)
```

**B-3. `content` property / `load()` / `reload()` 完全不动**（硬约束 1）。

> **热重载一致性**：`RuleBookLoader.reload()` 只重载文本；`SessionContext.refresh()` 会重新调用 `_sub_fetch_rule_book_sections()` 重新解析，故 `rule_book_sections` 随 `reload` 自然更新。

### C. 模块自检

```bash
# B1：frontmatter 完整（章节数 == 标签数）
docker exec emily-core sh -c 'echo "SEC=$(grep -c "^## " /app/rules/规则书.md)"; echo "FM=$(grep -c "^applicable_levels:" /app/rules/规则书.md)"'
→ 预期：SEC=7 FM=7

# B2：按 level 裁剪 + 摘要非空且 ≤200 字
docker exec emily-core python -c "
from emily_core.services.rule_book_loader import RuleBookLoader, parse_sections, render_brief
secs = parse_sections(RuleBookLoader().content)
print('SECTIONS:', len(secs), '| TITLES:', [s['title'] for s in secs])
b1 = render_brief(secs, 1); print('L1_LEN:', len(b1)); print(b1)
"
→ 预期：SECTIONS: 7；TITLES 含"一、身份与边界"…"七、文件处理原则"；L1_LEN ≤ 200

# B3：llm_inject=false 不进摘要 / 不进全文
docker exec emily-core python -c "
from emily_core.services.rule_book_loader import render_brief, render_full
secs=[{'title':'A','levels':[1],'llm_inject':True,'body':'可见内容'},
      {'title':'B','levels':[1],'llm_inject':False,'body':'不可见内容'}]
print('BRIEF:', render_brief(secs,1)); print('FULL_HAS_B:', '不可见内容' in render_full(secs,1))
"
→ 预期：BRIEF 含"可见内容"不含"不可见内容"；FULL_HAS_B: False

# B4：既有 fetch 行为不回归（{rule_book} 全文语义不变）
docker exec emily-core python -c "
from emily_core.services.rule_book_loader import RuleBookLoader
c=RuleBookLoader().content; print('HAS_CH7:', '七、文件处理原则' in c, '| CHARS:', len(c))
"
→ 预期：HAS_CH7: True（content 仍是全文，承载第七章）
```

**失败处理**：B1 `SEC != FM` → 逐个补齐 frontmatter；B3 含"不可见内容" → 检查 `llm_inject` 判定是否写成 `!= False`；B4 `HAS_CH7: False` → 说明误把 `content` 改成裁剪版，回退（违反硬约束 1）。

---

## DD-M1c：认知书摘要层

### A. 修改 `emily-core/emily_core/session/fetchers/fetch_system_description.py`

**A-1. 新增 `_truncate` / `render_full` / `render_brief`**（插在 `fetch()` 之后、`_format_filtered_text()` 之前）：

```python
def _truncate(text: str, max_chars: int) -> str:
    if len(text) <= max_chars:
        return text
    cut = text[:max_chars]
    nl = cut.rfind("\n")
    return cut[:nl] if nl > 0 else cut


def render_full(content_json: str, perms: dict) -> str:
    """按 db_perms 裁剪生成认知书全文（等价于改造前的 fetch 输出）。

    无 IO：content_json 由调用方提供（Session 缓存 / tool 层读取）。
    """
    try:
        content = json.loads(content_json or "{}")
    except (json.JSONDecodeError, TypeError, ValueError) as e:
        logger.warning("system description content_json parse failed: %s", e)
        return ""
    if not isinstance(content, dict) or not content:
        return ""
    return _format_filtered_text(content, perms)


def render_brief(content_json: str, perms: dict, max_chars: int = 200) -> str:
    """生成常驻摘要（≤max_chars）。

    只含"公共知识"部分：
      - D3 权限分级体系（参建线 / 建设线 / 你的位置与继承链）
      - D2 文件分类体系（分类名列表）
    不含 D1 数据表清单（属执行层资源，按 PRD §3.2 移出路由 prompt）。
    """
    try:
        content = json.loads(content_json or "{}")
    except (json.JSONDecodeError, TypeError, ValueError):
        return ""
    if not isinstance(content, dict) or not content:
        return ""
    lines: list[str] = []

    perm = content.get("permission") or {}
    levels = perm.get("levels") or []
    if levels:
        for line_name in ("参建线", "建设线"):
            ls = [l for l in levels if l.get("line") == line_name]
            if not ls:
                continue
            chain = " → ".join(f"L{l.get('level')} {l.get('name', '')}" for l in ls)
            lines.append(f"🔐 {line_name}: L1 访客 → {chain}")
        try:
            user_level = int(perms.get("level", 1) or 1)
            from ...permission.level import LEVEL_NAME, effective_levels
            level_name = LEVEL_NAME.get(user_level, "未知")
            eff = effective_levels(user_level)
            chain_str = "→".join(f"L{x}" for x in sorted(eff, reverse=True))
            lines.append(f"你的位置：{level_name}(L{user_level})，继承链 {chain_str}")
        except Exception as e:
            logger.debug("render_brief: user position skipped: %s", e)

    cats = (content.get("file") or {}).get("categories") or []
    if cats:
        lines.append("📁 文件分类：" + " / ".join(str(c.get("name", "")) for c in cats))

    return _truncate("\n".join(lines), max_chars)
```

**A-2. `fetch()` 改为复用 `render_full`**（保持输出逐字节等价）：

```python
def fetch(perms: dict) -> str:
    """获取认知书纯文本摘要（按用户权限裁剪）。

    行为不变：读 DB 最新记录 → 按 db_perms 裁剪 → 返回文本。
    实现改为委托 render_full（单一实现源）。
    """
    try:
        from ...repositories.system_description_repo import SystemDescriptionRepo
        desc = SystemDescriptionRepo.get_latest()
        if desc is None:
            return ""
        if not desc.content_json:
            return desc.content_text or ""
        return render_full(desc.content_json, perms)
    except Exception as e:
        logger.error("fetch_system_description failed: %s", e)
        return ""
```

**A-3. `_format_filtered_text()` 完全不动。**

### B. 模块自检

```bash
# C1：摘要 ≤200 字、含权限分级与文件分类、不含数据表清单
docker exec emily-core python -c "
import json
from emily_core.repositories.system_description_repo import SystemDescriptionRepo
from emily_core.session.fetchers.fetch_system_description import render_brief
d = SystemDescriptionRepo.get_latest()
b = render_brief(d.content_json, {'level': 3, 'db_perms': {'events':'read'}})
print('LEN:', len(b)); print(b)
print('HAS_TABLE_ROW:', any(x in b for x in ('表:','只读','读写')))
"
→ 预期：LEN ≤ 200；含"参建线"/"建设线"/"文件分类"；HAS_TABLE_ROW: False

# C2：权限裁剪（无权限表 → 不泄漏表名）
docker exec emily-core python -c "
from emily_core.session.fetchers.fetch_system_description import render_brief
b = render_brief('{\"database\":{\"visible_tables\":[{\"table_name\":\"financial\",\"display_name\":\"财务表\"}]},\"file\":{},\"permission\":{}}', {'level':1,'db_perms':{}})
print('FINANCIAL_LEAK:', '财务表' in b, '| BRIEF:', repr(b))
"
→ 预期：FINANCIAL_LEAK: False

# C3：fetch 行为不回归（与改造前输出一致）
docker exec emily-core python -c "
from emily_core.repositories.system_description_repo import SystemDescriptionRepo
from emily_core.session.fetchers.fetch_system_description import fetch, render_full
from emily_core.services.permission_service import PermissionService
import sys
uid = sys.argv[1]
perms = PermissionService().build_permission_dict(uid)
d = SystemDescriptionRepo.get_latest()
print('EQ:', fetch(perms) == render_full(d.content_json, perms))
" <真实用户UUID>
→ 预期：EQ: True
```

**失败处理**：C1 `HAS_TABLE_ROW: True` → `render_brief` 误调了 `render_full`；C3 `EQ: False` → 检查 `fetch` 是否漏掉 `not desc.content_json` 的降级分支。

---

## DD-M0：使命与边界（session.md 静态段）

### A. 修改 `emily-data/prompts/session.md`

**A-1. 头部注释补记新变量**（第 3-4 行注释）：

```markdown
<!-- 模板变量（阶段1 直接 replace）: {sop_catalog} -->
<!-- 模板变量（阶段2 Session 级，空值替换为"（无）"）: {user_name} {user_company} {user_company_type} {user_department} {user_position} {user_permission_level} {current_node_ids} {project_name} {project_type} {project_status} {user_memory} {rag_info} {project_brief} {rule_brief} {system_brief} -->
```

**A-2. 新增 §〇 使命与边界**（插在文件最顶部，第 7 行之后、原 §〇 之前）：

```markdown
## 〇、使命与边界

Emily 是企业工程项目管理团队的信息中枢，使命是让团队协作有据可查、流程规范受控、知识持续沉淀。
Emily 不替代人做组织决策，不归属任何个人，是团队的公共大脑。
本次调用中，你以 Emily 的路由视角理解用户诉求并输出路由结果。
```

> 该段**无占位符**，字节固定，随 base 缓存（不破坏 M4 前缀缓存）。

### B. 章节重排（同步 DD-M1 一并执行）

| 原编号 | 新编号 | 章节 |
|--------|--------|------|
| 〇 本次调用说明（先读） | **一** | 本次调用说明（先读） |
| 一 角色与定位 | **二** | 角色与定位 |
| 二 行为规范 | **三** | 行为规范 |
| 三 当前会话上下文 | **四** | 当前会话上下文 |
| 四 能力树 | **五** | 能力与资源 |

**理由**：与需求文档 §4「装配后的 Session Prompt 结构」保持一致（〇=使命、五=能力与资源）。章节号仅为 prompt 文本，无代码引用，改动零风险。

### C. 模块自检

```bash
# D1：使命卡出现在渲染后 prompt 的最前部
docker cp scripts/dump_session_prompt_live.py emily-core:/tmp/
docker exec -w /app emily-core python /tmp/dump_session_prompt_live.py --user-id <真实用户UUID> --out-dir /tmp/out
docker exec emily-core sh -c "grep -n '公共大脑' /tmp/out/*.txt /tmp/out/*.json 2>/dev/null | head -3"
→ 预期：命中 1 行，且行号位于 prompt 前 400 字符内

# D2：使命段无占位符（静态）
uv run python -c "
import re,io
from pathlib import Path
txt=Path('emily-data/prompts/session.md').read_text(encoding='utf-8')
sec=txt.split('## 〇、使命与边界')[1].split('## ')[0]
print('MISSION_VARS:', re.findall(r'\{[a-z_]+\}', sec))
"
→ 预期：MISSION_VARS: []
```

**失败处理**：D1 未命中 → 检查容器内 `/app/prompts/session.md` 是否为最新（bind-mount 同步）；D2 出现变量 → 使命段误插了占位符。

---

## DD-M1：摘要注入装配层

### A. 修改 `emily-core/emily_core/session/session_context.py`

**A-1. 新增数据类字段**（在现有 `system_description: str = ""` 之后）：

```python
    # ── 三书裁剪源数据（M1：供按操作者权限实时裁剪，避免每条消息 IO）──
    world_book_json: str = ""              # 世界书 content_json 原文
    system_description_json: str = ""      # 认知书 content_json 原文
    rule_book_sections: list[dict] = field(default_factory=list)   # 规则书解析后章节
```

**A-2. `create()` 灌注**（在 `ctx.system_description = ...` 之后）：

```python
        # 三书裁剪源数据（M1）
        ctx.world_book_json = snapshot.get("world_book_json", "")
        ctx.system_description_json = snapshot.get("system_description_json", "")
        ctx.rule_book_sections = list(snapshot.get("rule_book_sections", []))
```

**A-3. `refresh()` 热更新**（`_hot_fields` 字典末尾追加 3 项）：

```python
            "world_book_json": snapshot.get("world_book_json"),
            "system_description_json": snapshot.get("system_description_json"),
            "rule_book_sections": snapshot.get("rule_book_sections"),
```

**A-4. `get_prompt_variables()` 新增 3 键**

在函数体内补充 `db_perms` 的 actor 解析（紧邻现有 `authorized_node_ids` 解析之后）：

```python
        db_perms = a.get("db_perms")
        if db_perms is None:
            db_perms = self.db_perms
```

在 `return { ... }` 字典末尾追加 3 项：

```python
            "{rag_info}": self._format_rag_summary(),
            # M1: 三书常驻摘要（按当前操作者权限实时裁剪，阶段2渲染）
            "{project_brief}": _brief_world(self.world_book_json, authorized_node_ids),
            "{rule_brief}": _brief_rule(self.rule_book_sections, level),
            "{system_brief}": _brief_system(self.system_description_json,
                                            {"level": level, "db_perms": db_perms}),
```

同时更新 docstring（P1-1 那段之后追加）：

```python
        M1: 新增 3 个三书摘要映射（project_brief/rule_brief/system_brief）。
        摘要是权限相关变量（节点/等级/表权限），故走阶段2渲染路径：
        SessionAgent._PERM_PROMPT_KEYS 收录这 3 键，base 中保留占位符不替换。
```

**A-5. 模块级裁剪辅助函数**（放在文件末尾 `format_message_history = ...` 别名之前）：

```python
# ══════════════════════════════════════════════════════════════════════════════
# M1: 三书摘要裁剪辅助（fail-open：异常返回空串，不阻断 prompt 装配）
# ══════════════════════════════════════════════════════════════════════════════

def _brief_world(content_json: str, authorized_node_ids: list[str]) -> str:
    if not content_json:
        return ""
    try:
        from .fetchers.fetch_world_book import render_brief
        return render_brief(content_json, list(authorized_node_ids or []))
    except Exception as e:
        logger.warning("project_brief render failed: %s", e)
        return ""


def _brief_rule(sections: list[dict], level: int) -> str:
    if not sections:
        return ""
    try:
        from ..services.rule_book_loader import render_brief
        return render_brief(sections, int(level or 1))
    except Exception as e:
        logger.warning("rule_brief render failed: %s", e)
        return ""


def _brief_system(content_json: str, perms: dict) -> str:
    if not content_json:
        return ""
    try:
        from .fetchers.fetch_system_description import render_brief
        return render_brief(content_json, perms)
    except Exception as e:
        logger.warning("system_brief render failed: %s", e)
        return ""
```

### B. 修改 `emily-core/emily_core/session/session_data_fetcher.py`

**B-1. 新增 3 个子采集函数**（插在现有 `_sub_fetch_rule_book()` 之后）：

```python
def _sub_fetch_world_book_json(project_id: Optional[str]) -> str:
    """获取世界书 content_json 原文（供按操作者权限裁剪）。

    与 _sub_fetch_world_book() 各查一次单行索引记录；两次查询均为
    project_id 唯一索引命中，成本可忽略（如需省一次查询可后续合并读取）。
    """
    if not project_id:
        return ""
    try:
        from ..repositories.world_book_repo import ProjectWorldBookRepo
        wb = ProjectWorldBookRepo.get_by_project(project_id)
        return (wb.content_json or "") if wb is not None else ""
    except Exception as e:
        logger.error("_sub_fetch_world_book_json failed project=%s: %s", project_id, e)
        return ""


def _sub_fetch_system_description_json() -> str:
    """获取认知书 content_json 原文（供按操作者权限裁剪）。"""
    try:
        from ..repositories.system_description_repo import SystemDescriptionRepo
        desc = SystemDescriptionRepo.get_latest()
        return (desc.content_json or "") if desc is not None else ""
    except Exception as e:
        logger.error("_sub_fetch_system_description_json failed: %s", e)
        return ""


def _sub_fetch_rule_book_sections() -> list[dict]:
    """获取规则书解析后章节（供按操作者权限裁剪）。"""
    try:
        from ..services.rule_book_loader import RuleBookLoader, parse_sections
        return parse_sections(RuleBookLoader().content)
    except Exception as e:
        logger.error("_sub_fetch_rule_book_sections failed: %s", e)
        return []
```

**B-2. `fetch()` 的 `session_snapshot` 追加 3 键**（在 `"system_description": system_description,` 之后）：

```python
            # M1: 三书裁剪源数据（供 get_prompt_variables 按操作者权限实时裁剪）
            "world_book_json": _sub_fetch_world_book_json(project_id),
            "system_description_json": _sub_fetch_system_description_json(),
            "rule_book_sections": _sub_fetch_rule_book_sections(),
```

**B-3. `_empty_result()` 同步补齐 3 键**（在 `"system_description": "",` 之后）：

```python
            "world_book_json": "",
            "system_description_json": "",
            "rule_book_sections": [],
```

### C. 修改 `emily-core/emily_core/session/session_agent.py`（M1 部分）

**C-1. `_PERM_PROMPT_KEYS` 扩展**（第 178-182 行）：

```python
    # 权限变量集合：随当前操作者变化，不进 base 缓存，由 _build_rendered_system_prompt 每条消息渲染
    # M1: 追加 3 个三书摘要键（摘要按 actor 权限裁剪，属权限相关变量）
    _PERM_PROMPT_KEYS = frozenset({
        "{user_company}", "{user_company_type}", "{user_department}",
        "{user_level}", "{user_permission_level}", "{current_node_ids}",
        "{available_skills}",
        "{project_brief}", "{rule_brief}", "{system_brief}",
    })
```

> **base 稳定性证明**：`_build_session_prompt_base()` 遍历 `get_prompt_variables()` 时 `if key in self._PERM_PROMPT_KEYS: continue`，故这 3 个占位符**原样保留在 base**；`_build_rendered_system_prompt()` 每条消息替换为 actor 权限裁剪值。同 Session 同 actor 的渲染结果字节一致 → M4 缓存命中。

### D. 修改 `emily-data/prompts/session.md`（3 个占位符融入既有段落）

**D-1. §三 行为规范 → 新增两个子节**（插在 `### 路由规则` 之后、`### 输出要求` 之前）：

```markdown
### 信息处理原则（约束与边界）
{rule_brief}

### 文件处理原则（分类与关联）
文件按业务意图分类管理，并遵循版本链、附件链与业务关联三个维度；文件处理的具体判定由执行阶段按文件规则完成。
```

**D-2. §四 当前会话上下文 → 项目上下文段补摘要 + 框架说明**（替换原 `- 状态：{project_status}` 之后追加）：

```markdown
### 项目上下文
- 名称：{project_name}
- 类型：{project_type}
- 状态：{project_status}

{project_brief}

项目工作以"全景节点"树组织：里程碑（关键节点/阶段性成果）、工作包（可分解的工作分组）、任务（最小可执行单元）三级；节点按三态流转（条件未满足 → 进行中 → 已完成），用户通过加入节点参与协作。
```

**D-3. §五 能力与资源 → 新增两个子节**（插在 `### 业务流程目录（按类型树路由）` 之后、`### 知识库` 之前）：

```markdown
### 权限分级体系
{system_brief}

### 文件分类体系（文件管理框架）
项目文件按业务意图分类，支持版本链（同一文件的多版本）、附件链（附件挂载到主文件）、业务关联（关联到事件/任务/节点）三维度管理。
```

> **占位符仅出现一次**：`_build_rendered_system_prompt` 用 `str.replace(key, value)` 替换**全部出现**，故 `{rule_brief}` / `{system_brief}` 各只写 1 处，避免同一摘要被重复注入。`rule_brief` 的渲染结果本身已含"信息处理/文件处理"等多个适用章节的要点。

### E. 模块自检

```bash
# E1：3 个占位符无残留（渲染后）
docker exec -w /app emily-core python /tmp/dump_session_prompt_live.py --user-id <L1用户UUID> --out-dir /tmp/out_l1
docker exec emily-core sh -c "grep -o '{project_brief}\|{rule_brief}\|{system_brief}' /tmp/out_l1/* | wc -l"
→ 预期：0

# E2：摘要已注入（3 段非"（无）"）
docker exec emily-core sh -c "grep -c '我可见节点\|适用规则\|参建线' /tmp/out_l1/*"
→ 预期：≥1（有数据的项目/权限下 3 类摘要各出现）

# E3：base 字节稳定 + 占位符保留在 base（M4 缓存前提）
docker exec emily-core python -c "
import os
from emily_core import bootstrap
from emily_core.session.session_context import SessionContext
from emily_core.session.session_agent import SessionAgent
core=bootstrap.init(); core._init_permission_module(); core._init_skill_module()
ctx=SessionContext.create('<真实用户UUID>','conv-x','',core)
a=SessionAgent('conv-x',ctx,llm_client=core._llm_client,skill_registry=core._skill_registry)
b1=a._build_session_prompt_base(); b2=a._build_session_prompt_base()
print('BASE_STABLE:', b1==b2)
print('PLACEHOLDER_KEPT:', '{project_brief}' in b1 and '{rule_brief}' in b1 and '{system_brief}' in b1)
r=a._build_rendered_system_prompt(None)
print('RENDER_NO_LEFT:', not any(k in r for k in ('{project_brief}','{rule_brief}','{system_brief}')))
"
→ 预期：BASE_STABLE: True / PLACEHOLDER_KEPT: True / RENDER_NO_LEFT: True

# E4：权限裁剪对比（L6 摘要不短于 L1）
docker exec -w /app emily-core python /tmp/dump_session_prompt_live.py --user-id <L6用户UUID> --out-dir /tmp/out_l6
docker exec emily-core python -c "
import json,glob
f=lambda p: json.load(open(glob.glob(p)[0]))
a=f('/tmp/out_l1/*.json'); b=f('/tmp/out_l6/*.json')
print('L1_CHARS:', a.get('rendered_chars'), 'L6_CHARS:', b.get('rendered_chars'))
"
→ 预期：L6_CHARS ≥ L1_CHARS（权限更高摘要覆盖更多规则/节点）
```

**失败处理**：E1 非 0 → 占位符未进 `_PERM_PROMPT_KEYS` 或 `get_prompt_variables` 未返回该键；E3 `PLACEHOLDER_KEPT: False` → 摘要被 base 吞掉（M4 缓存将失效，必须修复）；E3 `RENDER_NO_LEFT: False` → `_build_rendered_system_prompt` 未覆盖新键。

---

## DD-M2：meta_cognition_read 工具

### A. 新增 `emily-core/emily_core/tools/meta_cognition_tool.py`

```python
"""meta_cognition_read —— 三书全文按需检索（按操作者权限裁剪）。

常驻摘要（{project_brief}/{rule_brief}/{system_brief}）不够用时，Agent 用本工具
拉取裁剪后的三书全文。归 base 类（所有 SOP 可用）+ FallbackPolicy basic 白名单
（兜底档可见，只读零写）。

参照模式：emily_core/tools/knowledge_search_tool.py（schema + handler + 签名注入 user_id）
"""

from __future__ import annotations

import asyncio
import logging

logger = logging.getLogger("emily.tool.meta_cognition")

_VALID_BOOKS = ("world", "rule", "system")

_EMPTY_REPLY = {
    "world": "当前没有可访问的项目态势信息（无项目世界书或你没有授权节点）。",
    "rule": "规则书暂不可用，请稍后重试或联系管理员。",
    "system": "系统能力描述暂不可用，请稍后重试或联系管理员。",
}

_META_COG_SCHEMA = {
    "type": "object",
    "properties": {
        "book": {
            "type": "string",
            "enum": ["world", "rule", "system"],
            "description": (
                "要检索的书：world=项目态势全文（项目/节点/事件/阻塞）；"
                "rule=组织规则全文（按你的权限等级裁剪）；"
                "system=系统能力全文（你可访问的数据表/文件分类/权限体系）"
            ),
        },
        "query": {
            "type": "string",
            "description": "可选关键词，用于在裁剪后的全文中做关键词过滤；不传则返回全文",
        },
    },
    "required": ["book"],
}

_META_COG_DESCRIPTION = (
    "按需检索「项目态势 / 组织规则 / 系统能力」三书的裁剪后全文。"
    "当常驻摘要信息不足、需要更详细的项目节点/规则条文/可访问数据表时调用。"
    "参数 book 必选（world/rule/system）；参数 query 可选，用于关键词过滤。"
    "返回内容已按当前调用者权限裁剪，不会越权。"
)


async def handle_meta_cognition_read(params: dict, user_id: str | None = None, core=None) -> dict:
    """处理三书全文检索。

    Args:
        params: {"book": "world|rule|system", "query": "可选关键词"}
        user_id: 当前操作者 UUID（框架按 handler 签名自动注入）
        core: EmilyCore 实例（registry 注册时 partial 注入）

    Returns:
        {"success": bool, "book": str, "matched_lines": int, "reply": str}
    """
    book = str((params or {}).get("book", "") or "").strip().lower()
    query = str((params or {}).get("query", "") or "").strip()

    if book not in _VALID_BOOKS:
        return {
            "success": False, "book": book, "matched_lines": 0,
            "reply": "参数 book 必须是 world / rule / system 之一。",
        }

    # 权限快照（fail-closed：取不到 → 空权限 → 最小可见集）
    perms: dict = {}
    if user_id:
        try:
            from ..session.session_data_fetcher import SessionDataFetcher
            perms = await asyncio.to_thread(
                SessionDataFetcher.fetch_actor_snapshot, user_id, core,
            ) or {}
        except Exception as e:
            logger.warning("meta_cognition_read perms fetch failed user=%s: %s", user_id, e)
            perms = {}

    # 读取 + 裁剪（sync IO 包 to_thread）
    try:
        text = await asyncio.to_thread(_render_book, book, perms, user_id)
    except Exception as e:
        logger.error("meta_cognition_read render failed book=%s: %s", book, e, exc_info=True)
        return {"success": False, "book": book, "matched_lines": 0, "reply": "读取失败，请稍后重试。"}

    if not text:
        return {"success": False, "book": book, "matched_lines": 0, "reply": _EMPTY_REPLY[book]}

    filtered, matched = _apply_query(text, query)
    if query and matched == 0:
        return {
            "success": True, "book": book, "matched_lines": 0,
            "reply": f"在{book}全文中未找到与「{query}」相关的内容。",
        }
    return {"success": True, "book": book, "matched_lines": matched, "reply": filtered}


def _render_book(book: str, perms: dict, user_id: str | None) -> str:
    """读取并裁剪指定书的全文（sync，纯 IO + 纯函数）。"""
    level = int(perms.get("level", 1) or 1)

    if book == "rule":
        from ..services.rule_book_loader import RuleBookLoader, parse_sections, render_full
        return render_full(parse_sections(RuleBookLoader().content), level)

    if book == "system":
        from ..repositories.system_description_repo import SystemDescriptionRepo
        from ..session.fetchers.fetch_system_description import render_full
        desc = SystemDescriptionRepo.get_latest()
        if desc is None:
            return ""
        return render_full(desc.content_json, perms)

    # book == "world"
    project_id = _resolve_project_id(user_id)
    if not project_id:
        return ""
    from ..repositories.world_book_repo import ProjectWorldBookRepo
    from ..session.fetchers.fetch_world_book import render_full
    wb = ProjectWorldBookRepo.get_by_project(project_id)
    if wb is None:
        return ""
    node_ids = list(perms.get("authorized_node_ids") or [])
    return render_full(wb.content_json, node_ids)


def _resolve_project_id(user_id: str | None) -> str:
    """解析用户所属项目 ID。"""
    if not user_id:
        return ""
    try:
        from ..repositories.user_repo import UserRepository
        user = UserRepository.get_by_id(user_id)
        return (getattr(user, "project_id", "") or "") if user is not None else ""
    except Exception as e:
        logger.warning("meta_cognition_read project resolve failed user=%s: %s", user_id, e)
        return ""
```

**A-1. 关键词过滤辅助**（同文件末尾）：

```python
def _apply_query(text: str, query: str) -> tuple[str, int]:
    """关键词过滤（大小写不敏感，按行保留命中行）。query 为空则返回全文。"""
    if not query:
        return text, len([ln for ln in text.splitlines() if ln.strip()])
    q = query.lower()
    hits = [ln for ln in text.splitlines() if q in ln.lower()]
    return "\n".join(hits), len(hits)
```

### B. 修改 `emily-core/emily_core/tools/registry.py`

**B-1. `_register_base()` 末尾追加**（`ocr_document` 之后）：

```python
    # meta_cognition_read —— 三书全文按需检索（M2）
    from .meta_cognition_tool import (
        handle_meta_cognition_read, _META_COG_SCHEMA, _META_COG_DESCRIPTION,
    )
    reg.register(_tool("meta_cognition_read", _META_COG_DESCRIPTION, _META_COG_SCHEMA,
                       partial(handle_meta_cognition_read, core=core),
                       category="base", permission_flag="all", write_mode="read"))
    _bc += 1
```

**B-2. 文件头 docstring 更新**（可选，保持描述准确）：
`base/` 行注释改为 `base/ __init__.py — 从原始平铺文件导入并分组导出（基座能力：query_data / knowledge_search / ocr / meta_cognition_read）`

### C. 修改 `emily-core/emily_core/infrastructure/tools_consistency.py`

**C-1. `REGISTERED_TOOLS`（base 组）追加**：

```python
    # base
    "query_data", "knowledge_search", "meta_cognition_read",
```

**C-2. `TOOL_META_MAP` 追加**（base 段 `knowledge_search` 之后）：

```python
    "meta_cognition_read": ("三书全文按需检索",     "base",     "all",   "meta"),
```

**C-3. `TOOL_SCHEMA_MAP` 追加**（`knowledge_search` 之后）：

```python
    "meta_cognition_read": ("emily_core.tools.meta_cognition_tool", "_META_COG_SCHEMA"),
```

### D. 修改 `emily-core/emily_core/workitem/langgraph_engine/agent/fallback_policy.py`

**D-1. `_BASIC_TOOLS` 追加**（唯一事实源，禁止他处重复登记）：

```python
    # 基础兜底工具集（R3/R6）：普惠只读，零写。
    # M2: meta_cognition_read 为只读检索（三书全文），归入普惠只读集。
    _BASIC_TOOLS: frozenset[str] = frozenset({
        "knowledge_search", "chat_archive", "meta_cognition_read",
    })
```

> **影响面**：`FallbackPolicy.resolve("basic")` 与 `resolve("advanced")`（后者含 `_ADVANCED_READ_TOOLS`）均会包含该工具；`assert_write_allowed` 因 `write_mode="read"` 直接放行。

### E. 新增脚本 `scripts/dump_meta_cognition.py`

```python
"""dump_meta_cognition —— 按 book(+query) 打印三书裁剪后全文。

用于验证 meta_cognition_read 工具的裁剪口径与输出规模。

用法：
    uv run python scripts/dump_meta_cognition.py --user-id <UUID> --book world
    uv run python scripts/dump_meta_cognition.py --user-id <UUID> --book rule --query 删除
"""

from __future__ import annotations

import argparse
import asyncio
import sys
from pathlib import Path

_CORE_DIR = Path(__file__).resolve().parent.parent / "emily-core"
if str(_CORE_DIR) not in sys.path:
    sys.path.insert(0, str(_CORE_DIR))
if Path("/app/emily_core").exists() and "/app" not in sys.path:
    sys.path.insert(0, "/app")


async def run(*, user_id: str, book: str, query: str = "", db_url: str = "") -> dict:
    """核心通道：返回 handler 原始结果 dict（供系统/其他脚本 import）。"""
    from emily_core.infrastructure.database import init_db
    init_db(db_url=db_url) if db_url else init_db()
    from emily_core.tools.meta_cognition_tool import handle_meta_cognition_read
    return await handle_meta_cognition_read({"book": book, "query": query}, user_id=user_id)


def main() -> None:
    sys.stdout.reconfigure(encoding="utf-8")
    p = argparse.ArgumentParser(description="三书裁剪后全文预览")
    p.add_argument("--user-id", required=True, help="真实用户 UUID")
    p.add_argument("--book", required=True, choices=["world", "rule", "system"])
    p.add_argument("--query", default="")
    p.add_argument("--db-url", default="")
    args = p.parse_args()

    result = asyncio.run(run(user_id=args.user_id, book=args.book,
                             query=args.query, db_url=args.db_url))
    print(f"[success={result.get('success')}] [matched_lines={result.get('matched_lines')}]")
    print(result.get("reply", ""))


if __name__ == "__main__":
    main()
```

### F. 模块自检

```bash
# F1：工具三步齐备 + 一致性检查通过
uv run python scripts/check_tools_consistency.py
→ 预期：无 error（V5 空 schema / V13a / V13b / V14 均通过）

# F2：DB 元数据已种子（重启后）
docker compose -f docker-compose-napcat.yml restart emily-core
docker exec emily-postgres psql -U emily -d emily -c \
  "SELECT id, category, permission_flag, exposure_mode, is_active FROM tool_registry WHERE id='meta_cognition_read';"
→ 预期：1 行，base / all / meta / t

# F3：裁剪 + 关键词过滤
uv run python scripts/dump_meta_cognition.py --user-id <真实UUID> --book rule --query 删除
→ 预期：[success=True] [matched_lines≥1]；reply 仅含命中行且均含"删除"

# F4：兜底档可见
uv run python -c "
from emily_core.workitem.langgraph_engine.agent.fallback_policy import FallbackPolicy
print('IN_BASIC:', 'meta_cognition_read' in FallbackPolicy.basic_tools())
print('READ_OK:', FallbackPolicy.assert_write_allowed('meta_cognition_read','basic','read')[0])
"
→ 预期：IN_BASIC: True / READ_OK: True
```

**失败处理**：F1 报 V14 → 补 `TOOL_SCHEMA_MAP`；F2 无行 → 检查 `TOOL_META_MAP` 条目与启动日志 `_ensure_tool_registry_seed`；F3 `success=False` 且 reply 为"暂不可用" → 检查规则书文件在容器内路径（`/app/rules/规则书.md`）。

---

## DD-M7：SessionOrchestrator（编排策略组件）

### A. 新增 `emily-core/emily_core/session/orchestrator.py`

```python
"""SessionOrchestrator —— 会话编排策略组件（Session 层）。

与 FocusLock / ConfirmQueue / SessionScheduler 同级，由 SessionAgent 持有：
    self.orchestrator = SessionOrchestrator(...)

职责边界（只做规划）：
  - plan()        ：路由 intent + 上下文 + 操作者 → 带依赖的 WI 计划（DAG）
  - on_wi_done()  ：WI 完成后回调，产出动态追加计划（M9）
  不做执行 / 合成 / 归档。

规划策略（混合模式）：
  1) 顺序连接词命中（"查X然后给Y发提醒"）→ 调 LLM 做目标分解 → 依赖 DAG
  2) is_compound 且无顺序语义 → 并行 WI（零 LLM，与现状一致）
  3) 其余（简单 / fallback / SYS-confirm 已在调用方分流）→ 单 WI（零 LLM）
  LLM 不可用 / 解析失败 → 回退 flat 拆分（fail-open，与现状一致）
"""

from __future__ import annotations

import logging
from dataclasses import dataclass, field
from typing import Any, Callable, TYPE_CHECKING

if TYPE_CHECKING:
    from .session_context import SessionContext
    from ..workitem import WorkItem

logger = logging.getLogger("emily.session.orchestrator")


@dataclass
class WorkItemPlan:
    """编排器产出的单个 WI 计划项。"""

    wi: "WorkItem"
    depends_on: list[str] = field(default_factory=list)
    dynamic: bool = False
    depth: int = 0


# 顺序语义连接词（规则判定：命中则不并行，交 LLM 做目标分解）
_ORDER_CONNECTORS = (
    "然后", "再", "之后", "接着", "随后", "并把", "并将", "并给", "再给", "并且给",
)

_PLAN_SYSTEM_PROMPT = (
    "你是任务编排器。把用户的复合请求拆成有先后依赖的执行步骤。"
    "只输出 JSON：{\"steps\":[{\"objective\":\"简短目标\",\"user_input\":\"该步骤的用户请求原文\","
    "\"sop_id\":\"可选，SOP 编号或 null\",\"needs_previous\":true}]}。"
    "steps 按执行顺序排列；needs_previous=true 表示需要上一步结果作为输入。"
)


class SessionOrchestrator:
    """会话编排策略组件。"""

    def __init__(
        self,
        llm_client: Any = None,
        skill_registry: Any = None,
        config: Any = None,
        wi_factory: Callable[[dict, dict | None], "WorkItem"] | None = None,
    ) -> None:
        self._llm = llm_client
        self._skill_registry = skill_registry
        self._config = config
        self._wi_factory = wi_factory
        self._dynamic_emitted = 0

    # ── 外部注入 ──

    def attach_core(self, core) -> None:
        """注入 EmilyCore（取 config；LLM 未注入时一并取）。"""
        self._config = getattr(core, "config", None) if core is not None else None
        if self._llm is None and core is not None:
            self._llm = getattr(core, "_llm_client", None)

    @property
    def enabled(self) -> bool:
        return bool(getattr(self._config, "orchestrator_enabled", True))

    @property
    def max_depth(self) -> int:
        return int(getattr(self._config, "orchestrator_max_depth", 3) or 3)

    @property
    def max_dynamic(self) -> int:
        return int(getattr(self._config, "orchestrator_max_dynamic_wis", 2) or 2)

    # ── 主入口：规划 ──

    async def plan(
        self,
        intent: dict,
        context: "SessionContext | None" = None,
        actor: dict | None = None,
        *,
        fallback_tier: str = "basic",
    ) -> list[WorkItemPlan]:
        """产出初始 WI 计划（混合策略）。"""
        self._dynamic_emitted = 0
        is_compound = bool(intent.get("is_compound"))
        sub_tasks = [st for st in (intent.get("sub_tasks") or []) if isinstance(st, dict)]

        # 1) 顺序语义（复合或单 SOP）→ 尝试依赖分解
        if self._is_ordered(intent) and (is_compound or intent.get("sop_id")):
            plans = await self._plan_dependency(intent, context, actor, sub_tasks)
            if plans:
                return plans
            logger.info("orchestrator: dependency planning unavailable, fallback to flat")

        # 2) 并行复合（零 LLM，与现状一致）
        if is_compound and sub_tasks:
            plans = [WorkItemPlan(wi=self._build(intent, st), depth=0) for st in sub_tasks[:5]]
            plans = [p for p in plans if p.wi is not None]
            if plans:
                return plans

        # 3) 单 WI（零 LLM）
        wi = self._build(intent, None)
        if wi is None:
            return []
        return [WorkItemPlan(wi=wi, depth=0)]

    @staticmethod
    def _is_ordered(intent: dict) -> bool:
        """顺序语义判定：显式标记或用户原文含顺序连接词。"""
        if intent.get("dependency") is True:
            return True
        text = str(intent.get("user_input", "") or "") + str(intent.get("reasoning", "") or "")
        return any(c in text for c in _ORDER_CONNECTORS)

    async def _plan_dependency(
        self,
        intent: dict,
        context: "SessionContext | None",
        actor: dict | None,
        sub_tasks: list[dict],
    ) -> list[WorkItemPlan]:
        """调 LLM 做目标分解 → 依赖 DAG。失败返回 []（调用方回退 flat）。"""
        if self._llm is None:
            return []
        try:
            messages = self._build_plan_messages(intent, context, sub_tasks)
            result = await self._llm.chat_messages(messages, json_mode=True)
            steps = (result.get("data") or {}).get("steps") or []
            if not isinstance(steps, list) or len(steps) < 2:
                return []

            plans: list[WorkItemPlan] = []
            prev_id: str | None = None
            for st in steps[:5]:
                if not isinstance(st, dict):
                    continue
                wi = self._build(intent, st)
                if wi is None:
                    continue
                deps = [prev_id] if (prev_id and st.get("needs_previous", True)) else []
                plans.append(WorkItemPlan(wi=wi, depends_on=deps, depth=0))
                prev_id = wi.id
            return plans if len(plans) >= 2 else []
        except Exception as e:
            logger.warning("orchestrator dependency planning failed: %s", e)
            return []

    def _build_plan_messages(
        self, intent: dict, context: "SessionContext | None", sub_tasks: list[dict]
    ) -> list[dict]:
        catalog = ""
        if self._skill_registry is not None:
            try:
                catalog = self._skill_registry.dump_as_text()
            except Exception as e:
                logger.debug("orchestrator: sop catalog dump failed: %s", e)
        user_req = str(intent.get("user_input", "") or "")
        subs = "；".join(str(st.get("user_input", "")) for st in sub_tasks) or "（无）"
        return [
            {"role": "system", "content": _PLAN_SYSTEM_PROMPT},
            {"role": "user", "content": (
                f"## 用户请求\n{user_req}\n\n"
                f"## 路由已识别的子任务\n{subs}\n\n"
                f"## 可用 SOP 目录（只能从中选 sop_id，或填 null）\n{catalog[:2000]}"
            )},
        ]

    def _build(self, intent: dict, subtask: dict | None) -> "WorkItem | None":
        """经 wi_factory 物化 WI（工厂由 SessionAgent 注入）。"""
        if self._wi_factory is None:
            logger.error("orchestrator: wi_factory 未注入，无法物化 WI")
            return None
        try:
            return self._wi_factory(intent, subtask)
        except Exception as e:
            logger.error("orchestrator: wi_factory failed: %s", e, exc_info=True)
            return None

    # ── M9：动态追加（跨域检索编排）──

    async def on_wi_done(
        self, wi: "WorkItem", done: list["WorkItem"], *, depth: int = 0
    ) -> list[WorkItemPlan]:
        """WI 完成后回调：信息不足时追加 READ 类检索 WI。"""
        if getattr(wi, "dynamic", False):
            return []                                  # 动态 WI 不再递归追加
        if wi.state.value != "DONE":
            return []                                  # 仅成功结果触发
        if self._dynamic_emitted >= self.max_dynamic:
            logger.info("orchestrator: dynamic WI cap %d reached", self.max_dynamic)
            return []
        if depth + 1 > self.max_depth:
            logger.info("orchestrator: append depth %d exceeds max %d", depth + 1, self.max_depth)
            return []

        spec = self._detect_gap(wi)
        if spec is None:
            return []
        objective, user_input, must_include = spec

        new_wi = self._build_retrieval_wi(objective, user_input, must_include)
        if new_wi is None:
            return []
        new_wi.dynamic = True
        new_wi.depth = depth + 1
        self._dynamic_emitted += 1
        logger.info("orchestrator: append retrieval WI %s after %s (depth=%d)",
                    new_wi.id, wi.id, new_wi.depth)
        return [WorkItemPlan(wi=new_wi, dynamic=True, depth=new_wi.depth)]

    @staticmethod
    def _detect_gap(wi: "WorkItem") -> tuple[str, str, list[str]] | None:
        """规则化信息缺口判定（不调 LLM）。

        Returns:
            (objective, user_input, must_include) 或 None（无缺口）
        """
        sr = getattr(wi, "structured_result", None)
        if sr is None:
            return None
        intent_name = str(getattr(sr, "intent", "") or "")
        is_empty = bool(getattr(sr, "is_empty", False))
        data_fields = getattr(sr, "data_fields", None) or {}
        user_input = (getattr(wi, "user_input", "") or "")[:60]

        # 缺口 1：查询结果为空且非知识检索类 → 补知识库/规则依据
        if is_empty and "knowledge" not in intent_name:
            return ("knowledge_lookup",
                    f"补充与「{user_input}」相关的规范、制度或项目背景信息",
                    ["依据", "来源"])
        # 缺口 2：项目态势类查询但缺节点进度 → 补项目态势全文
        if intent_name in ("query_project_summary", "query_node", "summary") \
                and not data_fields.get("node_progress"):
            return ("meta_cognition",
                    f"检索项目态势全文，补充「{user_input}」涉及的节点与进度",
                    ["节点", "进度"])
        return None

    def _build_retrieval_wi(
        self, objective: str, user_input: str, must_include: list[str]
    ) -> "WorkItem | None":
        """构造只读检索 WI（fallback 类，工具集由 FallbackPolicy 只读白名单裁剪）。"""
        pseudo = {
            "sop_id": None,
            "fallback": True,
            "confidence": "high",
            "output_spec": {
                "intent": objective, "detail": "standard",
                "format": "natural", "cite_source": True,
            },
            "result_constraints": {"must_include": list(must_include)},
        }
        wi = self._build(pseudo, None)
        if wi is None:
            return None
        wi.user_input = user_input
        return wi
```

### B. 修改 `emily-core/emily_core/workitem/workitem.py`

**B-1. 新增 3 个字段**（插在「多轮续接」段之后、Node 2 产出之前）：

```python
    # ── 编排（M7/M8）──
    depends_on: list[str] = field(default_factory=list)
    """依赖的前置 WI id 列表（由 SessionOrchestrator 规划写入）"""

    depth: int = 0
    """在 WI DAG 中的深度（0=初始层；动态追加 WI 递增）"""

    dynamic: bool = False
    """是否为编排器动态追加的 WI（dynamic=True 不再触发递归追加）"""
```

### C. 修改 `emily-core/emily_core/session/session_agent.py`（M7 部分）

**C-1. `__init__` 注入编排器**（在 `self.scheduler = SessionScheduler(...)` 之后）：

```python
        # M7: 编排策略组件（与 FocusLock / ConfirmQueue 同级；config 由 SessionFactory 注入）
        from .orchestrator import SessionOrchestrator
        self.orchestrator = SessionOrchestrator(
            llm_client=llm_client,
            skill_registry=skill_registry,
            config=None,
            wi_factory=self._build_workitem,
        )
        # 本轮编排计划（由 _split_into_workitems 写入，_handle_impl 消费）
        self._pending_plans: list | None = None
```

**C-2. 抽出 WI 物化工厂 `_build_workitem()`**（新增方法，放在 `_split_into_workitems` 之前）：

```python
    def _build_workitem(self, intent: dict, subtask: dict | None = None) -> WorkItem:
        """WI 物化工厂（供 SessionOrchestrator 回调用）。

        保留原有 output_spec / result_constraints / work_spec / 专家匹配 /
        SOP-005 query_type 预填 逻辑，确保编排路径与 legacy 路径产出等价 WI。
        """
        from ..workitem.langgraph_engine.agent.fallback_policy import FallbackPolicy

        actor = getattr(self, "_current_actor", None)
        actor_uid = (actor or {}).get("user_id") or self.context.user_id
        content = str(intent.get("user_input") or "")
        sub = subtask if isinstance(subtask, dict) else None

        sop_id = (sub.get("sop_id", intent.get("sop_id")) if sub else intent.get("sop_id"))
        is_fallback = bool(intent.get("fallback")) or not sop_id

        if is_fallback:
            wi = WorkItem(
                session_id=self.conversation_id,
                user_input=content,
                user_id=actor_uid,
                sop_id=None,
                intent_type="fallback",
                fallback_tier=FallbackPolicy.gate(actor).value,
                priority=1,
            )
        else:
            wi = WorkItem(
                session_id=self.conversation_id,
                user_input=(sub.get("user_input", content) if sub else content),
                user_id=actor_uid,
                sop_id=sop_id,
                intent_type="sop",
                priority=(sub.get("priority", 1) if sub else 1),
            )

        spec_source = sub or intent
        wi.output_spec = self._derive_output_spec(spec_source, wi.sop_id)
        wi.result_constraints = self._derive_constraints(intent)
        wi.work_spec = self._build_work_spec(
            intent, wi.sop_id or "", wi.user_input,
            wi.output_spec, wi.result_constraints,
            getattr(wi, "required_tools", set()),
        )

        if not is_fallback:
            self._match_expert(wi, sop_id)
            query_type = intent.get("query_type")
            if sop_id == "SOP-005-QRY" and query_type:
                setattr(wi, "_prefilled_params", {"query_type": query_type})
                logger.debug("Session[%s] prefilled query_type=%s for SOP-005-QRY",
                             self.conversation_id, query_type)
        return wi
```

**C-3. `_split_into_workitems()` 重构**（替换原方法整体）：

```python
    async def _split_into_workitems(self, message: "StandardMessage") -> list[WorkItem]:
        """Phase B: 基于 LLM 意图识别的 WorkItem 拆分（M7：经编排器规划）。"""
        content = message.content or ""
        intent = await self._recognize_intent(message)
        # ── 归档：意图识别段（BUS 之前，含 Prompt 注入信息）──
        await self._append_archive_intent(intent)
        # 供编排器做顺序语义判定（orchestrator 不持有原始消息）
        intent.setdefault("user_input", content)

        actor = getattr(self, "_current_actor", None)
        actor_uid = (actor or {}).get("user_id") or self.context.user_id

        # SYS-confirm 不走编排（需保留 _confirm_action / _confirm_event_id）
        if intent.get("sop_id") == "SYS-confirm":
            self._pending_plans = None
            return self._split_sys_confirm(intent, content, actor_uid)

        # 编排关闭 → 保持原路径（回退开关）
        if not self.orchestrator.enabled:
            self._pending_plans = None
            return self._split_flat(intent, content, actor_uid)

        logger.info(
            "Session[%s] intent: sop=%s conf=%s compound=%s fallback=%s sub_tasks=%d",
            self.conversation_id, intent.get("sop_id"), intent.get("confidence"),
            intent.get("is_compound"), intent.get("fallback"), len(intent.get("sub_tasks") or []),
        )

        from ..workitem.langgraph_engine.agent.fallback_policy import FallbackPolicy
        fallback_tier = FallbackPolicy.gate(actor).value
        try:
            plans = await self.orchestrator.plan(
                intent, self.context, actor, fallback_tier=fallback_tier,
            )
        except Exception as e:
            logger.warning("Session[%s] orchestrator.plan failed, fallback to flat: %s",
                           self.conversation_id, e, exc_info=True)
            self._pending_plans = None
            return self._split_flat(intent, content, actor_uid)

        work_items = [p.wi for p in plans]
        if not work_items:
            self._pending_plans = None
            return []
        self._pending_plans = plans
        return work_items

    def _split_sys_confirm(self, intent: dict, content: str, actor_uid: str) -> list[WorkItem]:
        """SYS-confirm 分支（保持原行为，不走编排器）。"""
        action = (intent.get("data") or {}).get("action", "confirm")
        pending_event = self._get_pending_event()
        if pending_event:
            wi = WorkItem(
                session_id=self.conversation_id, user_input=content, user_id=actor_uid,
                sop_id="SYS-confirm", intent_type="sop", priority=0,
            )
            setattr(wi, "_confirm_action", action)
            setattr(wi, "_confirm_event_id", pending_event.id)
            return [wi]
        from ..workitem.langgraph_engine.agent.fallback_policy import FallbackPolicy
        return [WorkItem(
            session_id=self.conversation_id, user_input=content, user_id=actor_uid,
            sop_id=None, intent_type="fallback",
            fallback_tier=FallbackPolicy.gate(getattr(self, "_current_actor", None)).value,
            priority=1,
        )]

    def _split_flat(self, intent: dict, content: str, actor_uid: str) -> list[WorkItem]:
        """Legacy flat 拆分（保留作为编排关闭 / 编排失败的回退路径）。"""
        if intent.get("fallback") or not intent.get("sop_id"):
            return [self._build_workitem(intent, None)]
        if intent.get("is_compound") and intent.get("sub_tasks"):
            items = [
                self._build_workitem(intent, st)
                for st in (intent.get("sub_tasks") or [])[:5]
                if isinstance(st, dict)
            ]
            return [wi for wi in items if wi is not None]
        return [self._build_workitem(intent, None)]
```

**C-4. `_handle_impl` 执行分支改造**（替换第 360-364 行的「③ 经 Pipeline BUS 执行」）：

```python
        # ③ 经 Pipeline BUS 执行（M7：编排开启时走 run_dag；否则保持原顺序执行）
        plans = getattr(self, "_pending_plans", None)
        if plans:
            self._pending_plans = None
            done = await self.scheduler.run_dag(
                plans,
                message,
                db_message_id=db_message_id,
                actor_snapshot=getattr(self, "_current_actor", None),
                max_depth=self.orchestrator.max_depth,
                on_wi_done=self.orchestrator.on_wi_done,
            )
        else:
            for wi in work_items:
                self.scheduler.enqueue(wi)
            done = await self.scheduler.run_all_with_message(
                message, db_message_id=db_message_id,
                actor_snapshot=getattr(self, "_current_actor", None),
            )
        self._last_turn_workitems = done
```

> `continue_wi`（多轮续接）分支不产生 `_pending_plans`，自然走 `run_all_with_message` 原路径。为防上一轮残留，续接分支显式 `self._pending_plans = None`。

### D. 修改 `emily-core/emily_core/adapters/session/session_factory.py`

**D-1. 注入 core 到编排器**（第 64 行 `agent.scheduler._core = self._core` 之后）：

```python
        # 将 EmilyCore 注入 scheduler（graph 引擎通过 _core 获取 _workitem_graph）
        agent.scheduler._core = self._core
        # M7: 将 EmilyCore 注入 orchestrator（取 config：orchestrator_enabled / max_depth / max_dynamic_wis）
        agent.orchestrator.attach_core(self._core)
```

> **不设置 `agent._core`**：`SessionAgent.handle()` 的 `getattr(self, "_core", None)` 目前恒为 `None`，`fetch_actor_snapshot` 走 Service 直建路径。设为真实 core 会改变权限加载路径（属越界改动），本 DD 明确不做。

### E. 模块自检

```bash
# G1：简单请求单计划、零 LLM 规划
docker exec emily-core python -c "
import asyncio
from emily_core.session.orchestrator import SessionOrchestrator
from emily_core.workitem import WorkItem
o=SessionOrchestrator(wi_factory=lambda i,s=None: WorkItem(sop_id=(i.get('sop_id') or 'X')))
async def m():
    p=await o.plan({'sop_id':'SOP-002-REC','confidence':'high','is_compound':False},None,{'level':3})
    print('PLANS:',len(p),'DEPS:',p[0].depends_on,'DEPTH:',p[0].depth)
asyncio.run(m())
"
→ 预期：PLANS: 1 DEPS: [] DEPTH: 0

# G2：并行复合 → 多计划且互不依赖
docker exec emily-core python -c "
import asyncio
from emily_core.session.orchestrator import SessionOrchestrator
from emily_core.workitem import WorkItem
o=SessionOrchestrator(wi_factory=lambda i,s=None: WorkItem(sop_id=(s or {}).get('sop_id','X')))
async def m():
    p=await o.plan({'sop_id':'SOP-002-REC','confidence':'high','is_compound':True,'user_input':'记录事件并查询任务',
                    'sub_tasks':[{'sop_id':'SOP-002-REC'},{'sop_id':'SOP-005-QRY'}]},None,{'level':3})
    print('PLANS:',len(p),'ALL_NO_DEPS:',all(not x.depends_on for x in p))
asyncio.run(m())
"
→ 预期：PLANS: 2 ALL_NO_DEPS: True

# G3：顺序语义但 LLM 不可用 → 回退 flat（不抛异常）
docker exec emily-core python -c "
import asyncio
from emily_core.session.orchestrator import SessionOrchestrator
from emily_core.workitem import WorkItem
o=SessionOrchestrator(wi_factory=lambda i,s=None: WorkItem(sop_id=(i.get('sop_id') or 'X')))
async def m():
    p=await o.plan({'sop_id':'SOP-005-QRY','confidence':'high','user_input':'查逾期节点然后给负责人发提醒'},None,{'level':5})
    print('FALLBACK_OK:', len(p)>=1, '| DEPS:', p[0].depends_on)
asyncio.run(m())
"
→ 预期：FALLBACK_OK: True | DEPS: []

# G4：依赖复合（有 LLM）→ DAG 依赖成型（需真实 LLM 环境）
uv run python .claude/skills/emy-test/cli.py --managed --llm \
  --message "帮我查一下翠湖庭院的逾期节点，然后给每个负责人发提醒" --sender "<真实管理员>"
docker exec mitmproxy tail -20 /app/logs/llm_trace.jsonl | grep -c '编排器'
→ 预期：出现编排规划调用；归档/日志可见 WI-1 与 WI-2 的 depends_on 关系
```

**失败处理**：G1 触发 LLM → 检查 `_is_ordered` 是否把 `reasoning` 里的连接词误判（`reasoning` 可能含"然后"）——若误判频繁，改为**只看 `user_input`**，不看 `reasoning`（见 DD-风险 R2）；G3 抛异常 → 检查 `_plan_dependency` 是否整体 try/except。

---

## DD-M8：Scheduler 依赖序执行（run_dag）

### A. 修改 `emily-core/emily_core/workitem/workitem_state.py`

**A-1. 新增 `SKIPPED` 枚举成员**（`ABANDONED` 之后）：

```python
    ABANDONED = "ABANDONED"                # 用户切换话题后放弃（终态）
    SKIPPED = "SKIPPED"                    # 编排中前置 WI 未成功，后继跳过（终态）
```

**A-2. `TRANSITIONS` 增加转换**（`CREATED` 行）：

```python
    WorkItemState.CREATED:         [WorkItemState.PLANNING, WorkItemState.FAILED,
                                    WorkItemState.SKIPPED],
```

**A-3. `TERMINAL_STATES` 纳入**：

```python
TERMINAL_STATES = frozenset({
    WorkItemState.DONE, WorkItemState.FAILED,
    WorkItemState.ABANDONED, WorkItemState.SKIPPED,
})
```

及模块 docstring 的状态图补一行：

```
    CREATED → PLANNING → EXECUTING → DONE
                  │           │
                  │           ├── WAITING_FOR_INPUT → EXECUTING（用户回复后恢复）
                  │           └── FAILED（不可恢复错误，终态）
                  └── SKIPPED（编排中前置未成功，终态）
```

### B. 修改 `emily-core/emily_core/workitem/scheduler.py`

**B-1. 新增 `run_dag()` 与状态标记辅助**（插在 `run_all_with_message` 之后、`_run_one` 之前）：

```python
    async def run_dag(
        self,
        plans: list,
        message=None,
        db_message_id: str = "",
        actor_snapshot=None,
        *,
        max_depth: int = 3,
        on_wi_done=None,
    ) -> list[WorkItem]:
        """按依赖拓扑序执行 WI 计划（层内并行，层间顺序）。

        Args:
            plans: list[WorkItemPlan]（含 wi / depends_on / dynamic / depth）
            message: 原始入站消息（透传给每个 WI 的 BusContext）
            db_message_id: 入站消息持久化 ID
            actor_snapshot: 当前操作者权限快照
            max_depth: DAG 最大深度（超出则丢弃追加计划）
            on_wi_done: 动态追加回调 async (wi, done, *, depth) -> list[WorkItemPlan]

        Returns:
            list[WorkItem]：全部 WI（含 DONE / FAILED / SKIPPED），按完成顺序
        """
        import asyncio as _asyncio

        self._current_actor = actor_snapshot
        results: list[WorkItem] = []
        done_ids: set[str] = set()
        failed_ids: set[str] = set()
        pending: list = list(plans)
        halted = False

        while pending and not halted:
            # 1) 当前可执行层（所有前置已完成）
            layer_idx = [
                i for i, p in enumerate(pending)
                if all(d in done_ids for d in (p.depends_on or []))
            ]
            if not layer_idx:
                # 依赖环 / 依赖永不满足 → 剩余全部 SKIPPED（防死循环）
                logger.warning("Scheduler[%s] run_dag: unresolvable deps, skip %d WI",
                               self.session_id, len(pending))
                for p in pending:
                    self._mark_skipped(p.wi)
                    results.append(p.wi)
                break

            layer = [pending[i] for i in layer_idx]
            keep_idx = set(layer_idx)
            pending = [p for i, p in enumerate(pending) if i not in keep_idx]

            # 2) 层内并行（create_task 拷贝上下文，隔离 set_bus_context / 日志 ContextVar）
            tasks = [
                _asyncio.create_task(
                    self._run_one(p.wi, message=message, db_message_id=db_message_id)
                )
                for p in layer
            ]
            layer_outcome = await _asyncio.gather(*tasks, return_exceptions=True)

            # 3) 汇总本层结果
            layer_wis: list[WorkItem] = []
            for p, outcome in zip(layer, layer_outcome):
                if isinstance(outcome, BaseException):
                    logger.error("Scheduler[%s] run_dag WI %s raised: %s",
                                 self.session_id, p.wi.id, outcome)
                    self._mark_failed(p.wi, str(outcome))
                    wi = p.wi
                else:
                    wi = outcome
                results.append(wi)
                layer_wis.append(wi)
                if wi.state == WorkItemState.DONE:
                    done_ids.add(wi.id)
                else:
                    failed_ids.add(wi.id)

            # 4) 后继依赖失败 → SKIPPED（不执行，不抛异常）
            remaining: list = []
            for p in pending:
                if any(d in failed_ids for d in (p.depends_on or [])):
                    self._mark_skipped(p.wi)
                    results.append(p.wi)
                    failed_ids.add(p.wi.id)
                else:
                    remaining.append(p)
            pending = remaining

            # 5) 多轮续接挂起 → 立即停止后续层（由 SessionAgent 处理续接）
            if any(w.state == WorkItemState.WAITING_FOR_INPUT for w in layer_wis):
                logger.info("Scheduler[%s] run_dag halted: WI waiting for input", self.session_id)
                halted = True
                break

            # 6) 动态追加（跨域检索编排）
            if on_wi_done is None:
                continue
            for wi in layer_wis:
                if wi.state != WorkItemState.DONE:
                    continue
                try:
                    added = await on_wi_done(wi, results, depth=getattr(wi, "depth", 0))
                except Exception as e:
                    logger.warning("Scheduler[%s] on_wi_done failed for %s: %s",
                                   self.session_id, wi.id, e)
                    continue
                for plan in (added or []):
                    if getattr(plan, "depth", 0) > max_depth:
                        logger.info("Scheduler[%s] drop dynamic plan depth=%d > max=%d",
                                    self.session_id, plan.depth, max_depth)
                        continue
                    pending.append(plan)

        return results

    @staticmethod
    def _mark_skipped(wi: WorkItem) -> None:
        """标记 WI 为 SKIPPED（终态）。"""
        try:
            wi.transition_to(WorkItemState.SKIPPED)
        except ValueError:
            wi.state = WorkItemState.SKIPPED
        wi.error_message = wi.error_message or "前置 WI 未成功，已跳过"

    @staticmethod
    def _mark_failed(wi: WorkItem, reason: str) -> None:
        """标记 WI 为 FAILED（终态）。"""
        if not wi.is_terminal:
            try:
                wi.transition_to(WorkItemState.FAILED)
            except ValueError:
                wi.state = WorkItemState.FAILED
        wi.error_message = wi.error_message or reason
```

**B-2. `run_all()` / `run_all_with_message()` / `_run_one()` / `_run_graph()` 完全不动**（硬约束 1、C8）。

### C. 修改 `emily-core/emily_core/config.py`

**C-1. 新增配置项**（插在 `agent_loop_max_iterations` 块之后，即第 214-216 行后）：

```python
    # ── 会话编排（M7/M8）──
    orchestrator_enabled: bool = True
    """会话编排总开关。关闭时 SessionAgent 走原 flat 拆分 + run_all_with_message"""

    orchestrator_max_depth: int = 3
    """WI DAG 最大深度（防无限编排；动态追加 WI 的 depth 超过此值即丢弃）"""

    orchestrator_max_dynamic_wis: int = 2
    """单轮动态追加 WI 上限（跨域检索编排；达到上限后 on_wi_done 返回空）"""
```

### D. 模块自检

```bash
# H1：拓扑序正确（WI-2 依赖 WI-1）
docker exec emily-core python -c "
import asyncio
from emily_core.workitem.scheduler import SessionScheduler
from emily_core.workitem.workitem_state import WorkItemState
from emily_core.session.orchestrator import WorkItemPlan
from emily_core.workitem import WorkItem
s=SessionScheduler('c1'); order=[]
async def fake(wi, message=None, db_message_id=''):
    order.append(wi.id); wi.transition_to(WorkItemState.PLANNING); wi.transition_to(WorkItemState.EXECUTING); wi.transition_to(WorkItemState.DONE); return wi
s._run_one=fake
w1=WorkItem(id='WI-1'); w2=WorkItem(id='WI-2'); w2.depends_on=['WI-1']
r=asyncio.run(s.run_dag([WorkItemPlan(wi=w1), WorkItemPlan(wi=w2, depends_on=['WI-1'])]))
print('ORDER:',order,'| COUNT:',len(r))
"
→ 预期：ORDER: ['WI-1', 'WI-2'] | COUNT: 2

# H2：前置失败 → 后继 SKIPPED，不抛异常
docker exec emily-core python -c "
import asyncio
from emily_core.workitem.scheduler import SessionScheduler
from emily_core.workitem.workitem_state import WorkItemState
from emily_core.session.orchestrator import WorkItemPlan
from emily_core.workitem import WorkItem
s=SessionScheduler('c1')
async def fake(wi, message=None, db_message_id=''):
    wi.transition_to(WorkItemState.PLANNING); wi.transition_to(WorkItemState.FAILED); return wi
s._run_one=fake
w1=WorkItem(id='WI-1'); w2=WorkItem(id='WI-2'); w2.depends_on=['WI-1']
r=asyncio.run(s.run_dag([WorkItemPlan(wi=w1), WorkItemPlan(wi=w2, depends_on=['WI-1'])]))
print('STATES:',{x.id:x.state.value for x in r})
"
→ 预期：STATES: {'WI-1': 'FAILED', 'WI-2': 'SKIPPED'}

# H3：深度上限（max_depth=1 → depth2 追加被丢弃）
docker exec emily-core python -c "
import asyncio
from emily_core.workitem.scheduler import SessionScheduler
from emily_core.workitem.workitem_state import WorkItemState
from emily_core.session.orchestrator import WorkItemPlan
from emily_core.workitem import WorkItem
s=SessionScheduler('c1')
async def fake(wi, message=None, db_message_id=''):
    wi.transition_to(WorkItemState.PLANNING); wi.transition_to(WorkItemState.EXECUTING); wi.transition_to(WorkItemState.DONE); return wi
s._run_one=fake
async def cb(wi, done, depth=0):
    return [WorkItemPlan(wi=WorkItem(id='WI-X'), dynamic=True, depth=depth+1)]
r=asyncio.run(s.run_dag([WorkItemPlan(wi=WorkItem(id='WI-1'))], max_depth=1, on_wi_done=cb))
print('IDS:',[x.id for x in r])
"
→ 预期：IDS: ['WI-1']（WI-X 被丢弃）

# H4：SKIPPED 为终态
uv run python -c "
from emily_core.workitem.workitem_state import WorkItemState, TERMINAL_STATES, TRANSITIONS
print('TERMINAL:', WorkItemState.SKIPPED in TERMINAL_STATES)
print('FROM_CREATED:', WorkItemState.SKIPPED in TRANSITIONS[WorkItemState.CREATED])
"
→ 预期：TERMINAL: True / FROM_CREATED: True

# H5：简单请求不回归（run_all_with_message 仍可用）
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "你好" --sender "<真实用户>"
→ 预期：正常回复（快捷路径不受影响）
```

**失败处理**：H1 顺序错乱 → 检查层选择是否按 `depends_on ⊆ done_ids`；H2 抛异常 → 检查 `_mark_skipped` 是否被 `_run_one` 覆盖（SKIPPED 分支不得调 `_run_one`）；并行场景出现上下文串扰（`bus_context` 混用）→ 确认用 `asyncio.create_task` 而非裸协程，必要时将层内改为顺序执行并登记设计偏差（见 DD-风险 R1）。

---

## DD-M9：跨域检索编排

M9 的实现即 `SessionOrchestrator.on_wi_done()` / `_detect_gap()` / `_build_retrieval_wi()`（见 DD-M7 §A 完整代码）。本节补充**判定规则表与边界**。

### A. 缺口判定规则（`_detect_gap`）

| # | 触发条件 | objective | 追加 WI 形态 | 预期工具 |
|---|---------|-----------|-------------|---------|
| G-1 | `structured_result.is_empty == True` 且 `intent` 不含 `knowledge` | `knowledge_lookup` | fallback WI，`result_constraints.must_include=["依据","来源"]` | `knowledge_search` / `meta_cognition_read` |
| G-2 | `intent ∈ {query_project_summary, query_node, summary}` 且 `data_fields` 缺 `node_progress` | `meta_cognition` | fallback WI，`must_include=["节点","进度"]` | `meta_cognition_read(book="world")` |

> 工具由 agent loop 从 **`session_api_ids ∩ FallbackPolicy.read`** 中自主选择（DD-风险 R4）。本设计**不新增工具钉选机制**，避免越界改动执行引擎。

### B. 硬性边界（代码位置）

| 边界 | 实现点 |
|------|-------|
| 不递归 | `on_wi_done` 首行 `if getattr(wi, "dynamic", False): return []` |
| 仅成功结果 | `if wi.state.value != "DONE": return []` |
| 只读零写 | 追加 WI 为 `intent_type="fallback"`；写工具对 basic/advanced 兜底本就不可见（`FallbackPolicy` 白名单） |
| 权限口径 | 追加 WI 的 `user_id` = 原请求者 `_current_actor.user_id`（`_build_workitem` 内解析） |
| 追加上限 | `self._dynamic_emitted >= self.max_dynamic` → 返回 `[]` |
| 深度上限 | `depth + 1 > self.max_depth` → 返回 `[]`；`run_dag` 二次校验 `plan.depth > max_depth` 丢弃 |
| 轮次隔离 | `plan()` 首行 `self._dynamic_emitted = 0` 重置计数 |

### C. 模块自检

```bash
# I1：缺口触发追加（只读 WI）
docker exec emily-core python -c "
import asyncio
from types import SimpleNamespace
from emily_core.session.orchestrator import SessionOrchestrator
from emily_core.workitem import WorkItem
from emily_core.workitem.workitem_state import WorkItemState
o=SessionOrchestrator(wi_factory=lambda i,s=None: WorkItem(sop_id='X', intent_type='fallback'))
wi=WorkItem(id='WI-1', sop_id='SOP-005-QRY', intent_type='sop', user_input='查翠湖庭院进度')
wi.transition_to(WorkItemState.PLANNING); wi.transition_to(WorkItemState.EXECUTING); wi.transition_to(WorkItemState.DONE)
wi.structured_result=SimpleNamespace(intent='query_project_summary', is_empty=True, data_fields={})
async def m():
    p=await o.on_wi_done(wi,[wi],depth=0)
    print('APPENDED:',len(p),'| DYNAMIC:',[x.dynamic for x in p],'| WTYPE:',[x.wi.intent_type for x in p])
asyncio.run(m())
"
→ 预期：APPENDED: ≥1 | DYNAMIC: [True] | WTYPE: ['fallback']

# I2：动态 WI 不递归 + 无缺口不追加
docker exec emily-core python -c "
import asyncio
from types import SimpleNamespace
from emily_core.session.orchestrator import SessionOrchestrator
from emily_core.workitem import WorkItem
from emily_core.workitem.workitem_state import WorkItemState
o=SessionOrchestrator(wi_factory=lambda i,s=None: WorkItem(sop_id='X'))
def mk(dyn):
    w=WorkItem(id='W'+str(dyn), dynamic=dyn)
    w.transition_to(WorkItemState.PLANNING); w.transition_to(WorkItemState.EXECUTING); w.transition_to(WorkItemState.DONE)
    w.structured_result=SimpleNamespace(intent='knowledge_lookup', is_empty=True, data_fields={})
    return w
async def m():
    print('RECURSE:', await o.on_wi_done(mk(True),[],depth=1))
    o._dynamic_emitted=99
    print('CAP:', await o.on_wi_done(mk(False),[],depth=1))
asyncio.run(m())
"
→ 预期：RECURSE: [] / CAP: []

# I3：跨域检索白名单只读（basic 档）
uv run python -c "
from emily_core.workitem.langgraph_engine.agent.fallback_policy import FallbackPolicy
t=FallbackPolicy.resolve('basic', with_write=False)
print('READ_ONLY:', all(not x.startswith(('record_','update_','delete_','link_','new_')) for x in t))
print('HAS_SEARCH:', 'knowledge_search' in t, '| HAS_META:', 'meta_cognition_read' in t)
"
→ 预期：READ_ONLY: True / HAS_SEARCH: True / HAS_META: True
```

**失败处理**：I1 未追加 → 检查 `wi.state.value` 是否为 `"DONE"`（测试需先做合法状态转换）；I2 `CAP` 非空 → 检查 `_dynamic_emitted` 比较是否在 `_detect_gap` **之前**；I3 `READ_ONLY: False` → 检查追加 WI 是否误设成 SOP 类。

---

## 新增脚本 `scripts/migrate_world_book_segments.py`

```python
"""migrate_world_book_segments —— 为存量世界书补 structure.node_segments。

M1a 兼容迁移：旧世界书 content_json 无 node_segments，会话侧摘要会降级为
"仅统计"版本。重跑 ProjectWorldBookBuilder 即可补齐（幂等：version +1）。

用法：
    uv run python scripts/migrate_world_book_segments.py --all --dry-run
    uv run python scripts/migrate_world_book_segments.py --project-id <UUID>
"""

from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path

_CORE_DIR = Path(__file__).resolve().parent.parent / "emily-core"
if str(_CORE_DIR) not in sys.path:
    sys.path.insert(0, str(_CORE_DIR))
if Path("/app/emily_core").exists() and "/app" not in sys.path:
    sys.path.insert(0, "/app")


def run(*, project_ids: list[str] | None = None, dry_run: bool = False, db_url: str = "") -> dict:
    """核心通道：为指定/全部世界书重建 content_json（含 node_segments）。

    Returns:
        {"dry_run": bool, "total": int, "items": [{"project_id", "node_segments", "has_segments"}]}
    """
    from emily_core.infrastructure.database import init_db
    init_db(db_url=db_url) if db_url else init_db()

    from emily_core.repositories.world_book_repo import ProjectWorldBookRepo
    from emily_core.services.world_book_builder import ProjectWorldBookBuilder

    books = ProjectWorldBookRepo.list_all()
    targets = [
        b.project_id for b in books
        if (not project_ids or b.project_id in set(project_ids))
    ]

    builder = ProjectWorldBookBuilder()
    items: list[dict] = []
    for pid in targets:
        result = builder.build(pid, generated_by="manual", dry_run=dry_run)
        try:
            structure = (json.loads(result.get("content_json") or "{}").get("structure") or {})
        except (json.JSONDecodeError, TypeError):
            structure = {}
        segs = structure.get("node_segments") or {}
        items.append({
            "project_id": pid,
            "node_segments": len(segs),
            "has_segments": bool(segs),
        })
    return {"dry_run": dry_run, "total": len(targets), "items": items}


def main() -> None:
    sys.stdout.reconfigure(encoding="utf-8")
    p = argparse.ArgumentParser(description="世界书 node_segments 兼容迁移")
    p.add_argument("--project-id", action="append", default=[], help="指定项目（可重复）")
    p.add_argument("--all", action="store_true", help="处理全部已有世界书")
    p.add_argument("--dry-run", action="store_true", help="仅预览，不写库")
    p.add_argument("--db-url", default="")
    args = p.parse_args()

    if not args.all and not args.project_id:
        print("请指定 --all 或 --project-id <UUID>")
        raise SystemExit(2)

    report = run(project_ids=args.project_id or None,
                 dry_run=args.dry_run, db_url=args.db_url)
    print(f"[dry_run={report['dry_run']}] 目标项目数={report['total']}")
    for it in report["items"]:
        flag = "OK" if it["has_segments"] else "EMPTY"
        print(f"  · {it['project_id']}  node_segments={it['node_segments']}  [{flag}]")


if __name__ == "__main__":
    main()
```

### 脚本自检

```bash
# J1：dry-run 预览
uv run python scripts/migrate_world_book_segments.py --all --dry-run
→ 预期：每个项目一行，node_segments=N（N>0 为 OK；EMPTY 说明该项目无节点或需先建节点）

# J2：实跑后复查
uv run python scripts/migrate_world_book_segments.py --all
docker exec emily-postgres psql -U emily -d emily -c \
  "SELECT project_id, version, jsonb_typeof(content_json::json->'structure'->'node_segments') FROM project_world_books;"
→ 预期：version 自增；jsonb_typeof = object
```

**失败处理**：`EMPTY` → 检查该项目 `project_nodes` 是否有非废弃节点；实跑报错 → 检查 `init_db()` 的 DB URL（容器内需 `EMILY_DATABASE_URL`）。

---

## 四、回归风险清单

| # | 风险 | 触发场景 | 影响 | 缓解 / 回退 |
|---|------|---------|------|------------|
| **R1** | `run_dag` 层内并行导致 `BusContext` / 日志上下文串扰 | 同层 ≥2 个 WI 同时执行 | 取数错乱、trace 归属错误 | 必须用 `asyncio.create_task`（拷贝上下文）；若实测仍串扰 → 将层内改为顺序执行（`for p in layer: await self._run_one(...)`），牺牲并行度保正确性，并登记设计偏差 |
| **R2** | `_is_ordered` 误判（`reasoning` 中偶含连接词） | 路由 LLM 的 `reasoning` 文本含"然后"等 | 简单请求多一次 LLM 规划调用（延迟增加），但结果仍正确 | 改为只扫 `intent["user_input"]`，不扫 `reasoning`；`_ORDER_CONNECTORS` 收敛为强顺序词 |
| **R3** | 大项目 `node_segments` 使 `content_json` 膨胀 | 单项目节点数 > 2000 | 世界书记录体积增大 | 不截断（截断破坏权限正确性）；改由 `content_text` 保持精简 + 会话侧只取摘要；必要时后续按 `parent_node_id` 分层加载 |
| **R4** | 动态追加 WI 依赖 LLM 自选工具，未必选中 `meta_cognition_read` | 缺口 objective 表述不够明确 | 检索 WI 可能选了别的只读工具 | 在 `user_input` / `result_constraints.must_include` 中显式写入"项目态势全文"，提高命中率；不改执行引擎 |
| **R5** | 规则书章节 frontmatter 改造遗漏 | 人工编辑规则书时漏加 | 该章节按"全员可见"处理（fail-open），不丢内容 | 用 B1 自检命令（SEC == FM）作为**编辑后必跑**检查；后续可加入 `self_check` |
| **R6** | 占位符重复出现导致摘要重复注入 | 在 session.md 写两次 `{rule_brief}` | prompt 变长、信息冗余 | 编码规范：3 个摘要占位符各只写 1 处；E1 自检可发现（残留为 0 但长度异常时人工复核） |
| **R7** | `{project_brief}` 等新键被误加入 base | 误从 `_PERM_PROMPT_KEYS` 移除 | **M4 前缀缓存失效 + 群聊越权** | E3 自检断言 `PLACEHOLDER_KEPT == True`，作为 CI 必过项 |
| **R8** | `SessionContext.refresh()` 未同步新字段 | 漏改 `_hot_fields` | 三书摘要热更新失效（改规则书后 prompt 不更新） | SD/DD 已列明；验收计划含"热重载"用例 |
| **R9** | `meta_cognition_read` 在 DB 未种子 | 未重启 emily-core | 工具对 agent 不可见 | F2 自检（重启后查 `tool_registry`）；部署 checklist 必含"重启 + 复查" |
| **R10** | 编排关闭时不生效 | `orchestrator_enabled=false` | 走原 flat 路径（功能降级） | 属设计内回退开关；验收计划含"开关关闭仍可用"用例 |

---

## 五、追溯矩阵（US ↔ 模块 ↔ DD 章节）

| US | 规格摘要（PRD §2.2） | 实现模块 | DD 章节 |
|----|---------------------|---------|---------|
| US-01 | 会话具备项目态势认知，且按可见节点范围裁剪 | M1a、M1 | DD-M1a、DD-M1 |
| US-02 | 有先后的复合诉求按依赖序执行，前置失败不中断会话 | M7、M8 | DD-M7、DD-M8 |
| US-03 | 会话具备系统能力认知，且按数据访问权限裁剪 | M1c、M1 | DD-M1c、DD-M1 |
| US-04 | 三书全文可按需查阅，只读、可按关键词过滤、零越权 | M1a、M1b、M1c、M2 | DD-M1a / M1b / M1c、DD-M2 |
| US-05 | 信息不足时可自主补齐（只读、原请求者权限、有上限、不递归） | M9（+M7、M8） | DD-M9、DD-M7、DD-M8 |
| US-06 | 会话始终体现"团队公共大脑"的身份与边界 | M1（子能力 M0） | DD-M0、DD-M1 §C-1 |
| US-07 | 规则按权限等级适用，人读条文不外泄，缺标注不丢条文 | M1b | DD-M1b |
| US-08 | 固定提示部分逐字节稳定，复用模型前缀缓存 | M1 | DD-M1 §C-1、§E（自检 E3） |
| US-09 | 三书更新后进行中会话自动获得新摘要（失败保留原值） | M1 | DD-M1 §A-3（`refresh()` 热更新） |
| US-10 | 简单诉求不因编排能力而变慢，且编排可开关 | M7、M8 | DD-M7 §C-3（`enabled` 开关）、DD-M8 §C |

**校验结论**：10 条 US 全部有 DD 章节落地；9 个模块全部承载至少 1 条 US，**无越界模块、无未设计 US**（与 SD V1「追溯矩阵（US → 模块）」一致）。

> 完整 `AC-US-xx.y`（PRD §2.1 共 30 条）的用例覆盖与 `US → 模块 → TC` 三向闭合校验见配套文档 [三书认知装配层_验收计划_V1.md](三书认知装配层_验收计划_V1.md)。

---

## 六、PRD 附录 B 应答（方案型技术问题 → 本 DD 章节）

> PRD 附录 B 登记了 11 项"用哪个方案实现"的问题；本节逐条给出**本轮设计的选定方案**及落地章节，闭合"规格 → 计划 → DD"的消费链。

| # | PRD 附录 B 问题 | 选定方案 | 落地章节 | 选型依据 |
|---|----------------|---------|---------|---------|
| 1 | 三类常驻摘要的裁剪结果存放位置 | **不新增存储**：三书原文缓存于 `SessionContext` 内存字段，摘要按每条消息实时裁剪（纯函数，零 IO） | DD-M1 §A | 摘要随操作者变化，持久化无意义；避免引入冗余副本 |
| 2 | 世界书是否扩展按节点分段骨架；存量如何兼容 | 在 `content_json.structure` 内新增 `node_segments` 键（**JSON 内容扩展，无列变更**）；缺键时降级为统计摘要；存量经迁移脚本重跑 Builder 补齐 | DD-M1a §A/§B、脚本 §`migrate_world_book_segments.py` | 复用既有 JSON 列，零 DDL；兼容策略满足 PRD 约束 #9 |
| 3 | 规则书适用等级的承载形式 | 章节级 YAML frontmatter（`applicable_levels` / `llm_inject`），解析用已有 `pyyaml` | DD-M1b §A/§B | 人读友好、不破坏规则书文件属性；满足 PRD 约束 #8 |
| 4 | 裁剪与渲染逻辑落位哪一层 | 裁剪为**纯函数**（Session/Fetcher 层）+ 变量装配在 Session 层；数据读取仍在 Repository / Service | DD-M1a/M1b/M1c/M1 §A | 满足 PRD 约束 #5 与宪法 C2 |
| 5 | 按需查阅的检索方式 | 关键词子串匹配（大小写不敏感，按行保留命中） | DD-M2 §A | 零额外延迟、结果可预期；语义检索列非目标 |
| 6 | 编排组件新建还是扩展现有方法 | **新建独立组件**（与 FocusLock / ConfirmQueue 同级注入） | DD-M7 §A/§C | 满足 US-10.3 的独立开关与回退要求 |
| 7 | 依赖序执行的调度实现 | 拓扑分层 + 层内独立任务并行（`asyncio.create_task` 隔离执行上下文）；异常时可降级为层内顺序 | DD-M8 §B | 兼顾"无依赖可并行"与上下文隔离 |
| 8 | 动态补齐的目标能力选定机制 | **不钉选**：补齐计划为兜底类，由执行层在只读白名单内自主选择 | DD-M9 §B | 避免改动执行层契约（PRD 约束 #4 的延伸） |
| 9 | 新增配置项命名与默认值 | `orchestrator_enabled=True` / `orchestrator_max_depth=3` / `orchestrator_max_dynamic_wis=2` | DD-M8 §C | 与既有配置命名风格一致 |
| 10 | 三书存量数据的补齐手段 | 提供一次性迁移脚本重跑 Builder（幂等：版本号自增），**不写 DDL** | 脚本 §`migrate_world_book_segments.py` | 可回滚、可 dry-run |
| 11 | 大项目场景下的体量控制 | **不截断节点骨架**（截断会破坏权限裁剪正确性）；`content_text` 保持精简，会话侧只取摘要；体量风险登记为 R3 | DD-风险清单 R3 | 正确性优先于体积；避免静默裁剪导致权限漏判 |

**未应答项**：**无**。

---

## 七、修订记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-09-10 | 初版（新规格体系）。基于 [PRD V1](三书认知装配层_PRD_V1.md)（纯规格）+ [SD V1](三书认知装配层_计划_V1.md) 产出 21 个文件的实现级设计；新增 §六「PRD 附录 B 应答」逐条闭合 11 项方案型技术问题；§五 追溯矩阵按 PRD 的 `US-01~US-10` 与 SD 的模块划分重列 |

---

*本 DD 为详细设计，基于 [三书认知装配层_计划_V1.md](三书认知装配层_计划_V1.md) 落地。编码完成须逐模块执行本文件自检命令。*
