# 对话流优化 — AI 执行计划

> **基于需求**：[对话流优化_需求_V1.md](需求/对话流优化_需求_V1.md)
> **计划版本**：v1.0
> **目标**：把单轮简单查询的同步 LLM 调用从 3 次降到 1-2 次、system prompt 从 5000+ 降到 ≤2000、查询类 SOP 回复成功率恢复到 ≥95%，并消除 reasoner 滥用与历史消息污染

---

## 你的角色

你作为 **Emily 开发者资深架构师** + **实施计划编制专家** + **LLM 调度优化工程师**，严格按以下模块顺序执行，逐模块验证，验证不通过不进入下一个模块。

P0（param_extractor chat_json 签名 bug）**已修复**，本计划仅覆盖 P1（M1-M3）与 P2（M4-M6）。

---

## 硬约束（违反即失败）

1. **不改已有方法签名**：`LLMClient.chat_messages` / `SessionAgent.handle` / `WorkItemAgent.node_handlers` 等已有方法签名只允许**新增可选参数**（带默认值），不修改现有参数顺序与含义
2. **分层不可跳**：`API → EmilyCore → Session → WorkItem → Application → Service → Repository → DB`。模型选择/prompt 组装属 Session/WorkItem 层，不污染 Service/Repository
3. **业务内核独立**：`emily_core` 不 import 任何 `astrbot.*` 包
4. **Sync repo + `asyncio.to_thread`**：Repository 全 sync，async Service 用 `asyncio.to_thread()` 包裹
5. **每模块验收**：每个模块验收必须通过（emy-test + 日志检查），否则停止并报告
6. **参照模式**：所有新代码参照下方"代码模式参照表"中的源文件，风格不一致视为失败
7. **reasoner 温度限制**：`deepseek-reasoner` 不支持 `temperature` 参数（传了 400），所有按调用覆盖模型的代码必须沿用 [client.py:117](emily-core/emily_core/infrastructure/llm/client.py#L117) 的 `"reasoner" not in model` 守门逻辑
8. **不改 SOP/Skill 业务语义**：M2 仅在路由侧派生 `query_type` 并预填，不改 SOP-005-QRY.skill.yaml 的 step 定义、不改 query_data handler 签名
9. **回退链保留**：所有优化必须保留 LLM 不可用时的降级路径（参照 [workitem_agent.py:822](emily-core/emily_core/workitem/workitem_agent.py#L822) 硬编码兜底），不破坏 fail-open
10. **P2 在 P1 验收后执行**：M4/M5/M6 依赖 M1-M3 的基线稳定，不得跳过 P1 直接做 P2

---

## 上下文（执行前必读）

### 现状画像（2026-07-24 实测）

| 调用 # | 节点 | model | prompt+completion | 问题 |
|--------|------|-------|-------------------|------|
| 0 / 3 | 路由（SessionAgent._recognize_intent） | reasoner | 5210+129 / 5256+182 | 结构化任务用 reasoner，杀鸡用牛刀；system prompt 含三书全文+SOP目录+工具列表 |
| 1 / 4 | 合成（WorkItemAgent._llm_synthesize_reply） | reasoner | 2193+95 / 2240+115 | step-01 失败后只能合成"抱歉"（P0 已修） |
| 2 / 5 | 审核（RealGuardian.review_reply） | reasoner | 340+63 / 345+90 | 审"抱歉"句，几乎永远 `{"issues":[]}`，纯浪费 |
| 6 | 摘要（SessionContext._consolidate_conversation_summary） | reasoner | 602+759 | 异步归档，本轮不阻塞，保留 reasoner |

### 已有的可复用组件

| 组件 | 位置 | 关键方法 | 本次怎么用 |
|------|------|----------|-----------|
| `LLMClient` | [client.py:19](emily-core/emily_core/infrastructure/llm/client.py#L19) | `chat_messages(messages, *, json_mode, tools, temperature, max_tokens)` | M1：新增 `model` 可选参数；`__init__` 新增 `router_model`/`guardian_model` |
| `Config` | [config.py:7](emily-core/emily_core/config.py#L7) | `llm_model` / `llm_temperature` | M1：新增 `llm_router_model` / `llm_guardian_model` 字段 |
| `EmilyCore` | [__init__.py:135](emily-core/emily_core/__init__.py#L135) | LLMClient 初始化处 | M1：传入 `router_model` / `guardian_model` |
| `SessionAgent._recognize_intent` | [session_agent.py:243](emily-core/emily_core/session/session_agent.py#L243) | `chat_messages(full_messages, json_mode=True)` | M1：传 `model=router_model`；M2：解析 `query_type` |
| `SessionAgent._split_into_workitems` | [session_agent.py:339](emily-core/emily_core/session/session_agent.py#L339) | 构造 WorkItem | M2：把 `query_type` 挂到 WorkItem |
| `WorkItemAgent.node4_summary` | [workitem_agent.py:648](emily-core/emily_core/workitem/workitem_agent.py#L648) | 调 `review_reply` | M3：加风险门控，L1 跳过 |
| `WorkItemAgent._skill_to_execution_plan` | [workitem_agent.py:134](emily-core/emily_core/workitem/workitem_agent.py#L134) | 硬编码 `risk_level="L2"` | M3：按工具类型判定 L1/L2 |
| `WorkItemAgent._execute_skill` | [workitem_agent.py:159](emily-core/emily_core/workitem/workitem_agent.py#L159) | 构造 SkillExecutionContext | M2：传 `prefilled_params` |
| `SkillExecutor.execute` | [executor.py:46](emily-core/emily_core/skill/executor.py#L46) | `resolve_params` | M2：prefilled_params 预填跳过 LLM |
| `SkillExecutionContext` | [executor.py:24](emily-core/emily_core/skill/executor.py#L24) | dataclass | M2：新增 `prefilled_params` 字段 |
| `ParamExtractor._extract_from_user_input` | [param_extractor.py:120](emily-core/emily_core/skill/param_extractor.py#L120) | `chat_json(prompt, user_input)` | P0 已修；M2 不改此处 |
| `RealGuardian.review_reply` | [real_guardian.py:110](emily-core/emily_core/workitem/pipeline/real_guardian.py#L110) | `chat_json(prompt, user_message)` | M1：传 `model=guardian_model` |
| `SessionContext.build_llm_messages` | [session_context.py:303](emily-core/emily_core/session/session_context.py#L303) | `full_messages.extend(self.message_history)` | M5：按 sender 过滤 |
| `SessionContext.record_turn` | [session_context.py:281](emily-core/emily_core/session/session_context.py#L281) | 窗口 > 40 触发压缩 | M5：阈值 40→20，加每 N 轮 consolidate |
| `SessionContext._consolidate_conversation_summary` | [session_context.py:418](emily-core/emily_core/session/session_context.py#L418) | 归档时合并摘要 | M5：抽取为可独立调用的方法 |

### 架构决策

1. **M1 选"按调用传 model override"而非"多 LLMClient 实例"**：多实例要改 EmilyCore 初始化、SessionAgent/WorkItemAgent 构造签名（违反硬约束 1 的精神）。单实例 + `model` 可选参数改动最小，且 `chat_messages` 已有 `temperature`/`max_tokens` 可选参数的先例，风格一致。
2. **M2 选"路由派生 query_type + SkillExecutor 预填"而非"改 Skill YAML 把 query_type 改 source=context"**：改 YAML 要动 9 个 Skill 文件且 query_type 本质来自用户语义；路由阶段 LLM 已在判断 SOP，顺带输出 query_type 零额外成本，且保留 Skill YAML 的声明式参数定义不变。
3. **M3 选"按工具类型判定 L1"而非"按 SOP 编号后缀"**：工具类型（query_*/knowledge_search vs record_*/create_*/discard_*）比 SOP 编号更通用，能覆盖未来新增 SOP，且与 [workitem_agent.py grade_risk](emily-core/emily_core/workitem/workitem_agent.py#L935) 的 operation_type 分级思路一致。
4. **M4 选"prompt 物理拆文件"而非"运行时按节点裁剪同一模板"**：拆文件后每个节点加载自己的 .md，职责清晰、可独立调优；运行时裁剪需维护裁剪规则，易腐化。
5. **M6 标记为"评估模块"**：快路径合并涉及管道架构变动，先评估 function calling 可行性再决定是否实施，避免盲目改造。

### 代码模式参照表

| 层 | 参照源（精确文件路径） | 要模仿的要点 |
|----|----------------------|-------------|
| LLMClient 可选参数 | [client.py:64-72](emily-core/emily_core/infrastructure/llm/client.py#L64-L72) | `*, json_mode, tools, temperature, max_tokens` keyword-only + None 默认值 |
| reasoner 温度守门 | [client.py:117](emily-core/emily_core/infrastructure/llm/client.py#L117) | `if "reasoner" not in (model or ""):` 判断是否传 temperature |
| Config dataclass 字段 | [config.py:35-41](emily-core/emily_core/config.py#L35-L41) | `field: type = default` + docstring 注释 |
| SkillExecutionContext dataclass | [executor.py:24-37](emily-core/emily_core/skill/executor.py#L24-L37) | `field(default_factory=...)` 用于可变默认值 |
| WorkItem 属性挂载 | [session_agent.py:370](emily-core/emily_core/session/session_agent.py#L370) | `setattr(wi, "_confirm_action", action)` 模式 |
| Guardian chat_json 调用 | [real_guardian.py:122-125](emily-core/emily_core/workitem/pipeline/real_guardian.py#L122-L125) | `chat_json(prompt, user_message)` 两参正确用法 |
| 风险等级判定 | [workitem_agent.py:948-960](emily-core/emily_core/workitem/workitem_agent.py#L948-L960) | `grade_risk` 按 intent_type/operation_type 分级 |
| prompt 模板变量替换 | [session_agent.py:264-273](emily-core/emily_core/session/session_agent.py#L264-L273) | `.replace("{key}", value)` 两阶段 format |

---

## 模块依赖图

```
P1（独立可并行，基线优化）
M1(模型按节点选择) ──┐
M2(路由派生query_type)─┼── 三者无相互依赖，可任意顺序
M3(Guardian风险门控) ──┘
                       │
                       v
P2（依赖 P1 基线稳定）
              M4(prompt分层裁剪) ── 依赖 M1/M2/M3 验收后，回复质量回归基线
              M5(历史过滤+摘要提频) ── 独立，可与 M4 并行
              M6(快路径合并评估) ── 依赖 M2（query_type 路由派生），产出评估报告
```

**构建顺序**：M1 → M2 → M3（P1 验收）→ M4 → M5 → M6（P2 验收）。M1/M2/M3 理论可并行，但建议串行以便逐模块回归。

---

## 交付物总览

| 模块 | 交付文件 | 新增/修改 | 核心改动 |
|------|----------|----------|----------|
| M1 | [client.py](emily-core/emily_core/infrastructure/llm/client.py) | 修改 | `chat_messages` 加 `model` 参数；`__init__` 加 `router_model`/`guardian_model` |
| M1 | [config.py](emily-core/emily_core/config.py) | 修改 | 加 `llm_router_model`/`llm_guardian_model` 字段 |
| M1 | [__init__.py](emily-core/emily_core/__init__.py) | 修改 | LLMClient 初始化传 `router_model`/`guardian_model` |
| M1 | [session_agent.py](emily-core/emily_core/session/session_agent.py) | 修改 | `_recognize_intent` 传 `model=router_model` |
| M1 | [real_guardian.py](emily-core/emily_core/workitem/pipeline/real_guardian.py) | 修改 | `review_reply`/`review_step` 传 `model=guardian_model` |
| M2 | [session.md](emily-data/prompts/session.md) | 修改 | 路由输出格式增加 `query_type` 字段说明 |
| M2 | [session_agent.py](emily-core/emily_core/session/session_agent.py) | 修改 | 解析 `query_type` 并挂到 WorkItem |
| M2 | [executor.py](emily-core/emily_core/skill/executor.py) | 修改 | `SkillExecutionContext` 加 `prefilled_params`；`execute` 预填 |
| M2 | [workitem_agent.py](emily-core/emily_core/workitem/workitem_agent.py) | 修改 | `_execute_skill` 传 `prefilled_params` |
| M3 | [workitem_agent.py](emily-core/emily_core/workitem/workitem_agent.py) | 修改 | `_skill_to_execution_plan` 按工具判 L1；`node4_summary` 风险门控 |
| M4 | `emily-data/prompts/session_routing.md` | 新增 | 路由专用精简 prompt |
| M4 | [session_agent.py](emily-core/emily_core/session/session_agent.py) | 修改 | `_recognize_intent` 改用 routing.md |
| M4 | [session.md](emily-data/prompts/session.md) | 修改 | 标注为 fallback/元认知路径用 |
| M5 | [session_context.py](emily-core/emily_core/session/session_context.py) | 修改 | `build_llm_messages` 加 sender 过滤；`record_turn` 阈值 20；摘要提频 |
| M6 | `需求/对话流优化_快路径评估_V1.md` | 新增 | 评估报告（非代码） |

---

## 现有模块改动清单

| 现有模块 | 改动类型 | 改动内容 |
|----------|----------|----------|
| [client.py](emily-core/emily_core/infrastructure/llm/client.py) | 修改 | `chat_messages` 签名加 `model` 参数；内部用 `effective_model`；trace callback 用 `effective_model` |
| [config.py](emily-core/emily_core/config.py) | 扩展 | 新增 2 个字段 |
| [__init__.py](emily-core/emily_core/__init__.py) | 修改 | LLMClient 初始化多传 2 个参数 |
| [session_agent.py](emily-core/emily_core/session/session_agent.py) | 修改 | `_recognize_intent` 传 model + 解析 query_type；`_split_into_workitems` 挂 query_type；M4 改 prompt 加载 |
| [workitem_agent.py](emily-core/emily_core/workitem/workitem_agent.py) | 修改 | `_skill_to_execution_plan` 风险判定；`node4_summary` 门控；`_execute_skill` 传 prefilled |
| [real_guardian.py](emily-core/emily_core/workitem/pipeline/real_guardian.py) | 修改 | 两个 review 方法传 model |
| [executor.py](emily-core/emily_core/skill/executor.py) | 修改 | dataclass 加字段；execute 预填逻辑 |
| [session_context.py](emily-core/emily_core/session/session_context.py) | 修改 | sender 过滤；阈值；摘要提频 |
| [session.md](emily-data/prompts/session.md) | 修改 | M2 加 query_type 输出；M4 标注用途 |
| `emily-data/prompts/session_routing.md` | 新增 | M4 路由专用 prompt |
| [SOP-005-QRY.skill.yaml](emily-data/skills/SOP-005-QRY.skill.yaml) | 不变 | — |

---

## M1: 模型按节点选择（P1-1）

**依赖**：无

**职责**：让路由/审核节点用 `deepseek-chat`（快、便宜），合成/摘要保留 `deepseek-reasoner`（需推理）。通过 `chat_messages` 新增 `model` override 参数实现，不破坏现有签名。

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | Config 新增 2 个模型字段 | `emily-core/emily_core/config.py` |
| 2 | LLMClient 支持 router_model/guardian_model + model override | `emily-core/emily_core/infrastructure/llm/client.py` |
| 3 | EmilyCore 初始化灌注新字段 | `emily-core/emily_core/__init__.py` |
| 4 | SessionAgent 路由调用传 model | `emily-core/emily_core/session/session_agent.py` |
| 5 | RealGuardian 审核调用传 model | `emily-core/emily_core/workitem/pipeline/real_guardian.py` |

### 代码

#### `emily-core/emily_core/config.py` — 在 `llm_max_tokens` 字段后（第 41 行后）追加

```python
    llm_router_model: str = "deepseek-chat"
    """路由/意图识别用模型（轻量结构化任务，用 chat 而非 reasoner，快且省）"""

    llm_guardian_model: str = "deepseek-chat"
    """Guardian 审核用模型（轻量结构化任务，用 chat 而非 reasoner）"""
```

#### `emily-core/emily_core/infrastructure/llm/client.py` — 修改 `__init__`（第 30-44 行）

```python
    def __init__(
        self,
        api_key: str,
        base_url: str = "https://api.deepseek.com",
        model: str = "deepseek-chat",
        temperature: float = 0.1,
        max_tokens: int = 1024,
        router_model: str = "",
        guardian_model: str = "",
    ):
        self.model = model
        self.router_model = router_model or model
        self.guardian_model = guardian_model or model
        self.temperature = temperature
        self.max_tokens = max_tokens
        self._client = AsyncOpenAI(
            api_key=api_key,
            base_url=base_url,
        )
        # M11: LLM 交互追踪回调
        self._trace_callback: Callable | None = None
        logger.info(
            "LLMClient initialized: model=%s, router_model=%s, guardian_model=%s, base_url=%s, temperature=%.2f",
            model, self.router_model, self.guardian_model, base_url, temperature,
        )
```

#### `emily-core/emily_core/infrastructure/llm/client.py` — 修改 `chat_messages` 签名与内部 model 使用（第 64-118 行）

在 `chat_messages` 签名新增 `model: str | None = None`，并把内部所有 `self.model` 替换为 `effective_model`：

```python
    async def chat_messages(
        self,
        messages: list[dict],
        *,
        json_mode: bool = False,
        tools: list[dict] | None = None,
        temperature: float | None = None,
        max_tokens: int | None = None,
        model: str | None = None,
    ) -> dict:
        """多轮对话主入口 —— 接受完整 OpenAI 格式 messages 列表。

        综合 chat_json（JSON 输出）+ chat_with_tools（多轮 + 工具）的能力。
        chat / chat_json / chat_with_tools 内部均转调此方法。

        Args:
            messages: OpenAI 格式消息列表 [{"role":..., "content":..., ...}]
            json_mode: 是否强制 JSON 输出（response_format）
            tools: 工具定义列表（可选）
            temperature: 采样温度（None 则用默认值）
            max_tokens: 最大输出 token（None 则用默认值）
            model: 按调用覆盖模型（None 则用 self.model）。用于路由/审核等
                   轻量节点切换到 chat 模型，合成/摘要保留 reasoner

        Returns:
            dict:
                - json_mode=True:  {"type": "json", "data": {...}, "finish_reason": str}
                - json_mode=False: {"type": "text", "content": str, "finish_reason": str}
                如果有 tool_call:     {"type": "tool_call", "tool_name": str, "tool_arguments": dict, ...}
        """
        # M11: trace callback — start
        call_seq = getattr(self, "_trace_call_seq", 0) + 1
        self._trace_call_seq = call_seq
        call_type = "chat_messages" + ("_json" if json_mode else "")
        effective_model = model or self.model
        if self._trace_callback:
            try:
                self._trace_callback({
                    "phase": "start",
                    "call_type": call_type,
                    "call_sequence": call_seq,
                    "model": effective_model,
                    "message_count": len(messages),
                    "tool_count": len(tools) if tools else 0,
                    "json_mode": json_mode,
                })
            except Exception as e:
                logger.debug("trace callback start failed: %s", e, exc_info=True)

        t0 = time.time()

        kwargs = dict(
            model=effective_model,
            messages=messages,
            max_tokens=max_tokens if max_tokens is not None else self.max_tokens,
        )
        # deepseek-reasoner 不支持 temperature 等采样参数（传了会 400），仅 chat 类模型传
        if "reasoner" not in (effective_model or ""):
            kwargs["temperature"] = temperature if temperature is not None else self.temperature
        if json_mode:
            kwargs["response_format"] = {"type": "json_object"}
        if tools:
            kwargs["tools"] = tools
```

> **注意**：`chat_messages` 内部两处 trace callback 的 `"end"` phase（第 161 行、第 194 行）里的 `"model": self.model` 也要改为 `"model": effective_model`。用 Edit 工具把这两处 `self.model` 替换为 `effective_model`（在 `chat_messages` 方法体内）。

#### `emily-core/emily_core/__init__.py` — 修改 LLMClient 初始化（第 135-141 行附近）

```python
                self._llm_client = LLMClient(
                    api_key=self.config.llm_api_key,
                    base_url=self.config.llm_base_url,
                    model=self.config.llm_model,
                    temperature=self.config.llm_temperature,
                    max_tokens=self.config.llm_max_tokens,
                    router_model=getattr(self.config, "llm_router_model", "") or self.config.llm_model,
                    guardian_model=getattr(self.config, "llm_guardian_model", "") or self.config.llm_model,
                )
```

> `getattr` 兜底是为兼容旧 core_config.json 未包含新字段的情况。

#### `emily-core/emily_core/session/session_agent.py` — 修改 `_recognize_intent` 的 LLM 调用（第 326 行）

把：
```python
            result = await self._llm.chat_messages(full_messages, json_mode=True)
```
改为：
```python
            router_model = getattr(self._llm, "router_model", None) or getattr(self._llm, "model", None)
            result = await self._llm.chat_messages(full_messages, json_mode=True, model=router_model)
```

#### `emily-core/emily_core/workitem/pipeline/real_guardian.py` — 修改 `review_step` 与 `review_reply` 的 chat_json 调用

`review_step`（第 91-94 行）把：
```python
            data = await self._llm.chat_json(
                prompt,
                f"审核步骤: {getattr(step_result, 'step_id', '?')}",
            )
```
改为：
```python
            guardian_model = getattr(self._llm, "guardian_model", None) or getattr(self._llm, "model", None)
            data = await self._llm.chat_json(
                prompt,
                f"审核步骤: {getattr(step_result, 'step_id', '?')}",
                model=guardian_model,
            )
```

> **注意**：`chat_json` 目前签名是 `chat_json(self, system_prompt, user_message)`（[client.py:243](emily-core/emily_core/infrastructure/llm/client.py#L243)），**不接受 model 参数**。需同步给 `chat_json` 加 `model: str | None = None` 并转传给 `chat_messages`：

#### `emily-core/emily_core/infrastructure/llm/client.py` — 修改 `chat_json`（第 243-249 行）

```python
    async def chat_json(self, system_prompt: str, user_message: str, model: str | None = None) -> dict:
        """单轮对话，强制 JSON 输出，返回解析后的 dict。"""
        result = await self.chat_messages([
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_message},
        ], json_mode=True, model=model)
        return result["data"]
```

`review_reply`（第 122-125 行）同理加 `model=guardian_model`：
```python
            guardian_model = getattr(self._llm, "guardian_model", None) or getattr(self._llm, "model", None)
            data = await self._llm.chat_json(
                prompt,
                f"审核回复: {draft_reply[:100]}",
                model=guardian_model,
            )
```

### 模块验收检测

```bash
# 验收 1：清 pycache + 重启 emily-core
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +
docker compose -f docker-compose-napcat.yml restart emily-core

# 验收 2：触发一次查询（用 users 表真实用户）
docker exec emily-postgres psql -U emily -d emily -c "SELECT id, username FROM users WHERE status='active' LIMIT 3;"
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "今天项目有什么进展？" --sender "李景利"

# 验收 3：检查 LLM trace —— 路由调用 model 应为 deepseek-chat，合成仍为 deepseek-reasoner
docker exec mitmproxy tail -5 /app/logs/llm_trace.jsonl
→ 预期：路由条目（含 "sop_id" 响应）的 model 字段为 "deepseek-chat"；
   合成条目（含 "reply" 响应）的 model 字段仍为 "deepseek-reasoner"

# 验收 4：Guardian 审核 model 为 deepseek-chat
grep -c '"model": "deepseek-chat"' emily-data/logs/llm_trace.jsonl
→ 预期：≥1（审核调用存在且用 chat 模型）
```

**失败处理**：若验收 3 路由仍是 reasoner，检查 `session_agent.py:326` 是否正确传 `model=router_model`、`__init__.py` 是否灌注 `router_model`（`docker logs emily-core 2>&1 | grep "LLMClient initialized"` 应显示三个模型名）。

---

## M2: 路由派生 query_type（P1-2）

**依赖**：无（与 M1 独立，但建议在 M1 后做以便用 chat 模型跑路由）

**职责**：路由阶段 LLM 已判定 SOP-005-QRY，顺带输出 `query_type` 枚举值，存入 WorkItem；SkillExecutor 执行 step-01 时预填该值，跳过一次 LLM 参数提取往返。单轮查询的同步 LLM 调用从 3 次降为 2 次。

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | session.md 路由输出格式增加 query_type | `emily-data/prompts/session.md` |
| 2 | SessionAgent 解析 query_type 并挂 WorkItem | `emily-core/emily_core/session/session_agent.py` |
| 3 | SkillExecutionContext 加 prefilled_params | `emily-core/emily_core/skill/executor.py` |
| 4 | SkillExecutor.execute 预填逻辑 | `emily-core/emily_core/skill/executor.py` |
| 5 | _execute_skill 传 prefilled_params | `emily-core/emily_core/workitem/workitem_agent.py` |

### 代码

#### `emily-data/prompts/session.md` — 修改"输出要求"段（第 123-128 行）

把原"输出要求"段替换为：

```markdown
### 输出要求
仅输出一个 JSON 对象（不要包含其他文字）：
sop_id 为匹配的 SOP 编号或 null，confidence 为 high/medium/low/none，is_compound 为 false 或 true，
sub_tasks 为子任务数组，fallback 为 false（无匹配时为 true）。

对于复合请求，sub_tasks 数组中每项包含 sop_id 和 user_input 字段。

### query_type 派生规则（仅 SOP-005-QRY 命中时输出）
当 sop_id 为 "SOP-005-QRY" 时，必须额外输出 query_type 字段，根据用户查询意图从以下枚举中选择最匹配的一个：
- event：查询事件（如"今天有什么事件"、"最近的事件"）
- task：查询任务（如"有哪些待办任务"、"张三的任务"）
- meeting：查询会议（如"最近的会议"、"会议记录"）
- file：查询文件（如"有哪些文件"、"图纸"）
- message：查询通讯记录（如"刚才聊了什么"）
- conversation：查询会话（如"之前的对话"）
- user：查询用户（如"张三的信息"、"谁负责"）
- project：查询项目（如"项目概况"、"参建方"）
- summary：查询综合概况/进展（如"项目进展"、"整体情况"、"最近怎么样"）

判断不准时根据语义推断选最相关的。sop_id 非 SOP-005-QRY 时不要输出 query_type 字段。
```

#### `emily-core/emily_core/session/session_agent.py` — 修改 `_split_into_workitems`（第 407-414 行，单 SOP 分支）

把：
```python
        return [WorkItem(
            session_id=self.conversation_id,
            user_input=content,
            user_id=self.context.user_id,
            sop_id=sop_id,
            intent_type="sop",
            priority=1,
        )]
```
改为：
```python
        wi = WorkItem(
            session_id=self.conversation_id,
            user_input=content,
            user_id=self.context.user_id,
            sop_id=sop_id,
            intent_type="sop",
            priority=1,
        )
        # M2: 路由派生的 query_type 预填给 SkillExecutor，省掉 step-01 的 LLM 参数提取
        query_type = intent.get("query_type")
        if sop_id == "SOP-005-QRY" and query_type:
            setattr(wi, "_prefilled_params", {"query_type": query_type})
            logger.debug("Session[%s] prefilled query_type=%s for SOP-005-QRY",
                         self.conversation_id, query_type)
        return [wi]
```

> 复合请求分支（第 393-405 行）的 sub_tasks 循环里也可类似挂载，但首版仅处理单 SOP 主路径；复合请求保留原 LLM 提取兜底。

#### `emily-core/emily_core/skill/executor.py` — 修改 `SkillExecutionContext`（第 24-37 行）

在 dataclass 末尾新增字段：
```python
@dataclass
class SkillExecutionContext:
    """Skill 执行上下文。"""

    skill: SkillDefinition
    user_input: str
    user_id: str
    message_id: str
    conversation_id: str
    session_context: dict          # 从 SessionContext 扁平化
    step_results: dict[str, dict]  # output_key → 前步 business_data
    business_flow_tools: BusinessFlowToolRegistry
    llm_client: Any = None         # ParamExtractor 用
    session_available_tools: list[dict] = field(default_factory=list)
    prefilled_params: dict = field(default_factory=dict)  # M2: 路由派生的预填参数
```

#### `emily-core/emily_core/skill/executor.py` — 修改 `execute` 方法（第 99-105 行，参数解析处）

把：
```python
                # 3. 解析参数 —— 区分 Skill 推荐路径 vs 元能力路径
                if step.tool_name in allowed_tools:
                    # Skill 推荐路径：有 ParamMapping，走现有逻辑
                    tool_params = await self._param_extractor.resolve_params(
                        step.tool_params, ctx.user_input, ctx.session_context, ctx.step_results,
                    )
```
改为：
```python
                # 3. 解析参数 —— 区分 Skill 推荐路径 vs 元能力路径
                if step.tool_name in allowed_tools:
                    # Skill 推荐路径：有 ParamMapping，走现有逻辑
                    # M2: 先剥离路由预填的参数，剩余参数才走 ParamExtractor
                    prefilled = ctx.prefilled_params or {}
                    pending_mappings = {
                        pname: m for pname, m in step.tool_params.items()
                        if pname not in prefilled
                    }
                    tool_params = dict(prefilled)  # 预填参数直接注入
                    if pending_mappings:
                        resolved = await self._param_extractor.resolve_params(
                            pending_mappings, ctx.user_input, ctx.session_context, ctx.step_results,
                        )
                        tool_params.update(resolved)
```

> **注意**：`resolve_params` 内部对 required 参数提取失败会 `raise ValueError`（[param_extractor.py:62](emily-core/emily_core/skill/param_extractor.py#L62)）。预填剥离后，SOP-005-QRY step-01 的 `query_type` 不再进入 resolve_params，不会再触发 P0 那样的失败链。`time_range`/`project_name` 非必填有 default，仍走 LLM 提取但失败也不阻断。

#### `emily-core/emily_core/workitem/workitem_agent.py` — 修改 `_execute_skill`（第 185-196 行）

在 `SkillExecutionContext(...)` 构造里加 `prefilled_params`：
```python
        # M2: 从 WorkItem 读取路由派生的预填参数
        prefilled_params = dict(getattr(wi, "_prefilled_params", {}) or {})

        skill_ctx = SkillExecutionContext(
            skill=skill,
            user_input=wi.user_input,
            user_id=context.user_id if hasattr(context, "user_id") else "",
            message_id=context.db_message_id if hasattr(context, "db_message_id") else "",
            conversation_id=context.message.conversation_id if context.message else "",
            session_context=session_context_dict,
            step_results={},
            business_flow_tools=self._business_flow_tools,
            llm_client=self._llm,
            session_available_tools=session_available_tools,
            prefilled_params=prefilled_params,
        )
```

### 模块验收检测

```bash
# 验收 1：重启
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +
docker compose -f docker-compose-napcat.yml restart emily-core

# 验收 2：查询测试
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "今天项目有什么进展？" --sender "李景利"
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "当前有哪些运转中的节点？" --sender "李景利"

# 验收 3：日志确认 query_type 预填 + step-01 不再 FAIL
docker logs --tail 50 emily-core 2>&1 | grep -E "prefilled query_type|step-01|param_extractor"
→ 预期：出现 "prefilled query_type=summary"（或 event/project 等枚举值）；
   不再出现 "Step step-01 failed: 必填参数 'query_type' 提取失败"

# 验收 4：单轮 LLM 调用数减少（路由 + 合成 = 2 次，审核由 M3 处理；M3 未做前仍 3 次）
docker exec mitmproxy grep -c "" /app/logs/llm_trace.jsonl
→ 预期：新一轮查询产生的 jsonl 行数较 M1 前减少（query_type 提取的那次 LLM 调用消失）

# 验收 5：回复不再是"抱歉"
→ 预期：emy-test 输出含实际查询结果或"未查到相关记录"，非"参数错误/暂时无法获取"
```

**失败处理**：若验收 3 仍出现 step-01 FAIL，检查 `executor.py` 的 `pending_mappings` 剥离逻辑是否正确排除 query_type、`workitem_agent.py` 是否传了 `prefilled_params`、`session_agent.py` 是否在单 SOP 分支挂载 `_prefilled_params`。

---

## M3: Guardian 风险门控（P1-3）

**依赖**：无

**职责**：L1 查询类 WorkItem 跳过 `review_reply` 审核（实测几乎永远 `{"issues":[]}`，纯浪费）。通过工具类型判定风险等级。

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | `_skill_to_execution_plan` 按工具判风险 | `emily-core/emily_core/workitem/workitem_agent.py` |
| 2 | `node4_summary` 风险门控 | `emily-core/emily_core/workitem/workitem_agent.py` |

### 代码

#### `emily-core/emily_core/workitem/workitem_agent.py` — 修改 `_skill_to_execution_plan`（第 134-157 行）

把硬编码 `risk_level="L2"` 改为按 step 的 tool_name 判定：
```python
    @staticmethod
    def _skill_to_execution_plan(skill) -> ExecutionPlan:
        """将 SkillDefinition 转换为 ExecutionPlan。"""
        from ..workitem.pipeline.interfaces.planning import PlanStep, ExecutionPlan

        steps = [
            PlanStep(
                step_id=s.id,
                description=s.description,
                tool_name=s.tool_name,
                tool_params={},  # 由 SkillExecutor 在执行时从 ParamMapping 解析
                expected_output="",
                depends_on=[],
            )
            for s in skill.steps
        ]

        # M3: 按工具类型判定风险等级
        # L1(查询类): 只调 query_*/knowledge_search/chat_archive/query_files
        # L2(录入类): 调 record_*/create_*/update_*/submit_*/confirm_*/mount_*/activate_*
        # L3(高风险): 调 discard_*/return_*/delete_*
        tool_names = {s.tool_name for s in skill.steps if s.tool_name}
        risk_level = WorkItemAgent._grade_skill_risk(tool_names)

        return ExecutionPlan(
            risk_level=risk_level,
            steps=steps,
            acceptance_criteria=[],
            estimated_steps=len(steps),
            _source="skill_definition",
        )

    @staticmethod
    def _grade_skill_risk(tool_names: set[str]) -> str:
        """根据 Skill 涉及的工具集合判定风险等级。"""
        if not tool_names:
            return "L1"  # 纯逻辑步骤（如 SOP-005 step-02/03），视为查询类
        l1_tools = {"query_data", "query_files", "query_node", "query_my_nodes",
                    "knowledge_search", "chat_archive", "fetch_inbox"}
        l3_tools = {"discard_nodes", "return_node_deliverable"}
        # 任一工具是 L3 → 整体 L3
        if tool_names & l3_tools:
            return "L3"
        # 全部工具都是 L1 → L1
        if tool_names <= l1_tools:
            return "L1"
        # 含 record_*/create_*/update_*/submit_*/confirm_*/mount_*/activate_* → L2
        return "L2"
```

#### `emily-core/emily_core/workitem/workitem_agent.py` — 修改 `node4_summary`（第 667-676 行）

把：
```python
        # Guardian 出站审核 —— 只标记不拦截
        if self._guardian:
            try:
                note = await self._guardian.review_reply(draft, wi)
                if note and note.issues:
                    for issue in note.issues:
                        wi.add_warning(f"[reply] {issue}")
            except Exception as e:
                logger.debug("Guardian review_reply failed (silent skip): %s", e)
```
改为：
```python
        # Guardian 出站审核 —— 只标记不拦截
        # M3: L1 查询类跳过审核（实测几乎永远 {"issues":[]}，省一次 LLM 调用）
        risk_level = getattr(wi, "risk_level", "L2") or "L2"
        should_review_reply = risk_level != "L1"
        if self._guardian and should_review_reply:
            try:
                note = await self._guardian.review_reply(draft, wi)
                if note and note.issues:
                    for issue in note.issues:
                        wi.add_warning(f"[reply] {issue}")
            except Exception as e:
                logger.debug("Guardian review_reply failed (silent skip): %s", e)
        elif not should_review_reply:
            logger.debug("WI %s node4: skip reply review (risk=L1 query-only)", wi.id)
```

> **注意**：`node3_execute` 里的 step 级 Guardian 审核（[workitem_agent.py:400-432](emily-core/emily_core/workitem/workitem_agent.py#L400-L432)）保留不变——step 级审核针对有实质数据的步骤，L1 查询若有 RAG 结果仍需审幻觉。M3 只关掉 node4 的 reply 审核。

### 模块验收检测

```bash
# 验收 1：重启
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +
docker compose -f docker-compose-napcat.yml restart emily-core

# 验收 2：查询测试（应跳过 reply 审核）
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "今天项目有什么进展？" --sender "李景利"

# 验收 3：日志确认跳过
docker logs --tail 30 emily-core 2>&1 | grep -E "skip reply review|review_reply"
→ 预期：出现 "skip reply review (risk=L1 query-only)"；不出现 L1 查询的 review_reply LLM 调用

# 验收 4：录入类仍审核
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我创建事件：样板段放线完成" --sender "李景利"
docker logs --tail 30 emily-core 2>&1 | grep -E "review_reply|Guardian"
→ 预期：录入类（record_event，L2）仍触发 review_reply

# 验收 5：单轮查询 LLM 调用数 = 2（路由 + 合成）
docker exec mitmproxy grep -c "" /app/logs/llm_trace.jsonl
→ 预期：新一轮查询仅 2 次同步 LLM 调用（M1+M2+M3 合计效果）
```

**失败处理**：若验收 4 录入类也跳过审核，检查 `_grade_skill_risk` 的 L1 工具集合是否误含 record_*；若查询类仍审核，检查 `wi.risk_level` 是否从 `execution_plan.risk_level` 正确传导（[workitem_agent.py:240](emily-core/emily_core/workitem/workitem_agent.py#L240) `wi.risk_level = plan.risk_level`）。

---

## P1 组装验证

M1+M2+M3 完成后，端到端回归：

```bash
# 清缓存重启
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +
docker compose -f docker-compose-napcat.yml restart emily-core

# 三类场景回归
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "今天项目有什么进展？" --sender "李景利"
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "当前有哪些运转中的节点？" --sender "李景利"
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我创建事件：样板段放线完成" --sender "李景利"

# 验证 P1 三项指标
docker exec mitmproxy tail -10 /app/logs/llm_trace.jsonl
```

**P1 通过标准**：
- 查询类：2 次同步 LLM 调用（路由 deepseek-chat + 合成 deepseek-reasoner），无审核调用
- 录入类：3 次同步 LLM 调用（路由 chat + 合成 reasoner + 审核 chat）
- 查询回复含实际结果或"未查到"，非"抱歉/参数错误"
- 日志无 `param_extractor` / `step-01 failed` WARNING

**P1 未通过则停止 P2**，先修复 P1 偏差。

---

## M4: system prompt 分层裁剪（P2-1）

**依赖**：M1 + M2 + M3（P1 验收通过，确保回复质量基线稳定，再做 prompt 瘦身防回归）

**职责**：把 session.md（5210 tokens）拆为路由专用精简版 session_routing.md（≤1500 tokens），去掉三书全文/工具列表/系统自我描述（这些路由不需要）。session.md 保留给 fallback/元认知路径。合成 prompt（workitem.md，2240 tokens）本轮暂不动（已相对精简，M4 聚焦路由）。

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | 路由专用精简 prompt | `emily-data/prompts/session_routing.md` |
| 2 | SessionAgent 路由改用新 prompt | `emily-core/emily_core/session/session_agent.py` |
| 3 | session.md 标注用途变更 | `emily-data/prompts/session.md` |

### 代码

#### `emily-data/prompts/session_routing.md` — 新建

```markdown
<!-- 路由专用精简 system prompt —— SessionAgent._recognize_intent() 使用 -->
<!-- M4: 从 session.md 裁剪，仅保留路由必需内容。三书全文/工具列表/系统自我描述移除，
     降级 fallback 路径仍用 session.md（含三书，供 LLM 基于系统自我描述回答元认知问题） -->
<!-- 模板变量（阶段1 直接 replace）: {sop_catalog}, {current_datetime} -->
<!-- 模板变量（阶段2 Session 级）: {user_name} {user_permission_level} {project_name} {conversation_summary} -->

## 一、角色定位（简）

你是 Emily 路由引擎，负责分析用户消息的核心意图并匹配到最合适的业务流程（SOP）。
你只做意图识别和路由，不直接回答业务问题、不调用工具。

## 二、当前上下文

### 用户
- 姓名：{user_name}
- 权限：{user_permission_level}

### 项目
- 名称：{project_name}

### 对话记忆
{conversation_summary}

### 当前时间
{current_datetime}

## 三、可用业务流程目录
{sop_catalog}

## 四、路由规则

1. 仔细分析用户消息的**核心意图**，而非表面关键词
2. 闲聊优先直接回复——问候/感谢/告别/自我介绍等不需要调用任何工具，直接友好回复
3. 如果消息包含多个独立请求（如"查一下A，然后处理B"），标记为复合请求 (is_compound=true)
4. 如果没有任何 SOP 能匹配用户意图，设置 fallback=true
5. 置信度判断标准：
   - high: 用户明确表达了某个业务意图，关键词高度匹配
   - medium: 用户意图可以推断但不够明确
   - low: 用户表达模糊，可能匹配多个 SOP
   - none: 无法匹配任何 SOP

6. **上下文确认响应规则**（TC-J03）：
   如果系统提示"当前存在待确认的录入项"，且用户消息表达了确认/取消/修改意图
   （如"确认"、"好的"、"嗯"、"可以"、"行"、"没问题"、"取消"、"算了"、"不对"、
   "改一下"、"不要了"等），必须输出 sop_id="SYS-confirm"，confidence="high"，
   并在 data 中指明具体操作。不要走其他 SOP 路由。
   - 用户确认 / 同意 → data.action="confirm"
   - 用户取消 / 放弃 → data.action="cancel"

7. **系统自我描述类问题**（bypass 规则）：
   如果用户询问关于 Emily 自身的权限分级、文件分类、功能范围、操作流程等元认知问题
   （如"权限是怎么分级的？"、"文件是怎么分类的？"、"你能做什么？"），设 fallback=true，
   SessionAgent 会在 fallback 路径中基于系统自我描述文本回答。

## 五、输出要求

仅输出一个 JSON 对象（不要包含其他文字）：
sop_id 为匹配的 SOP 编号或 null，confidence 为 high/medium/low/none，is_compound 为 false 或 true，
sub_tasks 为子任务数组，fallback 为 false（无匹配时为 true）。

对于复合请求，sub_tasks 数组中每项包含 sop_id 和 user_input 字段。

### query_type 派生规则（仅 SOP-005-QRY 命中时输出）
当 sop_id 为 "SOP-005-QRY" 时，必须额外输出 query_type 字段，根据用户查询意图从以下枚举中选择最匹配的一个：
event/task/meeting/file/message/conversation/user/project/summary
- event：查询事件
- task：查询任务
- meeting：查询会议
- file：查询文件
- message：查询通讯记录
- conversation：查询会话
- user：查询用户
- project：查询项目概况
- summary：查询综合概况/进展（如"项目进展"、"整体情况"）

判断不准时根据语义推断选最相关的。sop_id 非 SOP-005-QRY 时不要输出 query_type 字段。
```

#### `emily-core/emily_core/session/session_agent.py` — 修改 prompt 加载（第 52-57 行）

把：
```python
def _load_session_prompt() -> str:
    from ..infrastructure.llm.prompt_loader import load_prompt
    return load_prompt("session")


_SESSION_SYSTEM_PROMPT = _load_session_prompt()
```
改为：
```python
def _load_session_prompt() -> str:
    """fallback/元认知路径用（含三书全文）。"""
    from ..infrastructure.llm.prompt_loader import load_prompt
    return load_prompt("session")


def _load_routing_prompt() -> str:
    """M4: 路由专用精简 prompt（不含三书全文）。"""
    from ..infrastructure.llm.prompt_loader import load_prompt
    return load_prompt("session_routing")


_SESSION_SYSTEM_PROMPT = _load_session_prompt()
_ROUTING_SYSTEM_PROMPT = _load_routing_prompt()
```

#### `emily-core/emily_core/session/session_agent.py` — 修改 `_recognize_intent`（第 264-266 行）

把：
```python
        system_prompt = (_SESSION_SYSTEM_PROMPT
            .replace("{sop_catalog}", sop_catalog)
            .replace("{current_datetime}", _beijing_now_str()))
```
改为：
```python
        # M4: 路由用精简 prompt（≤1500 tokens），不含三书全文
        system_prompt = (_ROUTING_SYSTEM_PROMPT
            .replace("{sop_catalog}", sop_catalog)
            .replace("{current_datetime}", _beijing_now_str()))
```

> **注意**：`_recognize_intent` 后续的 `prompt_vars` 替换（第 270-273 行）会把 session 级变量逐个 replace 到 routing prompt 里。session_routing.md 只用了 `{user_name}`/`{user_permission_level}`/`{project_name}`/`{conversation_summary}`，其余变量 replace 时因模板无对应占位符而无效替换（无副作用）。`get_prompt_variables()` 返回的 key 集合是超集，安全。

#### `emily-data/prompts/session.md` — 在文件头注释（第 4 行）追加用途说明

把：
```markdown
<!-- 加载位置：SessionAgent._recognize_intent()；routing.md 已废弃，合入此文件统一管理 -->
```
改为：
```markdown
<!-- 加载位置：M4 起，路由用 session_routing.md（精简）；本文件 session.md 用于
     fallback/元认知路径（SessionAgent fallback 分支基于三书全文回答元认知问题） -->
```

### 模块验收检测

```bash
# 验收 1：重启
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +
docker compose -f docker-compose-napcat.yml restart emily-core

# 验收 2：路由 prompt 字符数检查（从 emily-core 日志的归档段或 llm_trace）
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "今天项目有什么进展？" --sender "李景利"
docker exec mitmproxy tail -2 /app/logs/llm_trace.jsonl
→ 预期：路由调用的 prompt_tokens 较 M1 前的 5210 显著下降（目标 ≤1800，因含 sop_catalog 变量）

# 验收 3：路由准确率回归（不能因 prompt 瘦身导致 SOP 匹配错）
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我创建事件：样板段放线完成" --sender "李景利"
docker logs --tail 20 emily-core 2>&1 | grep "intent:"
→ 预期：sop=SOP-002-REC（事件录入），不能误匹配为 SOP-005-QRY 或 fallback

# 验收 4：元认知问题走 fallback（session.md 三书路径）
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "你的权限分级是怎样的？" --sender "李景利"
→ 预期：fallback=true，回复基于系统自我描述（含 L1-L6 分级内容）
```

**失败处理**：若验收 2 prompt 未降，检查 `_recognize_intent` 是否用了 `_ROUTING_SYSTEM_PROMPT`、`session_routing.md` 是否误含三书占位符；若验收 3 路由错乱，回退到 `_SESSION_SYSTEM_PROMPT` 并检查 session_routing.md 的路由规则段是否完整。

---

## M5: 历史消息过滤 + 摘要提频（P2-2）

**依赖**：无（可与 M4 并行）

**职责**：解决历史消息混入其他 sender、压缩阈值过高（40）、摘要仅归档时合并三个问题。

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | build_llm_messages 加 sender 过滤 | `emily-core/emily_core/session/session_context.py` |
| 2 | record_turn 压缩阈值 40→20 | `emily-core/emily_core/session/session_context.py` |
| 3 | 每 N 轮触发摘要 consolidate | `emily-core/emily_core/session/session_context.py` + `session_agent.py` |
| 4 | _consolidate_conversation_summary 抽取为可独立调用 | `emily-core/emily_core/session/session_context.py` |

### 代码

#### `emily-core/emily_core/session/session_context.py` — 修改 `build_llm_messages`（第 303-344 行）

加 `sender_filter` 参数，过滤掉非当前 sender 的 user 消息：
```python
    def build_llm_messages(self, system_prompt_template: str,
                           current_user_msg: str = "",
                           sender_name: str = "",
                           pending_context: str = "",
                           sender_filter: str = "") -> list[dict]:
        """统一拼装 LLM messages 列表。

        Args:
            system_prompt_template: 已 format WorkItem 级变量的模板
            current_user_msg: 当前用户消息
            sender_name: 发送者名称
            pending_context: 待确认上下文文本
            sender_filter: M5 若提供，只保留该 sender 的 user 消息 + 全部 assistant 消息，
                           过滤掉其他 sender 的 user 消息（避免群聊/混合会话污染意图识别）

        Returns:
            OpenAI 格式 messages 列表
        """
        # 两阶段 format：Session 级变量替换
        prompt_vars = self.get_prompt_variables()
        system_prompt = system_prompt_template
        for key, value in prompt_vars.items():
            replacement = str(value) if value else "（无）"
            system_prompt = system_prompt.replace(key, replacement)

        full_messages: list[dict] = [{"role": "system", "content": system_prompt}]

        # M5: 按 sender 过滤历史
        for msg in self.message_history:
            role = msg.get("role", "user")
            if sender_filter and role == "user":
                msg_sender = msg.get("name", "") or ""
                if msg_sender and msg_sender != sender_filter:
                    continue  # 跳过其他 sender 的 user 消息
            full_messages.append(msg)

        # pending 上下文注入
        if pending_context:
            full_messages.append({
                "role": "system",
                "content": pending_context,
            })

        # 当前用户消息
        if current_user_msg:
            full_messages.append({
                "role": "user",
                "content": current_user_msg,
                "name": sender_name if sender_name else None,
            })

        return full_messages
```

> **注意**：`SessionAgent._recognize_intent`（[session_agent.py:276-299](emily-core/emily_core/session/session_agent.py#L276-L299)）目前是手写 `full_messages.extend(self.context.message_history)`，**未走 build_llm_messages**。M5 需同步改 `_recognize_intent` 用 `build_llm_messages` 或就地加过滤。最小改动方案：在 `_recognize_intent` 第 277 行后加过滤循环：

```python
        # 组装 messages
        full_messages = [{"role": "system", "content": system_prompt}]
        # M5: 按 sender 过滤历史，避免其他 sender 消息污染意图识别
        current_sender = getattr(message, "sender_name", "") or ""
        for msg in self.context.message_history:
            if current_sender and msg.get("role") == "user":
                msg_sender = msg.get("name", "") or ""
                if msg_sender and msg_sender != current_sender:
                    continue
            full_messages.append(msg)
```

同理改 `WorkItemAgent._llm_plan`（[workitem_agent.py:332-333](emily-core/emily_core/workitem/workitem_agent.py#L332-L333)）和 `_llm_synthesize_reply`（[workitem_agent.py:800-801](emily-core/emily_core/workitem/workitem_agent.py#L800-L801)）的 `full_messages.extend(message_history)`。三处共用同一过滤逻辑，建议在 `session_context.py` 加一个工具方法：

```python
    def filtered_history(self, sender_filter: str = "") -> list[dict]:
        """M5: 返回按 sender 过滤后的 message_history 副本。"""
        if not sender_filter:
            return list(self.message_history)
        result = []
        for msg in self.message_history:
            if msg.get("role") == "user":
                msg_sender = msg.get("name", "") or ""
                if msg_sender and msg_sender != sender_filter:
                    continue
            result.append(msg)
        return result
```

三处调用点改为 `full_messages.extend(session_ctx.filtered_history(sender_name))`。

#### `emily-core/emily_core/session/session_context.py` — 修改 `record_turn`（第 297 行）

把：
```python
        if len(self.message_history) > 40:
```
改为：
```python
        if len(self.message_history) > 20:
```

同步改 `SessionAgent.handle`（[session_agent.py:175](emily-core/emily_core/session/session_agent.py#L175)）的 `> 40` 为 `> 20`。

#### `emily-core/emily_core/session/session_context.py` — 修改 `_consolidate_conversation_summary`（第 418 行）

把方法改为 `public`（去掉下划线前缀）或新增公开入口，供 SessionAgent 每 N 轮调用：
```python
    async def consolidate_summary(self, llm_client) -> bool:
        """M5: 合并本次对话到 conversation_summary（每 N 轮触发，不等归档）。

        抽取自 _consolidate_conversation_summary，供 SessionAgent 定期调用。
        Returns: True 表示摘要已更新。
        """
        from ..repositories.user_repo import UserRepository

        user = UserRepository.get_by_id(self.user_id)
        if not user:
            return False

        existing_summary = user.conversation_summary or ""
        current_conversation = _format_message_history(self.message_history)

        if not current_conversation or current_conversation == "（无历史消息）":
            return False

        compress_messages = [
            {"role": "system", "content": (
                "你是一个对话摘要助手。将用户的「已有历史摘要」和「本次对话」合并为一份新的摘要。"
                "只保留关键事实：人物、事件、决策、任务、时间。不超过 500 字。"
            )},
            {"role": "user", "content": (
                f"## 已有历史摘要\n{existing_summary or '（无）'}\n\n"
                f"## 本次对话\n{current_conversation}\n\n"
                f"请输出合并后的完整摘要："
            )},
        ]

        try:
            result = await llm_client.chat_messages(compress_messages)
            new_summary = result.get("content", "") or ""
            if new_summary and len(new_summary) > 20:
                UserRepository.update_user(self.user_id, conversation_summary=new_summary)
                logger.info(
                    "SessionContext summary consolidated for user %s (%d→%d chars)",
                    self.user_id, len(existing_summary), len(new_summary),
                )
                return True
        except Exception as e:
            logger.warning("SessionContext summary consolidation failed: %s", e)
        return False
```

原 `_consolidate_conversation_summary`（第 418 行）改为调用新方法：
```python
    async def _consolidate_conversation_summary(self, llm_client) -> None:
        """归档时整合（保留原签名，转调 consolidate_summary）。"""
        await self.consolidate_summary(llm_client)
```

#### `emily-core/emily_core/session/session_agent.py` — 修改 `_record_turn`（第 591-602 行）

加每 5 轮触发摘要：
```python
    def _record_turn(self, message: "StandardMessage", reply_content: str) -> None:
        """记录一轮对话（委托 SessionContext.record_turn）。"""
        sender = getattr(message, "sender_name", "") or ""
        self.context.record_turn(
            user_content=(message.content or "")[:2000],
            assistant_content=(reply_content or "")[:2000],
            sender_name=sender,
        )

        # 每 10 轮检测热更新
        if len(self.context.message_history) % 20 == 0:
            asyncio.ensure_future(self._maybe_refresh_context())

        # M5: 每 5 轮（10 条消息）触发摘要 consolidate，不等归档
        if len(self.context.message_history) % 10 == 0 and self._llm:
            asyncio.ensure_future(self._maybe_consolidate_summary())

    async def _maybe_consolidate_summary(self) -> None:
        """M5: 每 5 轮合并一次对话摘要，避免会话存活期间历史无限增长。"""
        try:
            await self.context.consolidate_summary(self._llm)
        except Exception as e:
            logger.debug("Session[%s] summary consolidation skipped: %s",
                         self.conversation_id, e)
```

### 模块验收检测

```bash
# 验收 1：重启
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +
docker compose -f docker-compose-napcat.yml restart emily-core

# 验收 2：构造混合 sender 场景（用 emy-test 连续发不同 sender 消息后查询）
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "今天项目有什么进展？" --sender "李景利"

# 验收 3：检查 trace —— 路由 messages 不含其他 sender 的 user 消息
docker exec mitmproxy tail -2 /app/logs/llm_trace.jsonl
→ 预期：路由调用的 messages 列表中，user 角色消息的 name 字段均为当前 sender 或 null，
   不含其他 sender 名（如"张工"系列不再出现）

# 验收 4：连续 5 轮后触发摘要
# （连续发 5 条消息后检查日志）
docker logs --tail 50 emily-core 2>&1 | grep "summary consolidated"
→ 预期：出现 "summary consolidated for user"（每 5 轮一次）

# 验收 5：历史条数稳定（不随轮数线性增长）
docker logs --tail 100 emily-core 2>&1 | grep "message_history overflow\|compressed"
→ 预期：超过 20 条触发压缩（阈值从 40 降到 20）
```

**失败处理**：若验收 3 仍含其他 sender，检查三处 `full_messages.extend(message_history)` 是否都改为 `filtered_history(sender)`；若验收 4 未触发摘要，检查 `_record_turn` 的 `% 10` 逻辑和 `_maybe_consolidate_summary` 是否被 `asyncio.ensure_future` 正确调度。

---

## M6: 简单查询快路径合并评估（P2-3）

**依赖**：M2（query_type 路由派生已就位）

**职责**：**评估**（非实施）将路由+参数提取+工具调用合并为单次 `chat_with_tools` function calling 的可行性与收益。产出评估报告，由用户决定是否进入实施。

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | 快路径合并可行性评估报告 | `需求/对话流优化_快路径评估_V1.md` |

### 评估步骤

#### 1. 现状基线测量（M1-M5 完成后）

```bash
# 测量当前（M5 后）单轮查询的调用数和延迟
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "今天项目有什么进展？" --sender "李景利"
docker exec mitmproxy tail -5 /app/logs/llm_trace.jsonl
# 记录：调用次数、各次 prompt/completion tokens、端到端延迟（从 emy-test 输出时间戳）
```

#### 2. function calling 可行性探查

读以下文件确认 DeepSeek + 现有 LLMClient 是否支持 tools：
- [client.py:253](emily-core/emily_core/infrastructure/llm/client.py#L253) `chat_with_tools` 已存在
- [client.py:151](emily-core/emily_core/infrastructure/llm/client.py#L151) tool_call 分支已处理
- 确认 `deepseek-chat` 模型支持 function calling（DeepSeek 文档）

#### 3. 设计快路径方案（写入评估报告）

评估两种方案：
- **方案 A：路由阶段直接 function calling**——路由 prompt 带 query_data 工具定义，LLM 一次调用输出 sop_id + tool_call。省掉 node3 的 SkillExecutor 参数提取。但路由与执行耦合，违背"路由只识别意图"的职责分离。
- **方案 B：node2-node3 合并**——Skill 路径下，node2 规划与 node3 执行合并，query_type 已由路由派生（M2），step-01 直接调工具，step-02/03 摘要合成与 node4 合并。

#### 4. 评估报告内容（写入 `需求/对话流优化_快路径评估_V1.md`）

```markdown
# 对话流优化 — 快路径合并评估

## 1. 基线（M5 后）
- 单轮查询同步 LLM 调用数：X 次
- 端到端延迟：X 秒
- prompt token 总量：X

## 2. 方案 A 评估
- 可行性：...
- 预期收益：调用数 X→Y
- 风险：...

## 3. 方案 B 评估
- ...

## 4. 建议
- 是否实施：是/否
- 若实施，推荐方案：A/B
- 实施前置条件：...
```

### 模块验收检测

```bash
# 验收：评估报告已产出且含基线数据
test -f "需求/对话流优化_快路径评估_V1.md" && echo "报告存在"
grep -c "基线\|方案 A\|方案 B\|建议" "需求/对话流优化_快路径评估_V1.md"
→ 预期：≥4（四个段落齐全）
```

**失败处理**：若 function calling 不可行（DeepSeek 限制），评估报告标注"方案 A/B 均不可行，维持 M5 后状态"，M6 结束。

---

## 组装验证（P2 全部完成后）

```bash
# 清缓存重启
docker exec emily-core find /app/emily_core -name '__pycache__' -type d -exec rm -rf {} +
docker compose -f docker-compose-napcat.yml restart emily-core

# 全场景回归
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "今天项目有什么进展？" --sender "李景利"
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "当前有哪些运转中的节点？" --sender "李景利"
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "帮我创建事件：样板段放线完成" --sender "李景利"
uv run python .claude/skills/emy-test/cli.py --managed --llm --message "你的权限分级是怎样的？" --sender "李景利"

# 最终指标核对
docker exec mitmproxy wc -l /app/logs/llm_trace.jsonl
docker exec mitmproxy tail -10 /app/logs/llm_trace.jsonl
```

**最终通过标准**（对照需求文档目标）：
| 指标 | 目标 | 验证方式 |
|------|------|----------|
| 简单查询同步 LLM 调用数 | 1-2 次 | jsonl 行数 |
| 路由 system prompt token | ≤1800 | jsonl prompt_tokens |
| 查询类 SOP 回复成功率 | ≥95% | emy-test 回归无"抱歉/参数错误" |
| 单轮抓包行数 | ≤400 | llm_trace.md 行数 |
| 简单查询端到端延迟 | ≤2 秒 | emy-test 时间戳 |
| 历史不含其他 sender | 是 | jsonl messages 的 name 字段 |

---

## 阶段反思指令

每完成一个模块，在进入下一个模块之前，执行以下反思：

1. **检查产物**：列出本模块所有新建/修改的文件路径
2. **检查偏差**：是否有步骤与计划不符？记录差异
3. **判断是否继续**：
   - 如果偏差 ≤ 1 个文件路径变化 → 直接修改计划文档对应模块，继续
   - 如果偏差 2-4 个文件或步骤顺序调整 → 在计划文档末尾追加 "v1.1 修订记录"，继续
   - 如果偏差 > 4 个文件或架构方向变化 → **停止**，报告给用户，等用户决定是否重新生成计划

**P1/P2 阶段门控**：M1-M3 全部验收通过前，不得启动 M4-M6。P1 组装验证未通过则停止 P2。

---

*本计划为 AI 可执行操作手册，由 req-plan 技能生成。*
