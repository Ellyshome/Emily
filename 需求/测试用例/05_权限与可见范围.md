# 05 权限与可见范围

> **本类目标**：验证等级权限、项目归属推导、行级过滤、能力可见集 fail-closed、门禁单一判定点，以及权限矩阵接口。
> **用例数**：10　|　**编号前缀**：`FR-G*`、`FR-P*`
> **执行通道**：A（对话模拟）+ E（库核对）+ B（接口）
> **来源**：`LangGraph编排内核化_功能复现测试用例_V1.md` G 组；`LangGraph编排内核化_功能复现测试报告_V1.md` 第七节补测

## 前置

1. **真实用户**，覆盖 3 个对照面：L1 访客 / L3~L4 参建执行与管理 / L6 系统管理员。
2. 掌握对照项目：用户**参与**的项目 vs **非参与**的项目（如「星河湾」为典型非参与项目）。
3. 权限等级以**代码为准**：`emily_core/permission/level.py`（L1 访客 … L6 系统管理员）；`is_admin(level)=level>=5`，`is_sys_admin(level)=level>=6`。

## 用例

| 编号 | 目标特性 | 操作 | 预期（判定标准） | 断言 | 来源 |
|------|---------|------|----------------|------|------|
| FR-G1 | 等级权限（L1~L6 体系） | 访客级(L1)用户发业务提问 | 被拒或返回空可见集；**不泄露他方数据** | permission_block | FR-G1 |
| FR-G2 | 项目归属推导可见范围 | L3 用户查**非参与**项目「星河湾」 | 不返回该项目数据；回复提示无权限或无数据 | permission_block | FR-G2 |
| FR-G3 | 行级过滤（events/tasks/files/messages） | 低级别用户查事件 | 返回行数 **< 全表行数**；无他企业数据 | db_effect | FR-G3 |
| FR-G4 | 能力可见集 fail-closed | 越权能力调用（内核回放用例 4） | 门禁拒绝且**能力未被调用**（`exec=[]`）；回复为无权限说明 | permission_block | FR-G4 / KRN-04 |
| FR-G5 | 门禁为单一判定点 | 图运行日志 | 出现 `fast→understand→gate⇄execute→summarize, gate=True`；收敛说明日志可见 | log_contains | FR-G5 |
| FR-G6 | 行级过滤 fail-open 面（**存量收窄点**） | 构造无法安全注入的复杂查询 | **现状**：放行并打 WARNING；**修复后应为拒绝** | log_contains | FR-G6 |
| FR-G7 | 权限矩阵接口：授权 / 回收 / 校验 | `POST /api/v1/permission/check`，体 `{"user_id":"<uuid>","sop_id":"SOP-005-QRY"}`（及对应 grant/revoke 端点） | 授权/回收/校验返回符合预期；越权校验返回拒绝（`allowed=false`） | db_effect | FR-G7 |
| FR-G8 | 权限等级编号一致性 | 对照 `docs/Manual/权限矩阵.md` 与 `emily_core/permission/level.py` | **已知口径差**：文档 0 起、代码 1 起，名称相同编号差一位。**判定以代码为准** | — | FR-G8 |
| FR-P1 | 权限矩阵校验接口可用 | `POST /api/v1/permission/check` | 200 且返回 `allowed` 字段；L3 对查询类 SOP 应 `allowed=true` | db_effect | 补测 §7.1 |
| FR-G9 | 访客文件可见范围（**固定项**） | 固定以访客「周访客」ID（无公司、L1）查询文件可见集 | 可见集 == 全部公开文件；内部(1)/机密(2) **0 条泄露** | db_effect | 新增固定项 |

## 关键对照矩阵（执行时按此设计对照）

| 用户 | 等级 | 公开(0) | 内部(1) | 机密(2) | 说明 |
|------|------|--------|--------|--------|------|
| 访客/供应商 | L1 | **全员可见（不走节点判定）** | ✗ | ✗ | 公开文件全员可见；内部/机密一律不可见 |
| 参建执行 | L2 | 参与节点 | 参与节点 | ✗ | |
| 参建管理 | L3 | 参与节点 | 参与节点 | ✗（除自传/显式授权） | |
| 建设主管 | L4 | 参与节点 | 参与节点 | ✗（除自传/显式授权） | |
| 管理员 | L5 | 全局 | 全局 | ✗（除自传/显式授权） | L5 改密级为机密后**自己也不可见**（已确认可接受） |
| 系统管理员 | L6 | 全局 | 全局 | **全局原生可见** | 白名单之外的管理员通道 |

## 执行命令参考

```powershell
$CLI = "uv run python .claude/skills/emy-test/cli.py --managed --llm"

& $CLI --message "翠湖庭院最近有什么事件？" --sender "周文斌"     # L1 访客
& $CLI --message "查一下星河湾项目的事件" --sender "林建辉"       # L3 查非参与项目

curl -s -X POST http://localhost:18080/api/v1/permission/check `
  -H "Content-Type: application/json" `
  -d '{"user_id":"<uuid>","sop_id":"SOP-005-QRY"}'

docker exec emily-postgres psql -U emily -d emily -c "SELECT count(*) FROM events;"
docker logs --since 5m emily-core 2>&1 | Select-String "gate=True|fast.*understand.*summarize"

# 访客文件可见范围固定项（FR-G9 / rag_test_harness TC-03）
uv run python scripts/rag_test_harness.py --env --setup   # 先建库（幂等，缺失才建）
uv run python scripts/rag_test_harness.py --env           # 跑 TC，重点看 TC-03（访客锚点）
```

## 备注

- `FR-G6`（行级过滤 fail-open）与 `FR-G8`（编号口径差）**不是缺陷**，但分别为**待收窄点**与**文档口径差**；修复或统一后须复测。
- 权限类用例**必须先确认真实 UUID**（伪造会导致降级到访客路径，测出的"被拒绝"是假阳性）。
- **FR-G9 为固定项**：以后每次回归「文件可见范围」都必须带访客锚点——固定以「周访客」（无公司、L1，由 `014_seed_rag_visitor.sql` 注入，UUID 动态生成、按 username 解析）的 ID 校验：可见集 == 全部公开文件，且内部(1)/机密(2) 零泄露。对应可执行断言为 `rag_test_harness.py` 的 TC-03。
