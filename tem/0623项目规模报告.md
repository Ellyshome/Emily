# Emily 项目规模报告

> 生成日期：2026-06-24 | 基于 Phase 0 + Phase A 完成状态

---

## 1. 总体规模

| 指标 | 数值 |
|------|------|
| Python 总行数 | **19,386** |
| 空行 | 3,199 (17%) |
| 纯注释行 | 899 (5%) |
| **有效代码行** | **15,288 (79%)** |
| Python 源文件数 | ~105 个 |
| SOP 业务流文档 | 1,956 行 (10 个 .md) |
| Prompt 模板 | 387 行 |
| 配置文件 (JSON/YAML/YML) | 174 行 |

---

## 2. 按模块分布

### 2.1 emily-core 业务内核（14,259 行有效代码）

| 模块 | 有效代码行 | 占比 | 角色 |
|------|-----------|------|------|
| `agent/` | 3,579 | 25% | MasterAgent / BusinessFlowAgent / Guardian / 意图路由 / SOP 解析 — 真实大脑，Phase B/C 接线备用 |
| `services/` | 2,434 | 17% | 15 个业务服务（消息/事件/任务/会议/文件/查询/用户记忆/聊天存档/Agent追踪/检查点等） |
| `workitem/` | 2,088 | 15% | **Phase A 新建** — WorkItem/Pipeline BUS(4节点)/Scheduler/Injector/状态机/Mock 大脑 |
| `tools/` | 1,786 | 13% | 10 种工具 handler（事件/任务/会议/文件/查询/知识搜索/聊天存档/用户记忆/待处理问题） |
| `repositories/` | 1,464 | 10% | 10 个 ORM 数据仓库（消息/用户/事件/任务/会议/文件/聊天存档/Agent推理/LLM交互/工具调用） |
| `infrastructure/` | 845 | 6% | LLM Client + PostgreSQL 22 表 ORM 模型 |
| `providers/` | 503 | 4% | MaxKB 向量检索 + TF-IDF 本地回退 |
| `application/` | 374 | 3% | 5 个 app 入口（event/file/meeting/query/task） |
| `session/` | 296 | 2% | **Phase A 新建** — SessionAgent/状态机/SessionContext/FocusLock/ConfirmQueue |
| `adapters/session/` | 209 | 1% | **Phase A 新建** — SessionPoolManager/SessionFactory/SessionConfig |
| `adapters/standard/` | 227 | 2% | 跨平台标准消息协议（StandardMessage/ReplyMessage 等） |
| `api/` | 200 | 1% | **Phase 0 新建** — FastAPI 入口（server/routes/sse/middleware） |
| 顶层模块 (\_\_init\_\_.py + config.py + bootstrap.py + outbound_bus.py) | 618 | — | EmilyCore 编排入口 + 配置 + 出站事件总线 |

### 2.2 薄通信层插件（765 行有效代码）

| 模块 | 有效代码行 | 角色 |
|------|-----------|------|
| `main.py` | ~100 | 插件入口（去重→转换→HTTP转发→SSE监听） |
| `adapters/api_client.py` | ~130 | EmilyApiClient — HTTP 客户端 |
| `adapters/sse_listener.py` | ~155 | SSEListener — 出站消息监听 |
| `adapters/astrbot/` | ~280 | InboundAdapter + OutboundSender |
| `adapters/standard/` | ~100 | 标准协议副本 |

### 2.3 非代码资产

| 目录/文件 | 行数 | 说明 |
|-----------|------|------|
| `emily-data/sops/` | 1,956 | 10 个 SOP 业务流定义 (SOP-000 ~ SOP-999) |
| `emily-data/prompts/` | 387 | 领域知识 + 守护Agent提示 + 决策树 |
| `emily-data/config/hook_config.json` | 44 | 12 个 Hook 挂载点声明式配置 |
| `emily-data/config/core_config.json` | 28 | Core 运行时配置 |
| `docker-compose-napcat.yml` | 102 | 5 服务 Docker 编排 |
| `scripts/smoke_test.py` | 89 | 离线端到端冒烟测试 |

---

## 3. 架构分层代码占比

```
薄插件通信层 ████ 5%
Adapter 层    ██ 3%
Session 层    ██ 2%
WorkItem 层   ██████████████ 15%
基础设施层    ██████████████████████████████████████████████ 50%
真实大脑      ████████████████████████ 25%
```

- **基础设施层最重**（services + repos + tools + infra + providers = 50%），这是从 EmyBot 迁移复用的成熟资产
- **真实大脑次之**（agent/ = 25%），已迁移备用，Phase B/C 接线
- **本次新建编排层**（workitem + session + adapters/session + api = 21%），Phase 0 + Phase A 的核心交付

---

## 4. 演化阶段完成度

| 阶段 | 状态 | 新建代码量 |
|------|------|-----------|
| Phase 0（容器剥离） | ✅ 完成 | ~300 行 api/ + ~765 行插件 |
| Phase A（Session池+Pipeline BUS） | ✅ 完成 | ~2,600 行 workitem/ + session/ + adapters/session/ |
| Phase B（WorkItem-Agent单例+增量灌注） | ❌ 未开始 | 将升级 agent/ (3,579行现有) + 替换 Mock |
| Phase C（全真实实现+集成验证） | ❌ 未开始 | 替换全部 Mock → 真实大脑 |

---

## 5. 数据流验证

```
StandardMessage → SessionPool.route → SessionAgent.handle
  ├── 闲聊短路（直接回复）
  └── 非短路 → WorkItem → Pipeline BUS 4节点
        Node1[意图+拆分] → Node2[计划+标准] → Node3[执行+验收] → Node4[成果总结]
        ↓
  ReplyMessage → 出站
```

冒烟测试（`scripts/smoke_test.py`）：**✅ 全部通过**

---

## 6. 小结

- **总有效 Python 代码约 1.5 万行**，分布紧凑，无冗余
- 约 **50% 的代码**是成熟可复用资产（services / repos / infra / tools）
- 约 **21% 的代码**是本次 Phase 0 + Phase A 新建的 Session 主线编排层
- 约 **25% 的代码**是已迁移的真实大脑（agent/），等待 Phase B/C 接线
- 当前 Mock 路径下全链路已有冒烟测试覆盖，编排骨架验证通过
