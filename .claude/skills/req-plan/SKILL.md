---
name: req-plan
description: >
  需求→AI执行计划。根据需求文档制定模块化开发计划，专为AI自主编码设计——
  每模块含交付物清单、代码骨架、模块验收检测。模块按依赖序构建→独立验收→最终组装。
  触发：/req-plan、/实施计划、"帮我制定实施计划"、"根据需求生成开发计划"、"create implementation plan"。
  不适用：纯调研/分析任务（不需要编码）、已有实施计划的续写（用 req-review 审核即可）。
allowed-tools:
  - Read
  - Write
  - Glob
  - Grep
  - Bash
  - Edit
---

# 需求→AI执行计划（req-plan）

> **你的角色**：
>
> | 角色 | 固定/自适应 | 说明 |
> |------|------------|------|
> | **作为Emily开发者资深架构师** | 🔒 固定 | 深刻理解Emily项目架构（双容器+Session主线+WorkItem 4节点BUS）、29表数据模型、分层约束。所有计划必须建立在真实架构基础上，不允许架空设计 |
> | **{AI根据需求自行判断}** | 🔄 自适应 | 根据具体需求的内容和领域，自行判断需要叠加哪些专业角色（如实施计划编制专家、数据库架构师、安全工程师等），在计划开头声明 |
>
> **"作为Emily开发者资深架构师"是不可跳过的根基角色**，确保计划始终根植于Emily真实架构。在此基础上，根据需求自行判断并声明额外角色。

---

## 1. 核心原则

| # | 原则 | 说明 |
|---|------|------|
| 1 | **指令式，非描述式** | "打开 X 文件，在第 N 行后追加 Y 代码"——不是"应该在 X 模块下新建文件"。AI agent 需要具体的动作动词 |
| 2 | **代码完整可复制** | 计划中的代码块是最终产物的直接模板，不允许 `...` 省略关键逻辑。AI agent 的职责是复制→适配→验证，不是从零设计 |
| 3 | **模块化构建** | 以"模块"为组织单元，非"阶段"。每个模块有独立交付物和验收检测，可独立构建和测试，最后组装 |
| 4 | **验收嵌入模块** | 每个模块完成后有独立的验收检测命令。不是在全部完成后集中验证——那太晚了 |
| 5 | **深嵌项目现状** | 必须先探查现有代码再写计划。所有参照的文件路径、方法签名、模式必须来自真实代码，不是假设 |
| 6 | **参照已有模式** | 新代码风格、命名、分层必须与项目现有代码一致。每个新建文件标注参照源 |
| 7 | **硬约束前置** | 计划开头列出 AI 执行者绝对不能违反的铁律（不改已有方法签名、不跳过验证等） |
| 8 | **依赖图显式** | 模块间依赖关系必须用 ASCII 图表达，AI 据此决定构建顺序 |

---

## 2. 触发条件

### 使用此 skill 的场景

- 需求文档已完成（可能经过 `req-review` 审核），需要转化为开发计划
- 用户说"帮我制定实施计划"、"根据这个需求生成开发计划"、"create implementation plan"
- 用户使用 `/req-plan` 命令
- 用户说"这个需求怎么落地"、"分几个模块做"

### 不使用此 skill 的场景

- 需求尚未明确，还在讨论阶段 —— 先让用户用 `req-review` 审核需求
- 需求太简单（单文件、<50 行改动）—— 直接写代码，不需要计划
- 纯调研/分析任务 —— 不需要分模块编码
- 已有实施计划，需要审核计划本身 —— 用 `req-review`

---

## 3. 统一命名约定

本项目需求流水线（需求→审核→计划→测试报告）使用统一文件命名规则：

**命名格式**：`{模块标识}_{阶段}_V{版本号}.md`

| 阶段标记 | 含义 | 产出方 |
|---------|------|--------|
| `需求` | 原始需求文档 | 人工编写 |
| `审核` | 需求审核报告 | req-review |
| `计划` | AI 实施计划 | req-plan |
| `测试报告` | 验证测试报告 | emy-verify |

**版本号规则**：每份文档独立版本，从 V1 起始。同一模块同阶段产出新版时自动递增。

**模块标识提取**：
1. 若输入文档遵循命名约定（如 `全景节点图V2_需求_V1.md`），提取 `_需求_` 之前的模块标识
2. 若为旧格式文件名，以文档所在目录名作为模块标识
3. 模块标识在整个流水线中保持稳定

**文件名示例**：
```
全景节点图V2_需求_V1.md    全景节点图V2_审核_V1.md    全景节点图V2_计划_V1.md
```

---

## 4. 实施计划编制流程

### Step 1：读取并理解需求

- 读取用户指定的需求文档
- 如果存在对应的审核报告，必读。按统一命名约定查找：`{模块标识}_审核_V*.md`（取最新版本）。旧格式可能为 `xxx_审核意见.md` 或 `xxx-架构师完善版.md`
- 如果用户未指定路径但提到了文档名，用 Glob 在 `需求文件/` 下搜索

### Step 2：深度探查项目现状

> **这是最关键的一步**。计划必须建立在"真正了解现状"的基础上。不能凭记忆或假设。

按以下顺序探查：

```
1. CLAUDE.md           ← 已加载，确认当前架构版本、技术栈、分层约束
2. docs/数据库设计.md    ← 了解现有表 schema，避免重复或冲突
3. docs/业务模块与运转全景.md ← 了解现有模块，确认集成点
4. 相关现有代码          ← Read 关键源文件，了解现有实现、可复用组件、真实方法签名
5. 相关需求文档目录       ← Glob 同模块已有需求/计划/报告
```

**必须探查的具体内容**：

| 探查目标 | 方法 | 产出 |
|----------|------|------|
| 分层位置确认 | Read 对应 Service/Repository/Application 文件 | 确定新模块在分层链中的插入点 |
| 可复用组件 | Read 同层已有代码 | 列出可直接复用的类、函数、枚举、Mixin（精确到方法签名） |
| 已有模式参照 | Read 相似模块的完整实现 | 确定状态机模式、Repo 模式、Service 模式、ORM 列风格 |
| 数据库现状 | Read `models.py` 相关 ORM 类 | 确认不会创建重复表，确认 FK 关系，确认列类型风格 |
| 配置现状 | Read `config.py` | 确认新增配置 key 的命名一致性 |
| 已有 API 风格 | Read 已有路由文件 | 确认 URL 前缀、响应格式、错误处理风格 |

### Step 3：设计模块拆分方案

基于需求+现状探查结果，将需求拆解为**独立可构建、可验收的模块**。

**模块拆分原则**：

| 原则 | 说明 | 反例 |
|------|------|------|
| **单一职责** | 每个模块负责一个清晰的领域/功能 | ❌ M1 包含 schema + parser + registry |
| **独立可验** | 每个模块完成后有独立的验收检测命令 | ❌ "数据层写好了但没法测试" |
| **依赖显式** | 模块间依赖用 ASCII 图表达 | ❌ 模块间有隐式耦合 |
| **无循环依赖** | 依赖图必须是 DAG | ❌ M1→M2→M1 |
| **新增包不影响已有分层** | 新模块在分层链中的位置必须明确 | ❌ 新模块跨越 Service/Repository 层 |

**模块粒度参考**：

| 粒度 | 适用场景 | 示例 |
|------|----------|------|
| 细粒度（1-2 文件） | 纯数据结构/工具类/独立解析器 | Schema 定义、Parser、Validator |
| 中粒度（2-4 文件） | 带状态的注册表/服务/引擎 | Registry、Executor、Extractor |
| 粗粒度（3-5 文件+改现有） | 需要改造已有代码的集成模块 | Pipeline 集成、API 路由扩展 |

### Step 4：输出 AI 执行计划文档

用 Write 保存到需求文档同目录。

**文件命名规则**——遵循统一命名约定（见第 3 节）：
1. **提取模块标识**：从输入文件名中提取；旧格式用目录名
2. **确定版本号**：Glob 搜索 `{模块标识}_计划_V*.md`，取最大版本号 +1；无已有文件则用 V1
3. **最终文件名**：`{模块标识}_计划_V{版本号}.md`

例如：为 `全景节点图V2_需求_V1.md` 制定计划 → 输出 `全景节点图V2_计划_V1.md`

**按照下方模板（第 5 节）输出**——模板结构不可偏离，因为它是 AI 执行者解析计划时的约定格式。

---

## 5. AI 执行计划模板（必须严格遵循）

```markdown
# {模块名} — AI 执行计划

> **基于需求**：[{需求文档}]({路径})
> **计划版本**：v1.0
> **目标**：{一句话}

---

## 你的角色

你作为 **Emily开发者资深架构师** + **实施计划编制专家**，严格按以下模块顺序执行，逐模块验证，验证不通过不进入下一个模块。

---

## 硬约束（违反即失败）

1. **禁止修改已有方法签名**：除非计划明确标注"修改方法签名"，否则只能在已有类中新增方法，不修改现有方法
2. **{项目特有的铁律}**：{从 CLAUDE.md 中提取的关键约束，如分层不跳、sync repo 模式、不 import 特定包等}
3. **每模块验收**：每个模块的验收检测必须通过，否则停止并报告
4. **参照模式**：所有新代码必须参照下方"代码模式参照表"中的源文件。风格不一致视为失败

---

## 上下文（执行前必读）

### 已有的可复用组件

| 组件 | 位置 | 关键方法 | 本次怎么用 |
|------|------|----------|-----------|
| {ClassName} | {文件路径} | {method1(), method2()} | {直接调用 / 继承 / 参照模式} |

### 架构决策

{1-2 句：为什么选择这个方案。如有替代方案，简述为何不选。}

### 代码模式参照表

| 层 | 参照源（精确文件路径） | 要模仿的要点 |
|----|----------------------|-------------|
| 状态枚举 | {路径} | Enum + TRANSITIONS + TERMINAL_STATES |
| Repository | {路径} | @staticmethod + 可选 session + _impl 内函数 |
| Service | {路径} | async def + asyncio.to_thread 包裹 sync repo |
| ORM 模型 | {路径} | Column 风格、UUID 主键、DateTime 时区 |
| 路由 | {路径} | APIRouter + Pydantic schema + 错误处理 |

---

## 模块依赖图

{ASCII 图，表达模块间依赖关系。AI 据此决定构建顺序。}

```
M1({模块名}) ──→ M2({模块名}) ──→ M3({模块名}) ──→ M6({集成模块})
                                        │                    ↑
                                        ↓                    │
                                   M4({模块名}) ──→ M5({模块名})
```

---

## 交付物总览

| 模块 | 交付文件 | 新增/修改 | 核心类/函数 |
|------|----------|----------|------------|
| M1 | `path/to/file1.py` | 新增 | `ClassA`, `func_b()` |
| M2 | `path/to/file2.py` | 新增 | `ClassC` |
| M6 | `path/to/existing.py` | 修改 | 修改 `ExistingClass.method_d()` 新增 `method_e()` |

---

## 现有模块改动清单

| 现有模块 | 改动类型 | 改动内容 |
|----------|----------|----------|
| `{文件路径}` | 不变 | — |
| `{文件路径}` | 修改 | {具体改动} |
| `{文件路径}` | 扩展 | {新增什么} |

---

## M1: {模块名}

**依赖**：无（本模块为首建模块）

**职责**：{一句话}

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | {描述} | `{精确文件路径}` |
| 2 | {描述} | `{精确文件路径}` |

### 代码

#### `{精确文件路径}` — 新建

```python
# {完整、可复制粘贴的代码。不使用 ... 省略}
```

#### `{精确文件路径}` — 在 `{具体位置}` 追加

```python
# {完整代码}
```

### 模块验收检测

```bash
# 验收 1：{检测什么}
{具体的验证命令}
→ 预期输出：{明确写清楚期望看到什么}

# 验收 2：{检测什么}
{具体的验证命令}
→ 预期输出：{...}
```

**失败处理**：如果验收不通过，{回退 / 检查 / 诊断的具体动作}

---

## M2: {模块名}

**依赖**：M1

**职责**：{一句话}

### 交付物

| # | 交付物 | 文件路径 |
|---|--------|----------|
| 1 | {描述} | `{精确文件路径}` |

### 代码

#### `{精确文件路径}` — 新建

```python
# {完整代码}
```

### 模块验收检测

```bash
# 验收 1：{检测什么}
{具体的验证命令}
→ 预期输出：{...}
```

**失败处理**：{...}

---

...（更多模块，同结构）

---

## 组装验证

所有模块完成后，运行端到端组装验证：

```bash
{一条端到端命令，验证全部模块组装后的完整功能链}
→ 预期输出：{...}
```

---

## 阶段反思指令

每完成一个模块，在进入下一个模块之前，执行以下反思：

1. **检查产物**：列出本模块所有新建/修改的文件路径
2. **检查偏差**：是否有步骤与计划不符？记录差异
3. **判断是否继续**：
   - 如果偏差 ≤ 1 个文件路径变化 → 直接修改计划文档对应模块，继续
   - 如果偏差 2-4 个文件或步骤顺序调整 → 在计划文档末尾追加 "v1.1 修订记录"，继续
   - 如果偏差 > 4 个文件或架构方向变化 → **停止**，报告给用户，等用户决定是否重新生成计划

---

*本计划为 AI 可执行操作手册，由 req-plan 技能生成。*
```

---

## 6. AI 友好性检查清单（输出前逐条确认）

| # | 检查项 | ✅ 标准 |
|---|--------|---------|
| 1 | 角色已定义 | 模板"你的角色"段已填写，不是空模板 |
| 2 | 硬约束已提取 | 从 CLAUDE.md 中提取了项目特有的分层/模式/禁止事项 |
| 3 | 上下文有可复用组件 | 表格列出了本次实际会用到的已有类+方法+路径 |
| 4 | 模块依赖图存在 | ASCII DAG 图，无循环依赖 |
| 5 | 交付物总览表完整 | 每个模块的文件路径+新增/修改+核心类函数均已列出 |
| 6 | 现有模块改动清单完整 | 每个受影响的现有文件标注了改动类型和内容 |
| 7 | 每模块有完整代码 | 代码块不含 `...`，可直接复制粘贴 |
| 8 | 每模块有验收检测 | 每个模块完成后有独立的 `bash`/`sql`/`python` 验收命令，含预期输出 |
| 9 | 每模块有失败处理 | 验收不通过时做什么（检查文件/回退/报告） |
| 10 | 代码模式参照完整 | 每层有精确参照文件路径，不是模糊的"参照 xxx 模块" |
| 11 | 组装验证存在 | 全部模块完成后的端到端验证命令 |
| 12 | 反思指令含停损规则 | 偏差多大时停止等待用户决策，多大时自主修复 |
| 13 | 代码块带文件路径注释 | 每个代码块首行标注 `# {文件路径}` |
| 14 | 文件操作动词精确 | "追加在 X 类定义后"不是"修改 Y 文件"——指定插入位置 |

---

## 7. 反模式（不要做的事）

| # | ❌ 不要做 | ✅ 应该做 |
|---|----------|----------|
| 1 | 不探查代码就写计划 | 先 Read 相关源文件、已有模块实现、数据库 models.py |
| 2 | 用"Phase"分阶段（"Phase 1 先做 X，Phase 2 再做 Y"） | 用"Module"分模块（M1/M2/M3...），每模块独立可构建可验收 |
| 3 | 写模糊的文件描述（"在 services 下建文件"） | 给出精确路径和插入位置：`emily-core/emily_core/services/xxx_service.py` 末尾 |
| 4 | 验收标准写"功能正常" | 写具体验证命令+预期输出：`grep "class Xxx" path/to/file.py` → 应返回一行匹配 |
| 5 | 沿用已废弃的模式 | 确认参照源是当前有效代码（不是 M15 WorkOrder、不是 EmyBot 旧架构） |
| 6 | 计划脱离分层约束 | 新模块必须遵守项目的实际分层链 |
| 7 | 代码块用 `...` 省略 | 代码必须完整。如果某段逻辑确实太长，拆成多个步骤 |
| 8 | 计划写得像需求文档 | 指令式动词（"打开/追加/替换/运行"），不是描述式（"应该/需要/建议"） |
| 9 | 不给替代方案 | 在"架构决策"中 1-2 句简述为什么选此方案 |
| 10 | 模板结构自己发挥 | 严格按第 5 节模板——这是 AI 执行者解析计划的约定格式 |
| 11 | 模块间有隐式耦合 | 依赖图必须显式，构建顺序必须可从依赖图推导 |
| 12 | 只有最终验收没有模块验收 | 每个模块完成后必须有独立验收检测，不能等全部完成才测 |
| 13 | 不标注对现有代码的改动 | "现有模块改动清单"必须逐一标注每个受影响文件 |
