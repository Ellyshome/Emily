"""Project health-check sub-module (Phase 2 — reserved).

Will contain:
    checks.py       — individual health checks (data integrity, FK validity, etc.)
    health_index.py — weighted health-index computation
"""
