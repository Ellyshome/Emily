# Emily Core 容器依赖
# api 层
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# 业务内核
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
openai>=1.0.0
aiohttp>=3.9.0
aiosmtplib>=3.0.0
aioimaplib>=1.0.0
pyyaml>=6.0
croniter>=1.3.0                       # 调度器 CRON 作业 reschedule（next_execution.py）

# RAG 原子工具层
pgvector>=0.3                          # pgvector SQLAlchemy 扩展
langchain-text-splitters>=0.3          # 文本分块（不引入 langchain 主包）
docling-slim>=2.0                     # PDF 解析（基础版，无 ML 模型）
# ↑ 如需高精度版面分析/阅读顺序，换回 docling>=2.0（会引入 torch ~526MB）
markitdown>=0.0.1                      # Office 转 Markdown
camelot-py[cv]>=0.11                   # PDF 表格提取
openpyxl>=3.1                          # Excel 表格提取
pdf2image>=1.17                        # PDF 转图片（OCR 前置）

# ── LangGraph 执行引擎（WorkItem 内部执行引擎替换）──
langgraph>=0.2.0                       # StateGraph + Checkpoint + interrupt
tenacity>=8.2.0                        # 细粒度重试（langgraph 传递依赖，显式声明便于直接 import）
